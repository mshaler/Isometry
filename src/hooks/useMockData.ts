/**
 * useMockData - Re-export for backward compatibility
 *
 * The actual implementation lives in ./data/useMockData.ts
 */

export {
  useMockData,
  type MockQueryState,
} from './data/useMockData';
