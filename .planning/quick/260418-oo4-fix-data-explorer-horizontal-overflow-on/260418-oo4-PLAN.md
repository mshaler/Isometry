---
phase: quick
plan: 260418-oo4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/workbench.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Data Explorer panel content does not overflow horizontally onto DockNav"
    - "SuperGrid empty state ('No axes configured') stays within workbench-main boundaries"
  artifacts:
    - path: "src/styles/workbench.css"
      provides: "Overflow containment on slot containers"
  key_links:
    - from: ".workbench-slot-top"
      to: ".workbench-main"
      via: "overflow containment"
      pattern: "overflow.*hidden"
---

<objective>
Fix horizontal overflow where Data Explorer panel content bleeds onto the DockNav strip.

Purpose: The explorer slot containers (.workbench-slot-top, .workbench-slot-bottom) lack explicit overflow-x containment. In flex layouts, content can escape horizontally when children have width: 100% or inline styles with padding/centering that push beyond the flex item boundary. Adding overflow: hidden to the slot containers clips content to the workbench-main column.

Output: workbench.css with overflow containment on slot containers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/styles/workbench.css
@src/styles/data-explorer.css
@src/styles/dock-nav.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overflow containment to workbench slot containers</name>
  <files>src/styles/workbench.css</files>
  <action>
In src/styles/workbench.css, update `.workbench-slot-top` and `.workbench-slot-bottom` to add `overflow-x: hidden` (changing the existing `overflow-y: auto` to the shorthand `overflow: hidden auto` — hidden on x-axis, auto on y-axis).

This is a two-line change. The existing `overflow-y: auto` on each selector becomes `overflow: hidden auto`.

Do NOT touch any other selectors or properties. Surgical change only.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>
    - .workbench-slot-top has overflow: hidden auto (clips horizontal, scrolls vertical)
    - .workbench-slot-bottom has overflow: hidden auto (clips horizontal, scrolls vertical)
    - No other CSS rules changed
    - All existing tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Added overflow-x: hidden to workbench slot containers to prevent Data Explorer content from bleeding onto DockNav</what-built>
  <how-to-verify>
    1. Open Isometry in browser/native shell
    2. Open the Data Explorer panel (click Data in dock)
    3. Verify the panel content stays within its column — no horizontal bleed onto the dock strip
    4. If SuperGrid shows "No axes configured" empty state, verify it also stays contained
    5. Resize the window narrower to confirm content clips rather than overflows
  </how-to-verify>
  <resume-signal>Type "approved" or describe remaining overflow issues</resume-signal>
</task>

</tasks>

<verification>
- grep for "overflow: hidden auto" in workbench.css confirms both slots updated
- Visual inspection confirms no horizontal bleed onto DockNav
</verification>

<success_criteria>
Data Explorer panel content and SuperGrid empty states are fully contained within the workbench-main column with no horizontal overflow onto the DockNav strip.
</success_criteria>

<output>
After completion, create `.planning/quick/260418-oo4-fix-data-explorer-horizontal-overflow-on/260418-oo4-SUMMARY.md`
</output>
