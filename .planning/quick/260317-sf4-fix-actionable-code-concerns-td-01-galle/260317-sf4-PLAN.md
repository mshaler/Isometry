---
phase: quick
plan: sf4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/GalleryView.ts
  - tests/views/GalleryView.test.ts
  - src/database/Database.ts
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [TD-01, TD-02, BUG-01, TD-06]

must_haves:
  truths:
    - "GalleryView uses D3 data join with key function d => d.id on every render"
    - "GalleryView efficiently updates tiles (enter/update/exit) instead of rebuilding all"
    - "Database.applySchema() has no timing race — double-init is guarded"
    - "CONCERNS.md accurately reflects current state of all items"
  artifacts:
    - path: "src/views/GalleryView.ts"
      provides: "D3 data join gallery rendering"
      contains: ".data(cards, (d) => d.id)"
    - path: "tests/views/GalleryView.test.ts"
      provides: "GalleryView D3 data join tests"
      min_lines: 40
  key_links:
    - from: "src/views/GalleryView.ts"
      to: "d3"
      via: "import * as d3 from 'd3'"
      pattern: "selectAll.*data.*join"
---

<objective>
Fix four actionable code concerns from CONCERNS.md: convert GalleryView to D3 data join (TD-01), simplify schema loading (TD-02), guard against schema timing race (BUG-01), and mark TD-06 as resolved (table parsing already works).

Purpose: Bring GalleryView into compliance with D-003 (mandatory D3 key function), harden Database initialization, and clean up stale concern entries.
Output: Updated GalleryView with D3 data join, hardened Database.applySchema(), updated CONCERNS.md
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/views/GalleryView.ts
@src/views/GridView.ts (reference D3 data join pattern for HTML div equivalent)
@src/views/types.ts (CardDatum interface, IView contract)
@src/database/Database.ts
@.planning/codebase/CONCERNS.md

<interfaces>
<!-- Key types the executor needs -->

From src/views/types.ts:
```typescript
export interface CardDatum {
  id: string;
  name: string;
  folder: string | null;
  status: string | null;
  card_type: CardType;
  created_at: string;
  modified_at: string;
  priority: number;
  sort_order: number;
  due_at: string | null;
  body_text: string | null;
  source: string | null;
}

export interface IView {
  mount(container: HTMLElement): void;
  render(cards: CardDatum[]): void;
  destroy(): void;
}
```

From src/views/CardRenderer.ts:
```typescript
export const CARD_TYPE_ICONS: Record<string, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert GalleryView to D3 data join</name>
  <files>src/views/GalleryView.ts, tests/views/GalleryView.test.ts</files>
  <behavior>
    - Test 1: render() creates div.gallery-tile elements for each card with data-id attribute
    - Test 2: render() uses D3 data join — re-render with same cards does NOT destroy/recreate tiles (update path)
    - Test 3: render() with fewer cards removes exiting tiles (exit path)
    - Test 4: render() with additional cards appends new tiles (enter path)
    - Test 5: each tile has correct structure (tile-image or tile-icon + tile-name)
    - Test 6: audit data attributes are applied correctly
  </behavior>
  <action>
Convert GalleryView from pure HTML rebuild to D3 data join pattern. Use `@vitest-environment jsdom` annotation on the test file.

1. Add `import * as d3 from 'd3'` to GalleryView.ts
2. Replace the render() method's `while(grid.firstChild) grid.removeChild(grid.firstChild)` + loop pattern with:
   ```typescript
   d3.select(grid)
     .selectAll<HTMLDivElement, CardDatum>('div.gallery-tile')
     .data(cards, (d) => d.id)
     .join(
       (enter) => { /* create tile div with children */ },
       (update) => { /* update tile content */ },
       (exit) => { exit.remove(); return exit; }
     );
   ```
3. In the enter handler, create the tile div with className 'gallery-tile', set data-id, width/height/overflow/display/flexDirection/alignItems styles, then conditionally create img.tile-image or div.tile-icon child, plus span.tile-name child. Apply audit data attributes.
4. In the update handler, update the tile-name textContent, update audit data attributes, and update the image/icon if card_type changed. For efficiency, only update what changed.
5. Move the `makeFallbackIcon()` and `renderGalleryTile()` logic into the enter/update handlers. Remove renderGalleryTile() private method.
6. Update `_updateGalleryFocus()` to use d3.selectAll instead of querySelectorAll for consistency.
7. Keep all keyboard navigation (A11Y-08) and destroy() logic intact.

The test file should use jsdom environment, create a container div, instantiate GalleryView, call mount(), then test render() with various card arrays to verify enter/update/exit paths. Use `document.querySelectorAll('.gallery-tile')` to count tiles and verify data-id attributes.
  </action>
  <verify>
    <automated>npx vitest run tests/views/GalleryView.test.ts</automated>
  </verify>
  <done>GalleryView uses D3 selectAll().data(cards, d => d.id).join() pattern. No more innerHTML wipe-and-rebuild. Tests prove enter/update/exit paths work correctly. D-003 compliance achieved.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Database.applySchema() and simplify TD-02</name>
  <files>src/database/Database.ts</files>
  <action>
Two small improvements to Database.ts:

1. **BUG-01 guard**: Add an `_initialized` boolean flag to the Database class. Set it to `true` at the end of `initialize()`. At the top of `initialize()`, if `_initialized` is already `true`, throw an Error: "Database already initialized — call close() first". This prevents double-init race conditions if `initialize()` is called concurrently.

2. **TD-02 simplification**: The `applySchema()` method's environment detection is correct and load-bearing (Vitest needs node:fs, Vite needs ?raw). However, simplify the detection comment and variable name for clarity:
   - Rename `isNodeEnv` to `isTestEnv` since that's what it really detects (SQL_WASM_PATH is only set by Vitest globalSetup)
   - Update the comment to: "Test environment detection: SQL_WASM_PATH is set exclusively by Vitest globalSetup"
   - Keep the actual branching logic identical — this is a cosmetic/documentation improvement only

Do NOT change the actual schema loading mechanism — the node:fs vs ?raw split is correct and necessary.
  </action>
  <verify>
    <automated>npx vitest run tests/database/Database.test.ts</automated>
  </verify>
  <done>Database.initialize() has double-init guard. applySchema() has clearer variable naming. All existing Database tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update CONCERNS.md — mark resolved, downgrade remaining</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
Update CONCERNS.md to reflect the work done in this plan:

1. **TD-01**: Mark as resolved. Change to: "**Status:** Resolved — GalleryView now uses D3 data join with key function (d => d.id)"

2. **TD-02**: Downgrade to "accepted pattern". Change to: "**Status:** Accepted — node:fs vs ?raw split is a necessary Vite/Vitest duality, not technical debt. Variable naming clarified."

3. **BUG-01**: Mark as resolved. Change to: "**Status:** Resolved — Database.initialize() has double-init guard; Worker already has pendingQueue for pre-init messages"

4. **TD-06**: Mark as resolved. Change to: "**Status:** Resolved — parseTableToMarkdown() in attachments.ts already converts HTML tables from alto-index exports to Markdown. The original concern about CRDT parsing is moot since alto-index provides HTML."

5. Remove the "GalleryView D3 compliance" entry from the "Test Coverage Gaps" table since it will now be testable.

For each resolved item, keep the original entry text but add the Status line below the existing fields. Do not delete entries — they serve as historical record.
  </action>
  <verify>
    <automated>grep -c "Status: Resolved" .planning/codebase/CONCERNS.md</automated>
  </verify>
  <done>CONCERNS.md has 3 items marked Resolved (TD-01, BUG-01, TD-06), 1 marked Accepted (TD-02), and GalleryView removed from test coverage gaps.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/views/GalleryView.test.ts` — new D3 data join tests pass
- `npx vitest run tests/database/Database.test.ts` — existing Database tests still pass
- `grep "d => d.id\|(d) => d.id" src/views/GalleryView.ts` — confirms D3 key function present
- `grep "Status: Resolved" .planning/codebase/CONCERNS.md` — confirms 3 resolved items
</verification>

<success_criteria>
- GalleryView.render() uses D3 selectAll().data(cards, d => d.id).join() instead of wipe-and-rebuild
- GalleryView has test coverage proving enter/update/exit paths
- Database has double-init guard preventing race conditions
- CONCERNS.md accurately reflects current state of TD-01, TD-02, BUG-01, TD-06
</success_criteria>

<output>
After completion, create `.planning/quick/260317-sf4-fix-actionable-code-concerns-td-01-galle/260317-sf4-SUMMARY.md`
</output>
