---
phase: 42-build-health
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - native/Isometry/Isometry.xcodeproj/project.pbxproj
autonomous: false
requirements:
  - BUILD-03
  - BUILD-04

must_haves:
  truths:
    - "Xcode Run Script build phase finds package.json at the correct input path"
    - "Xcode builds the native macOS app without Run Script errors"
    - "make check passes with zero warnings"
  artifacts:
    - path: "native/Isometry/Isometry.xcodeproj/project.pbxproj"
      provides: "Corrected Run Script inputPaths"
      contains: "$(SRCROOT)/../../package.json"
  key_links:
    - from: "native/Isometry/Isometry.xcodeproj/project.pbxproj"
      to: "package.json"
      via: "Run Script inputPaths dependency"
      pattern: "SRCROOT.*package\\.json"
---

<objective>
Fix the Xcode Run Script build phase input path so it correctly references the project root package.json, and verify the native macOS build succeeds cleanly. Then checkpoint for provisioning profile regeneration (BUILD-04).

Purpose: Ensure one-click native builds work correctly (BUILD-03) and flag the manual provisioning profile step (BUILD-04).
Output: Fixed project.pbxproj, verified native build, provisioning profile checkpoint.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-build-health/42-CONTEXT.md
@.planning/phases/42-build-health/42-RESEARCH.md

@native/Isometry/Isometry.xcodeproj/project.pbxproj (lines 255-277, Run Script section)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Xcode Run Script input path and verify build</name>
  <files>native/Isometry/Isometry.xcodeproj/project.pbxproj</files>
  <action>
1. In `native/Isometry/Isometry.xcodeproj/project.pbxproj`, find the `PBXShellScriptBuildPhase` section (around line 265-266).

2. Change the `inputPaths` entry:
   BEFORE: `"$(SRCROOT)/../package.json",`
   AFTER:  `"$(SRCROOT)/../../package.json",`

   Rationale: `$SRCROOT` resolves to `native/Isometry` (2 levels deep from repo root). Going up 1 level (`../`) reaches `native/`, which has no package.json. Going up 2 levels (`../../`) correctly reaches the repo root where package.json lives.

   The shell script itself already correctly computes `REPO_ROOT` via `dirname(dirname($SRCROOT))`, so only the `inputPaths` declaration is wrong. This fix ensures Xcode's stale-detection for the Run Script phase works correctly.

3. Verify the fix by running `make check` -- this builds macOS with zero-warning enforcement. It must report BUILD SUCCEEDED.

   If `make check` fails due to unrelated issues (npm not found in PATH, web bundle errors), ensure `npm run build:native` succeeds first, then re-run `make check`.
  </action>
  <verify>
    <automated>make check</automated>
  </verify>
  <done>project.pbxproj inputPaths updated to $(SRCROOT)/../../package.json. make check reports BUILD SUCCEEDED with zero project warnings.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Regenerate provisioning profile (BUILD-04)</name>
  <files>none</files>
  <action>
This is a manual step that requires Apple Developer Console access. Claude cannot automate provisioning profile regeneration.

The Xcode project already has correct entitlements files with CloudKit and iCloud Documents capabilities configured. However, the provisioning profile itself must be regenerated to include these entitlements.

Steps for the user:
1. Open Xcode project: `open native/Isometry/Isometry.xcodeproj`
2. Select the Isometry target, then Signing and Capabilities tab
3. If "Automatically manage signing" is checked, Xcode should regenerate the profile automatically
4. If manual signing, go to Apple Developer Console, Certificates Identifiers and Profiles
5. Find or create the provisioning profile for the Isometry app ID
6. Ensure it includes: CloudKit, iCloud Documents entitlements
7. Download and install the updated profile
8. Build again to verify signing succeeds
  </action>
  <verify>
    <automated>xcodebuild -project native/Isometry/Isometry.xcodeproj -scheme Isometry -destination 'platform=macOS' -showBuildSettings 2>/dev/null | grep -q "PROVISIONING_PROFILE" && echo "Profile configured"</automated>
  </verify>
  <done>Provisioning profile includes CloudKit and iCloud Documents entitlements. Xcode build succeeds with proper signing.</done>
</task>

</tasks>

<verification>
1. `make check` reports BUILD SUCCEEDED with zero warnings (BUILD-03)
2. Provisioning profile includes CloudKit + iCloud entitlements (BUILD-04)
</verification>

<success_criteria>
- BUILD-03: Xcode Run Script input path correctly references $(SRCROOT)/../../package.json
- BUILD-04: Provisioning profile regenerated with required entitlements
- make check passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/42-build-health/42-02-SUMMARY.md`
</output>
