---
phase: 123
slug: directory-discovery
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 123 — UI Design Contract: Directory Discovery

> Visual and interaction contract for Phase 123. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Phase scope:** Native file picker triggers alto-index root directory selection; system enumerates
> known subdirectory types; user sees a pre-import confirmation list (DISC-01, DISC-02, DISC-03).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — CSS custom properties from `src/styles/design-tokens.css` |
| Preset | not applicable |
| Component library | none — pure TypeScript + D3.js |
| Icon library | none — text labels only (existing pattern in data-explorer) |
| Font | `-apple-system, BlinkMacSystemFont, "Helvetica Neue", system-ui` via `--font-sans` |

**Source:** `src/styles/design-tokens.css` (all tokens pre-exist; no new tokens needed)

shadcn gate: not applicable — stack is TypeScript + D3.js, not React/Next.js/Vite.

---

## Spacing Scale

Declared values — use existing `--space-*` tokens exclusively:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`--space-xs`) | Icon gaps, badge inner padding |
| sm | 8px (`--space-sm`) | Row internal padding, button gaps |
| md | 12px (`--space-md`) | Stat rows, list item horizontal padding |
| lg | 16px (`--space-lg`) | Button padding-x, section padding |
| xl | 24px (`--space-xl`) | Section breaks, dialog padding |

Exceptions:
- Directory list rows: `min-height: 36px` (matches `.dexp-recent-card-row` touch target pattern)
- Checkbox touch target: `min-width: 20px; min-height: 20px` (minimum tappable area)

**Source:** `src/styles/design-tokens.css` `:root` spacing block — no new tokens.

---

## Typography

| Role | Size | Weight | Line Height | Token |
|------|------|--------|-------------|-------|
| Body / list item title | 13px | 400 | 1.5 | `--text-base` |
| Label / type badge | 11px | 400 | 1.4 | `--text-sm` |
| Section heading | 14px | 600 | 1.3 | `--text-md` |
| Dialog heading | 16px | 600 | 1.3 | `--text-lg` |

**Rules:**
- All type tokens come from the established `--text-*` scale in `design-tokens.css`.
- Only 2 weights used: 400 (regular) and 600 (semibold). Never 500 or 700 in this phase.
- Monospace font (`--font-mono`) is not used in this phase — no code or numeric data displayed.

**Source:** `src/styles/design-tokens.css` typography block; `app-dialog.css` heading/message pattern.

---

## Color

| Role | Token | Value (dark) | Usage |
|------|-------|-------------|-------|
| Dominant (60%) | `--bg-primary` | `#1a1a2e` | Sheet background, dialog backdrop area |
| Secondary (30%) | `--bg-card` / `--bg-surface` | `#1e1e2e` / `#252540` | Directory list panel, dialog card |
| Accent (10%) | `--accent` | `#4a9eff` | CTA button fill, selected-row left border |
| Destructive | `--danger` | `#ff4a4a` | Not used in Phase 123 — no destructive actions |

**Accent reserved for:**
1. "Choose Alto-Index Folder" primary CTA button background (`--accent` fill)
2. Left border on a selected/active directory row in the discovered-list (`3px solid var(--accent)`)
3. Focus outline on keyboard-navigable rows and checkbox elements (`2px solid var(--accent)`)

Accent must NOT be applied to type badges, section labels, count chips, or hover backgrounds.

**Type badge colors — use existing source provenance tokens:**
| Subdirectory type | Token |
|-------------------|-------|
| notes | `--source-apple-notes` (`#fbbf24`) |
| contacts | `--source-native-notes` (`#fdba74`) |
| calendar | `--source-native-calendar` (`#fcd34d`) |
| messages | `--source-csv` (`#34d399`) — closest neutral green |
| books | `--source-markdown` (`#a78bfa`) |
| calls | `--source-json` (`#60a5fa`) |
| safari-history | `--source-html` (`#f472b6`) |
| kindle | `--source-excel` (`#2dd4bf`) |
| reminders | `--source-native-reminders` (`#c084fc`) |
| safari-bookmarks | `--source-html` (`#f472b6`) — shares safari color |
| voice-memos | `--source-native-reminders` (`#c084fc`) — shares system source color |

All token values are theme-aware — they shift correctly under light/system/nextstep/material themes automatically.

**Source:** `src/styles/design-tokens.css` (all tokens pre-defined).

---

## Component Inventory

Phase 123 introduces one new UI surface: the **Directory Discovery Sheet**.

This sheet is presented modally after the native file picker (NSOpenPanel / SwiftUI `fileImporter`) returns a root directory URL.

### Component: `DirectoryDiscoverySheet`

**Pattern:** Matches `.app-dialog` — fixed overlay, `--bg-card` background, `--radius-lg` corners, `--overlay-shadow-heavy`.

**Layout:**
```
┌─────────────────────────────────┐
│  Import Alto-Index Directory    │  ← .disc-sheet__title (--text-lg, weight 600)
│  11 sources found in ~/alto     │  ← .disc-sheet__subtitle (--text-sm, --text-secondary)
├─────────────────────────────────┤
│  [✓] notes       [notes]        │  ← .disc-row (min-height 36px)
│  [✓] contacts    [contacts]     │
│  [✓] calendar    [calendar]     │
│  [ ] messages    [messages]     │
│  ...                            │
├─────────────────────────────────┤
│  [Cancel]   [Import Selected]   │  ← .app-dialog__actions pattern
└─────────────────────────────────┘
```

**Component parts:**

| Class | Role | Tokens |
|-------|------|--------|
| `.disc-sheet` | Modal container | Same as `.app-dialog`: `--bg-card`, `--radius-lg`, `--overlay-shadow-heavy`, max-width 480px |
| `.disc-sheet__title` | Dialog heading | `--text-lg`, weight 600, `--text-primary` |
| `.disc-sheet__subtitle` | Discovered root path + count | `--text-sm`, weight 400, `--text-secondary` |
| `.disc-sheet__list` | Scrollable directory list | `list-style: none`, max-height 320px, `overflow-y: auto` |
| `.disc-row` | Single directory row | `display: flex`, `align-items: center`, `gap: --space-sm`, `min-height: 36px`, `padding: --space-xs --space-md` |
| `.disc-row:hover` | Hover state | `background: var(--cell-hover)` |
| `.disc-row input[type=checkbox]` | Select toggle | Native checkbox, `accent-color: var(--accent)` |
| `.disc-row__name` | Directory name | `--text-base`, weight 400, `--text-primary`, flex: 1 |
| `.disc-row__badge` | Type label chip | `--text-sm`, weight 400, source provenance color (see table above), `background: rgba(color, 0.12)`, `--radius-sm` padding `2px 6px` |
| `.disc-sheet__empty` | No subdirectories found | `--text-sm`, `--text-muted`, centered, `padding: --space-xl 0` |

**Buttons:** Reuse `.app-dialog__btn`, `.app-dialog__btn--confirm`, `.app-dialog__btn--cancel` exactly — no new button styles.

### Interaction Model

1. User presses "Choose Alto-Index Folder" CTA (lives in existing Data Explorer panel).
2. Native OS file picker opens (Swift side: `NSOpenPanel` on macOS, `fileImporter` on iOS).
3. On selection, Swift bridge sends discovered subdirectory list to TypeScript via `postMessage`.
4. TypeScript renders `DirectoryDiscoverySheet` as `<dialog>` element using `.showModal()`.
5. Each discovered subdirectory renders as a `.disc-row` with checkbox pre-checked.
6. User can uncheck rows to exclude subdirectories.
7. "Import Selected" sends selection list back to Swift bridge — Phase 124 handles the actual import.
8. "Cancel" calls `.close()` on the dialog — no state change.

**Keyboard:** Dialog traps focus. Tab cycles through checkboxes and action buttons. Escape triggers Cancel. Enter on a focused checkbox toggles it. Enter on "Import Selected" confirms.

**No-subdirectories state:** If Swift returns an empty list (root had no known subdirectories), the dialog renders with `.disc-sheet__empty` in place of the list, and "Import Selected" button is disabled.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA label (Data Explorer) | "Choose Alto-Index Folder" |
| Dialog title | "Import Alto-Index Directory" |
| Dialog subtitle (populated) | "{N} sources found in {folder-name}" — e.g. "11 sources found in alto-index" |
| Import action button | "Import Selected" |
| Cancel button | "Cancel" |
| Empty state heading | "No Sources Found" |
| Empty state body | "The selected folder contains no recognized alto-index subdirectories. Choose a different folder." |
| Error state (bridge failure) | "Could not read directory. Make sure the folder exists and try again." |
| Destructive confirmation | No destructive actions in Phase 123. |

**Naming rules:**
- Directory type labels in `.disc-row__badge`: use exact lowercase subdirectory names as returned by Swift (`notes`, `contacts`, `calendar`, `messages`, `books`, `calls`, `safari-history`, `kindle`, `reminders`, `safari-bookmarks`, `voice-memos`).
- Do not title-case badge labels.
- Subtitle path: show only the leaf folder name, not the full absolute path. Full path available as `title` attribute for hover.

---

## States and Transitions

| State | Trigger | Visual Change |
|-------|---------|---------------|
| Default (sheet hidden) | Before folder pick | CTA button visible in Data Explorer |
| Sheet open (populated) | Swift returns ≥1 subdirectories | Dialog visible with pre-checked rows |
| Sheet open (empty) | Swift returns 0 subdirectories | Empty state message; "Import Selected" disabled |
| Row checked | User click or keyboard space | Checkbox checked; no background change |
| Row unchecked | User click or keyboard space | Checkbox unchecked |
| All rows unchecked | User unchecks last row | "Import Selected" button disabled (`opacity: 0.5; cursor: not-allowed`) |
| Import confirmed | User clicks "Import Selected" | Dialog closes (`.close()`); message sent to Swift bridge |
| Cancel | User clicks "Cancel" or presses Escape | Dialog closes; no state change |

Transitions: dialog open/close uses `--transition-normal` (300ms) on opacity/transform. Checkbox state changes are instant (no animation).

---

## Accessibility Contract

- `<dialog>` element with `.showModal()` — native focus trap, ARIA dialog role.
- `.disc-sheet__title` is the dialog's accessible name via `aria-labelledby`.
- `.disc-sheet__subtitle` is the dialog's description via `aria-describedby`.
- Checkboxes use `<label>` wrapping or explicit `for`/`id` pairing — each label includes both the directory name and type badge text.
- "Import Selected" button has `aria-disabled="true"` when all rows are unchecked (matches WCAG 2.1 AA).
- Focus returns to the "Choose Alto-Index Folder" CTA after dialog closes.
- `@media (prefers-reduced-motion: reduce)`: dialog transition-duration set to 0.01ms.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none — no shadcn | n/a | not applicable |

No third-party component registries are used. All UI is implemented with vanilla TypeScript + CSS custom properties consistent with the existing codebase pattern.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
