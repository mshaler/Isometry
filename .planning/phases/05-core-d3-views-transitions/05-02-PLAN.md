---
phase: 05-core-d3-views-transitions
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/views/ListView.ts
  - src/views/GridView.ts
  - tests/views/ListView.test.ts
  - tests/views/GridView.test.ts
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02

must_haves:
  truths:
    - "ListView renders cards in a single-column vertical list with sort controls"
    - "GridView renders cards in a responsive grid that adapts to container width"
    - "Both views use stable key function d => d.id on every .data() call"
    - "Sort controls in ListView change sort order (name, created_at, modified_at, priority)"
    - "GridView computes column count as Math.floor(containerWidth / cellWidth)"
    - "ListView rows have card content left-aligned and date right-aligned"
    - "Both views implement IView interface (mount, render, destroy)"
  artifacts:
    - path: "src/views/ListView.ts"
      provides: "SVG-based single-column list view with sort toolbar"
      exports: ["ListView"]
    - path: "src/views/GridView.ts"
      provides: "SVG-based responsive grid view"
      exports: ["GridView"]
    - path: "tests/views/ListView.test.ts"
      provides: "ListView rendering, sort controls, data join tests"
    - path: "tests/views/GridView.test.ts"
      provides: "GridView rendering, responsive layout, data join tests"
  key_links:
    - from: "src/views/ListView.ts"
      to: "src/views/types.ts"
      via: "implements IView interface"
      pattern: "implements IView"
    - from: "src/views/ListView.ts"
      to: "src/views/CardRenderer.ts"
      via: "renderSvgCard for card content"
      pattern: "renderSvgCard"
    - from: "src/views/GridView.ts"
      to: "src/views/CardRenderer.ts"
      via: "renderSvgCard for card content"
      pattern: "renderSvgCard"
---

<objective>
Implement ListView and GridView as SVG-based D3 views following the IView contract, with stable key functions and the canonical data-join pattern.

Purpose: These are the first two concrete views. They establish the SVG rendering path that all LATCH views share and prove the data-join pattern works with real card data. ListView adds sort controls; GridView adds responsive column wrapping.

Output: ListView.ts, GridView.ts with full IView implementation, test suites verifying data-join correctness, sort behavior, and responsive grid layout.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-d3-views-transitions/05-CONTEXT.md
@.planning/phases/05-core-d3-views-transitions/05-RESEARCH.md
@.planning/phases/05-core-d3-views-transitions/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/views/types.ts (created in Plan 01):
```typescript
export interface IView {
  mount(container: HTMLElement): void;
  render(cards: CardDatum[]): void;
  destroy(): void;
}

export interface CardDatum {
  id: string;
  name: string;
  folder: string | null;
  status: string | null;
  card_type: 'note' | 'task' | 'event' | 'resource' | 'person';
  created_at: string;
  modified_at: string;
  priority: number;
  sort_order: number;
}
```

From src/views/CardRenderer.ts (created in Plan 01):
```typescript
export const CARD_DIMENSIONS: {
  width: 280; height: 48; gridWidth: 180; gridHeight: 120; padding: 12;
};
export function renderSvgCard(g: d3.Selection<SVGGElement, unknown, null, undefined>, d: CardDatum): void;
export const CARD_TYPE_ICONS: Record<CardType, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ListView with sort controls</name>
  <files>src/views/ListView.ts, tests/views/ListView.test.ts</files>
  <behavior>
    - ListView.mount(container) creates an SVG element inside the container + a sort toolbar div above it
    - ListView.render(cards) applies D3 data join with key function d => d.id
    - Cards render as SVG g.card groups positioned via translate(0, i * ROW_HEIGHT)
    - Each row: card name left-aligned, date (modified_at) right-aligned, type badge
    - Sort toolbar has: dropdown for sort field (name, created_at, modified_at, priority) + asc/desc toggle button
    - Changing sort field or direction triggers a re-sort of the cards array and calls render()
    - SVG height updates to match cards.length * ROW_HEIGHT on every render
    - destroy() removes all DOM elements and nulls references
    - Fixed ROW_HEIGHT = 48px for clean alignment
    - Enter: new g.card groups fade in (opacity 0 -> 1)
    - Exit: removed g.card groups fade out (opacity 1 -> 0, then remove)
  </behavior>
  <action>
    1. Create `tests/views/ListView.test.ts` (RED phase):
       - FIRST LINE: `// @vitest-environment jsdom`
       - Import ListView, create test CardDatum arrays (3-5 cards with varied names/dates/priorities)
       - Test: "mount creates SVG element and sort toolbar in container"
       - Test: "render creates g.card groups matching card count" — render 3 cards, expect 3 g.card elements
       - Test: "uses key function d => d.id" — render, re-render with reordered data, verify same g elements bound to same IDs (check __data__.id)
       - Test: "positions cards at translate(0, i * 48)" — verify transform attributes
       - Test: "shows card name left-aligned and date right-aligned" — check text element content
       - Test: "SVG height updates on card count change" — render 3, then render 5, check svg height attribute
       - Test: "sort controls change sort order" — trigger sort change, verify card order in DOM
       - Test: "destroy removes all DOM elements" — mount, destroy, verify container is empty
       - Test: "enter: new cards fade in; exit: removed cards removed from DOM" — render [A,B,C], re-render [A,B,D], verify C removed, D present
    2. Create `src/views/ListView.ts` (GREEN phase):
       - Import * as d3 from 'd3'
       - Import IView, CardDatum from './types'
       - Import renderSvgCard, CARD_DIMENSIONS from './CardRenderer'
       - Class ListView implements IView
       - Private fields: svg, toolbar, container, currentCards, currentSort: { field: keyof CardDatum, direction: 'asc' | 'desc' }
       - Constants: ROW_HEIGHT = 48, PADDING = 16
       - `mount(container)`:
         a. Store container reference
         b. Create sort toolbar: div.sort-toolbar with select dropdown (name/created_at/modified_at/priority options) + button.sort-direction (asc/desc toggle with arrow indicator)
         c. Wire dropdown change → re-sort + render
         d. Wire toggle click → flip direction, re-sort + render
         e. Create SVG element: d3.select(container).append('svg').attr('width', '100%')
       - `render(cards)`:
         a. Store currentCards = [...cards]
         b. Sort by currentSort field + direction
         c. Update SVG height: cards.length * ROW_HEIGHT + PADDING
         d. D3 data join: svg.selectAll<SVGGElement, CardDatum>('g.card').data(sortedCards, d => d.id).join(enter, update, exit)
         e. Enter: append g.card, call renderSvgCard, set opacity 0, transition to opacity 1
         f. Update: transition to new position translate(0, i * ROW_HEIGHT)
         g. Exit: transition opacity to 0, then remove
         h. All groups: transition().attr('transform', (d, i) => `translate(0, ${i * ROW_HEIGHT})`)
         i. Add date text on right side: text.card-date showing modified_at formatted
       - `destroy()`:
         a. Remove toolbar from DOM
         b. Remove SVG from DOM (svg.remove())
         c. Null all references
    3. Run tests — all must pass (GREEN)
    4. REFACTOR: extract sortCards() helper, ensure clean separation
  </action>
  <verify>
    <automated>npx vitest --run tests/views/ListView.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>ListView renders cards in SVG with stable key function, sort controls work, SVG height adapts, enter/exit transitions defined, destroy cleans up. All ListView tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GridView with responsive column wrapping</name>
  <files>src/views/GridView.ts, tests/views/GridView.test.ts</files>
  <behavior>
    - GridView.mount(container) creates an SVG element with width 100%
    - GridView.render(cards) applies D3 data join with key function d => d.id
    - Cards render as SVG g.card groups positioned in a grid layout
    - Column count = Math.max(1, Math.floor(containerWidth / CELL_WIDTH))
    - Cards wrap to next row when columns are filled
    - Position: translate(col * CELL_WIDTH, row * CELL_HEIGHT) where col = i % cols, row = floor(i / cols)
    - Fixed CELL_WIDTH = 180, CELL_HEIGHT = 120 (uniform tiles, no masonry)
    - SVG height updates to match Math.ceil(cards.length / cols) * CELL_HEIGHT
    - PAFVProvider axis mappings determine sort order (consumed via cards array ordering)
    - destroy() removes SVG and nulls references
  </behavior>
  <action>
    1. Create `tests/views/GridView.test.ts` (RED phase):
       - FIRST LINE: `// @vitest-environment jsdom`
       - Import GridView, create test CardDatum arrays
       - Test: "mount creates SVG element in container"
       - Test: "render creates g.card groups matching card count" — render 6 cards, expect 6 g.card elements
       - Test: "uses key function d => d.id" — verify __data__.id binding after re-render with reordered data
       - Test: "positions cards in grid layout" — with container width 540 (3 cols of 180), verify first row [0,180,360], second row starts at y=120
       - Test: "adapts column count to container width" — set width to 360 (2 cols), verify positions. Note: jsdom may need explicit width setting on container.
       - Test: "Math.max(1, ...) ensures at least 1 column for narrow containers" — width 100 → 1 column
       - Test: "SVG height adapts to row count" — 6 cards in 3 cols = 2 rows → height = 240
       - Test: "enter: new cards appear; exit: removed cards disappear" — render [A,B,C,D], re-render [A,B,E,F]
       - Test: "destroy removes SVG from container"
    2. Create `src/views/GridView.ts` (GREEN phase):
       - Import * as d3 from 'd3'
       - Import IView, CardDatum from './types'
       - Import renderSvgCard, CARD_DIMENSIONS from './CardRenderer'
       - Class GridView implements IView
       - Private fields: svg, container
       - Constants: CELL_WIDTH = 180, CELL_HEIGHT = 120, PADDING = 16
       - `mount(container)`:
         a. Store container reference
         b. Create SVG: d3.select(container).append('svg').attr('width', '100%')
       - `render(cards)`:
         a. Measure container width: this.container.clientWidth (in jsdom tests, may need to stub or set explicitly)
         b. Compute cols = Math.max(1, Math.floor(containerWidth / CELL_WIDTH))
         c. Compute rows = Math.ceil(cards.length / cols)
         d. Update SVG height: rows * CELL_HEIGHT + PADDING
         e. Position function: (d, i) => { col = i % cols; row = floor(i / cols); return `translate(${col * CELL_WIDTH}, ${row * CELL_HEIGHT})` }
         f. D3 data join: svg.selectAll<SVGGElement, CardDatum>('g.card').data(cards, d => d.id).join(enter, update, exit)
         g. Enter: append g.card, call renderSvgCard, set initial opacity 0, transition to opacity 1
         h. Update: update content if needed
         i. Exit: transition opacity to 0, remove
         j. All groups: transition to computed position
       - `destroy()`:
         a. Remove SVG (svg.remove())
         b. Null references
       - NOTE: For jsdom testing, GridView.render should accept an optional containerWidth parameter OR the test should set container.style.width before mount. Use Object.defineProperty(container, 'clientWidth', { value: 540 }) in tests to control width.
    3. Run tests — all must pass (GREEN)
    4. REFACTOR: extract computeGridPosition helper function
  </action>
  <verify>
    <automated>npx vitest --run tests/views/GridView.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>GridView renders cards in responsive grid with stable key function, column count adapts to width, positions computed correctly, enter/exit transitions defined, destroy cleans up. All GridView tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest --run tests/views/ListView.test.ts` — all tests pass
2. `npx vitest --run tests/views/GridView.test.ts` — all tests pass
3. `npx tsc --noEmit` — no type errors
4. `npx vitest --run` — all existing tests still pass
5. Both views use `d => d.id` key function on every .data() call (grep confirms)
</verification>

<success_criteria>
- ListView renders SVG cards in single column with ROW_HEIGHT = 48
- ListView sort controls change card order (name, created_at, modified_at, priority + asc/desc)
- GridView renders SVG cards in responsive grid with CELL_WIDTH = 180, CELL_HEIGHT = 120
- GridView column count adapts to container width via Math.floor(width / cellWidth)
- Both views use d => d.id key function on every .data() call
- Both views implement IView interface correctly
- Both views have passing test suites in jsdom environment
- Enter/exit transitions defined (opacity fade in/out)
</success_criteria>

<output>
After completion, create `.planning/phases/05-core-d3-views-transitions/05-02-SUMMARY.md`
</output>
