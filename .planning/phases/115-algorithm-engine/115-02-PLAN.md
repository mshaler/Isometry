---
phase: 115-algorithm-engine
plan: 02
type: execute
wave: 2
depends_on: ["115-01"]
files_modified:
  - tests/worker/graph-algorithms.bench.ts
autonomous: true
requirements: [ALGO-02]

must_haves:
  truths:
    - "Betweenness centrality at n=2000 completes in under 2 seconds with sqrt(n) sampling"
    - "Benchmark file exists and runs as part of the bench suite"
  artifacts:
    - path: "tests/worker/graph-algorithms.bench.ts"
      provides: "Performance benchmark for betweenness centrality at scale"
      contains: "2000"
  key_links:
    - from: "tests/worker/graph-algorithms.bench.ts"
      to: "src/worker/handlers/graph-algorithms.handler.ts"
      via: "handleGraphCompute call with centrality algorithm"
      pattern: "handleGraphCompute"
---

<objective>
Create a performance benchmark that validates betweenness centrality completes within 2 seconds at n=2000 nodes, confirming the sqrt(n) sampling threshold works correctly at scale.

Purpose: Guards against the O(n*m) timeout pitfall documented in PITFALLS.md -- ensures the sampling heuristic activates and keeps computation within budget.
Output: A benchmark file that can be run standalone or as part of CI bench suite.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/115-algorithm-engine/115-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/115-algorithm-engine/115-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create betweenness centrality performance benchmark at n=2000</name>
  <files>tests/worker/graph-algorithms.bench.ts</files>
  <read_first>
    - src/worker/handlers/graph-algorithms.handler.ts
    - src/database/queries/graph-metrics.ts
    - tests/worker/graph-algorithms-handler.test.ts
    - .planning/phases/115-algorithm-engine/115-CONTEXT.md
  </read_first>
  <action>
Create `tests/worker/graph-algorithms.bench.ts` following the v6.0 benchmark pattern:

1. **Setup**: Create a real in-memory sql.js Database, apply base schema + GRAPH_METRICS_DDL
2. **Generate random graph**: Create 2000 cards and ~6000 connections (avg degree ~6, Erdos-Renyi style):
   - Insert 2000 cards via createCard()
   - For each card, create ~3 random connections to other cards using createConnection()
   - This gives a realistic sparse graph topology
3. **Benchmark**: Call `handleGraphCompute(db, { algorithms: ['centrality'], renderToken: 1 })` and measure wall-clock time
4. **Assert**: `durationMs < 2000` (2 second budget)
5. **Also assert**: `result.cardCount === 2000` and `result.algorithmsComputed` includes `'centrality'`
6. **Cleanup**: db.close()

Use Vitest bench API (`bench()` from vitest) if available, otherwise use a standard `it()` test with `performance.now()` timing and explicit assertion on duration.

The test MUST use the same Database + handler setup as the unit tests (real sql.js, not mocks) to accurately measure the full pipeline including graph construction from sql.js + graphology computation + writeGraphMetrics.

Important: The 2000-node threshold is the exact cutoff where sqrt(n) sampling should activate. The benchmark validates that this threshold works correctly in practice.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/worker/graph-algorithms.bench.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/worker/graph-algorithms.bench.ts exists
    - File contains "2000" (node count)
    - File contains "centrality" (algorithm under test)
    - File imports handleGraphCompute from graph-algorithms.handler
    - File asserts duration < 2000ms
    - Benchmark passes when run via `npx vitest run tests/worker/graph-algorithms.bench.ts`
  </acceptance_criteria>
  <done>Performance benchmark validates betweenness centrality at n=2000 completes within 2-second budget with sqrt(n) sampling</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/worker/graph-algorithms.bench.ts` -- benchmark passes within 2s budget
2. `npx vitest run tests/worker/graph-algorithms-handler.test.ts` -- existing unit tests still pass (no regressions)
</verification>

<success_criteria>
- Benchmark file exists and runs successfully
- Betweenness centrality at n=2000 completes in under 2 seconds
- sqrt(n) sampling is confirmed to activate at the 2000-node threshold
</success_criteria>

<output>
After completion, create `.planning/phases/115-algorithm-engine/115-02-SUMMARY.md`
</output>
