---
phase: 155-css-namespace-design-token-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/algorithm-explorer.css
  - src/styles/network-view.css
  - src/styles/card-dimensions.css
  - src/styles/visual-explorer.css
  - src/ui/AlgorithmExplorer.ts
  - src/ui/VisualExplorer.ts
autonomous: true
requirements: [VCSS-03, VCSS-04]

must_haves:
  truths:
    - "AlgorithmExplorer renders identically after migrating .nv-pick-* selectors to .algorithm-explorer__* namespace"
    - "VisualExplorer renders identically after migrating .dim-btn/.dim-switcher selectors to .visual-explorer__* namespace"
    - "All AlgorithmExplorer CSS uses design tokens with zero fallback values"
    - "All VisualExplorer CSS uses design tokens with zero fallback values"
  artifacts:
    - path: "src/styles/algorithm-explorer.css"
      provides: "Fully namespaced AlgorithmExplorer CSS with zero fallbacks"
      contains: "algorithm-explorer__"
    - path: "src/styles/visual-explorer.css"
      provides: "Fully namespaced VisualExplorer CSS including dim-switcher selectors"
      contains: "visual-explorer__dim"
  key_links:
    - from: "src/ui/AlgorithmExplorer.ts"
      to: "src/styles/algorithm-explorer.css"
      via: "className string literals"
      pattern: "algorithm-explorer__pick"
    - from: "src/ui/VisualExplorer.ts"
      to: "src/styles/visual-explorer.css"
      via: "className string literals"
      pattern: "visual-explorer__dim"
---

<objective>
Migrate AlgorithmExplorer and VisualExplorer CSS to fully-compliant BEM namespaces and strip all hardcoded fallback values.

Purpose: Satisfy VCSS-03 (AlgorithmExplorer .nv-* to .algorithm-explorer__*) and VCSS-04 (VisualExplorer .dim-btn/.dim-switcher to .visual-explorer__*). These are the two explorers with cross-file selector leakage that must be corrected.
Output: Two fully-compliant CSS files with zero fallback values and zero cross-file selector references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-css-namespace-design-token-audit/155-CONTEXT.md
@.planning/phases/155-css-namespace-design-token-audit/155-UI-SPEC.md
@src/styles/design-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AlgorithmExplorer CSS — move .nv-pick-* selectors from network-view.css, strip all fallbacks and hardcoded values</name>
  <files>src/styles/algorithm-explorer.css, src/styles/network-view.css, src/ui/AlgorithmExplorer.ts</files>
  <read_first>
    - src/styles/algorithm-explorer.css (current state — 32 lines with fallback values)
    - src/styles/network-view.css (contains .nv-pick-instruction, .nv-pick-dropdowns, .algorithm-explorer__reset that belong to AlgorithmExplorer)
    - src/ui/AlgorithmExplorer.ts (TS class references: line 404 'nv-pick-instruction', line 413 'nv-pick-dropdowns')
    - src/styles/design-tokens.css (token vocabulary)
  </read_first>
  <action>
**Step 1: Move selectors from network-view.css to algorithm-explorer.css**

Move these selectors OUT of `network-view.css` and INTO `algorithm-explorer.css`, renaming to BEM namespace:

| Old selector (in network-view.css) | New selector (in algorithm-explorer.css) |
|---|---|
| `.nv-pick-instruction` | `.algorithm-explorer__pick-instruction` |
| `.nv-pick-dropdowns` | `.algorithm-explorer__pick-dropdowns` |
| `.nv-pick-dropdowns label` | `.algorithm-explorer__pick-dropdowns label` |
| `.nv-pick-dropdowns select` | `.algorithm-explorer__pick-dropdowns select` |
| `.algorithm-explorer__reset` (already correct name, just wrong file) | `.algorithm-explorer__reset` (move as-is) |
| `.algorithm-explorer__reset:hover` | `.algorithm-explorer__reset:hover` (move as-is) |

Delete these selectors from `network-view.css` after moving.

**Step 2: Strip all fallback values in algorithm-explorer.css**

Replace every `var(--token, fallback)` with `var(--token)` per D-03:

| Current | Replace with |
|---|---|
| `var(--text-sm, 13px)` | `var(--text-sm)` |
| `var(--text-xs, 11px)` | `var(--text-xs)` |
| `var(--text-secondary, #666)` | `var(--text-secondary)` |
| `var(--border, #ccc)` | `var(--border-subtle)` (note: `--border` is not a valid token; use `--border-subtle`) |
| `var(--surface, #fff)` | `var(--bg-card)` (note: `--surface` is not a valid token; use `--bg-card`) |
| `var(--surface-hover, #f5f5f5)` | `var(--bg-surface)` (note: `--surface-hover` is not a valid token) |
| `var(--audit-modified, #fb923c)` | `var(--audit-modified)` |
| `var(--radius-sm, 4px)` (in moved selectors) | `var(--radius-sm)` |
| `var(--border-muted)` (already correct in moved selectors) | keep as-is |

**Step 3: Replace hardcoded spacing values**

| Current | Replace with |
|---|---|
| `padding: 8px 12px` | `padding: var(--space-sm) var(--space-md)` |
| `gap: 8px` | `gap: var(--space-sm)` |
| `gap: 4px` | `gap: var(--space-xs)` |
| `gap: 6px` | `gap: var(--space-xs)` (nearest token, 4px) OR add comment noting remap |
| `margin-bottom: 4px` | `margin-bottom: var(--space-xs)` |
| `padding: 4px 16px` | `padding: var(--space-xs) var(--space-lg)` |
| `margin-left: 8px` | `margin-left: var(--space-sm)` |
| `margin-top: 8px` (in moved .algorithm-explorer__reset) | `margin-top: var(--space-sm)` |
| `padding: 4px 12px` (in moved .algorithm-explorer__reset) | `padding: var(--space-xs) var(--space-md)` |
| `margin: 8px 0` (in moved .nv-pick-instruction) | `margin: var(--space-sm) 0` |
| `gap: 8px` (in moved .nv-pick-dropdowns) | `gap: var(--space-sm)` |
| `gap: 4px` (in moved label) | `gap: var(--space-xs)` |
| `padding: 4px 8px` (in moved select) | `padding: var(--space-xs) var(--space-sm)` |

Structural dimensions to KEEP as plain values per D-04: `width: 80px` (input width), `min-width: 36px` (param value), `width: 8px; height: 8px` (stale dot), `border-radius: 50%` (dot), `border-radius: 4px` (button).

**Step 4: Update TS class references in AlgorithmExplorer.ts**

| Line | Old | New |
|---|---|---|
| ~404 | `'nv-pick-instruction'` | `'algorithm-explorer__pick-instruction'` |
| ~413 | `'nv-pick-dropdowns'` | `'algorithm-explorer__pick-dropdowns'` |

  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && grep -n "var(--.*," src/styles/algorithm-explorer.css | grep -v "transition" | wc -l | xargs test 0 -eq && echo "PASS: zero fallbacks" && grep -c "nv-pick" src/ui/AlgorithmExplorer.ts | xargs test 0 -eq && echo "PASS: no nv-pick refs in TS" && grep -c "nv-pick\|algorithm-explorer__reset" src/styles/network-view.css | xargs test 0 -eq && echo "PASS: selectors moved out of network-view.css"</automated>
  </verify>
  <acceptance_criteria>
    - algorithm-explorer.css contains zero `var(--token, fallback)` patterns (grep returns 0 matches)
    - algorithm-explorer.css contains `.algorithm-explorer__pick-instruction` and `.algorithm-explorer__pick-dropdowns` selectors
    - algorithm-explorer.css contains `.algorithm-explorer__reset` selector
    - network-view.css does NOT contain `.nv-pick-instruction`, `.nv-pick-dropdowns`, or `.algorithm-explorer__reset`
    - AlgorithmExplorer.ts contains `'algorithm-explorer__pick-instruction'` (not `'nv-pick-instruction'`)
    - AlgorithmExplorer.ts contains `'algorithm-explorer__pick-dropdowns'` (not `'nv-pick-dropdowns'`)
    - algorithm-explorer.css contains zero hardcoded px values for padding/margin/gap/font-size (all use `var(--space-*)` or `var(--text-*)`)
  </acceptance_criteria>
  <done>AlgorithmExplorer CSS is fully self-contained in algorithm-explorer.css with BEM namespace, zero fallbacks, zero hardcoded spacing/color values. All .nv-pick-* and .algorithm-explorer__reset selectors removed from network-view.css.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate VisualExplorer CSS — move .dim-btn/.dim-switcher from card-dimensions.css, verify gold-standard compliance</name>
  <files>src/styles/visual-explorer.css, src/styles/card-dimensions.css, src/ui/VisualExplorer.ts</files>
  <read_first>
    - src/styles/visual-explorer.css (current state — 164 lines, mostly compliant)
    - src/styles/card-dimensions.css (contains .dim-switcher and .dim-btn selectors at lines 219-263 used by VisualExplorer.ts)
    - src/ui/VisualExplorer.ts (TS references: line 124 'dim-switcher', line 131 'dim-btn', line 133 'dim-btn--active', line 145 '.dim-btn', line 262 '.dim-btn', line 265 'dim-btn--active')
    - src/styles/design-tokens.css (token vocabulary)
  </read_first>
  <action>
**Step 1: Move .dim-switcher/.dim-btn selectors from card-dimensions.css to visual-explorer.css**

Move these selectors OUT of `card-dimensions.css` (lines 215-263) and INTO `visual-explorer.css`, renaming to BEM namespace:

| Old selector (in card-dimensions.css) | New selector (in visual-explorer.css) |
|---|---|
| `.dim-switcher` | `.visual-explorer__dim-switcher` |
| `.dim-btn` | `.visual-explorer__dim-btn` |
| `.dim-btn:hover` | `.visual-explorer__dim-btn:hover` |
| `.dim-btn:focus-visible` | `.visual-explorer__dim-btn:focus-visible` |
| `.dim-btn--active` | `.visual-explorer__dim-btn--active` |
| `.dim-btn--active:hover` | `.visual-explorer__dim-btn--active:hover` |
| `.dim-btn + .dim-btn` | `.visual-explorer__dim-btn + .visual-explorer__dim-btn` |

Delete the comment block "Dimension Switcher" section (lines ~215-263) from `card-dimensions.css` after moving.

The CSS property values in these selectors already use tokens correctly (var(--bg-surface), var(--text-secondary), etc.) with one exception:
- `.dim-btn--active` has `color: #fff` -- replace with `color: var(--bg-primary)` (matches pattern in .notebook-tab--active)
- `.dim-btn--active:hover` has `color: #fff` -- replace with `color: var(--bg-primary)`

**Step 2: Verify visual-explorer.css has zero fallbacks**

The file is the "gold standard" per CONTEXT.md. Scan for any `var(--token, fallback)` patterns. The only potential issue is the `9px` font-size on `.visual-explorer__zoom-min-label` / `.visual-explorer__zoom-max-label` (line ~105). Per D-04, this is a structural dimension for tiny endpoint labels -- leave as-is but add a comment: `/* structural: below token scale */`.

**Step 3: Update TS class references in VisualExplorer.ts**

| Line | Old | New |
|---|---|---|
| ~124 | `'dim-switcher'` | `'visual-explorer__dim-switcher'` |
| ~131 | `'dim-btn'` | `'visual-explorer__dim-btn'` |
| ~133 | `'dim-btn--active'` | `'visual-explorer__dim-btn--active'` |
| ~145 | `'.dim-btn'` (querySelector) | `'.visual-explorer__dim-btn'` |
| ~262 | `'.dim-btn'` (querySelectorAll) | `'.visual-explorer__dim-btn'` |
| ~265 | `'dim-btn--active'` (classList.toggle) | `'visual-explorer__dim-btn--active'` |

  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && grep -c "dim-switcher\|dim-btn" src/styles/card-dimensions.css | xargs test 0 -eq && echo "PASS: dim selectors removed from card-dimensions.css" && grep -c "'dim-btn\|'dim-switcher\|\.dim-btn\|\.dim-switcher" src/ui/VisualExplorer.ts | xargs test 0 -eq && echo "PASS: no old dim- refs in TS" && grep -c "visual-explorer__dim" src/styles/visual-explorer.css | xargs test 1 -le && echo "PASS: dim selectors in visual-explorer.css"</automated>
  </verify>
  <acceptance_criteria>
    - visual-explorer.css contains `.visual-explorer__dim-switcher` selector
    - visual-explorer.css contains `.visual-explorer__dim-btn` selector
    - visual-explorer.css contains `.visual-explorer__dim-btn--active` selector
    - card-dimensions.css does NOT contain `.dim-switcher`, `.dim-btn`, or `.dim-btn--active`
    - VisualExplorer.ts contains `'visual-explorer__dim-switcher'` (not `'dim-switcher'`)
    - VisualExplorer.ts contains `'visual-explorer__dim-btn'` (not `'dim-btn'`)
    - VisualExplorer.ts contains `'visual-explorer__dim-btn--active'` (not `'dim-btn--active'`)
    - visual-explorer.css contains zero `#fff` hardcoded values (replaced with token)
    - visual-explorer.css contains zero `var(--token, fallback)` patterns
  </acceptance_criteria>
  <done>VisualExplorer CSS is fully self-contained in visual-explorer.css with BEM namespace, zero hardcoded color values, zero fallbacks. All .dim-* selectors removed from card-dimensions.css.</done>
</task>

</tasks>

<verification>
1. `grep -rn "nv-pick" src/ui/ src/styles/algorithm-explorer.css` returns only algorithm-explorer.css matches (not network-view.css or TS files)
2. `grep -rn "dim-btn\|dim-switcher" src/ui/ src/styles/visual-explorer.css` returns only visual-explorer.css matches (not card-dimensions.css or TS files with old names)
3. `grep -n "var(--.*," src/styles/algorithm-explorer.css` returns zero matches (no fallbacks)
4. `grep -n "#[0-9a-fA-F]" src/styles/algorithm-explorer.css src/styles/visual-explorer.css` returns zero matches (no hardcoded hex colors)
5. `npx vitest run --reporter=verbose 2>&1 | tail -5` -- existing tests still pass
6. `npx tsc --noEmit` -- no type errors introduced
</verification>

<success_criteria>
AlgorithmExplorer and VisualExplorer CSS files are fully BEM-namespaced, self-contained (no cross-file selector leakage), with zero hardcoded fallback values and zero hardcoded spacing/color values. All TS class references updated to match new selector names.
</success_criteria>

<output>
After completion, create `.planning/phases/155-css-namespace-design-token-audit/155-01-SUMMARY.md`
</output>
