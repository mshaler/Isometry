---
status: complete
phase: 119-swift-critical-path-tests
source: [119-01-SUMMARY.md, 119-02-SUMMARY.md, 119-03-SUMMARY.md]
started: 2026-03-23T12:00:00Z
updated: 2026-03-24T17:43:00Z
---

## Current Test

[testing complete]

## Tests

### 1. All Swift Tests Pass
expected: Run `xcodebuild test` for the Isometry scheme. All ProtobufToMarkdownTests (17), SyncManagerTests (16), and NotesAdapterTests (14) pass with 0 failures.
result: pass

### 2. Multi-Paragraph Prefix Bug Fix
expected: The atParagraphStart fix in ProtobufToMarkdown.swift correctly applies paragraph prefixes (checklist, heading, list) to consecutive paragraph-style AttributeRuns, not just the first run. Verify by checking that extractChecklistFromProtobuf test passes with both done and todo items getting correct `- [x]` / `- [ ]` prefixes.
result: pass

### 3. CKRecord Field Round-Trip
expected: CKRecord setCardFields/cardFieldsDictionary round-trip preserves string, int, and double field values exactly. The 4 new CKRecord tests in SyncManagerTests verify encoding fidelity, unset field omission, null clearing, and all-types round-trip.
result: pass

### 4. NotesAdapter Fixture DB Schema Detection
expected: NotesAdapter tests create a temp SQLite DB with Apple NoteStore schema, run production-equivalent SQL queries, and verify schema detection (ZTITLE1, ZCREATIONDATE3, ZISPASSWORDPROTECTED, ZNOTEDATA columns), folder hierarchy resolution, and encrypted/deleted note filtering.
result: pass

### 5. ZDATA Protobuf+Gzip Round-Trip
expected: NotesAdapter zdataBlobExtraction and zdataBlobRoundTripThroughSQLite tests create gzip-compressed protobuf fixtures, store in SQLite, extract via ProtobufToMarkdown, and verify markdown body output matches input text.
result: pass

### 6. Note-to-Note Link Extraction
expected: The zdataWithNoteLinkProducesNoteLinks test creates a ZDATA fixture with an applenotes:note/TARGET-NOTE-ID link-run and verifies noteLinks.count == 1, targetIdentifier == "TARGET-NOTE-ID", displayName == "linked note".
result: pass

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
