---
phase: 09-error-elimination
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified: [
  "src/components/FilterPresetDropdown.tsx",
  "src/components/Toolbar.tsx",
  "src/components/__tests__/LocationMapWidget.test.tsx",
  "src/components/notebook/D3VisualizationRenderer.tsx",
  "src/App.tsx"
]
autonomous: true

must_haves:
  truths:
    - "No unused variable warnings in lint output"
    - "Clean component interfaces without dead code"
    - "Proper error handling without unused error parameters"
  artifacts:
    - path: "src/components/FilterPresetDropdown.tsx"
      provides: "Clean error handling implementation"
      contains: "handleError"
    - path: "src/components/Toolbar.tsx"
      provides: "Active submenu functionality"
      contains: "openSubmenu"
  key_links:
    - from: "error handlers"
      to: "error processing logic"
      via: "proper error handling"
      pattern: "catch.*error.*console"
    - from: "component state"
      to: "UI interactions"
      via: "event handlers"
      pattern: "onClick.*set.*State"
---

<objective>
Eliminate remaining lint warnings by removing unused variables and implementing proper error handling.

Purpose: Achieve zero lint warnings for clean production deployment and improved code quality.
Output: Clean codebase with no unused variables and proper error handling patterns.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Implement proper error handling and remove unused parameters</name>
  <files>
    src/components/FilterPresetDropdown.tsx
    src/components/__tests__/LocationMapWidget.test.tsx
  </files>
  <action>
    Fix error handling and remove unused variables:

    1. FilterPresetDropdown.tsx:
    - Replace `_error` parameters with proper error handling
    - Implement actual error processing (logging, user feedback)
    - Use error parameters for meaningful error handling or remove catch blocks if not needed
    - Add proper error state management if appropriate

    2. LocationMapWidget.test.tsx:
    - Remove unused `_rerender` variable or implement actual re-rendering tests
    - Clean up test setup to only include necessary variables
    - Ensure all declared variables are actually used in tests

    Focus on either using the variables meaningfully or removing them entirely.
  </action>
  <verify>npm run lint shows no unused variable warnings in these files</verify>
  <done>Error handling is properly implemented and no unused variables remain</done>
</task>

<task type="auto">
  <name>Implement submenu functionality or clean up unused state</name>
  <files>
    src/components/Toolbar.tsx
    src/components/notebook/D3VisualizationRenderer.tsx
  </files>
  <action>
    Resolve unused state variables:

    1. Toolbar.tsx:
    - Implement submenu functionality using openSubmenu/setOpenSubmenu
    - Add submenu UI components and interaction handlers
    - OR remove submenu state if not needed for current implementation
    - Ensure state variables are actually connected to UI interactions

    2. D3VisualizationRenderer.tsx:
    - Remove unused HierarchyData import if not needed
    - OR implement hierarchy data functionality if planned
    - Clean up imports to only include what's actually used

    Choose to either implement the functionality or remove unused code.
  </action>
  <verify>npm run lint shows no unused variable warnings for state management</verify>
  <done>Component state management is clean with no unused variables</done>
</task>

<task type="auto">
  <name>Remove unused imports and clean up App.tsx</name>
  <files>
    src/App.tsx
    src/components/notebook/CaptureComponent.tsx
  </files>
  <action>
    Clean up unused imports and variables:

    1. App.tsx:
    - Remove unused MVPDemo import if not used
    - Clean up any other unused imports
    - Ensure all components imported are actually rendered

    2. CaptureComponent.tsx (if needed):
    - Remove unused menu navigation variables (_navigateMenu, _closeMenu)
    - OR implement menu navigation functionality
    - Ensure all event handlers are properly connected

    Focus on eliminating the final unused variable warnings.
  </action>
  <verify>npm run lint reports zero unused variable warnings</verify>
  <done>All unused imports and variables eliminated</done>
</task>

</tasks>

<verification>
- npm run lint should report zero warnings
- All components should have clean, functional code
- No dead code or unused variables remain
</verification>

<success_criteria>
- Zero lint warnings across entire codebase
- Proper error handling implementation
- Clean component interfaces
- Production-ready code quality
</success_criteria>

<output>
After completion, create `.planning/phases/09-error-elimination/09-05-SUMMARY.md`
</output>