---
phase: 157-persistence-schemaprovider-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/PropertiesExplorer.ts
  - tests/ui/PropertiesExplorer.test.ts
autonomous: true
requirements:
  - BEHV-04
  - BEHV-05

must_haves:
  truths:
    - "PropertiesExplorer column collapse state persists via bridge ui:set, not localStorage"
    - "PropertiesExplorer depth persists via bridge ui:set, not localStorage"
    - "PropertiesExplorer restores collapse and depth state from bridge ui:get on mount"
    - "No localStorage calls remain in PropertiesExplorer (grep returns zero)"
  artifacts:
    - path: "src/ui/PropertiesExplorer.ts"
      provides: "Unified persistence via bridge ui:set/ui:get"
      contains: "ui:set"
    - path: "tests/ui/PropertiesExplorer.test.ts"
      provides: "Persistence migration tests"
      contains: "props:col-collapse"
  key_links:
    - from: "src/ui/PropertiesExplorer.ts"
      to: "WorkerBridge"
      via: "bridge.send('ui:set'/'ui:get')"
      pattern: "ui:set.*props:"
---

<objective>
Migrate PropertiesExplorer from triple persistence (localStorage + bridge ui:set) to single canonical bridge ui:set pattern for all durable state.

Purpose: BEHV-04 requires all explorers to use bridge ui:set for durable state. BEHV-05 requires PropertiesExplorer to use a single persistence path. Currently PropertiesExplorer has 6 localStorage calls (5 for column collapse, 1 for depth) alongside 2 correct bridge ui:set calls (latch:overrides, latch:disabled).
Output: PropertiesExplorer with zero localStorage usage; all state persisted via bridge ui:set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/ui/PropertiesExplorer.ts
@tests/ui/PropertiesExplorer.test.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/worker/WorkerBridge.ts:
```typescript
// bridge.send('ui:set', { key: string, value: string }): Promise<void>
// bridge.send('ui:get', { key: string }): Promise<{ value: string | null }>
```

From src/ui/LatchExplorers.ts:
```typescript
export interface WorkerBridgeLike {
  send(type: 'ui:set', payload: { key: string; value: string }): Promise<unknown>;
  send(type: 'ui:get', payload: { key: string }): Promise<{ value: string | null }>;
}
```

Current localStorage keys to migrate:
- `workbench:prop-col-L` through `workbench:prop-col-H` (5 keys, boolean "true"/"false")
- `workbench:prop-depth` (number as string: "0", "1", "2", "3")

Target bridge keys:
- `props:col-collapse` (JSON object: `{"L":true,"A":false,"T":false,"C":true,"H":false}`)
- `props:depth` (string number: "1")
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistence migration tests</name>
  <files>tests/ui/PropertiesExplorer.test.ts</files>
  <behavior>
    - Test: mount() reads collapse state from bridge ui:get key 'props:col-collapse'
    - Test: mount() reads depth from bridge ui:get key 'props:depth'
    - Test: toggling a column collapse writes to bridge ui:set key 'props:col-collapse' (not localStorage)
    - Test: changing depth writes to bridge ui:set key 'props:depth' (not localStorage)
    - Test: when bridge returns null for collapse/depth keys, defaults apply (all expanded, depth=1)
  </behavior>
  <action>
Add a new describe block 'persistence migration (BEHV-04, BEHV-05)' to the existing PropertiesExplorer test file. Create a mock WorkerBridgeLike that records send() calls and returns configurable values for ui:get. Write RED tests that:
1. Construct PropertiesExplorer with bridge mock, call mount(), verify bridge.send('ui:get', { key: 'props:col-collapse' }) was called
2. Construct with bridge mock returning '{"L":true}', mount(), verify L column has collapsed class
3. Construct with bridge mock returning '2' for props:depth, mount(), verify getDepth() returns 2
4. Mount, click column header to toggle, verify bridge.send('ui:set', { key: 'props:col-collapse', value: ... }) was called
5. Mount, change depth dropdown, verify bridge.send('ui:set', { key: 'props:depth', value: '...' }) was called
6. Mock bridge returning null for both keys, mount(), verify defaults (all expanded, depth=1)

IMPORTANT: mount() is currently synchronous. The tests should expect mount() to become async (returns Promise) since ui:get is async. Await mount() in tests.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/ui/PropertiesExplorer.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>New test block exists with 5+ tests. Tests initially fail (RED) because PropertiesExplorer still uses localStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate PropertiesExplorer to bridge persistence</name>
  <files>src/ui/PropertiesExplorer.ts</files>
  <action>
Per BEHV-04 and BEHV-05, migrate all 6 localStorage calls to bridge ui:set/ui:get:

1. Make mount() async (return Promise&lt;void&gt;). At the start of mount(), before building DOM:
   - Read collapse state: `await bridge.send('ui:get', { key: 'props:col-collapse' })` -- parse JSON into `_columnCollapsed` Map. On null/error, keep all expanded (default).
   - Read depth: `await bridge.send('ui:get', { key: 'props:depth' })` -- parse into `_depth`. On null/error, keep default (1).

2. Remove all localStorage reads from constructor (lines ~107-117):
   - Delete the `for (const family of LATCH_ORDER)` loop reading `workbench:prop-col-*`
   - Delete the `storedDepth` block reading `workbench:prop-depth`

3. Replace localStorage writes in _toggleColumn() (line ~364):
   - Replace `localStorage.setItem(...)` with `void this._persistCollapseState()`
   - Add `private async _persistCollapseState(): Promise<void>` that serializes `_columnCollapsed` as JSON and calls `bridge.send('ui:set', { key: 'props:col-collapse', value: ... })`

4. Replace localStorage write in depth change handler (line ~196):
   - Replace `localStorage.setItem('workbench:prop-depth', ...)` with `void this._persistDepth()`
   - Add `private async _persistDepth(): Promise<void>` that calls `bridge.send('ui:set', { key: 'props:depth', value: String(this._depth) })`

5. Guard: if `this._config.bridge` is undefined, skip persistence reads/writes silently (graceful degradation for tests without bridge).

6. Verify zero localStorage references remain: grep the file for 'localStorage' -- must return zero matches.

Do NOT touch any other behavior -- surgical change to persistence path only.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/ui/PropertiesExplorer.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All PropertiesExplorer tests pass. grep 'localStorage' src/ui/PropertiesExplorer.ts returns zero matches. mount() is async. Column collapse and depth state round-trip through bridge ui:set/ui:get.</done>
</task>

</tasks>

<verification>
- `grep -c 'localStorage' src/ui/PropertiesExplorer.ts` returns 0
- `npx vitest run tests/ui/PropertiesExplorer.test.ts` passes all tests
- `npx tsc --noEmit` passes (no type errors from async mount change)
</verification>

<success_criteria>
1. PropertiesExplorer has zero localStorage calls
2. Column collapse state persists via bridge ui:set key 'props:col-collapse'
3. Depth persists via bridge ui:set key 'props:depth'
4. mount() restores state from bridge ui:get before rendering
5. All existing PropertiesExplorer tests still pass
6. TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/157-persistence-schemaprovider-wiring/157-01-SUMMARY.md`
</output>
