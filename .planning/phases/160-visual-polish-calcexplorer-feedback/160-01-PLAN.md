---
phase: 160-visual-polish-calcexplorer-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/workbench.css
  - src/styles/algorithm-explorer.css
  - src/styles/visual-explorer.css
  - src/styles/projection-explorer.css
  - src/styles/latch-explorers.css
  - src/styles/properties-explorer.css
  - src/styles/data-explorer.css
  - src/styles/notebook-explorer.css
  - src/styles/catalog-actions.css
autonomous: true
requirements: [VCSS-05, VCSS-06]

must_haves:
  truths:
    - "DockNav strip, top-slot explorers, view content, and bottom-slot explorers have distinct visual boundaries"
    - "All explorer headers use --text-base at font-weight 600"
    - "All explorer labels use --text-sm at font-weight 400"
    - "All explorer meta/hints use --text-xs at font-weight 400"
  artifacts:
    - path: "src/styles/workbench.css"
      provides: "Slot-level padding, collapsible header typography, calc-row token migration"
      contains: "padding: var(--space-md)"
    - path: "src/styles/algorithm-explorer.css"
      provides: "Typography hierarchy alignment"
    - path: "src/styles/data-explorer.css"
      provides: "Typography hierarchy alignment"
  key_links:
    - from: "src/styles/workbench.css"
      to: ".workbench-slot-top, .workbench-slot-bottom"
      via: "padding declaration"
      pattern: "padding: var\\(--space-md\\)"
    - from: "src/styles/workbench.css"
      to: ".collapsible-section__header"
      via: "font-size upgrade"
      pattern: "font-size: var\\(--text-base\\)"
---

<objective>
Establish clear visual boundaries between layout zones and standardize the 3-tier typography hierarchy across all 8 explorer panels.

Purpose: VCSS-05 requires distinct boundaries between DockNav/slots/view-content via border+spacing; VCSS-06 requires a consistent typography scale across all explorers.
Output: Updated CSS across workbench.css and all 8 explorer CSS files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-visual-polish-calcexplorer-feedback/160-CONTEXT.md
@.planning/phases/160-visual-polish-calcexplorer-feedback/160-UI-SPEC.md

<interfaces>
<!-- Design tokens from src/styles/design-tokens.css -->
Typography scale:
  --text-xs: 10px
  --text-sm: 11px
  --text-base: 13px
Spacing scale:
  --space-xs: 4px
  --space-sm: 8px
  --space-md: 12px
  --space-lg: 16px

<!-- Current collapsible section header (workbench.css line 169-183) -->
.collapsible-section__header {
  font-size: var(--text-sm);    /* NEEDS CHANGE: --text-sm (11px) -> --text-base (13px) per D-04 */
  font-weight: 600;             /* Already correct */
}

<!-- Current calc-row styles (workbench.css lines 548-573) -->
.calc-row {
  padding: 4px 8px;            /* NEEDS CHANGE: hardcoded -> var(--space-xs) var(--space-sm) */
  gap: 8px;                    /* NEEDS CHANGE: hardcoded -> var(--space-sm) */
}
.calc-row label {
  font-size: var(--text-sm, 13px);  /* NEEDS CHANGE: remove fallback, already correct token */
}
.calc-select {
  font-size: var(--text-xs, 11px);  /* NEEDS CHANGE: remove fallback */
  padding: 2px 4px;                 /* NEEDS CHANGE: -> var(--space-1, 2px) var(--space-xs) */
  border-radius: 4px;               /* NEEDS CHANGE: -> var(--radius-sm) */
  background: var(--bg-primary, #fff);  /* NEEDS CHANGE: remove fallback */
}

<!-- Current slot containers (workbench.css lines 77-107) -->
.workbench-slot-top — no padding currently
.workbench-slot-bottom — no padding currently
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slot-level padding + collapsible header typography + calc-row token migration</name>
  <files>src/styles/workbench.css</files>
  <read_first>
    - src/styles/workbench.css
    - src/styles/design-tokens.css
    - .planning/phases/160-visual-polish-calcexplorer-feedback/160-UI-SPEC.md
  </read_first>
  <action>
Edit workbench.css with these specific changes:

**Slot padding (VCSS-05, per D-03):**
- `.workbench-slot-top` (line ~77): add `padding: var(--space-md);`
- `.workbench-slot-bottom` (line ~98): add `padding: var(--space-md);`

**Collapsible section header typography (VCSS-06, per D-04):**
- `.collapsible-section__header` (line ~178): change `font-size: var(--text-sm)` to `font-size: var(--text-base)`
  (This upgrades ALL explorer panel headers from 11px to 13px with semibold 600 — already correct weight.)

**Calc-row token migration (zero-hardcoded rule from Phase 155 D-04):**
- `.calc-row` (line ~548): change `padding: 4px 8px` to `padding: var(--space-xs) var(--space-sm)`
- `.calc-row` (line ~548): change `gap: 8px` to `gap: var(--space-sm)`
- `.calc-row label` (line ~556): change `font-size: var(--text-sm, 13px)` to `font-size: var(--text-sm)` (remove fallback)
- `.calc-select` (line ~565): change `font-size: var(--text-xs, 11px)` to `font-size: var(--text-xs)` (remove fallback)
- `.calc-select` (line ~565): change `padding: 2px 4px` to `padding: 2px var(--space-xs)` (keep structural 2px, tokenize 4px)
- `.calc-select` (line ~565): change `border-radius: 4px` to `border-radius: var(--radius-sm)`
- `.calc-select` (line ~565): change `background: var(--bg-primary, #fff)` to `background: var(--bg-primary)` (remove fallback)

Do NOT change anything about DockNav (per D-02). Do NOT add background color shifts between zones (per D-01 — borders only).
  </action>
  <verify>
    <automated>grep -n 'padding: 4px 8px\|gap: 8px\|, 13px)\|, 11px)\|, #fff)' src/styles/workbench.css | wc -l | grep '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - workbench.css `.workbench-slot-top` contains `padding: var(--space-md)`
    - workbench.css `.workbench-slot-bottom` contains `padding: var(--space-md)`
    - workbench.css `.collapsible-section__header` contains `font-size: var(--text-base)` (NOT --text-sm)
    - workbench.css `.calc-row` contains `padding: var(--space-xs) var(--space-sm)` and `gap: var(--space-sm)`
    - workbench.css contains zero instances of `4px 8px`, `, 13px)`, `, 11px)`, `, #fff)` (no hardcoded fallbacks in calc-row section)
    - workbench.css `.calc-select` contains `border-radius: var(--radius-sm)` and `background: var(--bg-primary)` (no fallback)
  </acceptance_criteria>
  <done>Slot containers have consistent padding, collapsible headers upgraded to --text-base, calc-row has zero hardcoded values</done>
</task>

<task type="auto">
  <name>Task 2: Typography audit across all 8 explorer CSS files</name>
  <files>
    src/styles/algorithm-explorer.css
    src/styles/visual-explorer.css
    src/styles/projection-explorer.css
    src/styles/latch-explorers.css
    src/styles/properties-explorer.css
    src/styles/data-explorer.css
    src/styles/notebook-explorer.css
    src/styles/catalog-actions.css
  </files>
  <read_first>
    - src/styles/algorithm-explorer.css
    - src/styles/visual-explorer.css
    - src/styles/projection-explorer.css
    - src/styles/latch-explorers.css
    - src/styles/properties-explorer.css
    - src/styles/data-explorer.css
    - src/styles/notebook-explorer.css
    - src/styles/catalog-actions.css
    - .planning/phases/160-visual-polish-calcexplorer-feedback/160-UI-SPEC.md
  </read_first>
  <action>
Audit all 8 explorer CSS files against the 3-tier typography hierarchy (per D-04 through D-07):

**Tier 1 — Headers: `font-size: var(--text-base)` + `font-weight: 600`**
Explorer panel headers are `.collapsible-section__header` in workbench.css (handled in Task 1). Per-explorer section titles or legend elements that serve as sub-headers should also use `--text-base` + 600:
- `algorithm-explorer.css`: `.algorithm-explorer__radios legend` currently `font-weight: 600; font-size: var(--text-sm)` — change to `font-size: var(--text-base)`
- `data-explorer.css`: `.data-explorer__section-heading` (line ~24) `font-size: var(--text-sm); font-weight: 600` — change to `font-size: var(--text-base)`
- `data-explorer.css`: `.data-explorer__catalog-heading` (line ~150) `font-size: var(--text-sm); font-weight: 600` — change to `font-size: var(--text-base)`

**Tier 2 — Labels: `font-size: var(--text-sm)` + `font-weight: 400`**
These are already correct in most explorers. Verify and fix any outliers:
- `data-explorer.css`: `.data-explorer__field-value` (line ~108) has `font-weight: 500` — change to `font-weight: 400`
- `notebook-explorer.css`: `.notebook-explorer__tab` (line ~109) has `font-weight: 500` — change to `font-weight: 400`
- All other `--text-sm` elements with `font-weight: 400` or implicit normal are already correct — no change needed.

**Tier 3 — Meta/hints: `font-size: var(--text-xs)` + `font-weight: 400`**
Already correct across all explorers. Verify no outliers.

**Special cases to leave alone:**
- `visual-explorer.css` line 104: `font-size: 9px` with comment `/* structural: below token scale */` — leave as-is
- `properties-explorer.css` line 172: `font-size: 8px` with comment `/* structural: below token scale */` — leave as-is
- `latch-explorers.css` line 209: `font-size: 9px` — leave as-is (structural below token scale)
- `notebook-explorer.css` line 23: `font-size: var(--text-lg)` on `.notebook-explorer__title-input` — leave as-is (this is the card title, not an explorer header)

Do NOT touch any non-typography properties. Do NOT add new selectors. Do NOT change font-weight values that are already correct per the tier they belong to.
  </action>
  <verify>
    <automated>grep -n 'font-size\|font-weight' src/styles/algorithm-explorer.css src/styles/visual-explorer.css src/styles/projection-explorer.css src/styles/latch-explorers.css src/styles/properties-explorer.css src/styles/data-explorer.css src/styles/notebook-explorer.css src/styles/catalog-actions.css | grep -v '9px\|8px\|text-lg\|text-xl' | grep 'font-weight: 500' | wc -l | grep '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - algorithm-explorer.css `.algorithm-explorer__radios legend` contains `font-size: var(--text-base)`
    - data-explorer.css `.data-explorer__section-heading` contains `font-size: var(--text-base)`
    - data-explorer.css `.data-explorer__catalog-heading` contains `font-size: var(--text-base)`
    - data-explorer.css `.data-explorer__field-value` contains `font-weight: 400` (not 500)
    - notebook-explorer.css `.notebook-explorer__tab` contains `font-weight: 400` (not 500)
    - No explorer CSS file contains `font-weight: 500` (all weights are 400 or 600, matching the 3-tier scale)
    - structural below-token-scale sizes (9px, 8px) are untouched
  </acceptance_criteria>
  <done>All 8 explorer CSS files follow the 3-tier typography hierarchy: headers at --text-base/600, labels at --text-sm/400, meta at --text-xs/400</done>
</task>

</tasks>

<verification>
- `grep -rn 'padding: 4px 8px\|gap: 8px' src/styles/workbench.css` returns no matches (zero hardcoded values in calc-row)
- `grep -n 'font-size: var(--text-base)' src/styles/workbench.css` matches `.collapsible-section__header`
- `grep -n 'padding: var(--space-md)' src/styles/workbench.css` matches both `.workbench-slot-top` and `.workbench-slot-bottom`
- `grep -rn 'font-weight: 500' src/styles/*-explorer*.css` returns no matches
- `npm run build` succeeds (CSS parsed without errors)
</verification>

<success_criteria>
1. Slot containers have `padding: var(--space-md)` providing consistent spacing around embedded explorers
2. All collapsible section headers display at 13px semibold (--text-base, 600)
3. All explorer labels display at 11px normal (--text-sm, 400) — no stray font-weight: 500
4. All explorer meta/hints display at 10px normal (--text-xs, 400)
5. Calc-row section has zero hardcoded px/color values
6. No changes to DockNav styling; no background color shifts between zones
</success_criteria>

<output>
After completion, create `.planning/phases/160-visual-polish-calcexplorer-feedback/160-01-SUMMARY.md`
</output>
