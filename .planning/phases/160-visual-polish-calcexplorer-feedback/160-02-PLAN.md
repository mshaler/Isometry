---
phase: 160-visual-polish-calcexplorer-feedback
plan: 02
type: execute
wave: 2
depends_on: ["160-01"]
files_modified:
  - src/ui/CalcExplorer.ts
  - src/styles/workbench.css
autonomous: true
requirements: [EXPX-08, EXPX-09]

must_haves:
  truths:
    - "CalcExplorer columns with active aggregations (not 'off') show bold primary-colored label text"
    - "CalcExplorer columns with 'off' aggregation show normal secondary-colored label text"
    - "CalcExplorer numeric columns display a # glyph prefix in muted color"
    - "CalcExplorer text columns display an Aa glyph prefix in muted color"
  artifacts:
    - path: "src/ui/CalcExplorer.ts"
      provides: "Type glyph span + active class toggle in _render()"
      contains: "calc-row__type-glyph"
    - path: "src/styles/workbench.css"
      provides: "CSS rules for .calc-row--active and .calc-row__type-glyph"
      contains: "calc-row--active"
  key_links:
    - from: "src/ui/CalcExplorer.ts"
      to: "src/styles/workbench.css"
      via: ".calc-row--active class toggle and .calc-row__type-glyph element"
      pattern: "calc-row--active"
    - from: "src/ui/CalcExplorer.ts"
      to: "CalcExplorer._isNumeric()"
      via: "type glyph selection"
      pattern: "_isNumeric.*#.*Aa"
---

<objective>
Add visual feedback to CalcExplorer: active aggregation indicator (bold+primary label) and column type glyphs (# for numeric, Aa for text).

Purpose: EXPX-08 makes active aggregations visually distinguishable from disabled ones; EXPX-09 helps users identify which columns support numeric aggregations.
Output: Updated CalcExplorer._render() and new CSS rules in workbench.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-visual-polish-calcexplorer-feedback/160-CONTEXT.md
@.planning/phases/160-visual-polish-calcexplorer-feedback/160-UI-SPEC.md
@.planning/phases/160-visual-polish-calcexplorer-feedback/160-01-SUMMARY.md

<interfaces>
<!-- CalcExplorer._render() creates rows with this structure (src/ui/CalcExplorer.ts lines 188-263): -->
<!-- Current: <div class="calc-row"> <label for="calc-select-{field}">{displayName}</label> <select>...</select> </div> -->
<!-- Target:  <div class="calc-row calc-row--active?"> <label><span class="calc-row__type-glyph">#|Aa</span>{displayName}</label> <select>...</select> </div> -->

CalcExplorer._isNumeric(field: string): boolean
  — Returns true if field supports numeric aggregation (SUM, AVG, MIN, MAX)
  — Uses SchemaProvider.getNumericColumns() when available

CalcExplorer._displayName(field: string): string
  — Returns alias-aware display name for a field

CalcConfig.columns: Record<string, AggregationMode | 'off'>
  — Per-column aggregate setting; 'off' means disabled

<!-- CSS after Plan 01 (workbench.css calc-row section): -->
.calc-row { padding: var(--space-xs) var(--space-sm); gap: var(--space-sm); }
.calc-row label { font-size: var(--text-sm); color: var(--text-secondary); }
.calc-select { font-size: var(--text-xs); background: var(--bg-primary); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalcExplorer._render() — type glyph + active class</name>
  <files>src/ui/CalcExplorer.ts</files>
  <read_first>
    - src/ui/CalcExplorer.ts
    - .planning/phases/160-visual-polish-calcexplorer-feedback/160-UI-SPEC.md
  </read_first>
  <action>
Modify `_render()` method in CalcExplorer.ts (around line 218-260) with these changes:

**1. Active class on row (EXPX-08, per D-08):**
After `row.className = 'calc-row';` (line ~228), add:
```typescript
if (currentValue !== 'off') {
  row.classList.add('calc-row--active');
}
```
This must use the `currentValue` variable already defined at line ~225 (`this._config.columns[field] ?? defaultMode`).

**2. Type glyph span (EXPX-09, per D-10, D-11):**
Before setting `label.textContent = displayName` (line ~232), replace with:
```typescript
const glyph = document.createElement('span');
glyph.className = 'calc-row__type-glyph';
glyph.textContent = isNumeric ? '#' : 'Aa';
label.appendChild(glyph);
label.appendChild(document.createTextNode(displayName));
```
Remove the existing `label.textContent = displayName;` line since we now use appendChild.

**3. Update the label.htmlFor assignment:**
Keep `label.htmlFor = selectId;` as-is — it still works with child nodes.

**4. Also fix the empty state message (line ~209):**
Change `emptyMsg.style.fontSize = 'var(--text-sm, 13px)';` to `emptyMsg.style.fontSize = 'var(--text-sm)';` (remove fallback per Phase 155 D-03).

Do NOT change the select element styling (per D-09 — select stays neutral). Do NOT change the change event handler. Do NOT modify _isNumeric() or _displayName().
  </action>
  <verify>
    <automated>grep -c 'calc-row--active\|calc-row__type-glyph' src/ui/CalcExplorer.ts | grep -E '^[2-9]'</automated>
  </verify>
  <acceptance_criteria>
    - CalcExplorer.ts `_render()` contains `row.classList.add('calc-row--active')` guarded by `currentValue !== 'off'`
    - CalcExplorer.ts `_render()` creates a `<span>` with className `calc-row__type-glyph`
    - CalcExplorer.ts glyph textContent is `'#'` when `isNumeric` is true, `'Aa'` when false
    - CalcExplorer.ts does NOT contain `var(--text-sm, 13px)` (fallback removed from empty state)
    - CalcExplorer.ts label no longer uses `label.textContent = displayName` (uses appendChild instead)
  </acceptance_criteria>
  <done>CalcExplorer rows have .calc-row--active class when aggregation is not 'off', and type glyph spans are prepended to all column labels</done>
</task>

<task type="auto">
  <name>Task 2: CSS rules for .calc-row--active and .calc-row__type-glyph</name>
  <files>src/styles/workbench.css</files>
  <read_first>
    - src/styles/workbench.css
    - .planning/phases/160-visual-polish-calcexplorer-feedback/160-UI-SPEC.md
  </read_first>
  <action>
Add new CSS rules to workbench.css in the CalcExplorer section (after the existing `.calc-select` rule, before the `/* Crossfade transition */` comment):

**1. Active row label styling (EXPX-08, per D-08):**
```css
.calc-row--active label {
  font-weight: 600;
  color: var(--text-primary);
}
```
This overrides the base `.calc-row label` which has `font-weight: 400` (implicit) and `color: var(--text-secondary)`.

**2. Type glyph styling (EXPX-09, per D-10, D-11):**
```css
.calc-row__type-glyph {
  color: var(--text-muted);
  margin-right: var(--space-xs);
  font-size: var(--text-xs);
  font-weight: 400;
}
```
The `font-weight: 400` on the glyph ensures it stays normal-weight even inside `.calc-row--active label` (which sets `font-weight: 600`). The `font-size: var(--text-xs)` makes it slightly smaller than the label text. `color: var(--text-muted)` does not change with active/inactive state (per D-11). `margin-right: var(--space-xs)` provides 4px gap between glyph and label text.

Do NOT add any other rules. Do NOT modify existing `.calc-row` or `.calc-select` rules (those were already updated in Plan 01).
  </action>
  <verify>
    <automated>grep -c 'calc-row--active\|calc-row__type-glyph' src/styles/workbench.css | grep -E '^[2-9]'</automated>
  </verify>
  <acceptance_criteria>
    - workbench.css contains `.calc-row--active label` rule with `font-weight: 600` and `color: var(--text-primary)`
    - workbench.css contains `.calc-row__type-glyph` rule with `color: var(--text-muted)`, `margin-right: var(--space-xs)`, `font-size: var(--text-xs)`, `font-weight: 400`
    - No hardcoded px or color values in the new rules (all use design tokens)
    - No fallback values in any `var()` call (per Phase 155 D-03)
  </acceptance_criteria>
  <done>CSS rules for active aggregation indicator and type glyph are in place with correct token usage</done>
</task>

</tasks>

<verification>
- `grep -n 'calc-row--active' src/ui/CalcExplorer.ts src/styles/workbench.css` shows class used in both TS and CSS
- `grep -n 'calc-row__type-glyph' src/ui/CalcExplorer.ts src/styles/workbench.css` shows element created in TS and styled in CSS
- `grep -n "isNumeric ? '#' : 'Aa'" src/ui/CalcExplorer.ts` confirms type glyph logic
- `npm run build` succeeds
- `npx vitest run --reporter=verbose 2>&1 | tail -5` shows no test regressions
</verification>

<success_criteria>
1. CalcExplorer rows with active aggregations (not 'off') display label in --text-primary at font-weight 600
2. CalcExplorer rows with 'off' aggregation display label in --text-secondary at normal weight (default)
3. Every CalcExplorer column label is prefixed with # (numeric) or Aa (text) in --text-muted color
4. Type glyph color does not change between active and inactive states
5. Select dropdown stays visually neutral regardless of aggregation state
6. No hardcoded values in new CSS rules
</success_criteria>

<output>
After completion, create `.planning/phases/160-visual-polish-calcexplorer-feedback/160-02-SUMMARY.md`
</output>
