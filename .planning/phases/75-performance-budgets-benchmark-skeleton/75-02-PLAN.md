---
phase: 75-performance-budgets-benchmark-skeleton
plan: 02
type: execute
wave: 2
depends_on: [75-01]
files_modified:
  - tests/profiling/budget-render.test.ts
  - .benchmarks/main.json
autonomous: true
requirements: [RGRD-01, RGRD-03]

must_haves:
  truths:
    - "budget-render.test.ts runs in jsdom environment (// @vitest-environment jsdom comment)"
    - "Render budget assertions use it() + performance.now() timing with the same SuperGrid _renderCells pattern from render-timing.test.ts"
    - "Dual-axis 5K (506ms jsdom baseline) and triple-axis 20K (259.4ms baseline) assertions FAIL against BUDGET_RENDER_JSDOM_MS (128ms)"
    - "Single-axis 20K (37.8ms jsdom baseline) assertion PASSES against BUDGET_RENDER_JSDOM_MS (128ms)"
    - ".benchmarks/main.json contains all Phase 74 BOTTLENECKS.md measurements in a hand-authored schema (not vitest reporter JSON)"
    - ".benchmarks/main.json is committed to the repo for Phase 78 regression comparison"
    - ".benchmarks/ directory is NOT listed in .gitignore"
  artifacts:
    - path: "tests/profiling/budget-render.test.ts"
      provides: "Failing render budget assertions in jsdom environment"
    - path: ".benchmarks/main.json"
      provides: "Committed baseline measurements from Phase 74 for future regression detection"
  key_links:
    - from: "tests/profiling/budget-render.test.ts"
      to: "src/profiling/PerfBudget.ts"
      via: "import BUDGET_RENDER_JSDOM_MS"
      pattern: "import.*BUDGET_RENDER_JSDOM_MS"
    - from: "tests/profiling/budget-render.test.ts"
      to: "src/views/SuperGrid.ts"
      via: "import SuperGrid"
      pattern: "import.*SuperGrid"
---

<objective>
Create budget-render.test.ts with SuperGrid render timing assertions in jsdom, and commit .benchmarks/main.json baseline from Phase 74 data.

Purpose: Completes the TDD "red" step for render performance (dual/triple axis fail against 128ms jsdom budget) and establishes the committed baseline JSON that Phase 78 CI regression detection will compare against.
Output: budget-render.test.ts with mix of passing/failing assertions, .benchmarks/main.json committed to repo.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Depends on 75-01: PerfBudget.ts must exist (provides BUDGET_RENDER_JSDOM_MS = 128).

Render timing pattern from tests/profiling/render-timing.test.ts:
- jsdom environment (// @vitest-environment jsdom)
- makeSyntheticCells(), makeMockBridge(), makeMockProvider(), makeMockFilter(), makeMockDensity(), makeMockCoordinator() helpers
- mulberry32 PRNG for deterministic cell generation
- generateCombinations() for axis value cartesian product
- timeRender() helper: mount SuperGrid, call _renderCells(), measure with performance.now()
- p99() helper for percentile calculation
- SINGLE/DUAL/TRIPLE axis config constants

Budget derivation from Phase 74 BOTTLENECKS.md:
- Chrome 60fps frame = 16ms, jsdom factor = 8x -> BUDGET_RENDER_JSDOM_MS = 128ms
- single axis 20K jsdom p99: 37.8ms -> PASS (37.8 < 128)
- dual axis 5K jsdom p99: 506.0ms -> FAIL (506 > 128)
- triple axis 20K jsdom p99: 259.4ms -> FAIL (259.4 > 128)

.benchmarks/main.json schema: hand-authored JSON from Phase 74 data.
- NOT vitest reporter output (bench() has empty-samples bug in forks pool).
- Custom schema designed for Phase 78 regression comparison.
- Contains all measurements from BOTTLENECKS.md.

CRITICAL CONSTRAINTS:
- jsdom environment for render tests (separate file from node-env SQL/ETL tests).
- Copy ALL mock/helper functions from render-timing.test.ts — do not import from it (each test file is isolated in forks pool).
- Verify .benchmarks/ is NOT in .gitignore before committing main.json.
- Biome clean on all new files.
</context>

<tasks>
<task id="1">
<title>Create tests/profiling/budget-render.test.ts with render budget assertions</title>
<what>Create a jsdom-environment vitest test file with it()-based render timing assertions against BUDGET_RENDER_JSDOM_MS. Some must FAIL (dual/triple axis) and some must PASS (single axis).</what>
<how>
1. Create `tests/profiling/budget-render.test.ts` with `// @vitest-environment jsdom` at the top.
2. Import `BUDGET_RENDER_JSDOM_MS` from `../../src/profiling/PerfBudget`.
3. Copy ALL helper functions from `tests/profiling/render-timing.test.ts`:
   - mulberry32, generateCombinations, makeSyntheticCells
   - makeMockBridge, makeMockProvider, makeMockFilter, makeMockDensity, makeMockCoordinator
   - p99, timeRender
   - SINGLE, DUAL, TRIPLE axis config constants
   - Type imports: AxisMapping from providers/types, CellDatum from worker/protocol

4. Create describe('SuperGrid Render Budget Assertions'):
   - beforeAll: create container div, append to document.body.
   - afterAll: remove container.

   - it('single axis 20K renders within budget', () => { ... })
     Run timeRender(SINGLE.colAxes, SINGLE.rowAxes, 20_000, seed, 5, container).
     expect(result.p99ms).toBeLessThan(BUDGET_RENDER_JSDOM_MS).
     This should PASS (baseline 37.8ms < 128ms).
     Timeout: 60_000.

   - it('dual axis 5K renders within budget', () => { ... })
     Run timeRender(DUAL.colAxes, DUAL.rowAxes, 5_000, seed, 5, container).
     expect(result.p99ms).toBeLessThan(BUDGET_RENDER_JSDOM_MS).
     This MUST FAIL (baseline 506ms > 128ms).
     Timeout: 60_000.

   - it('triple axis 20K renders within budget', () => { ... })
     Run timeRender(TRIPLE.colAxes, TRIPLE.rowAxes, 20_000, seed, 3, container).
     expect(result.p99ms).toBeLessThan(BUDGET_RENDER_JSDOM_MS).
     This MUST FAIL (baseline 259.4ms > 128ms).
     Timeout: 60_000.

5. Add comments documenting the jsdom factor: "jsdom is 5-10x slower than Chrome. BUDGET_RENDER_JSDOM_MS = 128ms = 16ms Chrome frame * 8x jsdom factor."
6. Run `biome check tests/profiling/budget-render.test.ts` — must pass.
7. Run `npx vitest run tests/profiling/budget-render.test.ts` — confirm single-axis PASSES, dual and triple FAIL.

**Done-when:** File exists, runs in jsdom, single-axis passes, dual/triple fail. Biome clean.
</how>
</task>

<task id="2">
<title>Create .benchmarks/main.json baseline from Phase 74 data</title>
<what>Create the committed baseline JSON file containing all Phase 74 measured values for Phase 78 regression comparison.</what>
<how>
1. Verify `.benchmarks/` is not excluded by `.gitignore`:
   ```bash
   grep -n benchmarks .gitignore
   ```
   If no match, proceed. If excluded, remove the exclusion line.

2. Create `.benchmarks/main.json` with the following hand-authored schema:
   ```json
   {
     "generated": "2026-03-12",
     "phase": "74",
     "note": "Baseline measurements from Phase 74 BOTTLENECKS.md. Hand-authored schema (not vitest reporter) due to bench() empty-samples bug in forks pool. Phase 78 CI regression detection compares against these values using relative thresholds.",
     "measurements": {
       "sql_group_by_folder_card_type_20k_p99_ms": 24.93,
       "sql_group_by_status_20k_p99_ms": 1.87,
       "sql_strftime_month_20k_p99_ms": 20.64,
       "sql_fts_3word_20k_p99_ms": 1.70,
       "etl_apple_notes_20k_ms": 182,
       "etl_csv_20k_ms": 767,
       "etl_json_20k_ms": 1771,
       "etl_markdown_20k_ms": 1059,
       "render_single_20k_jsdom_p99_ms": 37.8,
       "render_dual_5k_jsdom_p99_ms": 506.0,
       "render_triple_20k_jsdom_p99_ms": 259.4,
       "bundle_js_total_gzip_kb": 361,
       "bundle_wasm_gzip_kb": 370
     },
     "budgets": {
       "query_group_by_20k_ms": 12,
       "query_strftime_20k_ms": 10,
       "query_status_20k_ms": 5,
       "query_fts_20k_ms": 5,
       "etl_20k_ms": 1000,
       "render_jsdom_ms": 128,
       "launch_cold_ms": 3000,
       "heap_steady_mb": 150
     }
   }
   ```

3. Verify JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('.benchmarks/main.json','utf8'))"`

**Done-when:** .benchmarks/main.json exists, is valid JSON, contains all Phase 74 measurements and Phase 75 budget targets, and is tracked by git (not gitignored).
</how>
</task>
</tasks>

<verification>
1. `biome check tests/profiling/budget-render.test.ts` — zero diagnostics
2. `npx vitest run tests/profiling/budget-render.test.ts` — single-axis PASSES, dual/triple FAIL
3. `node -e "JSON.parse(require('fs').readFileSync('.benchmarks/main.json','utf8'))"` — valid JSON
4. `grep -c benchmarks .gitignore` — 0 (not excluded)
5. `cat .benchmarks/main.json | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8')); console.log(Object.keys(j.measurements).length)"` — 13 measurements
6. `npm run bench:budgets` — runs both budget test files (from 75-01 script), produces mix of PASS/FAIL
</verification>
