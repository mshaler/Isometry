---
phase: 135.1-ui-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/supergrid.css
  - src/styles/design-tokens.css
  - src/views/supergrid/SuperZoom.ts
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-09
must_haves:
  truths:
    - "SuperGrid data cells and header cells use design token colors instead of hardcoded values"
    - "SuperGrid zoom (--sg-zoom) applies correctly when user scrolls/pinches in VisualExplorer"
    - "SuperGrid density size levels (1x/2x/5x) change row height and cell padding"
    - "SuperGrid appearance matches the design token theme (dark/light/nextstep/material)"
  artifacts:
    - path: "src/styles/supergrid.css"
      provides: "SuperGrid CSS reconnected to design token system"
      contains: "var(--sg-header-bg)"
    - path: "src/styles/design-tokens.css"
      provides: "SuperGrid token definitions for all themes"
      contains: "--sg-cell-alt-bg"
  key_links:
    - from: "src/styles/supergrid.css"
      to: "src/styles/design-tokens.css"
      via: "CSS custom property references"
      pattern: "var\\(--sg-"
    - from: "src/views/supergrid/SuperZoom.ts"
      to: "SuperPositionProvider"
      via: "zoomLevel getter/setter"
      pattern: "_positionProvider\\.zoomLevel"
---

<objective>
Fix 5 SuperGrid styling regressions from the plugin architecture migration by reconnecting CSS to the existing design token system and restoring zoom/density functionality.

Purpose: SuperGrid's visual appearance diverged during the v8.0 plugin migration. This plan reconnects all SuperGrid CSS classes to the existing `--sg-*` and `--color-*` design tokens, and ensures zoom and density controls function correctly.
Output: Fixed supergrid.css, verified design-tokens.css coverage, working zoom and density.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135.1-ui-ux-polish/135.1-CONTEXT.md
@.planning/phases/135.1-ui-ux-polish/135.1-UI-SPEC.md

<interfaces>
From src/styles/design-tokens.css (existing token definitions):
```css
/* Dark theme (default) */
--sg-header-bg: var(--bg-surface);
--sg-gridline: var(--border-subtle);
--sg-cell-padding-spreadsheet: 3px;
--sg-cell-padding-matrix: 6px;
--sg-cell-font-size: var(--text-sm);
```

From src/views/supergrid/SuperZoom.ts:
```typescript
export class SuperZoom {
  attach(gridEl: HTMLElement): void;
  detach(): void;
  applyZoom(): void;  // Sets --sg-col-width, --sg-row-height, --sg-zoom on gridEl
  resetZoom(): void;
}
```

From src/providers/SuperPositionProvider.ts:
```typescript
export class SuperPositionProvider {
  get zoomLevel(): number;
  set zoomLevel(value: number);
}
export const ZOOM_MIN = 0.5;
export const ZOOM_MAX = 3.0;
export const ZOOM_DEFAULT = 1.0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix SuperGrid CSS token reconnection (D-04, D-05, D-09)</name>
  <files>src/styles/supergrid.css, src/styles/design-tokens.css</files>
  <read_first>
    - src/styles/supergrid.css (full file — find all hardcoded hex colors, font sizes, and padding values)
    - src/styles/design-tokens.css (full file — find all --sg-* token definitions across all theme blocks)
    - .planning/phases/135.1-ui-ux-polish/135.1-UI-SPEC.md (SuperGrid Color Reconnection table)
  </read_first>
  <action>
**Step 1: Audit supergrid.css for hardcoded values.**
Search for any CSS rule using:
- Hardcoded hex colors (#xxx, #xxxxxx, rgb(), rgba() with literal values)
- Hardcoded font-size values (11px, 12px, 14px etc.) that should use var(--sg-cell-font-size)
- Hardcoded padding values that should use var(--sg-cell-padding-spreadsheet) or var(--sg-cell-padding-matrix)

**Step 2: Replace all hardcoded values with token references per UI-SPEC table:**
- Header backgrounds: `var(--sg-header-bg)` (which resolves to `var(--bg-surface)`)
- Cell borders/gridlines: `var(--sg-gridline)` (which resolves to `var(--border-subtle)`)
- Cell text size: `calc(var(--sg-cell-font-size) * var(--sg-zoom, 1))`
- Selection background: `var(--selection-bg)` (existing token)
- Selection outline: `var(--selection-outline)` (existing token)

**Step 3: Add missing --sg-cell-alt-bg token to design-tokens.css.**
In each theme block of design-tokens.css, add:
```css
/* Dark theme */
--sg-cell-alt-bg: rgba(255, 255, 255, 0.025);

/* Light theme */
--sg-cell-alt-bg: rgba(0, 0, 0, 0.02);
```
And reference it in supergrid.css for alternating row tint (if a `.sg-cell:nth-child(even)` or similar pattern exists, or add one).

**Step 4: Verify all 5 theme blocks in design-tokens.css have --sg-* tokens.**
The 5 themes are: dark (default `:root`), light (`[data-theme="light"]`), system (media query), nextstep (`[data-theme="nextstep"]`), material (`[data-theme="material"]`).
Each must have: `--sg-header-bg`, `--sg-gridline`, `--sg-cell-font-size`, `--sg-cell-padding-spreadsheet`, `--sg-cell-padding-matrix`, `--sg-cell-alt-bg`.

**Step 5: Verify `.sg-cell` and `.sg-header` classes use token references (they already do per supergrid.css lines 39-60 — confirm no regressions).**

Do NOT create new tokens beyond --sg-cell-alt-bg. Do NOT change token values — only reconnect CSS classes to existing tokens.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && grep -rn '#[0-9a-fA-F]\{3,6\}' src/styles/supergrid.css | grep -v '\/\*' | grep -v 'content-visibility' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - supergrid.css contains zero hardcoded hex color values in active CSS rules (comments excluded)
    - supergrid.css `.sg-cell` uses `var(--sg-gridline)` for borders and `var(--sg-cell-font-size)` for font-size
    - supergrid.css `.sg-header` uses `var(--sg-header-bg)` for background
    - design-tokens.css contains `--sg-cell-alt-bg` in at least the dark and light theme blocks
    - All 5 theme blocks in design-tokens.css include `--sg-header-bg` and `--sg-gridline`
    - TypeScript compiles: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SuperGrid CSS fully reconnected to design token system. No hardcoded colors remain. All themes covered.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix SuperGrid zoom and density (D-06, D-07)</name>
  <files>src/views/supergrid/SuperZoom.ts, src/styles/supergrid.css</files>
  <read_first>
    - src/views/supergrid/SuperZoom.ts (full file — applyZoom sets --sg-zoom, --sg-col-width, --sg-row-height on gridEl)
    - src/providers/SuperDensityProvider.ts (density state management — viewMode spreadsheet/matrix)
    - src/styles/supergrid.css (verify all zoom-dependent sizes use calc() with var(--sg-zoom, 1))
    - src/ui/VisualExplorer.ts (zoom slider — verify it calls SuperPositionProvider.zoomLevel setter)
  </read_first>
  <action>
**D-06 Zoom Fix:**

1. Read SuperZoom.ts `applyZoom()` method. It should set three CSS custom properties on `_gridEl`:
   - `--sg-col-width: ${Math.round(BASE_COL_WIDTH * zoomLevel)}px`
   - `--sg-row-height: ${Math.round(BASE_ROW_HEIGHT * zoomLevel)}px`
   - `--sg-zoom: ${zoomLevel}`

2. Verify the `_gridEl` target element. SuperZoom.attach() receives `gridEl` — confirm this is the `.pv-root` or `.sg-root` element where CSS custom properties are set. If the plugin migration changed the SuperGrid root element class name, SuperZoom may be attaching to the wrong element.

3. Search for any code that clears or overwrites `--sg-zoom` after SuperZoom sets it. The plugin migration may have added code that resets style properties.

4. Verify supergrid.css uses `var(--sg-zoom, 1)` in all zoom-dependent calc() expressions:
   - `.supergrid-card` font-size and padding: `calc(... * var(--sg-zoom, 1))`
   - `.sg-cell` font-size: `calc(var(--sg-cell-font-size) * var(--sg-zoom, 1))`
   - Any other size that should scale with zoom

5. Check the VisualExplorer zoom slider callback chain:
   - VisualExplorer slider → SuperPositionProvider.zoomLevel setter → onZoomChange callback → SuperZoom.applyZoom()
   Verify all links in this chain are intact. If a callback was disconnected during plugin migration, reconnect it.

**D-07 Density Fix:**

1. Read how SuperDensityProvider viewMode/axisGranularity affects rendering. The 1x/2x/5x density levels are controlled by VisualExplorer dimension buttons, which call `viewManager.setDimension()`.

2. Check ViewManager.setDimension() — verify it sets appropriate CSS classes or custom properties that supergrid.css uses for row height scaling.

3. Verify supergrid.css has rules for density levels. Look for `--sg-row-height` being set differently per density level, or CSS classes like `.density-1x`, `.density-2x`, `.density-5x`.

4. If density CSS rules are missing or disconnected, add them. The 1x/2x/5x levels should control:
   - Row height (e.g., 1x = 28px, 2x = 40px, 5x = 80px — or whatever the existing constants define)
   - Cell padding (use `--sg-cell-padding-spreadsheet` or `--sg-cell-padding-matrix` based on viewMode)

For both D-06 and D-07: the root cause is likely that the plugin migration changed how SuperGrid initializes and the zoom/density providers' applyZoom/setDimension methods are no longer called or their target elements changed. Trace the call chain and reconnect.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | head -10 && grep -c 'var(--sg-zoom' src/styles/supergrid.css</automated>
  </verify>
  <acceptance_criteria>
    - SuperZoom.ts `applyZoom()` sets `--sg-zoom` on a valid grid element
    - supergrid.css contains at least 2 occurrences of `var(--sg-zoom` in calc() expressions
    - supergrid.css `.sg-cell` min-height references `var(--sg-row-height`
    - No TypeScript compilation errors
    - The zoom callback chain from VisualExplorer to SuperZoom is intact (no broken references)
  </acceptance_criteria>
  <done>SuperGrid zoom and density controls function correctly. Zoom applies via --sg-zoom CSS custom property. Density levels change row height and cell padding.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test` passes
- No hardcoded hex colors in supergrid.css active rules
- --sg-zoom referenced in calc() expressions in supergrid.css
- design-tokens.css has --sg-cell-alt-bg in dark and light themes
</verification>

<success_criteria>
- SuperGrid CSS reconnected to design token system (D-04, D-05, D-09)
- Zoom (--sg-zoom) applies correctly (D-06)
- Density levels (1x/2x/5x) apply row height and cell padding (D-07)
- All 5 themes include SuperGrid token definitions
</success_criteria>

<output>
After completion, create `.planning/phases/135.1-ui-ux-polish/135.1-02-SUMMARY.md`
</output>
