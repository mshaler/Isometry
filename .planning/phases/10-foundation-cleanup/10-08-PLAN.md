---
phase: 10-foundation-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/db/__tests__/migration-safety-validation.test.ts",
  "src/db/__tests__/schemaLoader.test.ts",
  "src/db/migration-safety.ts",
  "src/db/schemaLoader.ts",
  "src/dsl/__tests__/autocomplete-dynamic.test.ts",
  "src/hooks/useSlashCommands.ts",
  "src/test/webview-migration-integration.test.ts",
  "src/utils/__tests__/enhanced-sync.test.ts",
  "src/utils/__tests__/webview-bridge-reliability.test.ts",
  "src/utils/performance-monitor.ts",
  "src/utils/sync-manager.ts",
  "src/utils/webview-bridge.ts"
]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "npm run lint returns '✖ 0 problems (0 errors, 0 warnings)'"
    - "All unused variables are removed or properly prefixed with underscore"
    - "Test files have clean imports without unused definitions"
  artifacts:
    - path: "src/db/__tests__/migration-safety-validation.test.ts"
      provides: "Clean test imports without unused variables"
      min_lines: 100
    - path: "src/utils/performance-monitor.ts"
      provides: "Active performance monitoring without dead code"
      min_lines: 400
    - path: "src/utils/__tests__/enhanced-sync.test.ts"
      provides: "Clean sync tests with proper type annotations"
      min_lines: 300
  key_links:
    - from: "Test imports"
      to: "Actual usage"
      via: "Remove unused imports and variables"
      pattern: "import.*from|const.*=|let.*="
    - from: "Function parameters"
      to: "ESLint compliance"
      via: "Underscore prefix for intentionally unused params"
      pattern: "\\(.*_\\w+.*\\)"
---

<objective>
Clean up unused variables and remaining ESLint warnings to achieve absolute zero lint problems

Purpose: Complete the final cleanup of Phase 10 by eliminating all remaining ESLint warnings (16 unused vars, remaining any types)
Output: Completely clean codebase with zero ESLint problems, achieving the foundational cleanup goal
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-cleanup/10-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up unused variables in test files</name>
  <files>src/db/__tests__/migration-safety-validation.test.ts, src/db/__tests__/schemaLoader.test.ts, src/dsl/__tests__/autocomplete-dynamic.test.ts, src/test/webview-migration-integration.test.ts, src/utils/__tests__/enhanced-sync.test.ts, src/utils/__tests__/webview-bridge-reliability.test.ts</files>
  <action>
    Remove unused variables and imports in test files:

    migration-safety-validation.test.ts:
    - Line 11: Remove 'migrationSafety' import if not used in tests
    - Line 151: Remove 'callCount' variable if not needed for test logic
    - Line 189: Remove 'mockData' variable if not needed for assertions

    schemaLoader.test.ts:
    - Lines 13-14: Remove 'SchemaField' and 'TableInfo' imports if not used
    - Line 18: Replace 'any' with proper mock interface type
    - Line 19: Prefix 'params' parameter with underscore or remove if truly unused

    autocomplete-dynamic.test.ts:
    - Line 18: Prefix 'params' parameter with underscore: _params
    - Line 18: Replace 'any' type with proper interface

    webview-migration-integration.test.ts:
    - Line 28: Remove 'mockEnvironment' if not used in test
    - Line 153: Remove 'method' variable if not needed
    - Line 276: Remove 'transactionStarted' if not used
    - Line 511: Remove 'healthBefore' if not used for assertions

    enhanced-sync.test.ts:
    - Line 8: Remove 'OfflineChange' import if not used
    - Lines 53, 366: Replace 'any' with proper type interfaces

    webview-bridge-reliability.test.ts:
    - Line 125: Remove 'error' variable if not used for test logic

    Follow pattern: Remove if truly unused, prefix with _ if intentionally unused for API compliance.
  </action>
  <verify>npm run lint on test files should show 0 no-unused-vars and no-explicit-any warnings</verify>
  <done>All test files have clean imports and variables without ESLint warnings</done>
</task>

<task type="auto">
  <name>Task 2: Clean up source files and finish any type elimination</name>
  <files>src/db/migration-safety.ts, src/db/schemaLoader.ts, src/hooks/useSlashCommands.ts, src/utils/performance-monitor.ts, src/utils/sync-manager.ts, src/utils/webview-bridge.ts</files>
  <action>
    Final cleanup of source files:

    migration-safety.ts:
    - Line 1007: Remove 'safetyBackup' variable if not used for safety operations

    schemaLoader.ts:
    - Line 170: Prefix 'name' parameter with underscore: _name

    useSlashCommands.ts:
    - Line 2: Remove 'CursorPosition' import if not used

    performance-monitor.ts:
    - Line 8-9: Remove unused imports: 'createBaselineFromBridgeResults', 'bridgePerformanceTest', 'BridgePerformanceTargets'
    - Line 404: Replace 'any' with proper interface for performance data

    sync-manager.ts:
    - Line 21: Remove 'SyncEvent' import if not used

    webview-bridge.ts:
    - Line 630: Prefix 'retries' parameter with underscore: _retries

    Ensure all remaining 'any' types are replaced with proper interfaces.
    Gap reason: These unused variables and any types are the final ESLint warnings blocking zero-problem achievement.
  </action>
  <verify>npm run lint should return exactly "✖ 0 problems (0 errors, 0 warnings)"</verify>
  <done>Complete ESLint zero-problem status achieved across entire codebase</done>
</task>

</tasks>

<verification>
Execute npm run lint and confirm output is exactly "✖ 0 problems (0 errors, 0 warnings)"
</verification>

<success_criteria>
- ESLint reports exactly "✖ 0 problems (0 errors, 0 warnings)"
- All unused variables either removed or properly prefixed with underscore
- All explicit 'any' types replaced with proper TypeScript interfaces
- Phase 10 foundational cleanup goal fully achieved
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-cleanup/10-08-SUMMARY.md`
</output>