---
phase: 10-foundation-cleanup
plan: 20
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/__tests__/officeDocumentProcessor.test.ts",
  "src/features/ConfigurationProvider.tsx",
  "src/components/views/D3ListView.tsx",
  "src/components/views/ReactViewRenderer.tsx",
  "src/features/__tests__/SystemIntegrationPerformance.test.tsx",
  "src/utils/d3Performance.ts"
]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "npm run lint returns '✖ 0 problems (0 errors, 0 warnings)'"
    - "TypeScript compiler produces no errors with syntax corrections"
    - "All explicit any types eliminated with proper interfaces"
  artifacts:
    - path: "src/__tests__/officeDocumentProcessor.test.ts"
      provides: "Valid test file syntax"
      contains: "proper mock object comma placement"
    - path: "src/features/ConfigurationProvider.tsx"
      provides: "JSX/TypeScript generic compatibility"
      contains: "resolved generic function syntax"
    - path: "src/components/views/D3ListView.tsx"
      provides: "Type-safe D3 view rendering"
      contains: "proper D3 selection interfaces"
    - path: "src/components/views/ReactViewRenderer.tsx"
      provides: "Type-safe view renderer"
      contains: "proper component typing"
  key_links:
    - from: "ESLint parser"
      to: "officeDocumentProcessor.test.ts"
      via: "syntax validation"
      pattern: "no syntax errors in test files"
    - from: "TypeScript compiler"
      to: "ConfigurationProvider.tsx"
      via: "JSX parsing"
      pattern: "no JSX/generic conflicts"
---

<objective>
Complete Phase 10 Foundation Cleanup by eliminating final 11 ESLint problems through targeted syntax fixes, type safety improvements, and unused code cleanup.

Purpose: Achieve absolute zero lint problems goal by addressing the remaining syntax errors, explicit any types, and unused variables that prevent TypeScript strict mode compliance.
Output: Complete lint problem elimination with functional syntax and proper TypeScript interfaces
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Critical Syntax Error Resolution</name>
  <files>
    src/__tests__/officeDocumentProcessor.test.ts
    src/features/ConfigurationProvider.tsx
  </files>
  <action>
    Fix the 2 critical syntax errors blocking ESLint/TypeScript parsing:

    1. In officeDocumentProcessor.test.ts line 32: Add missing comma in mock object definition to resolve parsing error
    2. In ConfigurationProvider.tsx line 701: Resolve JSX/TypeScript generic conflict by adjusting generic function syntax to be compatible with JSX parsing

    These syntax fixes are blocking further compilation and must be resolved first to enable TypeScript strict mode validation.
  </action>
  <verify>npm run lint shows 2 fewer errors and TypeScript compilation no longer blocked by syntax issues</verify>
  <done>ESLint parser successfully processes both files without syntax errors</done>
</task>

<task type="auto">
  <name>Explicit Any Type Elimination</name>
  <files>
    src/components/views/D3ListView.tsx
    src/components/views/ReactViewRenderer.tsx
    src/features/__tests__/SystemIntegrationPerformance.test.tsx
  </files>
  <action>
    Replace the remaining 4 explicit 'any' type warnings with proper TypeScript interfaces:

    1. D3ListView.tsx: Replace 1 'any' type with proper D3 selection interface
    2. ReactViewRenderer.tsx: Replace 1 'any' type with proper React component typing
    3. SystemIntegrationPerformance.test.tsx: Replace 2 'any' types with proper test interface definitions

    Use established patterns from previous Phase 10 cleanup work to maintain consistency with D3 component interfaces and React event typing standards.
  </action>
  <verify>npm run lint shows 4 fewer explicit-any warnings</verify>
  <done>Zero '@typescript-eslint/no-explicit-any' warnings in ESLint output</done>
</task>

<task type="auto">
  <name>Unused Variable Cleanup</name>
  <files>
    src/features/__tests__/SystemIntegrationPerformance.test.tsx
    src/utils/d3Performance.ts
  </files>
  <action>
    Clean up the remaining 5 unused variable warnings:

    1. SystemIntegrationPerformance.test.tsx: Remove or properly use 3 unused variables
    2. d3Performance.ts: Remove or properly use 2 unused variables in performance monitoring code

    Apply selective import destructuring pattern established in previous Phase 10 cleanup work - extract only needed properties to avoid unused variable warnings while maintaining API compliance.
  </action>
  <verify>npm run lint shows 5 fewer unused variable warnings</verify>
  <done>Zero '@typescript-eslint/no-unused-vars' warnings in ESLint output</done>
</task>

</tasks>

<verification>
Run comprehensive lint and build verification:
- `npm run lint` returns '✖ 0 problems (0 errors, 0 warnings)'
- `tsc --noEmit` succeeds with syntax corrections enabling compilation
- `npm run build` produces successful production build
- All 11 remaining problems eliminated from ESLint output
</verification>

<success_criteria>
- ESLint output shows exactly 0 problems (0 errors, 0 warnings)
- TypeScript compiler processes all files without syntax errors
- Production build completes successfully
- Phase 10 absolute zero lint goal finally achieved
- All explicit 'any' types replaced with proper interfaces
- No unused variables or imports remain in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-cleanup/10-20-SUMMARY.md`
</output>