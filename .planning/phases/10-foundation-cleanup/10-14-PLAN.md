---
phase: 10-foundation-cleanup
plan: 14
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/utils/officeDocumentProcessor.ts"
]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Office document processor has zero TypeScript 'any' type usage"
    - "Transform function parameters properly typed"
    - "ESLint passes with zero warnings for document processor"
  artifacts:
    - path: "src/utils/officeDocumentProcessor.ts"
      provides: "Type-safe document transformation interface"
      contains: "DocumentTransform interface"
  key_links:
    - from: "src/utils/officeDocumentProcessor.ts"
      to: "mammoth document conversion"
      via: "transform function parameter"
      pattern: "transform:\\s*\\(.*DocumentTransform.*\\)"
---

<objective>
Fix the final ESLint 'any' warning in office document processor by implementing proper TypeScript interface for the transform function.

Purpose: Eliminate the last remaining ESLint warning to achieve Phase 10's "absolute zero lint problems" goal
Output: Type-safe office document processor with zero ESLint warnings
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-foundation-cleanup/10-VERIFICATION.md

# Gap closure context - fixing final ESLint warning
@src/utils/officeDocumentProcessor.ts
</context>

<tasks>

<task type="auto">
  <name>Implement DocumentTransform interface</name>
  <files>src/utils/officeDocumentProcessor.ts</files>
  <action>
    Replace 'any' type in transform function parameter with proper TypeScript interface:

    1. Define DocumentTransform interface for mammoth document processing:
    ```typescript
    interface DocumentTransform {
      element: string;
      attributes?: Record<string, string>;
      children?: DocumentTransform[];
      text?: string;
      type?: 'element' | 'text';
    }
    ```

    2. Locate line 310 with transform function parameter using 'any':
    Current: `transform: (element: any) => { ... }`

    3. Replace with properly typed parameter:
    ```typescript
    transform: (element: DocumentTransform) => {
      // Ensure all element property access is type-safe
      if (element.type === 'element') {
        return { element: element.element, attributes: element.attributes };
      }
      return null;
    }
    ```

    4. Verify all usages of element parameter within transform function are type-safe

    Gap reason: ESLint warning "Unexpected any. Specify a different type @typescript-eslint/no-explicit-any" at line 310
  </action>
  <verify>npm run lint | grep -c "officeDocumentProcessor.ts" should return 0</verify>
  <done>officeDocumentProcessor.ts produces zero ESLint warnings and document imports function correctly</done>
</task>

</tasks>

<verification>
- Run `npm run lint` and confirm zero warnings from office document processor
- Test document import functionality still works correctly
- Verify Word document conversion preserves formatting and structure
</verification>

<success_criteria>
- Final 'any' warning eliminated from office document processor
- ESLint warning count reduced to exactly 0 (absolute zero achieved)
- Document import functionality maintains full compatibility
- Phase 10 "absolute zero lint problems" goal achieved for ESLint warnings
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-cleanup/10-14-SUMMARY.md`
</output>