---
phase: 10-foundation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/test/performance-regression.test.ts",
  "src/test/migration-validator.test.ts",
  "src/test/conflict-resolution.test.ts",
  "src/test/batch-operations.test.ts",
  "src/test/data-synchronization.test.ts",
  "src/hooks/useBridgeStatus.ts",
  "src/hooks/useBridgeMetrics.ts",
  "src/hooks/useNotebookCache.ts",
  "src/utils/performance-benchmarks.ts",
  "src/d3/graph-renderer.ts",
  "src/d3/node-renderer.ts",
  "src/visualization/d3-types.ts"
]
autonomous: true

must_haves:
  truths:
    - "Developer runs npm run lint and sees zero warnings"
    - "TypeScript compiler produces no errors with strict mode enabled"
    - "All test files have proper parameter naming conventions"
    - "Production build completes without any lint violations"
    - "IDE shows no ESLint warnings in any source files"
  artifacts:
    - path: "src/test/performance-regression.test.ts"
      provides: "Clean test file with proper underscore prefixes"
      pattern: "^_\\w+ parameters for unused test arguments"
    - path: "src/utils/performance-benchmarks.ts"
      provides: "Type-safe performance interfaces"
      pattern: "interface \\w+Benchmark|interface \\w+Metrics"
    - path: "src/d3/graph-renderer.ts"
      provides: "Complete D3 type definitions"
      pattern: "interface D3\\w+|type \\w+Selection"
  key_links:
    - from: "Test parameter names"
      to: "ESLint no-unused-vars rule"
      via: "underscore prefix pattern"
      pattern: "^_\\w+"
    - from: "D3 visualization code"
      to: "TypeScript strict checking"
      via: "comprehensive type definitions"
      pattern: "d3\\.[a-zA-Z]+"
    - from: "Performance utilities"
      to: "Production build process"
      via: "zero lint violations"
      pattern: "npm run lint.*0 problems"
---

<objective>
Complete absolute zero lint elimination to achieve production-ready code quality for App Store submission.

Purpose: Finish the remaining 139 warnings to reach the absolute zero target (205→0), ensuring App Store submission readiness and establishing professional development standards.
Output: Production-ready codebase with zero ESLint warnings and comprehensive type safety across all modules.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-cleanup/10-RESEARCH.md
@.planning/phases/10-foundation-cleanup/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete unused test parameter cleanup</name>
  <files>src/test/performance-regression.test.ts src/test/migration-validator.test.ts src/test/conflict-resolution.test.ts src/test/batch-operations.test.ts src/test/data-synchronization.test.ts</files>
  <action>
    Apply systematic underscore prefix pattern to all remaining unused test parameters. Based on Phase 10-01 success pattern, use underscore prefixes for parameters required by test interfaces but not used in implementation. Focus on test files which commonly have unused scenario, provider, snapshot, and checkpoint parameters.

    Specific fixes needed:
    - Function parameters like (scenario, provider) → (_scenario, _provider)
    - Callback arguments like (snapshot) → (_snapshot)
    - Event handler parameters like (checkpoint) → (_checkpoint)

    Use established pattern from Phase 10-01: preserve parameter names with underscore prefix to maintain interface compatibility while eliminating ESLint warnings.
  </action>
  <verify>npm run lint | grep "no-unused-vars" | wc -l should be significantly reduced</verify>
  <done>Test files use proper underscore prefix convention for all unused parameters, eliminating majority of remaining no-unused-vars warnings</done>
</task>

<task type="auto">
  <name>Task 2: Complete complex D3 type definitions</name>
  <files>src/d3/graph-renderer.ts src/d3/node-renderer.ts src/visualization/d3-types.ts</files>
  <action>
    Create comprehensive TypeScript interfaces for remaining D3 visualization any types. Based on Phase 10-01 patterns, create domain-specific interfaces rather than using generic unknown types.

    Target remaining D3 any types in visualization code:
    - D3 selection types: create specific selection interfaces for graph and node elements
    - Data binding patterns: define interfaces for bound data structures
    - Event handler types: create interfaces for D3 event objects
    - Scale and axis types: define interfaces for visualization scales

    Follow Phase 10-01 pattern: create meaningful interfaces like D3GraphSelection, D3NodeData, D3ScaleConfig rather than generic types. This provides better IDE support and type safety than any or unknown.
  </action>
  <verify>npm run lint | grep "no-explicit-any" | wc -l should be significantly reduced in D3 files</verify>
  <done>D3 visualization code uses comprehensive type definitions with zero any types, providing full TypeScript safety for complex visualization operations</done>
</task>

<task type="auto">
  <name>Task 3: Final validation and absolute zero verification</name>
  <files>src/hooks/useBridgeStatus.ts src/hooks/useBridgeMetrics.ts src/hooks/useNotebookCache.ts src/utils/performance-benchmarks.ts</files>
  <action>
    Complete final cleanup of remaining no-explicit-any warnings in utility hooks and performance code. Apply Phase 10-01 established patterns for comprehensive interface definitions.

    Target remaining utility any types:
    - Hook return types: create specific interfaces for bridge status, metrics, and cache operations
    - Performance benchmark types: define interfaces for benchmark results and timing data
    - Error handling types: create interfaces for error states and recovery operations

    Perform final lint validation to ensure absolute zero target achievement. Run comprehensive lint check and verify zero errors and zero warnings across entire codebase.

    If any issues remain after systematic cleanup, apply final targeted fixes using established Phase 10-01 patterns.
  </action>
  <verify>npm run lint returns "✓ 0 problems (0 errors, 0 warnings)" with no violations</verify>
  <done>Complete codebase achieves absolute zero lint problems, ready for App Store submission with professional code quality standards</done>
</task>

</tasks>

<verification>
1. Run `npm run lint` and confirm exactly "✓ 0 problems (0 errors, 0 warnings)"
2. Verify TypeScript compilation succeeds with no errors: `npx tsc --noEmit`
3. Confirm all 471 tests continue passing: `npm test`
4. Validate ESLint configuration properly handles mixed environment (no environment-related warnings)
5. Check that build process completes successfully: `npm run build`
</verification>

<success_criteria>
1. ESLint reports exactly zero errors and zero warnings across entire codebase
2. TypeScript strict mode compilation succeeds without errors
3. All existing test suite continues to pass (471 tests)
4. Production build completes without lint violations
5. Professional code quality standards achieved for App Store submission
6. v2.4 Error Elimination milestone completed with absolute zero target achieved
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-cleanup/10-02-SUMMARY.md`
</output>