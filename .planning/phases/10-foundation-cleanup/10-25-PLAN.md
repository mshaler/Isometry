---
phase: 10-foundation-cleanup
plan: 25
type: execute
wave: 1
depends_on: []
files_modified: [src/components/SuperGridDemo.tsx, src/components/Toolbar.tsx, src/components/ui/ErrorBoundary.tsx]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TypeScript strict mode error count significantly reduced from ~521 baseline"
    - "Critical high-impact component type safety improved"
    - "Established type safety patterns applied systematically"
  artifacts:
    - path: "src/components/SuperGridDemo.tsx"
      provides: "CoordinateSystem interface compliance"
      min_lines: 80
    - path: "src/components/Toolbar.tsx"
      provides: "Correct MenuItem property usage"
      min_lines: 70
    - path: "src/components/ui/ErrorBoundary.tsx"
      provides: "Safe void expression handling"
      min_lines: 110
  key_links:
    - from: "TypeScript compiler"
      to: "Component type definitions"
      via: "strict mode type checking"
      pattern: "TS2739.*missing.*properties|TS2561.*unknown.*properties|TS1345.*void.*truthiness"
---

<objective>
Systematically reduce TypeScript strict mode errors by applying Phase 10-23 patterns to high-impact components

Purpose: Continue Phase 10 TypeScript strict mode foundation by targeting critical components with established type safety patterns
Output: Measurable TypeScript error reduction and improved component type safety
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-cleanup/10-23-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix CoordinateSystem Interface Compliance</name>
  <files>src/components/SuperGridDemo.tsx</files>
  <action>
    Fix the CoordinateSystem type error on line 91 by ensuring all required properties are provided: pattern, scale, viewportWidth, viewportHeight. Apply comprehensive interface pattern from Phase 10-23 to define missing properties or provide proper defaults.

    Reference existing code: Use established interface extraction pattern from Phase 10-23 (DateRangeInfo interface) to create complete CoordinateSystem interface compliance.

    Gap reason: Missing CoordinateSystem properties causing strict mode compilation failure.
  </action>
  <verify>npx tsc --noEmit | grep -c "SuperGridDemo" || echo "0"</verify>
  <done>SuperGridDemo.tsx compiles without CoordinateSystem property errors</done>
</task>

<task type="auto">
  <name>Correct Toolbar MenuItem Properties</name>
  <files>src/components/Toolbar.tsx</files>
  <action>
    Fix the '_label' property errors on lines 76-85 by correcting the property names from '_label' to 'label'. These are clear typos where underscore-prefixed properties are being used instead of the correct 'label' property name.

    Apply consistent naming pattern: Ensure all MenuItem objects use proper 'label' property to match the expected interface definition.

    Gap reason: Underscore-prefixed property names (_label) don't match MenuItem interface specification.
  </action>
  <verify>npx tsc --noEmit | grep -c "Toolbar.tsx" || echo "0"</verify>
  <done>Toolbar.tsx compiles without unknown property errors</done>
</task>

<task type="auto">
  <name>Fix ErrorBoundary Void Expression Safety</name>
  <files>src/components/ui/ErrorBoundary.tsx</files>
  <action>
    Fix the void expression truthiness test on line 117 by applying undefined guard pattern from Phase 10-23. Replace direct void expression test with proper return value checking using optional chaining or explicit undefined comparison.

    Reference existing patterns: Use optional chaining safety pattern established in Phase 10-23 for potentially undefined nested properties to prevent runtime errors.

    Gap reason: Testing void expressions for truthiness violates TypeScript strict mode safety.
  </action>
  <verify>npx tsc --noEmit | grep -c "ErrorBoundary" || echo "0"</verify>
  <done>ErrorBoundary.tsx compiles without void expression truthiness errors</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit 2>&1 | wc -l` to measure TypeScript error count reduction
</verification>

<success_criteria>
Measurable reduction in TypeScript strict mode errors from ~521 baseline with improved component type safety
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-cleanup/10-25-SUMMARY.md`
</output>