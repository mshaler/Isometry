---
phase: 162-substrate-layout
plan: 02
type: tdd
wave: 2
depends_on:
  - 162-01
files_modified:
  - tests/superwidget/SuperWidget.test.ts
autonomous: true
requirements:
  - SLAT-01
  - SLAT-02
  - SLAT-03
  - SLAT-04
  - SLAT-05
  - SLAT-06
  - SLAT-07

must_haves:
  truths:
    - "Tests verify mount/destroy lifecycle including idempotency and cleanup"
    - "Tests verify all four slots exist with correct data-slot attributes"
    - "Tests verify config gear is last child in tabs with data-tab-role=config"
    - "Tests verify status slot has zero height behavior (min-height: 0 in DOM)"
    - "Tests verify tab overflow mask-image CSS is applied"
    - "Tests verify root element has flex and min-height: 0 styles"
    - "Tests verify CSS import exists (no link tag injection)"
  artifacts:
    - path: "tests/superwidget/SuperWidget.test.ts"
      provides: "Comprehensive test suite for SuperWidget DOM skeleton"
      min_lines: 100
  key_links:
    - from: "tests/superwidget/SuperWidget.test.ts"
      to: "src/superwidget/SuperWidget.ts"
      via: "import { SuperWidget }"
      pattern: "import.*SuperWidget.*from"
---

<objective>
Create comprehensive TDD test suite for SuperWidget verifying all 7 SLAT requirements: DOM structure, lifecycle, slot attributes, config gear positioning, status slot zero-height, tab overflow mask, root flex properties, and --sw-* token namespace.

Purpose: Tests are the acceptance criteria for the DOM skeleton. Every SLAT requirement gets at least one test assertion.
Output: tests/superwidget/SuperWidget.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-substrate-layout/162-CONTEXT.md
@.planning/phases/162-substrate-layout/162-UI-SPEC.md
@.planning/phases/162-substrate-layout/162-01-SUMMARY.md

<interfaces>
From src/superwidget/SuperWidget.ts (created in Plan 01):
```typescript
export class SuperWidget {
  get headerEl(): HTMLElement;
  get canvasEl(): HTMLElement;
  get statusEl(): HTMLElement;
  get tabsEl(): HTMLElement;
  get rootEl(): HTMLElement;
  get mounted(): boolean;
  mount(container: HTMLElement): void;
  destroy(): void;
}
```

DOM structure (from UI-SPEC):
```
[data-component="superwidget"]     — root
  [data-slot="header"]             — zone theme label
  [data-slot="tabs"]               — scrolling flex row
    [data-tab-role="tab"] x N      — tab buttons
    [data-tab-role="config"]       — config gear (last child)
  [data-slot="canvas"]             — 1fr canvas
  [data-slot="status"]             — min-height: 0 when empty
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write SuperWidget test suite covering all SLAT requirements</name>
  <files>tests/superwidget/SuperWidget.test.ts</files>
  <read_first>
    - src/superwidget/SuperWidget.ts (the implementation to test)
    - src/styles/superwidget.css (CSS rules to verify)
    - .planning/phases/162-substrate-layout/162-UI-SPEC.md (DOM structure contract, interaction contract)
    - tests/seams/ui/WorkbenchShell.seam.test.ts (existing test patterns — if it exists, otherwise check tests/ for any .test.ts pattern)
  </read_first>
  <behavior>
    SLAT-05 Lifecycle:
    - Test: new SuperWidget() does not append to document (not mounted until mount() called)
    - Test: mount(container) appends root element to container, mounted getter returns true
    - Test: mount() called twice on same container is idempotent (only one child added)
    - Test: destroy() removes root from DOM, mounted getter returns false
    - Test: destroy() called when not mounted does not throw

    SLAT-01 Four slots:
    - Test: root contains exactly 4 direct children with data-slot attributes
    - Test: data-slot values are exactly ["header", "tabs", "canvas", "status"] in DOM order
    - Test: root element has data-component="superwidget"
    - Test: headerEl/canvasEl/statusEl/tabsEl getters return the correct elements

    SLAT-02 Config gear:
    - Test: tabs slot contains element with data-tab-role="config"
    - Test: config gear is the last child of tabs slot
    - Test: config gear has aria-label="Configure"
    - Test: config gear textContent contains gear unicode (U+2699)

    SLAT-03 Status slot zero-height:
    - Test: status slot is in DOM (not removed or display:none via class)
    - Test: status slot has no children when empty (height is determined by CSS min-height: 0)

    SLAT-04 Tab overflow mask:
    - Test: superwidget.css contains mask-image rule on [data-slot="tabs"] (grep or stylesheet check)
    - Test: superwidget.css contains scrollbar-width: none on tabs slot

    SLAT-06 CSS namespace:
    - Test: SuperWidget.ts contains import of superwidget.css (no link tag injection)
    - Test: superwidget.css only uses --sw-* for custom property declarations (no --sg-*, --pv-* declarations)

    SLAT-07 Root flex:
    - Test: superwidget.css contains flex: 1 1 auto on root selector
    - Test: superwidget.css contains min-height: 0 on root selector

    Tab buttons:
    - Test: tabs slot contains tab buttons with data-tab-role="tab"
    - Test: first tab has data-tab-active="true"
    - Test: tabs appear before config gear in DOM order
  </behavior>
  <action>
Create `tests/superwidget/SuperWidget.test.ts` using Vitest.

**Test file structure:**
```typescript
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { SuperWidget } from '../../src/superwidget/SuperWidget';
```

Use `@vitest-environment jsdom` annotation at top of file if the project uses per-file environment annotations (check existing test files for this pattern).

**Test groups:**
1. `describe('SuperWidget lifecycle (SLAT-05)')` — mount/destroy/idempotency
2. `describe('SuperWidget slots (SLAT-01)')` — 4 slots, data-slot attributes, DOM order, getters
3. `describe('Config gear (SLAT-02)')` — position, data-tab-role, aria-label
4. `describe('Status slot (SLAT-03)')` — present in DOM, empty when no content
5. `describe('Tab overflow (SLAT-04)')` — CSS file contains mask-image (read file or check stylesheet)
6. `describe('CSS namespace (SLAT-06)')` — import statement, --sw-* only
7. `describe('Root flex (SLAT-07)')` — flex: 1 1 auto, min-height: 0

For CSS verification tests (SLAT-04, SLAT-06, SLAT-07): since jsdom does not load real CSS, use `fs.readFileSync` to read superwidget.css as text and assert on its content. Import `fs` from `node:fs` and `path` from `node:path`. Resolve the CSS path relative to the test file: `path.resolve(__dirname, '../../src/styles/superwidget.css')`.

For DOM tests: create a container div, instantiate SuperWidget, call mount(container), then query the DOM.

**beforeEach/afterEach pattern:**
```typescript
let container: HTMLElement;
let widget: SuperWidget;

beforeEach(() => {
  container = document.createElement('div');
  document.body.appendChild(container);
  widget = new SuperWidget();
});

afterEach(() => {
  widget.destroy();
  container.remove();
});
```

Each test assertion should map to one specific requirement. Add inline comments with requirement IDs (e.g., `// SLAT-01: four named slots`).
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/superwidget/SuperWidget.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/superwidget/SuperWidget.test.ts exists
    - Test file contains `import { SuperWidget } from '../../src/superwidget/SuperWidget'`
    - Test file contains describe blocks for all 7 SLAT requirements (grep for SLAT-01 through SLAT-07)
    - Test file contains at least 15 `it(` or `test(` calls
    - All tests pass (`npx vitest run tests/superwidget/SuperWidget.test.ts` exits 0)
    - Test file contains lifecycle tests: mount idempotency, destroy cleanup, destroy-when-not-mounted
    - Test file contains DOM order assertion: header, tabs, canvas, status
    - Test file contains config gear last-child assertion
    - Test file contains CSS content assertions using fs.readFileSync for mask-image, flex, min-height, --sw-*
  </acceptance_criteria>
  <done>All tests pass. Every SLAT requirement has at least one test assertion. Test file is in tests/superwidget/ per project convention.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/superwidget/SuperWidget.test.ts` exits 0
- `grep -c "SLAT-0" tests/superwidget/SuperWidget.test.ts` returns 7+ (all requirements referenced)
- `grep -c "it\|test(" tests/superwidget/SuperWidget.test.ts` returns 15+
</verification>

<success_criteria>
- All SuperWidget tests pass
- Every SLAT-01..07 requirement has at least one test
- Tests verify DOM structure, lifecycle, CSS properties, and slot behavior
- Test file follows project conventions (tests/superwidget/ directory)
</success_criteria>

<output>
After completion, create `.planning/phases/162-substrate-layout/162-02-SUMMARY.md`
</output>
