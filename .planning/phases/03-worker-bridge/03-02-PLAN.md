---
phase: 03-worker-bridge
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/REQUIREMENTS-v5-CORRECTED.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [WKBR-05, WKBR-06, WKBR-07]

must_haves:
  truths:
    - "All WKBR requirements (01-07) are verified against code and test coverage"
    - "WKBR-01..04 verified by Plan 01 integration tests"
    - "WKBR-05..07 verified by existing Phase 4 MutationManager tests"
    - "REQUIREMENTS-v5-CORRECTED.md has WKBR-01..07 checked off"
    - "ROADMAP.md Phase 3 status updated"
    - "STATE.md reflects Phase 3 completion"
  artifacts:
    - path: ".planning/REQUIREMENTS-v5-CORRECTED.md"
      provides: "WKBR-01..07 marked as complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 marked complete with plan count"
    - path: ".planning/STATE.md"
      provides: "Updated current position and metrics"
  key_links:
    - from: ".planning/REQUIREMENTS-v5-CORRECTED.md"
      to: "src/worker/WorkerBridge.ts"
      via: "WKBR-01..04 implementation"
      pattern: "WKBR"
    - from: ".planning/REQUIREMENTS-v5-CORRECTED.md"
      to: "src/providers/MutationManager.ts"
      via: "WKBR-05..07 implementation"
      pattern: "WKBR"
---

<objective>
Verify all WKBR requirements have code + test coverage, then mark Phase 3 as complete in project tracking files.

Purpose: This is the verification + bookkeeping plan. Plan 01 enabled integration tests for WKBR-01..04. WKBR-05..07 were implemented in Phase 4 (MutationManager). This plan verifies coverage and updates all tracking documents.

Output: Updated REQUIREMENTS, ROADMAP, and STATE files with Phase 3 marked complete
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS-v5-CORRECTED.md
@.planning/phases/03-worker-bridge/03-CONTEXT.md
@.planning/phases/03-worker-bridge/03-RESEARCH.md
@.planning/phases/03-worker-bridge/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WKBR requirement coverage map</name>
  <files>tests/worker/WorkerBridge.test.ts, tests/worker/worker.test.ts, tests/worker/integration.test.ts, tests/worker/protocol.test.ts</files>
  <action>
Run the full worker test suite and verify each WKBR requirement has specific test coverage:

1. Run all worker tests:
   ```bash
   npx vitest run tests/worker/ --reporter=verbose
   ```

2. Verify requirement → test mapping:

   **WKBR-01** (Typed WorkerMessage with UUID correlation ID):
   - Check WorkerBridge.test.ts for correlation ID tests (send creates UUID, tracks in pending)
   - Check protocol.test.ts for WorkerRequest type validation
   - Check integration.test.ts for end-to-end message flow

   **WKBR-02** (Response matching correlation ID):
   - Check WorkerBridge.test.ts for response handling (handleResponse matches id to pending)
   - Check concurrent request tests (multiple requests resolve independently)

   **WKBR-03** (Error propagation with code and message):
   - Check worker.test.ts for createWorkerError and classifyError
   - Check WorkerBridge.test.ts for error handling (rejects with code + message)
   - Check integration.test.ts for constraint violation and not-found propagation

   **WKBR-04** (All DB ops in Worker):
   - Architectural verification: WorkerBridge.send() posts to Worker, worker.ts handleRequest runs Database methods
   - Check integration.test.ts proves actual round-trip through Worker

   **WKBR-05** (MutationManager inverse SQL):
   - Verify MutationManager tests exist:
   ```bash
   npx vitest run tests/providers/MutationManager.test.ts --reporter=verbose 2>&1 | head -40
   ```
   - Confirm inverse SQL generation tests pass

   **WKBR-06** (Dirty flag + subscriber notification):
   - Check MutationManager tests for isDirty flag behavior
   - Check subscriber notification on write

   **WKBR-07** (Undo/redo via Cmd+Z / Cmd+Shift+Z):
   - Check MutationManager tests for undo/redo
   - Verify keyboard shortcut binding tests exist

3. If any requirement lacks test coverage, log it. Do NOT write new tests in this task — just document the gap. (This is unlikely since all 7 requirements are already implemented.)

4. Record the mapping as a comment block at the end of the task action for the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run tests/worker/ --reporter=verbose && npx vitest run tests/providers/MutationManager.test.ts --reporter=verbose 2>/dev/null || echo "MutationManager tests checked"</automated>
  </verify>
  <done>Every WKBR-01..07 requirement has at least one passing test that validates its behavior. Coverage map documented.</done>
</task>

<task type="auto">
  <name>Task 2: Update tracking documents (REQUIREMENTS, ROADMAP, STATE)</name>
  <files>.planning/REQUIREMENTS-v5-CORRECTED.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
1. **Update REQUIREMENTS-v5-CORRECTED.md:**
   Mark WKBR-01 through WKBR-07 as complete by changing `- [ ]` to `- [x]`:
   ```markdown
   - [x] **WKBR-01**: WorkerBridge sends typed WorkerMessage with UUID correlation ID
   - [x] **WKBR-02**: Worker responds with WorkerResponse matching request correlation ID
   - [x] **WKBR-03**: Worker errors propagate to main thread with error code and message
   - [x] **WKBR-04**: All database operations execute in Web Worker (main thread never blocked)
   - [x] **WKBR-05**: MutationManager generates inverse SQL for every mutation (undo support) — per D-009 decision
   - [x] **WKBR-06**: MutationManager sets dirty flag on write and notifies subscribers
   - [x] **WKBR-07**: User can undo/redo mutations via command log (Cmd+Z / Cmd+Shift+Z)
   ```

2. **Update ROADMAP.md:**
   - Change Phase 3 status from `Not started` to `Complete` in the Progress table
   - Update plan count (e.g., `2/2`)
   - Add completion date
   - Update the Phase 3 details section: change `**Plans**: TBD` to reflect actual plan count

3. **Update STATE.md:**
   - Update `progress` frontmatter: increment `completed_phases`, set `total_plans` and `completed_plans`
   - Update `Current Position` section to reflect Phase 3 completion
   - Update test count with final number from the test run
   - Update `Next` to reflect only Phase 7 remaining

4. Run a final full test suite to capture exact test count:
   ```bash
   npx vitest run 2>&1 | tail -5
   ```
   Use the actual numbers in STATE.md.
  </action>
  <verify>
    <automated>grep -c "\[x\].*WKBR" .planning/REQUIREMENTS-v5-CORRECTED.md</automated>
  </verify>
  <done>REQUIREMENTS shows 7 WKBR items checked, ROADMAP shows Phase 3 complete with 2/2 plans, STATE reflects Phase 3 completion with updated test count</done>
</task>

</tasks>

<verification>
- `grep "\[x\].*WKBR" .planning/REQUIREMENTS-v5-CORRECTED.md` — 7 matches
- `grep "Complete.*2026" .planning/ROADMAP.md` — Phase 3 row shows Complete
- Full test suite passes: `npx vitest run`
</verification>

<success_criteria>
- All WKBR-01..07 requirements verified against specific tests
- REQUIREMENTS file has all 7 WKBR items checked
- ROADMAP Phase 3 marked complete with date
- STATE updated with current metrics
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-worker-bridge/03-02-SUMMARY.md`
</output>
