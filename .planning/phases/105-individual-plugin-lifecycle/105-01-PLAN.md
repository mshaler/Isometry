---
phase: 105-individual-plugin-lifecycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/views/pivot/BasePlugins.test.ts
  - tests/views/pivot/SuperStackSpans.test.ts
  - tests/views/pivot/SuperStackCollapse.test.ts
  - tests/views/pivot/SuperStackAggregate.test.ts
  - tests/views/pivot/SuperZoom.test.ts
  - tests/views/pivot/SuperSize.test.ts
  - tests/views/pivot/SuperSort.test.ts
  - tests/views/pivot/SuperCalc.test.ts
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04]

must_haves:
  truths:
    - "All 15 plugins in this plan execute each implemented hook without error via harness pipeline"
    - "Plugins with undefined hooks have explicit assertions documenting the missing hook"
    - "Every plugin's destroy() is callable without throwing, including double-destroy"
    - "Event listener cleanup is verified via addEventListener/removeEventListener spy pairing"
  artifacts:
    - path: "tests/views/pivot/BasePlugins.test.ts"
      provides: "Lifecycle coverage for base.grid, base.headers, base.config"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperStackSpans.test.ts"
      provides: "Lifecycle coverage for superstack.spanning"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperStackCollapse.test.ts"
      provides: "Lifecycle coverage for superstack.collapse"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperStackAggregate.test.ts"
      provides: "Lifecycle coverage for superstack.aggregate"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperZoom.test.ts"
      provides: "Lifecycle coverage for superzoom.slider, superzoom.scale"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperSize.test.ts"
      provides: "Lifecycle coverage for supersize.col-resize, supersize.header-resize, supersize.uniform-resize"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperSort.test.ts"
      provides: "Lifecycle coverage for supersort.header-click, supersort.chain"
      contains: "describe('Lifecycle'"
    - path: "tests/views/pivot/SuperCalc.test.ts"
      provides: "Lifecycle coverage for supercalc.footer, supercalc.config"
      contains: "describe('Lifecycle'"
  key_links:
    - from: "tests/views/pivot/*.test.ts"
      to: "tests/views/pivot/helpers/makePluginHarness.ts"
      via: "import { makePluginHarness }"
      pattern: "makePluginHarness"
    - from: "tests/views/pivot/*.test.ts"
      to: "tests/views/pivot/helpers/usePlugin.ts"
      via: "import { usePlugin }"
      pattern: "usePlugin"
---

<objective>
Add lifecycle test coverage (transformData, transformLayout, afterRender, destroy) for the first 15 plugins: Base (3), SuperStack (3), SuperZoom (2), SuperSize (3), SuperSort (2), SuperCalc (2).

Purpose: Each plugin's hook contract is explicitly tested -- implemented hooks assert correct output shape/mutations, undefined hooks are documented, destroy verifies listener cleanup and double-destroy safety.

Output: 8 existing test files extended with `describe('Lifecycle')` blocks using makePluginHarness/usePlugin from Phase 104.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-individual-plugin-lifecycle/105-CONTEXT.md
@.planning/phases/104-test-infrastructure/104-01-SUMMARY.md

<interfaces>
<!-- Phase 104 test infrastructure consumed by this plan -->

From tests/views/pivot/helpers/makePluginHarness.ts:
```typescript
export interface HarnessOptions {
  rows?: number;
  cols?: string[];
  containerHeight?: number;
  data?: Map<string, number | null>;
}
export interface PluginHarness {
  registry: PluginRegistry;
  catalog: typeof FEATURE_CATALOG;
  ctx: RenderContext;
  enable(id: string): void;
  disable(id: string): void;
  runPipeline(): { cells: CellPlacement[]; layout: GridLayout };
}
export function makePluginHarness(opts?: HarnessOptions): PluginHarness;
```

From tests/views/pivot/helpers/usePlugin.ts:
```typescript
export function usePlugin(harness: PluginHarness, pluginId: string): PluginHook;
```

From src/views/pivot/plugins/PluginTypes.ts:
```typescript
export interface PluginHook {
  transformData?(cells: CellPlacement[], ctx: RenderContext): CellPlacement[];
  transformLayout?(layout: GridLayout, ctx: RenderContext): GridLayout;
  afterRender?(root: HTMLElement, ctx: RenderContext): void;
  onPointerEvent?(type: string, e: PointerEvent, ctx: RenderContext): boolean;
  onScroll?(scrollLeft: number, scrollTop: number, ctx: RenderContext): void;
  destroy?(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle describe blocks to Base + SuperStack + SuperZoom + SuperSize test files</name>
  <files>
    tests/views/pivot/BasePlugins.test.ts
    tests/views/pivot/SuperStackSpans.test.ts
    tests/views/pivot/SuperStackCollapse.test.ts
    tests/views/pivot/SuperStackAggregate.test.ts
    tests/views/pivot/SuperZoom.test.ts
    tests/views/pivot/SuperSize.test.ts
  </files>
  <read_first>
    tests/views/pivot/BasePlugins.test.ts
    tests/views/pivot/SuperStackSpans.test.ts
    tests/views/pivot/SuperStackCollapse.test.ts
    tests/views/pivot/SuperStackAggregate.test.ts
    tests/views/pivot/SuperZoom.test.ts
    tests/views/pivot/SuperSize.test.ts
    tests/views/pivot/helpers/makePluginHarness.ts
    tests/views/pivot/helpers/usePlugin.ts
    src/views/pivot/plugins/PluginTypes.ts
    src/views/pivot/plugins/BaseGrid.ts
    src/views/pivot/plugins/BaseHeaders.ts
    src/views/pivot/plugins/BaseConfig.ts
    src/views/pivot/plugins/SuperStackSpans.ts
    src/views/pivot/plugins/SuperStackCollapse.ts
    src/views/pivot/plugins/SuperStackAggregate.ts
    src/views/pivot/plugins/SuperZoomWheel.ts
    src/views/pivot/plugins/SuperZoomSlider.ts
    src/views/pivot/plugins/SuperSizeColResize.ts
    src/views/pivot/plugins/SuperSizeHeaderResize.ts
    src/views/pivot/plugins/SuperSizeUniformResize.ts
  </read_first>
  <action>
For each of the 13 plugins across these 6 test files, add a `describe('Lifecycle — {plugin-id}')` block that:

1. **Replace inline makeCtx()** — Remove any inline makeCtx() helper and replace with `import { makePluginHarness } from './helpers/makePluginHarness'` and `import { usePlugin } from './helpers/usePlugin'`. When an old test duplicates coverage from the new lifecycle test, remove the old test.

2. **Hook presence assertions** — For each of the 4 hooks (transformData, transformLayout, afterRender, destroy), assert either:
   - `typeof hook.{hookName}` is `'function'` (if implemented), OR
   - `hook.{hookName}` is `undefined` (if NOT implemented — documents the contract)

3. **transformData tests** (if implemented):
   - Enable plugin via `usePlugin(harness, '{id}')`, build cells via `harness.runPipeline()`, assert returned array preserves keys and length is correct (e.g., for base plugins that don't filter, length === input length)

4. **transformLayout tests** (if implemented):
   - Assert expected mutations: e.g., superzoom.scale multiplies cellWidth/cellHeight by zoom factor; supersize.col-resize sets colWidths entries

5. **afterRender tests** (if implemented):
   - Assert expected DOM elements exist after `harness.runPipeline()`: e.g., base.grid creates `.pv-cell` elements, superstack.spanning creates `.pv-col-span` elements

6. **Destroy tests** (every plugin):
   - **Single destroy**: `expect(() => hook.destroy!()).not.toThrow()` (or `hook.destroy?.()` for optional)
   - **Double destroy**: Call destroy() twice, assert no throw on second call
   - **Listener spy** (for plugins that add event listeners): `vi.spyOn(element, 'addEventListener')` before init, then after destroy assert `removeEventListener` was called for each registered listener. For render-only plugins (base.grid, base.headers, superstack.spanning, superstack.aggregate) that add zero listeners: spy shows zero addEventListener calls — assert that fact.

**Plugin-to-hook mapping for this task:**

| Plugin ID | transformData | transformLayout | afterRender | destroy | Listeners? |
|-----------|:---:|:---:|:---:|:---:|:---:|
| base.grid | undefined | undefined | yes (no-op) | yes (no-op) | no |
| base.headers | undefined | undefined | yes (no-op) | undefined | no |
| base.config | undefined | undefined | yes (no-op) | yes (no-op) | no |
| superstack.spanning | undefined | undefined | yes | undefined | no |
| superstack.collapse | undefined | undefined | yes | yes | yes (click) |
| superstack.aggregate | undefined | undefined | yes | undefined | no |
| superzoom.slider | undefined | undefined | yes | yes | yes (input) |
| superzoom.scale | undefined | transformLayout | undefined | yes | yes (wheel) |
| supersize.col-resize | undefined | transformLayout | yes | yes | yes (pointer) |
| supersize.header-resize | undefined | transformLayout | yes | yes | yes (pointer) |
| supersize.uniform-resize | undefined | transformLayout | yes | yes | yes (pointer) |

Read each plugin source to confirm exact hook presence before writing assertions. The table above is a guide — the source code is the truth.
  </action>
  <verify>
    <automated>npx vitest run tests/views/pivot/BasePlugins.test.ts tests/views/pivot/SuperStackSpans.test.ts tests/views/pivot/SuperStackCollapse.test.ts tests/views/pivot/SuperStackAggregate.test.ts tests/views/pivot/SuperZoom.test.ts tests/views/pivot/SuperSize.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 6 test files contains `describe('Lifecycle` (grep-verifiable)
    - Each test file imports `makePluginHarness` from `./helpers/makePluginHarness`
    - Each test file imports `usePlugin` from `./helpers/usePlugin`
    - No inline `function makeCtx()` remains in any of these 6 files
    - All 6 files pass vitest run with zero failures
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 13 plugins (base x3, superstack x3, superzoom x2, supersize x3, supersort x0, supercalc x0) have lifecycle describe blocks with hook presence, behavioral, destroy, and listener-spy assertions. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add lifecycle describe blocks to SuperSort + SuperCalc test files</name>
  <files>
    tests/views/pivot/SuperSort.test.ts
    tests/views/pivot/SuperCalc.test.ts
  </files>
  <read_first>
    tests/views/pivot/SuperSort.test.ts
    tests/views/pivot/SuperCalc.test.ts
    tests/views/pivot/helpers/makePluginHarness.ts
    tests/views/pivot/helpers/usePlugin.ts
    src/views/pivot/plugins/SuperSortHeaderClick.ts
    src/views/pivot/plugins/SuperSortChain.ts
    src/views/pivot/plugins/SuperCalcFooter.ts
    src/views/pivot/plugins/SuperCalcConfig.ts
  </read_first>
  <action>
For the remaining 4 plugins (supersort.header-click, supersort.chain, supercalc.footer, supercalc.config), add `describe('Lifecycle — {plugin-id}')` blocks following the same pattern as Task 1:

1. **Replace inline makeCtx()** with `makePluginHarness` and `usePlugin` imports. Remove overlapping old tests.

2. **Hook presence assertions** for all 4 hooks.

3. **transformData tests**:
   - supersort.header-click: has transformData — sorts cells by value at sortState.colIdx. Assert returned cells are reordered (not same reference when sort is active)
   - supersort.chain: check if transformData exists; if so, verify chained sort
   - supercalc.footer: check if transformData exists
   - supercalc.config: check if transformData exists

4. **transformLayout tests**: Assert for any that implement it.

5. **afterRender tests**:
   - supersort.header-click: assert sort indicators (arrow glyphs) in DOM after render
   - supercalc.footer: assert `.pv-calc-footer` element exists in DOM
   - supercalc.config: assert `.hns-calc-config` section exists in DOM

6. **Destroy tests**: Single destroy, double destroy, listener spy for each.

**Plugin-to-hook mapping:**

| Plugin ID | transformData | transformLayout | afterRender | destroy | Listeners? |
|-----------|:---:|:---:|:---:|:---:|:---:|
| supersort.header-click | yes | undefined | yes | yes | yes (pointer) |
| supersort.chain | yes | undefined | yes | yes | yes (pointer) |
| supercalc.footer | undefined | undefined | yes | yes | no |
| supercalc.config | undefined | undefined | yes | yes | yes (input/select) |

Read each plugin source to confirm exact hooks before writing assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/views/pivot/SuperSort.test.ts tests/views/pivot/SuperCalc.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both test files contain `describe('Lifecycle` (grep-verifiable)
    - Both test files import `makePluginHarness` from `./helpers/makePluginHarness`
    - Both test files import `usePlugin` from `./helpers/usePlugin`
    - No inline `function makeCtx()` remains in either file
    - Both files pass vitest run with zero failures
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 4 plugins (supersort x2, supercalc x2) have lifecycle describe blocks with hook presence, behavioral, destroy, and listener-spy assertions. All tests pass.</done>
</task>

</tasks>

<verification>
All 8 test files pass:
```bash
npx vitest run tests/views/pivot/BasePlugins.test.ts tests/views/pivot/SuperStackSpans.test.ts tests/views/pivot/SuperStackCollapse.test.ts tests/views/pivot/SuperStackAggregate.test.ts tests/views/pivot/SuperZoom.test.ts tests/views/pivot/SuperSize.test.ts tests/views/pivot/SuperSort.test.ts tests/views/pivot/SuperCalc.test.ts
```

No TypeScript errors:
```bash
npx tsc --noEmit
```

No inline makeCtx() remaining in any modified file:
```bash
grep -l 'function makeCtx' tests/views/pivot/BasePlugins.test.ts tests/views/pivot/SuperStackSpans.test.ts tests/views/pivot/SuperStackCollapse.test.ts tests/views/pivot/SuperStackAggregate.test.ts tests/views/pivot/SuperZoom.test.ts tests/views/pivot/SuperSize.test.ts tests/views/pivot/SuperSort.test.ts tests/views/pivot/SuperCalc.test.ts
# Should return no results
```
</verification>

<success_criteria>
- 15 plugins have lifecycle describe blocks covering all 4 hooks
- makePluginHarness/usePlugin used consistently (no inline makeCtx)
- All tests pass
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/105-individual-plugin-lifecycle/105-01-SUMMARY.md`
</output>
