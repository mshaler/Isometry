---
phase: 168-tab-system
plan: 02
type: execute
wave: 2
depends_on: [168-01]
files_modified:
  - tests/superwidget/ExplorerCanvas.test.ts
autonomous: true
requirements: [EXCV-02, EXCV-03]

must_haves:
  truths:
    - "Tests verify 3 tab containers exist after mount"
    - "Tests verify tab switching via onProjectionChange changes visible container"
    - "Tests verify Apps section is inside Import/Export tab container"
    - "Tests verify tab click calls commitProjection with updated activeTabId"
  artifacts:
    - path: "tests/superwidget/ExplorerCanvas.test.ts"
      provides: "Tab system unit tests"
      min_lines: 80
  key_links:
    - from: "tests/superwidget/ExplorerCanvas.test.ts"
      to: "src/superwidget/ExplorerCanvas.ts"
      via: "imports and mounts ExplorerCanvas with mock config + commitProjection"
      pattern: "ExplorerCanvas"
---

<objective>
Add unit tests for the ExplorerCanvas tab system: tab container rendering, tab switching via onProjectionChange, Apps-in-Import/Export stacking, and commitProjection callback wiring.

Purpose: Verify EXCV-02 (three tabs render correct content) and EXCV-03 (tab switching via commitProjection) with automated tests.
Output: Extended ExplorerCanvas.test.ts with tab system coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/168-tab-system/168-CONTEXT.md
@.planning/phases/168-tab-system/168-01-SUMMARY.md

<interfaces>
<!-- Executor reads Plan 01 output files to understand exact API -->

From src/superwidget/ExplorerCanvas.ts (after Plan 01):
- constructor(config: DataExplorerPanelConfig, commitProjection: (proj: Projection) => void)
- mount(container: HTMLElement): void
- destroy(): void
- onProjectionChange(proj: Projection): void
- getPanel(): DataExplorerPanel | null

From src/superwidget/projection.ts:
- switchTab(proj: Projection, tabId: string): Projection
- CanvasComponent interface with optional onProjectionChange
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tab system tests to ExplorerCanvas.test.ts</name>
  <files>tests/superwidget/ExplorerCanvas.test.ts</files>
  <read_first>
    - tests/superwidget/ExplorerCanvas.test.ts (existing tests — extend, don't replace)
    - src/superwidget/ExplorerCanvas.ts (current implementation from Plan 01)
    - src/superwidget/projection.ts (Projection type, switchTab)
  </read_first>
  <behavior>
    - Test: mount() creates exactly 3 elements with [data-tab-container] attribute
    - Test: mount() creates [data-slot="tab-bar"] with 3 button children having [data-tab-id]
    - Test: mount() sets import-export container as active by default (has .active class)
    - Test: mount() puts catalog and db-utilities containers as hidden (no .active class)
    - Test: onProjectionChange with activeTabId='catalog' shows catalog container and hides others
    - Test: onProjectionChange with activeTabId='db-utilities' shows db-utilities container and hides others
    - Test: onProjectionChange updates data-tab-active attribute on correct button
    - Test: clicking a tab button calls commitProjection callback with new projection containing the clicked tabId
    - Test: clicking the already-active tab does NOT call commitProjection (switchTab returns same reference)
    - Test: import-export container has at least 2 CollapsibleSection children (Import/Export + Apps per D-01)
    - Test: destroy() removes all DOM (wrapper, tab bar, containers)
  </behavior>
  <action>
Add a new `describe('tab system')` block inside the existing ExplorerCanvas test file. Do NOT remove or modify existing tests.

Create a helper to build a mock DataExplorerPanelConfig (reuse the existing mock pattern from the file).

Create a base Projection object for tests:
```typescript
const baseProj: Projection = {
  canvasType: 'Explorer',
  canvasBinding: 'Unbound',
  zoneRole: 'primary',
  canvasId: 'explorer-1',
  activeTabId: 'import-export',
  enabledTabIds: ['import-export', 'catalog', 'db-utilities'],
};
```

For each test:
1. Create ExplorerCanvas with mock config and a `vi.fn()` commitProjection spy
2. Create a container div, call `canvas.mount(container)`
3. Call `canvas.onProjectionChange(baseProj)` to set initial state (simulating what SuperWidget does after mount)
4. Assert DOM state

For the click test:
1. Mount and set initial projection
2. Find the tab button with `[data-tab-id="catalog"]`
3. Dispatch a click event on it
4. Assert commitProjection spy was called
5. Assert the Projection arg has `activeTabId: 'catalog'`

For the no-op click test:
1. Mount and set initial projection (activeTabId='import-export')
2. Click the import-export button
3. Assert commitProjection spy was NOT called

For the Apps stacking test:
1. Mount
2. Find `[data-tab-container="import-export"]`
3. Query for `.collapsible-section` elements inside it
4. Assert count >= 2 (Import/Export section + Apps section)

Use `@vitest-environment jsdom` annotation at top of file if not already present.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/superwidget/ExplorerCanvas.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ExplorerCanvas.test.ts contains `describe('tab system'`
    - ExplorerCanvas.test.ts contains at least 8 test cases (it/test blocks) in the tab system describe
    - ExplorerCanvas.test.ts contains `data-tab-container` assertions
    - ExplorerCanvas.test.ts contains `data-tab-active` assertions
    - ExplorerCanvas.test.ts contains `commitProjection` spy assertions
    - ExplorerCanvas.test.ts contains `.collapsible-section` assertion for Apps stacking
    - All tests pass: `npx vitest run tests/superwidget/ExplorerCanvas.test.ts` exits 0
    - No existing tests broken: `npx vitest run tests/superwidget/` all pass
  </acceptance_criteria>
  <done>Tab system tests cover container rendering, switching, button clicks, Apps stacking, and destroy cleanup; all tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/superwidget/ExplorerCanvas.test.ts` — all tests pass including new tab system tests
- `npx vitest run tests/superwidget/` — full superwidget test suite passes (no regressions)
</verification>

<success_criteria>
- At least 8 new tab system tests added to ExplorerCanvas.test.ts
- Tests cover: container creation, CSS hide/show, button active state, click → commitProjection, Apps stacking, destroy cleanup
- Zero regressions in existing superwidget tests
</success_criteria>

<output>
After completion, create `.planning/phases/168-tab-system/168-02-SUMMARY.md`
</output>
