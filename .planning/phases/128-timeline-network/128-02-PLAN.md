---
phase: 128-timeline-network
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/NetworkView.ts
  - tests/views/NetworkView.test.ts
autonomous: true
requirements: [NETW-01, NETW-02, NETW-03, NETW-04]

must_haves:
  truths:
    - "NetworkView renders card nodes with connection edges and stable force-directed layout after graph:simulate"
    - "When cards exist but zero connections returned, graph renders nodes only (no empty panel, no edges)"
    - "Algorithm overlays apply visual encoding: community colors on fills, centrality/pagerank sizing, path/MST edge highlighting"
    - "AlgorithmExplorer radio group, Run button, and parameter sliders are visible and functional"
    - "Source/target picker select elements render and legend panel toggles visibility on algorithm activation"
  artifacts:
    - path: "src/views/NetworkView.ts"
      provides: "NetworkView with verified render path, empty state handling"
      contains: "No cards to display"
    - path: "tests/views/NetworkView.test.ts"
      provides: "Test coverage for render, empty state, algorithm encoding"
      contains: "No cards to display"
  key_links:
    - from: "src/views/ViewManager.ts"
      to: "src/views/NetworkView.ts"
      via: "currentView.render(cards)"
      pattern: "render\\(cards\\)"
    - from: "src/views/NetworkView.ts"
      to: "worker bridge"
      via: "bridge.send('db:exec') for connections + bridge.send('graph:simulate')"
      pattern: "bridge\\.send"
    - from: "src/ui/AlgorithmExplorer.ts"
      to: "src/views/NetworkView.ts"
      via: "applyAlgorithmEncoding callback wired in main.ts"
      pattern: "applyAlgorithmEncoding"
---

<objective>
Verify and fix NetworkView rendering pipeline: ensure nodes render with edges from force simulation (NETW-01), algorithm overlays apply correctly (NETW-02), AlgorithmExplorer controls function (NETW-03), and source/target picker + legend display (NETW-04).

Purpose: NetworkView has a complex three-step async data pipeline (card query -> connection query -> graph:simulate). This plan verifies each step works end-to-end and adds the contextual empty state panel that the UI-SPEC requires when zero cards are returned.
Output: NetworkView with verified data path, proper empty state, and passing tests.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-timeline-network/128-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/views/NetworkView.ts:
```typescript
export interface NetworkViewConfig {
  bridge: WorkerBridgeLike;
  selectionProvider?: SelectionProvider;
}

export interface AlgorithmEncodingParams {
  algorithm: string;
  pathCardIds?: string[];
  mstEdges?: Array<[string, string]>;
  reachable?: boolean;
  edgeBetweenness?: Record<string, number>;
  spDepths?: Record<string, number>;
}
```

From src/views/types.ts:
```typescript
export interface WorkerBridgeLike {
  send(channel: string, payload: unknown): Promise<unknown>;
}
```

From src/ui/AlgorithmExplorer.ts:
```typescript
export interface AlgorithmExplorerConfig {
  bridge: WorkerBridge;
  schema: SchemaProvider;
  filter: FilterProvider;
  container: HTMLElement;
  coordinator: { scheduleUpdate(): void };
  mutationManager?: { subscribe(cb: () => void): () => void };
}
```

NetworkView._clearGraph() at line 1428: clears linksGroup and nodesGroup SVG children.

ViewManager._showEmpty() at line 543: handles zero-row empty state at ViewManager level.
ViewManager passes VIEW_EMPTY_MESSAGES[network] with icon "⛑", heading "No connections found".

Key data path:
1. ViewManager._fetchAndRender() queries db:query -> gets cards
2. If cards.length === 0 -> ViewManager._showEmpty() (filtered-empty panel)
3. If cards.length > 0 -> view.render(cards)
4. NetworkView.render(cards) -> bridge.send('db:exec') for connections -> bridge.send('graph:simulate')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify NetworkView data path and fix empty state handling</name>
  <files>src/views/NetworkView.ts, tests/views/NetworkView.test.ts</files>
  <read_first>
    - src/views/NetworkView.ts (full file — 1569 lines)
    - tests/views/NetworkView.test.ts (full file — 487 lines, existing test patterns)
    - src/views/ViewManager.ts (lines 55-86 — VIEW_EMPTY_MESSAGES; lines 385-463 — _fetchAndRender; lines 714-750 — _showFilteredEmpty)
    - src/styles/views.css (lines 110-170 — .view-empty* CSS classes)
    - .planning/phases/128-timeline-network/128-UI-SPEC.md (NetworkView states section)
  </read_first>
  <behavior>
    - Test: NetworkView.render([]) calls _clearGraph and does NOT throw
    - Test: when cards are provided, bridge.send('db:exec') is called for connections query
    - Test: when bridge.send('graph:simulate') returns positions, nodes are rendered with correct cx/cy from positions
    - Test: when connections query returns zero edges, nodes render without edges (valid state, no empty panel)
    - Test: edges are filtered to only include source/target pairs where both cards are in the visible set
    - Test: node radius scales by degree using d3.scaleSqrt
  </behavior>
  <action>
Diagnostic verification and fixes for `src/views/NetworkView.ts`:

1. **Verify render data path (NETW-01):** Read the full render() method. The three-step pipeline (cards -> connections query -> graph:simulate) is already implemented. Verify:
   - The `db:exec` SQL query for connections uses parameterized IN clause with card IDs
   - Edge filtering (`edges.filter(e => cardMap.has(e.source) && cardMap.has(e.target))`) correctly scopes edges
   - Fallback positions are generated when graph:simulate fails
   - Node D3 data join uses key function `d => d.id`

2. **Empty state at ViewManager level:** The ViewManager._fetchAndRender() already handles `cards.length === 0` by calling `_showEmpty()` which shows the filtered-empty panel. The VIEW_EMPTY_MESSAGES for 'network' uses icon "⛑", heading "No connections found", description "Import data with relationships to see the network graph". **Update** these messages to match UI-SPEC:
   - In `src/views/ViewManager.ts` (line ~71-75), change network empty config:
     - icon: `'◉'` (Unicode node glyph per UI-SPEC)
     - heading: `'No cards to display'`
     - description: `'Import data or adjust your filters to see connections in the network.'`

3. **NetworkView.render([]) path:** When `cards.length === 0`, `_clearGraph()` is called (line 301-303). This is correct — ViewManager handles the empty panel display. No change needed to NetworkView itself for zero-card case.

4. **Connections-only empty state:** When cards exist but zero connections are returned (line 337-340), the code correctly falls through to render nodes without edges. This matches UI-SPEC: "Render nodes only (no edges) — this is a valid state, NOT an empty state." No change needed.

5. **Update tests** in `tests/views/NetworkView.test.ts`:
   - Add or verify test: "renders nodes at positions returned by graph:simulate"
   - Add test: "renders nodes without edges when connection query returns empty array"
   - Add test: "filters edges to only source/target pairs in visible card set"
   - Ensure existing tests still pass with any ViewManager message changes

6. **Fix ViewManager empty state messages** in `src/views/ViewManager.ts`:
   - Change line ~71-75 network entry to:
     ```typescript
     network: {
       icon: '\u25C9',  // ◉
       heading: 'No cards to display',
       description: 'Import data or adjust your filters to see connections in the network.',
     },
     ```
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/views/NetworkView.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/views/ViewManager.ts contains the string "No cards to display" in the network empty config
    - src/views/ViewManager.ts contains the string "Import data or adjust your filters to see connections in the network."
    - src/views/ViewManager.ts contains '\u25C9' or '◉' as the network icon
    - src/views/NetworkView.ts contains "d => d.id" (key function on node data join)
    - src/views/NetworkView.ts contains "graph:simulate" (Worker call)
    - src/views/NetworkView.ts contains "db:exec" (connections query)
    - tests/views/NetworkView.test.ts passes (exit code 0)
  </acceptance_criteria>
  <done>NetworkView data path verified: nodes render from force simulation positions, edges from connection query, empty state messages match UI-SPEC. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify AlgorithmExplorer controls and source/target picker rendering</name>
  <files>src/ui/AlgorithmExplorer.ts, src/views/NetworkView.ts</files>
  <read_first>
    - src/ui/AlgorithmExplorer.ts (full file — 688 lines)
    - src/views/NetworkView.ts (lines 648-900 — applyAlgorithmEncoding, resetEncoding, legend rendering)
    - src/main.ts (lines 1344-1370 — AlgorithmExplorer-to-NetworkView wiring)
    - .planning/phases/128-timeline-network/128-UI-SPEC.md (NETW-02, NETW-03, NETW-04 sections)
  </read_first>
  <action>
Diagnostic verification of AlgorithmExplorer controls (NETW-03), algorithm overlays (NETW-02), and source/target picker + legend (NETW-04):

1. **AlgorithmExplorer controls (NETW-03):** Read `src/ui/AlgorithmExplorer.ts` and verify:
   - Radio group renders all 6 algorithms (shortest_path, centrality, community, clustering, spanning_tree, pagerank) with `.algorithm-explorer__radios` class
   - Run button `.algorithm-explorer__run` dispatches `graph:compute` through bridge
   - Parameter sliders for louvain resolution and pagerank alpha render with value readout
   - Status text `.algorithm-explorer__status` shows "Running..." during compute and "Done" after

2. **Algorithm encoding wiring (NETW-02):** Verify in `src/main.ts` the callback wiring:
   - `algorithmExplorer._onResult` callback calls `NetworkView.applyAlgorithmEncoding(params)`
   - `algorithmExplorer._onReset` callback calls `NetworkView.resetEncoding()`
   - These are already wired at lines ~1344-1354 of main.ts

3. **Source/target picker (NETW-04):** Verify in AlgorithmExplorer:
   - When shortest_path algorithm selected, two `<select>` elements render for source and target card selection
   - `.nv-pick-dropdowns` container with `.nv-pick-instruction` text
   - Picker populates with card names from filtered card IDs

4. **Legend panel (NETW-04):** Verify in NetworkView:
   - `.nv-legend` element created in mount() (line ~193-198)
   - `_updateLegend()` method toggles `.nv-legend--visible` class when algorithm active
   - Legend shows community color swatches or metric gradient based on active algorithm

5. **If any issues found:** Fix them. Common issues to check:
   - AlgorithmExplorer.render() called during mount but might fail if container not in DOM
   - Pick mode click handler wiring through main.ts
   - Legend content generation using the correct community color palette (schemeTableau10)

6. **Run existing AlgorithmExplorer tests** (if any exist) and NetworkView tests to confirm:
   ```bash
   npx vitest run tests/ --grep "Algorithm" --reporter=verbose
   npx vitest run tests/views/NetworkView.test.ts --reporter=verbose
   ```
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/views/NetworkView.test.ts --reporter=verbose 2>&1 | tail -20 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/AlgorithmExplorer.ts contains "algorithm-explorer__radios" (radio group class)
    - src/ui/AlgorithmExplorer.ts contains "algorithm-explorer__run" (Run button class)
    - src/ui/AlgorithmExplorer.ts contains "algorithm-explorer__status" (status text class)
    - src/ui/AlgorithmExplorer.ts contains "graph:compute" (Worker dispatch)
    - src/views/NetworkView.ts contains "nv-legend" (legend panel)
    - src/views/NetworkView.ts contains "applyAlgorithmEncoding" (public API)
    - src/main.ts contains "applyAlgorithmEncoding" (wiring callback)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - npx vitest run tests/views/NetworkView.test.ts exits 0
  </acceptance_criteria>
  <done>AlgorithmExplorer controls, algorithm overlays, source/target picker, and legend panel verified functional. Wiring from AlgorithmExplorer through main.ts to NetworkView confirmed. No TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/views/NetworkView.test.ts` — all tests pass
2. `npx tsc --noEmit` — no TypeScript errors
3. `grep -n "No cards to display" src/views/ViewManager.ts` — updated empty state copy
4. `grep -n "algorithm-explorer__run" src/ui/AlgorithmExplorer.ts` — Run button exists
5. `grep -n "applyAlgorithmEncoding" src/main.ts` — wiring exists
</verification>

<success_criteria>
- NetworkView renders nodes with edges from force simulation (NETW-01)
- Algorithm overlays work: community colors, centrality sizing, path/MST highlighting (NETW-02)
- AlgorithmExplorer radio group, Run button, parameter sliders visible and functional (NETW-03)
- Source/target picker and legend panel display correctly (NETW-04)
- Empty state messages match UI-SPEC copy
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/128-timeline-network/128-02-SUMMARY.md`
</output>
