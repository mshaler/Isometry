---
phase: 165-canvas-stubs-registry
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/superwidget/ExplorerCanvasStub.ts
  - src/superwidget/ViewCanvasStub.ts
  - src/superwidget/EditorCanvasStub.ts
  - tests/superwidget/ExplorerCanvasStub.test.ts
  - tests/superwidget/ViewCanvasStub.test.ts
  - tests/superwidget/EditorCanvasStub.test.ts
autonomous: true
requirements: [CANV-01, CANV-02, CANV-03, CANV-07]

must_haves:
  truths:
    - "ExplorerCanvasStub mounts an element with data-canvas-type='Explorer' and data-render-count incrementing on each mount()"
    - "ViewCanvasStub in Bound mode creates a data-sidecar child; in Unbound mode no sidecar exists"
    - "EditorCanvasStub mounts with data-canvas-type='Editor' and no sidecar"
    - "All three stub files begin with '// STUB -- placeholder for replacement in v13.1+' comment"
    - "All three stubs implement the CanvasComponent interface (mount/destroy)"
  artifacts:
    - path: "src/superwidget/ExplorerCanvasStub.ts"
      provides: "Explorer canvas stub implementing CanvasComponent"
      contains: "implements CanvasComponent"
    - path: "src/superwidget/ViewCanvasStub.ts"
      provides: "View canvas stub with Bound/Unbound sidecar logic"
      contains: "data-sidecar"
    - path: "src/superwidget/EditorCanvasStub.ts"
      provides: "Editor canvas stub implementing CanvasComponent"
      contains: "implements CanvasComponent"
    - path: "tests/superwidget/ExplorerCanvasStub.test.ts"
      provides: "TDD tests for CANV-01, CANV-07"
    - path: "tests/superwidget/ViewCanvasStub.test.ts"
      provides: "TDD tests for CANV-02"
    - path: "tests/superwidget/EditorCanvasStub.test.ts"
      provides: "TDD tests for CANV-03, CANV-07"
  key_links:
    - from: "src/superwidget/ExplorerCanvasStub.ts"
      to: "src/superwidget/projection.ts"
      via: "import type { CanvasComponent }"
      pattern: "import type.*CanvasComponent.*from.*projection"
    - from: "src/superwidget/ViewCanvasStub.ts"
      to: "src/superwidget/projection.ts"
      via: "import type { CanvasBinding, CanvasComponent }"
      pattern: "import type.*CanvasBinding.*CanvasComponent.*from.*projection"
---

<objective>
Implement three canvas stub classes (Explorer, View, Editor) that satisfy the CanvasComponent interface from projection.ts.

Purpose: These stubs provide the concrete CanvasComponent implementations that the registry (Plan 02) will wire into SuperWidget's CanvasFactory. They are explicitly labeled as stubs for replacement in v13.1+.

Output: Three stub source files + three test files. All stubs implement mount()/destroy() with correct data attributes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-canvas-stubs-registry/165-CONTEXT.md
@.planning/phases/165-canvas-stubs-registry/165-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/superwidget/projection.ts:
```typescript
export type CanvasType = 'Explorer' | 'View' | 'Editor';
export type CanvasBinding = 'Bound' | 'Unbound';

export interface CanvasComponent {
  mount(container: HTMLElement): void;
  destroy(): void;
}
```

From src/superwidget/SuperWidget.ts:
```typescript
export type CanvasFactory = (canvasId: string) => CanvasComponent | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — Three canvas stubs (RED then GREEN)</name>
  <files>
    src/superwidget/ExplorerCanvasStub.ts,
    src/superwidget/ViewCanvasStub.ts,
    src/superwidget/EditorCanvasStub.ts,
    tests/superwidget/ExplorerCanvasStub.test.ts,
    tests/superwidget/ViewCanvasStub.test.ts,
    tests/superwidget/EditorCanvasStub.test.ts
  </files>
  <read_first>
    src/superwidget/projection.ts,
    src/superwidget/SuperWidget.ts,
    tests/superwidget/commitProjection.test.ts,
    tests/superwidget/SuperWidget.test.ts
  </read_first>
  <behavior>
    ExplorerCanvasStub (CANV-01, CANV-07):
    - mount() appends a div with dataset.canvasType = 'Explorer' to container
    - mount() sets dataset.renderCount = '1' on first call, '2' on second, etc.
    - mount() sets textContent to '[Explorer: {canvasId}]'
    - destroy() removes the element from DOM
    - destroy() before mount() does not throw
    - File starts with '// STUB -- placeholder for replacement in v13.1+'

    ViewCanvasStub (CANV-02, CANV-07):
    - Constructor takes (canvasId: string, binding: CanvasBinding)
    - mount() appends a div with dataset.canvasType = 'View' to container
    - mount() sets dataset.renderCount incrementing on each call
    - mount() sets textContent to '[View: {canvasId}]'
    - When binding is 'Bound', mount() creates a child div with dataset.sidecar = ''
    - When binding is 'Unbound', no data-sidecar child exists
    - destroy() removes the element from DOM
    - File starts with '// STUB -- placeholder for replacement in v13.1+'

    EditorCanvasStub (CANV-03, CANV-07):
    - Constructor takes only (canvasId: string) — no binding parameter
    - mount() appends a div with dataset.canvasType = 'Editor' to container
    - mount() sets dataset.renderCount incrementing on each call
    - mount() sets textContent to '[Editor: {canvasId}]'
    - No sidecar element ever created
    - destroy() removes the element from DOM
    - File starts with '// STUB -- placeholder for replacement in v13.1+'
  </behavior>
  <action>
    RED phase — Create three test files. Each test file must start with `// @vitest-environment jsdom` as the first line (required for document access in Vitest node default).

    tests/superwidget/ExplorerCanvasStub.test.ts:
    - Import ExplorerCanvasStub from '../../src/superwidget/ExplorerCanvasStub'
    - Tests: data-canvas-type is 'Explorer', textContent is '[Explorer: canvas-1]', data-render-count is '1' after first mount, '2' after destroy+mount cycle, destroy removes element, destroy before mount does not throw
    - CANV-07 test: use readFileSync to read src/superwidget/ExplorerCanvasStub.ts and assert first line starts with '// STUB'

    tests/superwidget/ViewCanvasStub.test.ts:
    - Import ViewCanvasStub from '../../src/superwidget/ViewCanvasStub'
    - Tests: data-canvas-type is 'View', textContent is '[View: canvas-1]', Bound mode has child with dataset.sidecar === '', Unbound mode has no child with dataset.sidecar, data-render-count increments, destroy removes element
    - CANV-07 test: readFileSync asserts first line starts with '// STUB'

    tests/superwidget/EditorCanvasStub.test.ts:
    - Import EditorCanvasStub from '../../src/superwidget/EditorCanvasStub'
    - Tests: data-canvas-type is 'Editor', textContent is '[Editor: canvas-1]', no sidecar child exists after mount, data-render-count increments, destroy removes element
    - CANV-07 test: readFileSync asserts first line starts with '// STUB'

    Run tests — all must FAIL (classes don't exist yet). Commit RED.

    GREEN phase — Create three stub source files:

    src/superwidget/ExplorerCanvasStub.ts:
    - First line: `// STUB -- placeholder for replacement in v13.1+`
    - `import type { CanvasComponent } from './projection';`
    - Class `ExplorerCanvasStub implements CanvasComponent`
    - Private fields: `_canvasId: string`, `_el: HTMLElement | null = null`, `_renderCount = 0`
    - Constructor takes `(canvasId: string)`
    - mount(container): increment _renderCount, createElement('div'), set dataset.canvasType='Explorer', dataset.renderCount=String(_renderCount), textContent=`[Explorer: ${_canvasId}]`, store as _el, appendChild to container
    - destroy(): `this._el?.remove(); this._el = null;` (do NOT reset _renderCount)

    src/superwidget/ViewCanvasStub.ts:
    - First line: `// STUB -- placeholder for replacement in v13.1+`
    - `import type { CanvasBinding, CanvasComponent } from './projection';`
    - Class `ViewCanvasStub implements CanvasComponent`
    - Private fields: `_canvasId: string`, `_binding: CanvasBinding`, `_el: HTMLElement | null = null`, `_renderCount = 0`
    - Constructor takes `(canvasId: string, binding: CanvasBinding)`
    - mount(container): increment _renderCount, createElement('div'), set dataset.canvasType='View', dataset.renderCount=String(_renderCount), textContent=`[View: ${_canvasId}]`. If `_binding === 'Bound'`: create child div with `dataset.sidecar = ''` and append to el. Store _el, appendChild to container.
    - destroy(): `this._el?.remove(); this._el = null;`

    src/superwidget/EditorCanvasStub.ts:
    - First line: `// STUB -- placeholder for replacement in v13.1+`
    - `import type { CanvasComponent } from './projection';`
    - Class `EditorCanvasStub implements CanvasComponent`
    - Private fields: `_canvasId: string`, `_el: HTMLElement | null = null`, `_renderCount = 0`
    - Constructor takes `(canvasId: string)` — no binding param (per Claude's Discretion, Editor is Unbound-only)
    - mount(container): increment _renderCount, createElement('div'), set dataset.canvasType='Editor', dataset.renderCount=String(_renderCount), textContent=`[Editor: ${_canvasId}]`, store as _el, appendChild to container
    - destroy(): `this._el?.remove(); this._el = null;`

    Run tests — all must PASS. Commit GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/superwidget/ExplorerCanvasStub.test.ts tests/superwidget/ViewCanvasStub.test.ts tests/superwidget/EditorCanvasStub.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/superwidget/ExplorerCanvasStub.test.ts exits 0
    - tests/superwidget/ViewCanvasStub.test.ts exits 0
    - tests/superwidget/EditorCanvasStub.test.ts exits 0
    - src/superwidget/ExplorerCanvasStub.ts first line contains '// STUB'
    - src/superwidget/ViewCanvasStub.ts first line contains '// STUB'
    - src/superwidget/EditorCanvasStub.ts first line contains '// STUB'
    - src/superwidget/ExplorerCanvasStub.ts contains 'implements CanvasComponent'
    - src/superwidget/ViewCanvasStub.ts contains 'implements CanvasComponent'
    - src/superwidget/EditorCanvasStub.ts contains 'implements CanvasComponent'
    - src/superwidget/ViewCanvasStub.ts contains "dataset['sidecar']" or "dataset.sidecar"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All three stubs pass their TDD tests. data-canvas-type, data-render-count, textContent, sidecar behavior, and CANV-07 stub labeling all verified.</done>
</task>

</tasks>

<verification>
npx vitest run tests/superwidget/ExplorerCanvasStub.test.ts tests/superwidget/ViewCanvasStub.test.ts tests/superwidget/EditorCanvasStub.test.ts && npx tsc --noEmit
</verification>

<success_criteria>
- Three stub files exist in src/superwidget/ with correct CanvasComponent implementation
- All stub tests pass with CANV-01, CANV-02, CANV-03, CANV-07 coverage
- TypeScript strict mode passes (tsc --noEmit)
</success_criteria>

<output>
After completion, create `.planning/phases/165-canvas-stubs-registry/165-01-SUMMARY.md`
</output>
