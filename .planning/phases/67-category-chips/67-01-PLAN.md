---
phase: 67-category-chips
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/LatchExplorers.ts
  - src/styles/latch-explorers.css
  - tests/ui/LatchExplorers.test.ts
autonomous: false
requirements:
  - LTPB-03
  - LTPB-04
---

# Phase 67 Plan 01: Category Chips

Replace checkbox lists with interactive category chips (pill buttons with count badges) in the LATCH explorer Category and Hierarchy sections.

## Tasks

1. **Replace _renderCheckboxes with _renderChips** — fetchDistinctValuesWithCounts GROUP BY query, ChipDatum type, D3 button.latch-chip join, click toggle via setAxisFilter
2. **Chip CSS classes** — .latch-chip pill styling, .latch-chip--active accent state, .latch-chip__count badge, remove old .latch-checkbox rules
3. **Update tests** — chip rendering assertions, count badge test, click toggle test, GROUP BY query verification
4. **Biome + tsc gate** — zero diagnostics
