---
phase: 178-css-code-hygiene-audit
plan: 02
type: execute
wave: 2
depends_on: ["178-01"]
files_modified:
  - src/styles/design-tokens.css
  - src/styles/diff-preview.css
  - src/styles/audit.css
  - src/styles/harness.css
  - src/styles/tour.css
  - src/styles/accessibility.css
  - src/styles/algorithm-explorer.css
  - src/styles/data-explorer.css
  - src/styles/command-palette.css
  - src/styles/help-overlay.css
  - src/styles/import-toast.css
  - src/styles/preset-suggestion-toast.css
  - src/styles/catalog-actions.css
  - src/styles/pivot.module.css
  - src/styles/app-dialog.css
  - src/styles/action-toast.css
  - src/styles/view-tab-bar.css
  - src/ui/ViewTabBar.ts
  - src/worker/handlers/ui-state.handler.ts
autonomous: true
requirements: [HYG-01, HYG-02, HYG-03, HYG-04]

must_haves:
  truths:
    - "All 32 CSS files in src/styles/ have near-zero hardcoded px values (excluding 0 and 1px borders)"
    - "Every overflow:hidden across all CSS files is either fixed or annotated with /* intentional: [reason] */"
    - "The 3 known TS workarounds are resolved"
    - "All existing unit tests and E2E tests pass without regression"
  artifacts:
    - path: "src/ui/ViewTabBar.ts"
      provides: "Clean insertBefore hack removed"
    - path: "src/worker/handlers/ui-state.handler.ts"
      provides: "Misleading workaround comment clarified"
  key_links:
    - from: "src/styles/*.css"
      to: "src/styles/design-tokens.css"
      via: "var(--*) references"
      pattern: "var\\(--"
---

<objective>
Sweep the remaining 17 CSS files for tokenization and overflow audit, resolve all 3 TS workarounds, and run final regression verification.

Purpose: Complete the hygiene audit by covering the lower-priority CSS files (each <37 hardcoded px), fixing the 3 known TS workarounds, and confirming zero functional regression across unit and E2E tests.

Output: All 32 CSS files cleaned + 2 TS files fixed + green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/178-css-code-hygiene-audit/178-CONTEXT.md
@.planning/phases/178-css-code-hygiene-audit/178-01-SUMMARY.md

@src/styles/design-tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenize remaining 17 CSS files + overflow audit</name>
  <files>src/styles/design-tokens.css, src/styles/diff-preview.css, src/styles/audit.css, src/styles/harness.css, src/styles/tour.css, src/styles/accessibility.css, src/styles/algorithm-explorer.css, src/styles/data-explorer.css, src/styles/command-palette.css, src/styles/help-overlay.css, src/styles/import-toast.css, src/styles/preset-suggestion-toast.css, src/styles/catalog-actions.css, src/styles/pivot.module.css, src/styles/app-dialog.css, src/styles/action-toast.css, src/styles/view-tab-bar.css</files>
  <read_first>
    src/styles/design-tokens.css
    src/styles/diff-preview.css
    src/styles/audit.css
    src/styles/harness.css
    src/styles/tour.css
    src/styles/accessibility.css
    src/styles/algorithm-explorer.css
    src/styles/data-explorer.css
    src/styles/command-palette.css
    src/styles/help-overlay.css
    src/styles/import-toast.css
    src/styles/preset-suggestion-toast.css
    src/styles/catalog-actions.css
    src/styles/pivot.module.css
    src/styles/app-dialog.css
    src/styles/action-toast.css
    src/styles/view-tab-bar.css
  </read_first>
  <action>
Apply same tokenization rules from Plan 01:

1. Replace hardcoded px with tokens from design-tokens.css (reuse tokens from Plan 01; add new ones only if truly new unique sizes appear)
2. Audit overflow:hidden (~17 remaining occurrences across these files):
   - Band-aids: replace with proper flex/grid layout fixes (min-width: 0, text-overflow: ellipsis, overflow: auto)
   - Intentional: annotate with `/* intentional: [reason] */`
3. Keep `0`, keep `1px` borders as literals
4. For `harness.css` (test harness): tokenize where tokens already exist but do NOT create new tokens specifically for test harness sizes — use existing tokens or keep literals. Test harness is not user-facing.
5. For `pivot.module.css` (CSS module): same rules apply — CSS modules can use `var()` refs to design tokens

Files sorted by px count (target reductions):
- harness.css: 37 → <10 (test UI, less aggressive)
- audit.css: 32 → <8
- diff-preview.css: 13 → <5
- tour.css: 10 → <3
- accessibility.css: 9 → <3
- algorithm-explorer.css: 9 → <3
- data-explorer.css: 8 → <3
- command-palette.css: 7 → <3
- help-overlay.css: 6 → <2
- import-toast.css: 6 → <2
- preset-suggestion-toast.css: 6 → <2
- catalog-actions.css: 6 → <2
- pivot.module.css: 5 → <2
- app-dialog.css: 5 → <2
- action-toast.css: 4 → <2
- view-tab-bar.css: 2 → 0
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Aggregate hardcoded px count across ALL 32 CSS files (excluding design-tokens.css) is below 80 — check with: total=0; for f in src/styles/*.css; do [ "$f" = "src/styles/design-tokens.css" ] && continue; c=$(grep -cE '[0-9]+px' "$f"); total=$((total+c)); done; echo "Total: $total"
    - Every `overflow: hidden` across ALL 32 files either has `/* intentional:` comment or is gone — check with: grep -rn 'overflow.*hidden' src/styles/ | grep -v 'intentional:'
    - Tests pass (vitest exit code 0)
  </acceptance_criteria>
  <done>All 32 CSS files have near-zero hardcoded px values and fully audited overflow:hidden</done>
</task>

<task type="auto">
  <name>Task 2: Resolve 3 TS workarounds + final regression check</name>
  <files>src/ui/ViewTabBar.ts, src/worker/handlers/ui-state.handler.ts</files>
  <read_first>
    src/ui/ViewTabBar.ts
    src/worker/handlers/ui-state.handler.ts
    .planning/phases/178-css-code-hygiene-audit/178-CONTEXT.md
  </read_first>
  <action>
Fix the 3 known TS workarounds per D-03:

**1. src/ui/ViewTabBar.ts — Remove "insertBefore hack"**
- Line 19 has a comment about `appendTo` param; line 81 uses a legacy insertBefore path
- The `appendTo` parameter already exists in the constructor/mount signature
- Remove the `insertBefore` fallback code path — callers should always provide `appendTo`
- If any callers still rely on the insertBefore path, update them to pass `appendTo` instead
- Search for all call sites: `grep -rn "ViewTabBar" src/` to find constructor invocations
- Remove the "hack" comment and the dead code branch

**2. src/worker/handlers/ui-state.handler.ts — Clarify misleading comment**
- Line 67 says "bind param workaround" but the code is actually correct (using db.prepare() is the right approach for parameterized SQL in the Worker context, per the critical sql.js fix from v5.2)
- Replace the comment with: `/* db.prepare() required for parameterized SQL in Worker context (sql.js bind constraint) */`
- Do NOT change the code — only the comment

**3. src/styles/superwidget.css — focus-visible TODO (already handled in Plan 01 Task 1)**
- Verify it was addressed in Plan 01. If not, add `:focus-visible` outline rule for tab bar buttons: `outline: 2px solid var(--accent); outline-offset: -2px;`

**Final regression check:**
- Run `npx vitest run` — all unit tests pass
- Run `npx vite build` — production build succeeds
- Run `npx playwright test` — E2E specs pass (per D-06)
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run 2>&1 | tail -3 && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/ViewTabBar.ts does NOT contain the string "insertBefore hack" — check with: grep -c "insertBefore hack" src/ui/ViewTabBar.ts (must return 0)
    - src/worker/handlers/ui-state.handler.ts does NOT contain "bind param workaround" — check with: grep -c "bind param workaround" src/worker/handlers/ui-state.handler.ts (must return 0)
    - src/worker/handlers/ui-state.handler.ts contains "sql.js bind constraint" — the clarified comment
    - src/styles/superwidget.css does NOT contain "TODO: :focus-visible"
    - vitest exits with code 0
    - vite build exits with code 0
  </acceptance_criteria>
  <done>All 3 TS workarounds resolved, all tests green, build succeeds</done>
</task>

</tasks>

<verification>
Full codebase verification:
- `npx vitest run` — all unit tests pass
- `npx vite build` — production build succeeds with no errors
- `npx playwright test` — E2E specs pass (per D-06)
- Aggregate hardcoded px across all 32 CSS files (excl. design-tokens.css): < 80
- `grep -rn 'overflow.*hidden' src/styles/ | grep -v 'intentional:'` returns empty (all audited)
- `grep -rn 'insertBefore hack\|bind param workaround\|TODO.*focus-visible' src/` returns empty
</verification>

<success_criteria>
- All 32 CSS files have near-zero hardcoded px (aggressive tokenization per D-01)
- Every overflow:hidden is either a proper layout fix or annotated per D-02
- All 3 TS workarounds are resolved per D-03
- No new test infrastructure needed (existing suite catches regressions per D-06)
- Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/178-css-code-hygiene-audit/178-02-SUMMARY.md`
</output>
