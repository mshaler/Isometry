---
phase: 159-dataexplorer-catalog-completion
plan: 02
type: execute
wave: 2
depends_on: ["159-01"]
files_modified:
  - src/main.ts
autonomous: true
requirements: [EXPX-05, EXPX-06]

must_haves:
  truths:
    - "User can re-import a web source (json/csv/excel/markdown/html) from the Catalog actions column"
    - "User can delete a dataset from the Catalog with confirmation dialog (already works — verify only)"
  artifacts:
    - path: "src/main.ts"
      provides: "Web reimport else branch in onReimportDataset callback"
  key_links:
    - from: "src/main.ts onReimportDataset"
      to: "bridge.importFile"
      via: "File picker for web sources"
      pattern: "importFileHandler"
---

<objective>
Wire web source reimport so clicking the reimport button on a non-native dataset opens the file picker, and verify delete continues working.

Purpose: EXPX-05 requires reimport per dataset. Currently only alto_index_* native sources are handled — web sources (json, csv, excel, etc.) silently no-op. EXPX-06 requires delete with confirmation — this already works via onDeleteDataset + AppDialog.show, so this plan only verifies it.
Output: Complete reimport support for both native and web datasets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-dataexplorer-catalog-completion/159-01-SUMMARY.md
@src/main.ts (lines 1659-1697 — onReimportDataset callback)

<interfaces>
<!-- From src/main.ts lines 1659-1694: -->
<!-- onReimportDataset receives (datasetId, name, sourceType) -->
<!-- Currently: queries datasets:query to find ds, checks isNative && directoryPath for alto reimport -->
<!-- Missing: else branch for web sources (non-native or no directoryPath on non-alto) -->
<!-- importFileHandler is already defined in main.ts — it opens a file picker dialog -->
<!-- After import completes, coordinator.scheduleUpdate() + refreshDataExplorer() are called -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add web source reimport via file picker</name>
  <files>src/main.ts</files>
  <action>
In the onReimportDataset callback (around line 1694), after the `else if (isNative && !directoryPath)` block, add an else branch for web sources:

```typescript
} else {
  // Web source re-import: open file picker so user can select updated file
  // importFileHandler already handles file selection, parsing, and coordinator update
  importFileHandler();
}
```

This is intentionally simple. The reimport UX for web sources is: user clicks reimport button -> file picker opens -> user selects new file -> existing import flow runs. The new import will create a new dataset entry (or update the existing one depending on dedup). This matches the existing import flow without introducing a custom "reimport into same dataset" path.

Do NOT:
- Add a custom reimport-into-same-dataset pathway (overengineering — user can delete old + import new)
- Add source type filtering on the file picker (browser file pickers don't reliably support this)
- Touch the native reimport paths (already working)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>Clicking reimport on a web-sourced dataset opens the file picker. Native reimport paths unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify delete and reimport E2E readiness</name>
  <files>src/views/CatalogSuperGrid.ts</files>
  <action>
Verification-only task — no code changes expected unless issues found.

1. Read CatalogSuperGrid._renderActionButtons() and confirm both action buttons (reimport + delete) are rendered with correct data-action and data-dataset-id attributes.

2. Read the click handler in mount() and confirm:
   - action === 'delete' routes to config.onDeleteDataset
   - action === 'reimport' routes to config.onReimportDataset
   - Both look up dataset from lastDatasets for name/sourceType/cardCount

3. Read the onDeleteDataset callback in main.ts and confirm:
   - AppDialog.show with confirm variant and danger confirmVariant
   - On confirm: bridge.send('datasets:delete', { datasetId })
   - Followed by catalogGrid.refresh() and refreshDataExplorer()

4. If any wiring issue found, fix it. If everything checks out, this task produces no diff — just the verification log in SUMMARY.

5. Run full typecheck to confirm no regressions from Task 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/views/CatalogSuperGrid.test.ts --reporter=verbose</automated>
  </verify>
  <done>Delete and reimport action buttons are wired correctly. TypeScript compiles cleanly. CatalogSuperGrid tests pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npx vitest run tests/views/CatalogSuperGrid.test.ts` — all tests pass
3. Visual: open Catalog, click reimport on a web-sourced dataset — file picker opens
4. Visual: click delete on any dataset — confirmation dialog appears, confirming deletes the dataset
</verification>

<success_criteria>
- Reimport button on web sources opens the file picker (not a silent no-op)
- Reimport button on native sources continues to use alto reimport path
- Delete button continues to show confirmation and delete on confirm
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/159-dataexplorer-catalog-completion/159-02-SUMMARY.md`
</output>
