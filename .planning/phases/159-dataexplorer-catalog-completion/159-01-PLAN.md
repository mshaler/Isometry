---
phase: 159-dataexplorer-catalog-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/CatalogSuperGrid.ts
  - src/styles/data-explorer.css
  - tests/views/CatalogSuperGrid.test.ts
autonomous: true
requirements: [EXPX-04, EXPX-07]

must_haves:
  truths:
    - "Catalog cells show human-readable field values (name, source_type, card count, date) not numeric 0/1 counts"
    - "Active dataset row has visible accent highlight (background + left border)"
    - "Column headers show human-readable labels not raw field names"
  artifacts:
    - path: "src/views/CatalogSuperGrid.ts"
      provides: "_renderFieldValues() method, _stampRowKeys() stamps data-col-key, CATALOG_FIELD_LABELS map"
    - path: "src/styles/data-explorer.css"
      provides: "Fixed active row CSS selector targeting .pv-data-cell"
    - path: "tests/views/CatalogSuperGrid.test.ts"
      provides: "Unit tests for field value rendering and active row highlight"
  key_links:
    - from: "src/views/CatalogSuperGrid.ts"
      to: "src/views/pivot/PivotGrid.ts"
      via: "MutationObserver stamps data-col-key from colIdx -> CATALOG_FIELDS mapping"
      pattern: "data-col-key"
---

<objective>
Fix CatalogSuperGrid so cells display human-readable field values instead of numeric counts, column headers show friendly labels, and the active dataset row is visually highlighted.

Purpose: EXPX-04 requires dataset list with source type, card count, and import date. EXPX-07 requires active row highlighting. Currently cells show 0/1 (the is_active count), not field text; and the active row CSS targets .sg-cell (SuperGrid) but PivotTable renders .pv-data-cell.
Output: Working catalog display with correct cell content and active row accent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/views/CatalogSuperGrid.ts
@src/styles/data-explorer.css
@src/views/pivot/PivotGrid.ts

<interfaces>
<!-- PivotGrid renders cells as td.pv-data-cell with these data attributes: -->
<!-- data-key (getCellKey), data-row (rowIdx), data-col (colIdx), data-row-parity -->
<!-- Cell text is set to the numeric value from DataAdapter.fetchData() -->
<!-- CatalogSuperGrid._stampRowKeys() already stamps data-row-key on tr and .pv-data-cell -->
<!-- CATALOG_FIELDS = ['name','source_type','card_count','connection_count','last_imported_at','actions'] -->
<!-- CatalogBridgeAdapter.lastDatasets holds the raw dataset objects with all field values -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp data-col-key + render field values + friendly column headers</name>
  <files>src/views/CatalogSuperGrid.ts, tests/views/CatalogSuperGrid.test.ts</files>
  <behavior>
    - Test 1: _stampRowKeys stamps data-col-key on each .pv-data-cell matching CATALOG_FIELDS[colIdx]
    - Test 2: _renderFieldValues replaces numeric cell text with field value from lastDatasets (e.g., "My Dataset" not "1")
    - Test 3: _renderFieldValues formats last_imported_at as locale date string
    - Test 4: _renderFieldValues leaves actions cell empty (buttons rendered separately)
    - Test 5: CATALOG_FIELD_LABELS maps raw field names to human labels (e.g., 'source_type' -> 'Source', 'card_count' -> 'Cards')
  </behavior>
  <action>
1. Add CATALOG_FIELD_LABELS constant mapping field names to display labels:
   ```
   const CATALOG_FIELD_LABELS: Record<CatalogField, string> = {
     name: 'Name',
     source_type: 'Source',
     card_count: 'Cards',
     connection_count: 'Connections',
     last_imported_at: 'Imported',
     actions: '',
   };
   ```

2. In CatalogDataAdapter._colDims, use CATALOG_FIELD_LABELS values instead of raw CATALOG_FIELDS for the `values` array. This makes PivotGrid column headers render "Name", "Source", etc. Keep field-name-based keys in the cell data for col_key lookups.

   WAIT — the column dimension `values` are used as colPath elements in getCellKey(). Changing them would break data mapping. Instead, add a _renderColumnHeaders() method that finds PivotGrid's overlay header cells and replaces raw field names with labels. Call from MutationObserver callback.

3. In _stampRowKeys(), after stamping data-row-key on each .pv-data-cell, also stamp data-col-key using the cell's data-col attribute index into CATALOG_FIELDS:
   ```typescript
   const colIdx = Number(cell.dataset['col']);
   if (!Number.isNaN(colIdx) && colIdx < CATALOG_FIELDS.length) {
     cell.dataset['colKey'] = CATALOG_FIELDS[colIdx];
   }
   ```

4. Add _renderFieldValues() method called from MutationObserver (after _stampRowKeys):
   - Uses MutationObserver disconnect/reconnect pattern (already established in _stampRowKeys)
   - For each row (tr with data-row-key), look up dataset from lastDatasets by id
   - For each .pv-data-cell with data-col-key, replace textContent with the actual field value:
     - 'name': ds.name
     - 'source_type': ds.source_type
     - 'card_count': String(ds.card_count)
     - 'connection_count': String(ds.connection_count)
     - 'last_imported_at': formatted date or '—'
     - 'actions': '' (leave empty — _renderActionButtons handles this)
   - Skip cells where data-col-key is undefined

5. Add _renderColumnHeaders() method called from MutationObserver:
   - Find all leaf header elements in the overlay (span elements in .pv-overlay with text matching a CATALOG_FIELDS entry)
   - Replace text with CATALOG_FIELD_LABELS[field]
   - Use disconnect/reconnect pattern

6. Update MutationObserver callback in mount() to call in order:
   _stampRowKeys() -> _renderFieldValues() -> _renderColumnHeaders() -> _applyActiveRowHighlight() -> _renderActionButtons()

7. Create tests/views/CatalogSuperGrid.test.ts:
   - Use jsdom environment (no WASM needed — testing DOM manipulation only)
   - Create a minimal mock: build a table with tbody > tr > td.pv-data-cell structure, set d3 datum on tr elements, set data-col attributes on cells
   - Test _stampRowKeys stamps data-col-key correctly
   - Test _renderFieldValues replaces cell text with correct field values
   - Test date formatting for last_imported_at
   - Test actions cell left empty
   - Export the private methods via a test-only accessor or test the public refresh() -> DOM state cycle
   
   Since _stampRowKeys and _renderFieldValues are private, test via the observable DOM outcome: mount a CatalogSuperGrid with a mock bridge that returns known datasets, then inspect cell textContent after render. Use a minimal WorkerBridge mock that resolves datasets:query with test data.
  </action>
  <verify>
    <automated>npx vitest run tests/views/CatalogSuperGrid.test.ts --reporter=verbose</automated>
  </verify>
  <done>Catalog cells display human-readable field values. Column headers show friendly labels. data-col-key stamped on every .pv-data-cell.</done>
</task>

<task type="auto">
  <name>Task 2: Fix active row CSS selector</name>
  <files>src/styles/data-explorer.css</files>
  <action>
Fix the active row left-border CSS selector. Current rule targets `.sg-cell:first-child` which is a SuperGrid class. PivotTable renders `.pv-data-cell`. Change:

```css
/* BEFORE (broken — .sg-cell doesn't exist in PivotTable) */
.data-explorer__catalog-row--active .sg-cell:first-child {
  border-left: 3px solid var(--accent);
}

/* AFTER */
.data-explorer__catalog-row--active .pv-data-cell:first-child {
  border-left: 3px solid var(--accent);
}
```

The `.data-explorer__catalog-row--active` background rule is correct (applied to tr and cells via _applyActiveRowHighlight). Only the left-border child selector is broken.
  </action>
  <verify>
    <automated>grep -n "pv-data-cell:first-child" src/styles/data-explorer.css && ! grep -n "sg-cell" src/styles/data-explorer.css</automated>
  </verify>
  <done>Active dataset row shows accent background AND 3px accent left border on first cell. No references to .sg-cell remain in data-explorer.css.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/views/CatalogSuperGrid.test.ts` — all tests pass
2. `npx tsc --noEmit` — no type errors
3. Visual: open DataExplorer Catalog section — cells show dataset names/sources/counts/dates, active row highlighted
</verification>

<success_criteria>
- Catalog cells display field values (name, source type, card count, connection count, formatted date)
- Column headers show human-readable labels (Name, Source, Cards, Connections, Imported)
- Active dataset row has accent background and left border accent
- All existing functionality preserved (click handler, action buttons, refresh)
</success_criteria>

<output>
After completion, create `.planning/phases/159-dataexplorer-catalog-completion/159-01-SUMMARY.md`
</output>
