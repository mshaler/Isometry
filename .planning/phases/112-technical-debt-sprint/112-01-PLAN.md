---
phase: 112-technical-debt-sprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knip.json
  - src/components/SuperGridCSSDemo.tsx
  - src/components/supergrid/SuperGridScrollTest.tsx
  - src/components/supergrid/SuperDynamic.tsx
  - src/components/supergrid/SuperGridCSS.tsx
  - src/components/notebook/editor/nodes/EmbedToolbar.tsx
  - src/components/notebook/PropertyEditor.tsx
  - src/utils/import-export/office/index.ts
  - src/features/abtest/hooks.ts
  - src/utils/performance/performance-monitor.ts
  - src/utils/performance/rendering-performance.ts
  - src/state/FilterContext.tsx
autonomous: true

must_haves:
  truths:
    - "knip config recognizes barrel exports as entry points"
    - "No files have duplicate named + default exports"
    - "knip unused export count is reduced by at least 50"
  artifacts:
    - path: "knip.json"
      provides: "Updated knip configuration with barrel file entries"
      contains: "src/components/ui/index.ts"
    - path: "src/components/supergrid/SuperGridCSS.tsx"
      provides: "Single export pattern (no duplicate)"
      min_lines: 50
  key_links:
    - from: "knip.json"
      to: "src/**/index.ts"
      via: "entry array configuration"
      pattern: "entry.*index\\.ts"
---

<objective>
Update knip.json configuration to eliminate false positives from barrel exports and remove duplicate exports (named + default) from 11 files.

Purpose: Establish accurate baseline for knip unused export detection by fixing configuration and removing export duplication patterns that inflate counts.

Output: Updated knip.json, 11 files with single export pattern, reduced knip issue count.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-technical-debt-sprint/112-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update knip.json with barrel file entries</name>
  <files>knip.json</files>
  <action>
Update knip.json to add barrel/index files as entry points. Add the following to the `entry` array:

```json
{
  "entry": [
    "src/main.tsx",
    "src/App.tsx",
    "src/components/ui/index.ts",
    "src/components/shell/index.ts",
    "src/components/views/index.ts",
    "src/components/supergrid/cssgrid/index.ts",
    "src/contexts/index.ts",
    "src/d3/index.ts",
    "src/d3/hooks/index.ts",
    "src/d3/SuperDensityRenderer/index.ts",
    "src/dsl/index.ts",
    "src/filters/index.ts",
    "src/hooks/index.ts",
    "src/hooks/capture/index.ts",
    "src/hooks/embed/index.ts",
    "src/hooks/data/index.ts",
    "src/hooks/database/index.ts",
    "src/hooks/performance/index.ts",
    "src/hooks/system/index.ts",
    "src/hooks/ui/index.ts",
    "src/hooks/visualization/index.ts",
    "src/state/index.ts",
    "src/superstack/index.ts",
    "src/superstack/demos/index.ts",
    "src/superstack/hooks/index.ts",
    "src/services/terminal/index.ts",
    "src/types/index.ts",
    "src/utils/editor/index.ts",
    "src/utils/performance/index.ts",
    "src/utils/webview/index.ts",
    "src/utils/d3-visualization/index.ts",
    "src/components/ui/inputs/index.ts",
    "src/components/ui/buttons/index.ts"
  ]
}
```

Also add `@vitest/coverage-v8` to `ignoreDependencies` array to silence the unlisted dependency warning.

This fixes the false positive where knip flags barrel re-exports as unused because it doesn't recognize them as entry points.
  </action>
  <verify>Run `npx knip --reporter compact 2>&1 | head -50` and verify barrel file warnings are reduced.</verify>
  <done>knip.json updated with 33 barrel file entry points, @vitest/coverage-v8 added to ignoreDependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate exports from 11 files</name>
  <files>
    src/components/SuperGridCSSDemo.tsx
    src/components/supergrid/SuperGridScrollTest.tsx
    src/components/supergrid/SuperDynamic.tsx
    src/components/supergrid/SuperGridCSS.tsx
    src/components/notebook/editor/nodes/EmbedToolbar.tsx
    src/components/notebook/PropertyEditor.tsx
    src/utils/import-export/office/index.ts
    src/features/abtest/hooks.ts
    src/utils/performance/performance-monitor.ts
    src/utils/performance/rendering-performance.ts
    src/state/FilterContext.tsx
  </files>
  <action>
For each file with duplicate exports (both named export and `export default`), standardize to a single named export pattern. This improves tree-shaking and eliminates knip duplicate warnings.

**Pattern to apply:**

BEFORE (duplicate):
```typescript
export const ComponentName = function ComponentName() { /* ... */ };
export default ComponentName;
```

AFTER (single named export):
```typescript
export function ComponentName() { /* ... */ }
// Remove: export default ComponentName;
```

Files to update:

1. **src/components/SuperGridCSSDemo.tsx** - Remove `export default SuperGridCSSDemo` (line ~343), keep named export
2. **src/components/supergrid/SuperGridScrollTest.tsx** - Remove `export default SuperGridScrollTest` (line ~636), keep named export
3. **src/components/supergrid/SuperDynamic.tsx** - Remove `export default SuperDynamic` (line ~338), keep named export
4. **src/components/supergrid/SuperGridCSS.tsx** - Remove `export default SuperGridCSS` (line ~389), keep named export
5. **src/components/notebook/editor/nodes/EmbedToolbar.tsx** - Remove `export default EmbedToolbar` (line ~147), keep named export
6. **src/components/notebook/PropertyEditor.tsx** - Remove duplicate, keep one export
7. **src/utils/import-export/office/index.ts** - Remove duplicate OfficeDocumentProcessor export
8. **src/features/abtest/hooks.ts** - Remove duplicate useABTestVariant/useExperimentVariant
9. **src/utils/performance/performance-monitor.ts** - Remove duplicate simplePerformanceMonitor/performanceMonitor
10. **src/utils/performance/rendering-performance.ts** - Remove duplicate simpleRenderingMonitor/renderingPerformanceMonitor
11. **src/state/FilterContext.tsx** - Remove duplicate useFilters/useFilter (keep useFilters as primary)

After each file edit, search codebase for any imports using the removed default export pattern and update to named imports if found.
  </action>
  <verify>Run `npx knip --reporter json 2>/dev/null | grep -c '"duplicates":\[\]'` - should show increased count of files without duplicates.</verify>
  <done>All 11 files converted to single export pattern, no duplicate export warnings from knip for these files.</done>
</task>

<task type="auto">
  <name>Task 3: Verify knip improvement and run tests</name>
  <files>knip.json</files>
  <action>
1. Run full knip audit and capture metrics:
   ```bash
   npx knip --reporter compact 2>&1 | tail -20
   ```

2. Compare before/after counts:
   - Previous baseline: ~275 unused exports (from STATE.md)
   - Target: Reduction of at least 50 (config fixes + duplicate removals)

3. Run test suite to ensure no regressions:
   ```bash
   npm run gsd:test
   ```

4. Run TypeScript compilation:
   ```bash
   npm run gsd:build
   ```

5. If any imports broke due to export changes, fix them by updating to named import syntax:
   ```typescript
   // BEFORE (if using default import)
   import SuperGridCSS from './SuperGridCSS';

   // AFTER (named import)
   import { SuperGridCSS } from './SuperGridCSS';
   ```
  </action>
  <verify>
    - `npx knip --reporter compact 2>&1 | grep -E "unused|exports"` shows fewer issues than baseline
    - `npm run gsd:build` passes with 0 errors
    - `npm run gsd:test` passes
  </verify>
  <done>Knip unused export count reduced by 50+, all tests pass, TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. Run `npx knip --reporter compact` - verify reduced issue count
2. Run `npm run gsd:build` - TypeScript compilation passes
3. Run `npm run gsd:test` - all existing tests pass
4. No duplicate export warnings in knip output for updated files
</verification>

<success_criteria>
1. knip.json includes all 33 barrel file entry points
2. All 11 files with duplicate exports converted to single export pattern
3. Knip unused export count reduced from ~275 to <225 (50+ reduction)
4. All existing tests pass
5. TypeScript compiles with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/112-technical-debt-sprint/112-01-SUMMARY.md`
</output>
