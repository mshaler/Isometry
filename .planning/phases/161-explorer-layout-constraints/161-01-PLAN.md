---
phase: 161-explorer-layout-constraints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/workbench.css
autonomous: true
requirements: [LAYT-01, LAYT-02]

must_haves:
  truths:
    - "Opening 3 top-slot explorers simultaneously does not push view content below the fold"
    - "Top slot scrolls internally when combined explorer height exceeds 50vh"
    - "Bottom slot scrolls internally when combined explorer height exceeds 30vh"
  artifacts:
    - path: "src/styles/workbench.css"
      provides: "max-height constraints on .workbench-slot-top and .workbench-slot-bottom"
      contains: "max-height"
  key_links: []
---

<objective>
Add CSS height constraints to workbench slots so explorers never starve the view content area.

Purpose: Prevent viewport starvation when multiple explorers are open simultaneously.
Output: Two CSS properties added to workbench.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/workbench.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add max-height constraints to workbench slots</name>
  <files>src/styles/workbench.css</files>
  <action>
Add two CSS properties to existing rules in workbench.css:

1. `.workbench-slot-top` (line ~77): Add `max-height: 50vh;` — the rule already has `overflow-y: auto` so internal scrolling is automatic.

2. `.workbench-slot-bottom` (line ~93): Add `max-height: 30vh;` — same, `overflow-y: auto` already present.

Do NOT change any other properties. Do NOT add new selectors. This is a 2-line change.
  </action>
  <verify>
    <automated>grep -n 'max-height' src/styles/workbench.css | grep -E '50vh|30vh'</automated>
  </verify>
  <done>workbench-slot-top has max-height: 50vh, workbench-slot-bottom has max-height: 30vh, both scroll internally via existing overflow-y: auto</done>
</task>

</tasks>

<verification>
- grep for `max-height: 50vh` in `.workbench-slot-top` rule
- grep for `max-height: 30vh` in `.workbench-slot-bottom` rule
- Confirm no other rules were modified
</verification>

<success_criteria>
Top slot capped at 50vh with internal scroll. Bottom slot capped at 30vh with internal scroll. No other CSS changes.
</success_criteria>

<output>
After completion, create `.planning/phases/161-explorer-layout-constraints/161-01-SUMMARY.md`
</output>
