---
phase: 109
plan: "03"
type: implementation
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/styles/chrome/sidebar.css
  - src/styles/chrome/accordion.css
  - src/styles/chrome/dialog.css
  - src/styles/chrome-index.css
autonomous: true
must_haves:
  truths:
    - "Sidebar animates between expanded/collapsed via CSS only"
    - "Accordion uses native <details> element and works without JS"
    - "Dialog uses native <dialog> element with showModal()"
    - "Escape key closes dialog natively"
    - "No React re-renders for sidebar, accordion, or dialog interactions"
    - "Chrome animations run at 60fps"
  artifacts:
    - path: "src/styles/chrome/sidebar.css"
      provides: "CSS-driven sidebar with data-collapsed attribute"
      min_lines: 50
    - path: "src/styles/chrome/accordion.css"
      provides: "Native details accordion with CSS styling"
      min_lines: 60
    - path: "src/styles/chrome/dialog.css"
      provides: "Native dialog modal with backdrop blur"
      min_lines: 80
  key_links:
    - from: "src/styles/chrome-index.css"
      to: "src/styles/chrome/sidebar.css"
      via: "@import statement"
    - from: "src/styles/chrome-index.css"
      to: "src/styles/chrome/accordion.css"
      via: "@import statement"
    - from: "src/styles/chrome-index.css"
      to: "src/styles/chrome/dialog.css"
      via: "@import statement"
---

<objective>
Implement CSS-driven sidebar collapse, native `<details>` accordions, and native `<dialog>` modals — replacing React state management with one-line JS toggles and native browser APIs.

Purpose: Eliminate React state for UI chrome by leveraging native HTML elements and CSS transitions.
Output: Sidebar CSS, accordion CSS, dialog CSS — all using browser-native behavior.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-css-chrome-primitives/109-01-SUMMARY.md
@.planning/phases/109-css-chrome-primitives/109-02-SUMMARY.md
@src/styles/tokens.css
@src/styles/chrome-index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sidebar.css</name>
  <files>
    - src/styles/chrome/sidebar.css (new)
  </files>
  <action>
Create `src/styles/chrome/sidebar.css` with:

```css
/* CSS-driven sidebar — one-line JS toggle, CSS owns animation */

.iso-sidebar {
  width: var(--iso-sidebar-w);
  background: var(--iso-bg-raised);
  border-right: 1px solid var(--iso-border-default);
  overflow: hidden;
  transition: width var(--iso-transition-slow);
  display: flex;
  flex-direction: column;
}

/* Collapsed state — controlled by data attribute */
.iso-sidebar[data-collapsed="true"] {
  width: var(--iso-sidebar-collapsed-w);
}

/* Hide labels when collapsed */
.iso-sidebar[data-collapsed="true"] .iso-sidebar__label {
  opacity: 0;
  visibility: hidden;
  transition: opacity var(--iso-transition-fast),
              visibility var(--iso-transition-fast);
}

/* Show labels when expanded */
.iso-sidebar .iso-sidebar__label {
  opacity: 1;
  visibility: visible;
  transition: opacity var(--iso-transition-fast) 150ms,
              visibility var(--iso-transition-fast) 150ms;
  white-space: nowrap;
  overflow: hidden;
}

/* Sidebar navigation items */
.iso-sidebar__nav {
  flex: 1;
  overflow-y: auto;
  padding: var(--iso-space-md);
}

.iso-sidebar__item {
  display: flex;
  align-items: center;
  gap: var(--iso-space-md);
  padding: var(--iso-space-md);
  border-radius: var(--iso-radius-md);
  color: var(--iso-fg-secondary);
  cursor: pointer;
  transition: background var(--iso-transition-fast),
              color var(--iso-transition-fast);
}

.iso-sidebar__item:hover {
  background: var(--iso-bg-hover);
  color: var(--iso-fg-primary);
}

.iso-sidebar__item--active {
  background: var(--iso-bg-glass);
  color: var(--iso-fg-accent);
}

/* Icon container (always visible) */
.iso-sidebar__icon {
  flex-shrink: 0;
  width: 20px;
  height: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Toggle button */
.iso-sidebar__toggle {
  padding: var(--iso-space-md);
  border-top: 1px solid var(--iso-border-subtle);
  display: flex;
  justify-content: center;
}

.iso-sidebar__toggle button {
  background: none;
  border: none;
  color: var(--iso-fg-secondary);
  cursor: pointer;
  padding: var(--iso-space-sm);
  border-radius: var(--iso-radius-sm);
  transition: color var(--iso-transition-fast);
}

.iso-sidebar__toggle button:hover {
  color: var(--iso-fg-primary);
}
```

One-line toggle: `sidebar.dataset.collapsed = sidebar.dataset.collapsed !== 'true'`
  </action>
  <verify>
    ls -la src/styles/chrome/sidebar.css
    grep 'data-collapsed="true"' src/styles/chrome/sidebar.css
  </verify>
  <done>
    - sidebar.css exists with data-collapsed attribute selector
    - Transition on width for smooth animation
    - Label visibility toggles with collapse state
  </done>
</task>

<task type="auto">
  <name>Task 2: Create accordion.css</name>
  <files>
    - src/styles/chrome/accordion.css (new)
  </files>
  <action>
Create `src/styles/chrome/accordion.css` with:

```css
/* Native <details> accordion — no React state, SSR-friendly */

.iso-accordion {
  border: 1px solid var(--iso-border-default);
  border-radius: var(--iso-radius-md);
  overflow: hidden;
}

.iso-accordion + .iso-accordion {
  margin-top: -1px;
  border-top-left-radius: 0;
  border-top-right-radius: 0;
}

.iso-accordion:first-child {
  border-top-left-radius: var(--iso-radius-md);
  border-top-right-radius: var(--iso-radius-md);
}

.iso-accordion:last-child {
  border-bottom-left-radius: var(--iso-radius-md);
  border-bottom-right-radius: var(--iso-radius-md);
}

/* Summary (clickable header) */
.iso-accordion > summary {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--iso-space-lg) var(--iso-space-xl);
  background: var(--iso-bg-raised);
  color: var(--iso-fg-primary);
  font-size: var(--iso-font-size-md);
  font-weight: var(--iso-font-weight-medium);
  cursor: pointer;
  user-select: none;
  list-style: none;
  transition: background var(--iso-transition-fast);
}

.iso-accordion > summary::-webkit-details-marker {
  display: none;
}

.iso-accordion > summary:hover {
  background: var(--iso-bg-hover);
}

.iso-accordion > summary:focus-visible {
  outline: 2px solid var(--iso-border-accent);
  outline-offset: -2px;
}

/* Chevron indicator */
.iso-accordion > summary::after {
  content: '';
  width: 8px;
  height: 8px;
  border-right: 2px solid var(--iso-fg-secondary);
  border-bottom: 2px solid var(--iso-fg-secondary);
  transform: rotate(-45deg);
  transition: transform var(--iso-transition-fast);
  flex-shrink: 0;
}

.iso-accordion[open] > summary::after {
  transform: rotate(45deg);
}

/* Content panel */
.iso-accordion__content {
  padding: var(--iso-space-xl);
  background: var(--iso-bg-base);
  border-top: 1px solid var(--iso-border-subtle);
}

/* Animated open/close (requires JS for animate, but works without) */
.iso-accordion[data-animate] .iso-accordion__content {
  animation: accordion-open var(--iso-transition-slow) ease-out;
}

@keyframes accordion-open {
  from {
    opacity: 0;
    transform: translateY(-8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* Group container for connected accordions */
.iso-accordion-group {
  display: flex;
  flex-direction: column;
}

.iso-accordion-group .iso-accordion {
  border-radius: 0;
  border-bottom: none;
}

.iso-accordion-group .iso-accordion:first-child {
  border-top-left-radius: var(--iso-radius-md);
  border-top-right-radius: var(--iso-radius-md);
}

.iso-accordion-group .iso-accordion:last-child {
  border-bottom-left-radius: var(--iso-radius-md);
  border-bottom-right-radius: var(--iso-radius-md);
  border-bottom: 1px solid var(--iso-border-default);
}
```

Usage:
```html
<details class="iso-accordion">
  <summary>Section Title</summary>
  <div class="iso-accordion__content">Content here</div>
</details>
```
  </action>
  <verify>
    ls -la src/styles/chrome/accordion.css
    grep "iso-accordion" src/styles/chrome/accordion.css
  </verify>
  <done>
    - accordion.css exists with details/summary styling
    - Chevron rotates on open
    - Works without JavaScript (native details behavior)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create dialog.css</name>
  <files>
    - src/styles/chrome/dialog.css (new)
  </files>
  <action>
Create `src/styles/chrome/dialog.css` with:

```css
/* Native <dialog> modal — backdrop blur, Escape to close, no React re-render */

.iso-dialog {
  position: fixed;
  inset: 0;
  margin: auto;
  max-width: min(90vw, 560px);
  max-height: 85vh;

  /* Styling */
  padding: 0;
  background: var(--iso-bg-raised);
  border: 1px solid var(--iso-border-default);
  border-radius: var(--iso-radius-lg);
  box-shadow: var(--iso-shadow-deep);
  color: var(--iso-fg-primary);

  /* Open animation */
  opacity: 0;
  transform: scale(0.95) translateY(-10px);
  transition: opacity var(--iso-transition-normal),
              transform var(--iso-transition-normal),
              display var(--iso-transition-normal) allow-discrete,
              overlay var(--iso-transition-normal) allow-discrete;
}

.iso-dialog[open] {
  opacity: 1;
  transform: scale(1) translateY(0);
}

/* Starting style for animation (CSS @starting-style) */
@starting-style {
  .iso-dialog[open] {
    opacity: 0;
    transform: scale(0.95) translateY(-10px);
  }
}

/* Backdrop */
.iso-dialog::backdrop {
  background: rgba(0, 0, 0, 0.6);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  opacity: 0;
  transition: opacity var(--iso-transition-normal),
              display var(--iso-transition-normal) allow-discrete,
              overlay var(--iso-transition-normal) allow-discrete;
}

.iso-dialog[open]::backdrop {
  opacity: 1;
}

@starting-style {
  .iso-dialog[open]::backdrop {
    opacity: 0;
  }
}

/* Dialog header */
.iso-dialog__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--iso-space-lg) var(--iso-space-xl);
  border-bottom: 1px solid var(--iso-border-subtle);
}

.iso-dialog__title {
  margin: 0;
  font-size: var(--iso-font-size-lg);
  font-weight: var(--iso-font-weight-semi);
  color: var(--iso-fg-primary);
}

.iso-dialog__close {
  background: none;
  border: none;
  color: var(--iso-fg-secondary);
  cursor: pointer;
  padding: var(--iso-space-sm);
  border-radius: var(--iso-radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: color var(--iso-transition-fast),
              background var(--iso-transition-fast);
}

.iso-dialog__close:hover {
  color: var(--iso-fg-primary);
  background: var(--iso-bg-hover);
}

/* Dialog body */
.iso-dialog__body {
  padding: var(--iso-space-xl);
  overflow-y: auto;
  max-height: calc(85vh - 140px);
}

/* Dialog footer */
.iso-dialog__footer {
  display: flex;
  justify-content: flex-end;
  gap: var(--iso-space-md);
  padding: var(--iso-space-lg) var(--iso-space-xl);
  border-top: 1px solid var(--iso-border-subtle);
}

/* Button styles for dialogs */
.iso-dialog__btn {
  padding: var(--iso-space-md) var(--iso-space-xl);
  border-radius: var(--iso-radius-md);
  font-size: var(--iso-font-size-sm);
  font-weight: var(--iso-font-weight-medium);
  cursor: pointer;
  transition: background var(--iso-transition-fast),
              border-color var(--iso-transition-fast);
}

.iso-dialog__btn--secondary {
  background: transparent;
  border: 1px solid var(--iso-border-default);
  color: var(--iso-fg-primary);
}

.iso-dialog__btn--secondary:hover {
  background: var(--iso-bg-hover);
  border-color: var(--iso-border-strong);
}

.iso-dialog__btn--primary {
  background: var(--iso-fg-accent);
  border: 1px solid var(--iso-fg-accent);
  color: var(--iso-fg-white);
}

.iso-dialog__btn--primary:hover {
  background: #3d96ef;
  border-color: #3d96ef;
}

/* Danger variant */
.iso-dialog__btn--danger {
  background: var(--iso-priority-high);
  border: 1px solid var(--iso-priority-high);
  color: var(--iso-fg-white);
}

.iso-dialog__btn--danger:hover {
  background: #d32f2f;
  border-color: #d32f2f;
}

/* Size variants */
.iso-dialog--sm {
  max-width: min(90vw, 400px);
}

.iso-dialog--lg {
  max-width: min(90vw, 720px);
}

.iso-dialog--full {
  max-width: 95vw;
  max-height: 95vh;
}
```

Usage:
```html
<dialog class="iso-dialog" id="myDialog">
  <header class="iso-dialog__header">
    <h2 class="iso-dialog__title">Dialog Title</h2>
    <button class="iso-dialog__close" onclick="this.closest('dialog').close()">×</button>
  </header>
  <div class="iso-dialog__body">Content here</div>
  <footer class="iso-dialog__footer">
    <button class="iso-dialog__btn iso-dialog__btn--secondary" onclick="this.closest('dialog').close()">Cancel</button>
    <button class="iso-dialog__btn iso-dialog__btn--primary">Confirm</button>
  </footer>
</dialog>
```

Open: `document.getElementById('myDialog').showModal()`
Close: Native Escape key works automatically, or `dialog.close()`
  </action>
  <verify>
    ls -la src/styles/chrome/dialog.css
    grep "iso-dialog" src/styles/chrome/dialog.css
    grep "::backdrop" src/styles/chrome/dialog.css
  </verify>
  <done>
    - dialog.css exists with native dialog styling
    - Backdrop blur effect defined
    - Open/close animations with @starting-style
    - Button variants (primary, secondary, danger)
  </done>
</task>

<task type="auto">
  <name>Task 4: Update chrome-index.css with all Phase 109 imports</name>
  <files>
    - src/styles/chrome-index.css (modify)
  </files>
  <action>
Update `src/styles/chrome-index.css` to include all Phase 109 chrome files:

```css
/* Chrome CSS aggregator — Phase 109 */
@import './chrome/sticky-headers.css';
@import './chrome/selection.css';
@import './chrome/scroll-shadows.css';
@import './chrome/tooltip.css';
@import './chrome/sidebar.css';
@import './chrome/accordion.css';
@import './chrome/dialog.css';
```

This task depends on 109-01 and 109-02 completing first to establish the chrome-index.css file with their imports.
  </action>
  <verify>
    grep "sidebar.css" src/styles/chrome-index.css
    grep "accordion.css" src/styles/chrome-index.css
    grep "dialog.css" src/styles/chrome-index.css
    npm run gsd:build
  </verify>
  <done>
    - chrome-index.css includes all 7 chrome CSS modules
    - Build passes with zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. **Sidebar collapse:**
   - Add `data-collapsed="false"` to sidebar element
   - Click toggle — `data-collapsed` changes to `"true"`
   - Sidebar animates from 260px to 44px
   - Labels fade out, icons remain visible
   - No React state involved — check DevTools

2. **Accordion panels:**
   - Create `<details class="iso-accordion">` element
   - Click summary — content expands
   - Click again — content collapses
   - Works without JavaScript (native behavior)
   - Animation plays on open (with `data-animate` attribute)
   - SSR-friendly — no hydration required

3. **Dialogs:**
   - Create `<dialog class="iso-dialog">` element
   - Call `dialog.showModal()` — dialog appears with backdrop blur
   - Press Escape — dialog closes (native behavior)
   - Click backdrop — dialog should close (if `onclick` added)
   - Check no React re-renders on open/close
</verification>

<success_criteria>
- [ ] Sidebar animates between expanded/collapsed via CSS only
- [ ] One-line JS toggle: `el.dataset.collapsed = !el.dataset.collapsed`
- [ ] Accordion uses native `<details>` element, works without JS
- [ ] Accordion has smooth open/close animation
- [ ] Dialog uses native `<dialog>` element with showModal()
- [ ] Dialog backdrop has blur effect
- [ ] Escape key closes dialog natively
- [ ] No React re-renders for any of these interactions
</success_criteria>

<output>
After completion, create `.planning/phases/109-css-chrome-primitives/109-03-SUMMARY.md`
</output>
