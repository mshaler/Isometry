---
phase: 35-pafv-grid-core
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [
  src/components/DataFlowMonitor.tsx,
  src/contexts/LiveDataContext.tsx,
  src/components/debug/QueryClientDiagnostic.tsx,
  src/services/queryClient.ts,
  src/components/performance/PerformanceBaseline.tsx,
  src/components/performance/RenderingMetricsPanel.tsx,
  src/components/settings/NotesIntegrationSettings.tsx,
  src/components/shared/ConnectionStatus.tsx,
  src/components/supergrid/AdvancedSuperGridDemo.tsx,
  src/components/supergrid/SuperDensity.tsx,
  src/components/SuperGridDemo.tsx,
  src/components/SuperGridView.tsx,
  src/utils/logging/dev-logger.ts,
  src/utils/logging/index.ts
]
autonomous: true

must_haves:
  truths:
    - "TypeScript compilation succeeds without errors"
    - "Export/import mismatches are resolved across all modules"
    - "Type definitions match actual implementations"
    - "Logging utilities have consistent export patterns"
    - "Component interfaces align with context provider exports"
  artifacts:
    - path: "src/contexts/LiveDataContext.tsx"
      provides: "Properly exported useLiveDataContext and related hooks"
      exports: ["useLiveDataContext", "useLiveDataGlobalState", "useLiveDataMetrics"]
    - path: "src/services/queryClient.ts"
      provides: "Properly exported cacheUtils and queryKeys"
      exports: ["cacheUtils", "queryKeys"]
    - path: "src/utils/logging/dev-logger.ts"
      provides: "Consistent logging utility exports"
      exports: ["contextLogger", "bridgeLogger", "performanceLogger", "createLogger"]
    - path: "src/components/SuperGridView.tsx"
      provides: "Proper JanusDensityState interface usage"
      contains: "extentMode, valueMode, zoomLevel, panOffset"
  key_links:
    - from: "src/components/DataFlowMonitor.tsx"
      to: "src/contexts/LiveDataContext.tsx"
      via: "useLiveDataContext import"
      pattern: "import.*useLiveDataContext.*from.*LiveDataContext"
    - from: "src/components/debug/QueryClientDiagnostic.tsx"
      to: "src/services/queryClient.ts"
      via: "cacheUtils import"
      pattern: "import.*cacheUtils.*from.*queryClient"
    - from: "src/components/SuperGridDemo.tsx"
      to: "src/utils/logging/dev-logger.ts"
      via: "contextLogger import"
      pattern: "import.*contextLogger.*from.*dev-logger"
---

<objective>
Systematic TypeScript error resolution focusing on export/import mismatches and type definitions.

Purpose: Stabilize the codebase foundation by resolving critical type errors that prevent compilation
Output: Clean TypeScript compilation with zero type errors for core SuperGrid functionality
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current TypeScript errors require systematic cleanup per CLAUDE.md guidelines
@/Users/mshaler/Developer/Projects/Isometry/CLAUDE.md

# Previous SuperGrid implementation for context
@.planning/phases/35-pafv-grid-core/35-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix LiveDataContext Export/Import Mismatches</name>
  <files>
    src/contexts/LiveDataContext.tsx
    src/components/DataFlowMonitor.tsx
    src/components/settings/NotesIntegrationSettings.tsx
    src/components/shared/ConnectionStatus.tsx
    src/components/test/LiveQueryTest.tsx
    src/context/ConnectionContext.tsx
  </files>
  <action>
    Fix export/import mismatches for LiveDataContext by:
    1. Add missing exports to LiveDataContext.tsx: useLiveDataContext, useLiveDataGlobalState, useLiveDataMetrics
    2. Update imports in consuming components to use correct export names
    3. Ensure consistent naming: useLiveDataContext (not LiveDataContextType) for the hook
    4. Verify all context-related exports match their actual implementations
    5. Follow established React context pattern from existing working components

    Apply CLAUDE.md cleanup GSD pattern: surgical fixes only, no behavior changes.
  </action>
  <verify>npm run check:types 2>&1 | grep -E "(useLiveDataContext|useLiveDataGlobalState|useLiveDataMetrics)" | wc -l</verify>
  <done>All LiveDataContext export/import errors resolved, hook names consistent across codebase</done>
</task>

<task type="auto">
  <name>Fix QueryClient and Service Export Mismatches</name>
  <files>
    src/services/queryClient.ts
    src/components/debug/QueryClientDiagnostic.tsx
    src/utils/cacheInvalidation.ts
  </files>
  <action>
    Fix export/import mismatches for QueryClient services by:
    1. Add missing exports to queryClient.ts: cacheUtils, queryKeys
    2. Update imports in consuming components to use correct export names
    3. Ensure service exports match actual service implementations
    4. Verify query client interface consistency across all usages
    5. Follow established service export pattern from DatabaseService.ts

    Apply surgical fixes: add missing exports without changing service behavior.
  </action>
  <verify>npm run check:types 2>&1 | grep -E "(cacheUtils|queryKeys)" | wc -l</verify>
  <done>All QueryClient export/import errors resolved, service interfaces consistent</done>
</task>

<task type="auto">
  <name>Fix Logging Utility Export Patterns</name>
  <files>
    src/utils/logging/dev-logger.ts
    src/utils/logging/index.ts
    src/components/SuperGridDemo.tsx
    src/components/SuperGridView.tsx
    src/components/ViewSwitcher.tsx
  </files>
  <action>
    Fix export/import mismatches for logging utilities by:
    1. Standardize dev-logger.ts exports: contextLogger, bridgeLogger, performanceLogger, createLogger
    2. Update index.ts to use correct import/export syntax for each logger
    3. Fix consuming components to use correct import syntax (named vs default imports)
    4. Ensure consistent logging interface across all components
    5. Follow established utility export pattern from existing working utilities

    Apply mechanical fixes: standardize export patterns without changing logging behavior.
  </action>
  <verify>npm run check:types 2>&1 | grep -E "(contextLogger|bridgeLogger|performanceLogger|createLogger)" | wc -l</verify>
  <done>All logging utility export/import errors resolved, consistent import patterns established</done>
</task>

</tasks>

<verification>
Run `npm run check:types` and verify:
1. Zero TypeScript compilation errors for all modified files
2. Export/import consistency across modules
3. Type definitions match actual implementations
4. No regression in existing functionality
</verification>

<success_criteria>
1. TypeScript compilation passes without errors (npm run check:types exits 0)
2. All export/import mismatches resolved for LiveDataContext, QueryClient, and logging utilities
3. Type interfaces properly align with actual implementations
4. Consistent naming patterns established across the codebase
5. Foundation ready for Phase 36+ development without type conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/35-pafv-grid-core/35-06-SUMMARY.md`
</output>