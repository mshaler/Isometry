---
phase: 110-alto-index-e2e
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/alto-index/generate-alto-fixtures.mjs
  - tests/fixtures/alto-index/notes.json
  - e2e/alto-index.spec.ts
autonomous: true
requirements:
  - ALTO-04
gap_closure: true

must_haves:
  truths:
    - "Total alto-index fixture cards exceed 500 (strict >), triggering isBulkImport=true in etl-import-native.handler.ts"
    - "FTS5 bulk rebuild describe block asserts totalCount > 500 (not >= 500)"
  artifacts:
    - path: "tests/fixtures/alto-index/notes.json"
      provides: "252 note cards (252 + 250 others = 502 total, crossing > 500 threshold)"
      contains: "alto_notes"
    - path: "tests/fixtures/alto-index/generate-alto-fixtures.mjs"
      provides: "Generator with generateNotes default of 252"
      contains: "generateNotes(252)"
    - path: "e2e/alto-index.spec.ts"
      provides: "FTS5 describe block with > 500 assertion"
      contains: "toBeGreaterThan(500)"
  key_links:
    - from: "e2e/alto-index.spec.ts"
      to: "src/worker/handlers/etl-import-native.handler.ts"
      via: "importAltoIndex triggers etl:import-native with 502 cards, crossing isBulkImport = totalCards > 500"
      pattern: "toBeGreaterThan\\(500\\)"
---

<objective>
Close the FTS5 bulk rebuild gap: bump notes.json from 250 to 252 cards so total fixture count is 502 (exceeding the strict > 500 threshold), and update the E2E spec assertion from >= 500 to > 500.

Purpose: The isBulkImport code path in etl-import-native.handler.ts triggers at totalCards > 500 (strict). Current fixtures produce exactly 500 cards, so the bulk FTS5 rebuild path is never exercised. Adding 2 more notes crosses the threshold.
Output: Regenerated notes.json (252 cards), updated generator script, updated spec assertion.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-alto-index-e2e/110-CONTEXT.md
@.planning/phases/110-alto-index-e2e/110-VERIFICATION.md
@.planning/phases/110-alto-index-e2e/110-01-SUMMARY.md

<interfaces>
From src/worker/handlers/etl-import-native.handler.ts (line 58):
```typescript
const isBulkImport = totalCards > 500;
```
This is the threshold that must be crossed. At exactly 500, isBulkImport is false.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump notes fixture to 252 cards and update spec assertion</name>
  <files>tests/fixtures/alto-index/generate-alto-fixtures.mjs, tests/fixtures/alto-index/notes.json, e2e/alto-index.spec.ts</files>
  <read_first>
    - tests/fixtures/alto-index/generate-alto-fixtures.mjs
    - e2e/alto-index.spec.ts
    - src/worker/handlers/etl-import-native.handler.ts (lines 50-65 for isBulkImport threshold)
  </read_first>
  <action>
Three targeted changes:

1. **generate-alto-fixtures.mjs** -- Change the generateNotes default parameter and the call site:
   - Line 45: change `function generateNotes(count = 250)` to `function generateNotes(count = 252)`
   - Line 889: change `const notes = generateNotes(250)` to `const notes = generateNotes(252)`

2. **Regenerate notes.json:**
   ```bash
   node tests/fixtures/alto-index/generate-alto-fixtures.mjs
   ```
   This produces notes.json with 252 cards. Total across 11 types: 252 + (10 * 25) = 502, which crosses the `totalCards > 500` threshold.

3. **e2e/alto-index.spec.ts** -- Update the FTS5 describe block (around line 110-123):
   - Change the test name from `'500-card import populates FTS5 and cards are searchable'` to `'502-card import triggers FTS5 bulk rebuild and cards are searchable'`
   - Change the comment on line 115 from `(250 notes + 10 x 25 = 500 cards)` to `(252 notes + 10 x 25 = 502 cards)`
   - Change line 119 comment from `Fixtures produce exactly 500 cards — assert at or above threshold` to `Fixtures produce 502 cards — must exceed 500 for isBulkImport=true`
   - Change line 122 message from `'Total card count must be >= 500 to exercise the FTS5 bulk path'` to `'Total card count must exceed 500 for isBulkImport=true'`
   - Change line 123 from `.toBeGreaterThanOrEqual(500)` to `.toBeGreaterThan(500)`

Do NOT change any other fixture files or describe blocks. Only the FTS5 block assertion and the notes fixture.
  </action>
  <verify>
    <automated>node -e "const d=require('./tests/fixtures/alto-index/notes.json'); const total=d.length+25*10; console.log('notes:',d.length,'total:',total); if(total<=500){process.exit(1)}" && grep -n "toBeGreaterThan(500)" e2e/alto-index.spec.ts && grep -n "generateNotes(252)" tests/fixtures/alto-index/generate-alto-fixtures.mjs</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/alto-index/notes.json contains exactly 252 JSON objects
    - generate-alto-fixtures.mjs line 45 reads `function generateNotes(count = 252)`
    - generate-alto-fixtures.mjs line 889 reads `const notes = generateNotes(252)`
    - e2e/alto-index.spec.ts FTS5 block uses `.toBeGreaterThan(500)` (NOT `.toBeGreaterThanOrEqual(500)`)
    - e2e/alto-index.spec.ts FTS5 test name contains "502-card" (not "500-card")
    - Total fixture count across 11 types is 502 (252 + 250)
    - All other fixture files remain at 25 cards each
    - All other describe blocks in alto-index.spec.ts are unchanged
  </acceptance_criteria>
  <done>notes.json has 252 cards, generator updated to 252, spec asserts > 500, total fixture count is 502 crossing the isBulkImport threshold</done>
</task>

</tasks>

<verification>
- `node -e "const d=require('./tests/fixtures/alto-index/notes.json'); console.log(d.length)"` outputs 252
- `grep "toBeGreaterThan(500)" e2e/alto-index.spec.ts` matches exactly 1 line
- `grep "toBeGreaterThanOrEqual(500)" e2e/alto-index.spec.ts` matches 0 lines
- `grep "generateNotes(252)" tests/fixtures/alto-index/generate-alto-fixtures.mjs` matches 2 lines (default + call)
- `npx playwright test e2e/alto-index.spec.ts` passes all 4 describe blocks
</verification>

<success_criteria>
- notes.json contains 252 cards, total fixture count is 502
- FTS5 spec assertion is strictly > 500 (not >= 500)
- The isBulkImport=true code path is now exercised during E2E test execution
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/110-alto-index-e2e/110-03-SUMMARY.md`
</output>
