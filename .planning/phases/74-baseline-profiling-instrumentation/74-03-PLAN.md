---
phase: 74-baseline-profiling-instrumentation
plan: 03
type: execute
wave: 2
depends_on: [74-02]
files_modified:
  - vite.config.analyze.ts
  - package.json
  - .gitignore
  - .planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md
autonomous: true
requirements: [PROF-07]

must_haves:
  truths:
    - "npm run analyze produces a treemap HTML file with gzip sizes for all production chunks"
    - "stats.html is gitignored and regenerated on demand"
    - "BOTTLENECKS.md exists with ranked numeric evidence from all 4 domains (render, query, import, bundle)"
    - "BOTTLENECKS.md contains ms/MB tables at 1K/5K/20K scale gating Phase 76 and 77 work"
  artifacts:
    - path: "vite.config.analyze.ts"
      provides: "Vite config with rollup-plugin-visualizer for bundle treemap"
    - path: ".planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md"
      provides: "Ranked bottleneck list with numeric ms/MB evidence"
  key_links:
    - from: "package.json"
      to: "vite.config.analyze.ts"
      via: "npm run analyze script"
      pattern: "vite build --config vite.config.analyze.ts"
---

<objective>
Install rollup-plugin-visualizer, create the bundle analysis config (PROF-07), run all bench files, and produce the ranked BOTTLENECKS.md document with numeric evidence across all 4 performance domains.

Purpose: BOTTLENECKS.md is the phase gate — it contains the ranked list of bottlenecks with ms/MB values that Phase 75 uses to derive budgets and Phase 76/77 use to prioritize optimization work.
Output: vite.config.analyze.ts, npm run analyze script, BOTTLENECKS.md with data from bench runs + bundle analysis.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-baseline-profiling-instrumentation/74-RESEARCH.md
@.planning/phases/74-baseline-profiling-instrumentation/74-02-SUMMARY.md

@vite.config.ts (base config pattern)
@vite.config.native.ts (reference for plugin setup)
@package.json (scripts section)
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install rollup-plugin-visualizer + create analyze config</name>
  <files>vite.config.analyze.ts, package.json, .gitignore</files>
  <action>
    1. **Install dependency:**
       ```
       npm install -D rollup-plugin-visualizer
       ```

    2. **Create `vite.config.analyze.ts`:**
       - Import `defineConfig` from 'vite', `visualizer` from 'rollup-plugin-visualizer', `viteStaticCopy` from 'vite-plugin-static-copy'
       - Export defineConfig with:
         - plugins: viteStaticCopy (same WASM target as vite.config.ts) + visualizer({ filename: 'stats.html', template: 'treemap', gzipSize: true, brotliSize: true, open: true })
         - worker: { format: 'es' }
         - build: same target (es2022), assetsInlineLimit: 0, outDir: 'dist-analyze' (separate from main dist)
         - define: { __PERF_INSTRUMENTATION__: false } (analyze production-like build)
       - Do NOT include build.lib or rollupOptions.external — this is an app build (like vite.config.native.ts), not a library build, so all chunks including sql.js are visible in the treemap

    3. **Add npm script to `package.json`:**
       - Add `"analyze": "vite build --config vite.config.analyze.ts"` to scripts section

    4. **Update `.gitignore`:**
       - Add `stats.html` entry
       - Add `dist-analyze/` entry

    5. **Verify:** Run `npm run analyze` — should produce `stats.html` and open in browser. The treemap should show module sizes for sql.js, D3, marked, DOMPurify, and app code.
  </action>
  <verify>
    <automated>npm run analyze 2>&1 | tail -5 && test -f stats.html && echo "stats.html exists"</automated>
  </verify>
  <done>npm run analyze produces stats.html treemap with gzip sizes. stats.html and dist-analyze/ are gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Run profiling and create BOTTLENECKS.md</name>
  <files>.planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md</files>
  <action>
    1. **Run all bench files and capture output:**
       ```
       npx vitest bench tests/profiling/ --run --reporter=verbose 2>&1 > /tmp/bench-results.txt
       ```
       Also run with JSON output if available:
       ```
       npx vitest bench tests/profiling/ --run --outputJson /tmp/bench-results.json 2>/dev/null
       ```

    2. **Run bundle analysis and capture key numbers:**
       - Run `npm run analyze` (already done in Task 1, but re-run if needed)
       - Note the top modules by size (raw + gzip) from the stats.html treemap
       - Record: total bundle size (raw + gzip), sql.js size, D3 size, app code size, WASM file size

    3. **Create `.planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md`:**

       Structure the document as follows:

       ```markdown
       # Phase 74: Bottleneck Analysis

       **Date:** {today}
       **Purpose:** Ranked bottleneck list gating Phase 76 (Render) and Phase 77 (Import/Launch/Memory) optimization work.
       **Methodology:** Vitest bench p99 values at 1K/5K/20K scale + rollup-plugin-visualizer bundle analysis.

       ## Ranked Bottlenecks (by user-perceived severity)

       | Rank | Domain | Bottleneck | Severity | Evidence |
       |------|--------|-----------|----------|----------|
       | 1 | ... | ... | freq x latency x impact | p99: Xms at 20K |
       | ... | ... | ... | ... | ... |

       Ranking formula: frequency (how often user triggers) x latency (ms at 20K) x user-facing impact (blocks interaction vs background).

       ## Domain: SQL Query (PROF-04)

       | Query | 1K p99 | 5K p99 | 20K p99 | ops/sec (20K) |
       |-------|--------|--------|---------|---------------|
       | GROUP BY folder, card_type | Xms | Xms | Xms | N |
       | GROUP BY status | ... | ... | ... | ... |
       | GROUP BY created_at (month) | ... | ... | ... | ... |
       | FTS 3-word search | ... | ... | ... | ... |

       ## Domain: SuperGrid Render (PROF-05)

       | Axis Config | Cell Count | p99 | ops/sec |
       |-------------|-----------|-----|---------|
       | single (folder) | 1K | ... | ... |
       | dual (folder x card_type) | 5K | ... | ... |
       | triple (folder x card_type x status) | 20K | ... | ... |

       ## Domain: ETL Import (PROF-06)

       | Source Type | 1K cards/s | 5K cards/s | 20K cards/s | p99 total (20K) |
       |------------|-----------|-----------|------------|-----------------|
       | apple_notes | ... | ... | ... | ... |
       | csv | ... | ... | ... | ... |
       | json | ... | ... | ... | ... |
       | markdown | ... | ... | ... | ... |

       ## Domain: Bundle Size (PROF-07)

       | Module | Raw Size | Gzip Size | % of Total |
       |--------|----------|-----------|------------|
       | sql.js | ... | ... | ... |
       | D3.js | ... | ... | ... |
       | App code | ... | ... | ... |
       | WASM (sql-wasm-fts5) | ... | ... | ... |
       | **Total** | ... | ... | 100% |

       ## Phase 76/77 Inputs

       - Phase 76 (Render): Top N SQL query bottlenecks by p99 at 20K
       - Phase 77 (Import): ETL throughput limiting factor per source type
       - Phase 77 (Launch): WASM + bundle gzip overhead for cold start
       - Phase 77 (Memory): Noted for Phase 77 physical device measurement (not measurable in vitest)

       ## Notes

       - All measurements on {machine description}
       - Memory measurements deferred to Phase 77 (requires physical device + Xcode Instruments)
       - No budgets proposed — Phase 75 derives budgets from this data
       ```

       Fill all tables with actual measured values from the bench runs. Where bench output is in the verbose reporter, extract the p99 column values. For cards/second, calculate from `hz` (ops/sec) * cards per operation.
  </action>
  <verify>
    <automated>test -f .planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md && grep -c "p99" .planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md</automated>
  </verify>
  <done>BOTTLENECKS.md exists with ranked bottleneck list, numeric ms/MB evidence tables for all 4 domains at 1K/5K/20K scale, and Phase 76/77 input pointers. No budgets proposed (deferred to Phase 75).</done>
</task>

</tasks>

<verification>
1. `npm run analyze` — produces stats.html without error
2. `test -f stats.html` — file exists
3. `git check-ignore stats.html` — returns stats.html (gitignored)
4. `test -f .planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md` — exists
5. `grep "Ranked Bottlenecks" .planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md` — contains ranking section
6. `grep -c "|" .planning/phases/74-baseline-profiling-instrumentation/BOTTLENECKS.md` — has markdown tables with data
</verification>

<success_criteria>
- rollup-plugin-visualizer installed as devDependency
- vite.config.analyze.ts produces treemap with gzip + brotli sizes
- npm run analyze script works end-to-end
- stats.html + dist-analyze/ gitignored
- BOTTLENECKS.md has numeric evidence from all 4 domains
- BOTTLENECKS.md ranked by user-perceived severity (frequency x latency x impact)
- BOTTLENECKS.md contains no budget proposals (Phase 75 responsibility)
- BOTTLENECKS.md ready to gate Phase 76 and 77 optimization work
</success_criteria>

<output>
After completion, create `.planning/phases/74-baseline-profiling-instrumentation/74-03-SUMMARY.md`
</output>
