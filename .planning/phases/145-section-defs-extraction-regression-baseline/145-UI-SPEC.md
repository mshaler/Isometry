---
phase: 145
slug: section-defs-extraction-regression-baseline
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-09
---

# Phase 145 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase UI Surface Assessment

Phase 145 is a **zero user-facing UI change** phase. Its scope is:

1. Extract `SECTION_DEFS`, `DOCK_DEFS`, interfaces, and `viewOrder` from `SidebarNav.ts` and `main.ts` into `src/ui/section-defs.ts`.
2. Add ShortcutRegistry unit and integration regression tests covering all Cmd+1-9 bindings.

No new components are rendered. No existing components are visually modified. The design contract below enforces consistency with the existing system and governs any incidental test-runner or console output formatting.

**shadcn gate result:** `components.json` not present. Stack is TypeScript + D3 (no React/Next.js/Vite framework components). shadcn does not apply. Tool: none.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | codebase scan — no components.json |
| Preset | not applicable | no shadcn |
| Component library | none (vanilla TypeScript + D3.js) | MEMORY.md |
| Icon library | Lucide SVG strings via `src/ui/icons.ts` (MIT) | codebase scan |
| Font | -apple-system, BlinkMacSystemFont, "Helvetica Neue", system-ui, sans-serif | design-tokens.css `:root` |
| Monospace font | ui-monospace, "SF Mono", "Cascadia Code", Menlo, monospace | design-tokens.css `:root` |

---

## Spacing Scale

Existing tokens from `src/styles/design-tokens.css`. Phase 145 introduces no new spacing. All existing code must continue to use these tokens — no new `px` literals.

| Token | Value | Usage |
|-------|-------|-------|
| `--space-xs` | 4px | Icon gaps, inline padding |
| `--space-sm` | 8px | Compact element spacing |
| `--space-md` | 12px | Default element spacing |
| `--space-lg` | 16px | Section padding |
| `--space-xl` | 24px | Major section breaks |

Note: The project spacing scale uses 4px increments but the named tokens do not align to a strict 8-point grid at `--space-md` (12px). This is an existing project decision — do not alter it in this phase.

Exceptions: none for Phase 145.

---

## Typography

Existing tokens from `src/styles/design-tokens.css`. Phase 145 introduces no new typographic elements.

| Role | Token | Size | Weight | Line Height |
|------|-------|------|--------|-------------|
| Label / section header | `--text-sm` | 11px | 600 (semibold) | 1.4 |
| Body / item label | `--text-base` | 13px | 400 (regular) | 1.5 |
| Secondary / muted copy | `--text-md` | 14px | 400 (regular) | 1.5 |
| Heading / display label | `--text-lg` | 16px | 600 (semibold) | 1.2 |

Font weights declared: 400 (regular) and 600 (semibold) only.

---

## Color

Existing tokens from `src/styles/design-tokens.css`. All 5 themes supported (dark, light, system, nextstep, material). Phase 145 introduces no color changes.

| Role | Dark Token / Value | Light Token / Value | Usage |
|------|-------------------|---------------------|-------|
| Dominant (60%) | `--bg-primary` (#1a1a2e) | `--bg-primary` (#ffffff) | App background, main surfaces |
| Secondary (30%) | `--bg-secondary` (#16213e) / `--bg-surface` (#252540) | `--bg-secondary` (#f7f7f8) / `--bg-surface` (#eeeeef) | Sidebar, nav panels, cards |
| Accent (10%) | `--accent` (#4a9eff) | `--accent` (#2563eb) | Active item highlight, keyboard focus ring |
| Destructive | `--danger` (#ff4a4a) | `--danger` (#dc2626) | Destructive actions only |

Accent reserved for: active navigation item indicator only (no new accent usage introduced in Phase 145).

---

## Copywriting Contract

Phase 145 has no user-facing UI copy. The copywriting contract below applies to developer-visible test output and any incidental console messaging from the refactored module.

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable — no user-facing CTA in this phase |
| Empty state heading | Not applicable — no new empty states rendered |
| Empty state body | Not applicable |
| Test suite pass message | "ShortcutRegistry: all Cmd+1-9 bindings fire correct handlers" |
| Test suite failure message | "ShortcutRegistry: Cmd+{N} did not activate {viewType} — expected setActiveItem('visualization', '{viewType}')" |
| Error state (console) | Not applicable — extraction is non-breaking; no runtime error paths added |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

---

## Interaction Contract

Phase 145 preserves all existing interactions without modification. The following table documents the existing keyboard shortcuts that regression tests must cover.

| Shortcut | Binding | Expected behavior | Test level |
|----------|---------|-------------------|------------|
| Cmd+1 | `viewOrder[0]` | `setActiveItem('visualization', viewType)` + `switchTo(viewType, factory)` | Unit + Integration |
| Cmd+2 | `viewOrder[1]` | Same pattern | Unit + Integration |
| Cmd+3 | `viewOrder[2]` | Same pattern | Unit + Integration |
| Cmd+4 | `viewOrder[3]` | Same pattern | Unit + Integration |
| Cmd+5 | `viewOrder[4]` | Same pattern | Unit + Integration |
| Cmd+6 | `viewOrder[5]` | Same pattern | Unit + Integration |
| Cmd+7 | `viewOrder[6]` | Same pattern | Unit + Integration |
| Cmd+8 | `viewOrder[7]` | Same pattern | Unit + Integration |
| Cmd+9 | `viewOrder[8]` | Same pattern | Unit + Integration |

Each binding must have an individual test case (no loop-based coverage per CONTEXT.md D-07).

---

## Module Export Contract

`src/ui/section-defs.ts` must export the following — this is a load-bearing naming and structure contract consumed by Phase 146.

| Export | Type | Notes |
|--------|------|-------|
| `SidebarItemDef` | interface | Moved from SidebarNav.ts |
| `SidebarSectionDef` | interface | Moved from SidebarNav.ts |
| `SECTION_DEFS` | `SidebarSectionDef[]` | Moved from SidebarNav.ts; no key string literals remain in SidebarNav |
| `viewOrder` | `string[]` | Moved from main.ts; Cmd+1-9 loop and CommandRegistry loop import from here |
| `DOCK_DEFS` | array (DockItemDef or SidebarItemDef) | New — Phase 146 verb-noun taxonomy: Integrate, Visualize, Analyze, Activate, Help |

The `"sectionKey:itemKey"` composite string convention is load-bearing (STATE.md decision) — no change to key format.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | not applicable | not applicable — no shadcn, no third-party registries |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
