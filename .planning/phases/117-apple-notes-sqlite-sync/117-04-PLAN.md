---
phase: 117-apple-notes-sqlite-sync
plan: 04
type: execute
wave: 4
depends_on: [117-03]
files_modified:
  - src/hooks/useAppleNotesSync.ts
  - src/components/Toolbar.tsx
  - src/components/SyncProgressModal.tsx
  - src/etl/alto-importer.ts
  - src/etl/alto-parser.ts
  - src/hooks/useAltoIndexImport.ts
  - src/components/IntegratedLayout.tsx
autonomous: true

must_haves:
  truths:
    - "User can click 'Sync Apple Notes' in File menu and see progress modal"
    - "Sync progress indicator shows extracting → writing → cleanup → complete phases"
    - "Toast notification appears on sync complete with node/edge counts"
    - "alto-index.json import is marked deprecated with console warning"
  artifacts:
    - path: "src/hooks/useAppleNotesSync.ts"
      provides: "React hook wrapping AppleNotesSyncService with state management"
      exports: ["useAppleNotesSync"]
    - path: "src/components/SyncProgressModal.tsx"
      provides: "Modal displaying sync progress phases"
      exports: ["SyncProgressModal"]
    - path: "src/components/Toolbar.tsx"
      provides: "File menu with 'Sync Apple Notes' item"
      contains: "Sync Apple Notes"
  key_links:
    - from: "src/hooks/useAppleNotesSync.ts"
      to: "AppleNotesSyncService"
      via: "mock adapter injection (Tauri IPC wiring deferred)"
      pattern: "AppleNotesSyncService.*fullSync"
    - from: "src/components/Toolbar.tsx"
      to: "useAppleNotesSync"
      via: "hook invocation in menu action"
      pattern: "startSync|triggerSync"
---

<objective>
Complete Phase 117 by adding UI sync trigger, progress display, and alto-index.json deprecation.

Purpose: Enable users to sync Apple Notes directly from the UI while deprecating the error-prone alto-index.json pipeline.

Output:
- `useAppleNotesSync` React hook with progress state
- `SyncProgressModal` component for visual feedback
- File menu "Sync Apple Notes" entry
- Deprecation warnings on alto-index imports
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-apple-notes-sqlite-sync/117-02-SUMMARY.md
@.planning/phases/117-apple-notes-sqlite-sync/117-03-SUMMARY.md
@src/services/sync/AppleNotesSyncService.ts
@src/hooks/useAltoIndexImport.ts
@src/components/Toolbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useAppleNotesSync hook</name>
  <files>src/hooks/useAppleNotesSync.ts</files>
  <action>
Create React hook that wraps AppleNotesSyncService with state management.

The hook should provide:
1. `syncStatus`: 'idle' | 'syncing' | 'complete' | 'error'
2. `progress`: SyncProgress from AppleNotesSyncService (phase, current, total, message)
3. `result`: SyncResult (nodesWritten, edgesWritten, duration, error) or null
4. `startFullSync()`: Trigger full sync
5. `startIncrementalSync()`: Trigger incremental sync

RUNTIME BOUNDARY (from 117-02 RUNTIME-BOUNDARY-01):
AppleNotesAdapter uses better-sqlite3 (Node.js native) and cannot run in browser.
For now, create a MOCK adapter that returns sample data:
- mockAdapter.fullSync() returns { nodes: [], edges: [], syncState: { itemCount: 0 } }
- This allows UI to be wired and tested
- Real Tauri IPC wiring is out of scope (future phase)

Add JSDoc comment explaining the mock adapter and referencing RUNTIME-BOUNDARY-01.

Pattern to follow: useAltoIndexImport.ts (status, progress, result state pattern)

Export from src/hooks/index.ts barrel.
  </action>
  <verify>
npm run gsd:build passes (no TS errors)
Import `{ useAppleNotesSync }` from '@/hooks' works
  </verify>
  <done>
useAppleNotesSync hook exported with syncStatus, progress, result, startFullSync, startIncrementalSync
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SyncProgressModal and wire to Toolbar</name>
  <files>src/components/SyncProgressModal.tsx, src/components/Toolbar.tsx</files>
  <action>
Create SyncProgressModal.tsx:
1. Props: `isOpen`, `onClose`, `progress: SyncProgress | null`, `result: SyncResult | null`
2. Display phases: extracting → writing → cleanup → complete
3. Show progress bar (current/total during writing phase)
4. Show result summary on complete (nodes written, edges written, duration)
5. Show error message if result.error exists
6. Use shadcn Dialog or simple modal pattern matching existing ImportWizard style

Update Toolbar.tsx:
1. Add "Sync Apple Notes…" to File menu (after Import Web Page, before separator)
2. Import and use useAppleNotesSync hook
3. Add state for sync modal: `const [syncModalOpen, setSyncModalOpen] = useState(false)`
4. Menu action: `() => { startFullSync(); setSyncModalOpen(true); }`
5. Render SyncProgressModal after ImportWizard

Add toast notification on sync complete:
- If using sonner: `toast.success(\`Synced \${result.nodesWritten} notes\`)`
- If no toast library, use console.log for now (toast integration optional)
  </action>
  <verify>
npm run gsd:build passes
SyncProgressModal renders when Toolbar menu item clicked
  </verify>
  <done>
File menu shows "Sync Apple Notes" item
Clicking it opens SyncProgressModal with progress display
  </done>
</task>

<task type="auto">
  <name>Task 3: Deprecate alto-index.json pipeline</name>
  <files>src/etl/alto-importer.ts, src/etl/alto-parser.ts, src/hooks/useAltoIndexImport.ts</files>
  <action>
Mark alto-index.json pipeline as deprecated:

1. In `src/etl/alto-importer.ts`:
   - Add `@deprecated` JSDoc tag to file header
   - Add console.warn at top of main export function:
     `console.warn('[DEPRECATED] alto-importer.ts is deprecated. Use Apple Notes direct sync instead.');`

2. In `src/etl/alto-parser.ts`:
   - Add `@deprecated` JSDoc tag to file header
   - Add console.warn at top of main export function:
     `console.warn('[DEPRECATED] alto-parser.ts is deprecated. Use Apple Notes direct sync instead.');`

3. In `src/hooks/useAltoIndexImport.ts`:
   - Add `@deprecated` JSDoc tag to file header
   - Add useEffect that warns once per session:
     ```typescript
     useEffect(() => {
       console.warn('[DEPRECATED] useAltoIndexImport is deprecated. Apple Notes direct sync is now available in File menu.');
     }, []);
     ```

DO NOT delete these files — they're still used by IntegratedLayout.tsx and SuperGridScrollTest.tsx.
Migration of those components to useAppleNotesSync is a future phase.
  </action>
  <verify>
npm run gsd:build passes
Browser console shows deprecation warning when useAltoIndexImport is used
  </verify>
  <done>
alto-importer.ts, alto-parser.ts, useAltoIndexImport.ts marked deprecated with console warnings
  </done>
</task>

</tasks>

<verification>
1. `npm run gsd:build` passes with zero TS errors
2. `npm run test` passes all existing tests
3. Manual verification in browser:
   - Open app
   - File menu shows "Sync Apple Notes…" item
   - Clicking it opens modal with progress display
   - Console shows deprecation warning for alto-index imports
</verification>

<success_criteria>
1. useAppleNotesSync hook provides sync state and progress tracking
2. SyncProgressModal displays extraction → writing → cleanup → complete phases
3. Toolbar File menu includes "Sync Apple Notes…" entry
4. alto-index pipeline files marked deprecated with console warnings
5. All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/117-apple-notes-sqlite-sync/117-04-SUMMARY.md`
</output>
