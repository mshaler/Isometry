---
phase: 24-typescript-error-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/components/Toolbar.tsx",
  "src/components/ui/ErrorBoundary.tsx",
  "src/components/ViewRendererDemo.tsx",
  "src/components/ViewRendererTest.tsx"
]
autonomous: true

must_haves:
  truths:
    - "Toolbar.tsx compiles without property name errors"
    - "ErrorBoundary.tsx handles void expressions correctly"
    - "ViewRenderer components compile with proper React imports"
    - "MemoryInfo type is properly defined for performance monitoring"
  artifacts:
    - path: "src/components/Toolbar.tsx"
      provides: "Toolbar button configurations with proper property names"
      contains: "label: string"
    - path: "src/components/ui/ErrorBoundary.tsx"
      provides: "Error boundary with proper conditional logic"
      min_lines: 100
    - path: "src/types/performance.ts"
      provides: "MemoryInfo interface definition"
      contains: "interface MemoryInfo"
  key_links:
    - from: "src/components/ViewRendererTest.tsx"
      to: "src/types/performance.ts"
      via: "MemoryInfo import"
      pattern: "import.*MemoryInfo.*from.*performance"
---

<objective>
Eliminate quick-win TypeScript errors in core UI components by fixing property names, void expression logic, unused imports, and missing type definitions.

Purpose: Address the most straightforward TypeScript compilation errors to reduce the overall error count and establish momentum for systematic error resolution.

Output: Clean compilation for Toolbar, ErrorBoundary, and ViewRenderer components with proper type definitions.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Previous TypeScript work foundation
@.planning/phases/10-foundation-cleanup/10-23-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix Toolbar Property Name Errors</name>
  <files>src/components/Toolbar.tsx</files>
  <action>
    Fix the property name errors in Toolbar.tsx where '_label' should be 'label':

    1. Read the current file to understand the button configuration structure
    2. Replace all instances of '_label' with 'label' in the button configurations
    3. Ensure the interface definition matches (should be label: string, not _label: string)
    4. Verify all button objects use consistent 'label' property name

    This is a simple typo fix but blocks compilation of the entire Toolbar component.
  </action>
  <verify>TypeScript compilation of Toolbar.tsx succeeds with no property errors</verify>
  <done>All toolbar buttons use 'label' property correctly without TypeScript errors</done>
</task>

<task type="auto">
  <name>Fix ErrorBoundary Void Expression Logic</name>
  <files>src/components/ui/ErrorBoundary.tsx</files>
  <action>
    Fix the void expression tested for truthiness error in ErrorBoundary.tsx:

    1. Locate the problematic void expression that's being tested for truthiness
    2. Replace with proper boolean logic - likely involves a function call that returns void
    3. Use proper conditional structure:
       - If checking function execution success: wrap in try/catch
       - If checking side effects: use appropriate boolean return pattern
       - If checking component state: use proper boolean evaluation

    Apply the established pattern from Phase 10 for safe conditional checks.
  </action>
  <verify>ErrorBoundary.tsx compiles without void expression errors</verify>
  <done>ErrorBoundary uses proper boolean logic instead of void expression testing</done>
</task>

<task type="auto">
  <name>Fix ViewRenderer Component Imports and Types</name>
  <files>src/components/ViewRendererDemo.tsx, src/components/ViewRendererTest.tsx, src/types/performance.ts</files>
  <action>
    Clean up ViewRenderer components and add missing type definitions:

    1. Remove unused React imports from ViewRendererDemo.tsx and ViewRendererTest.tsx
    2. Create src/types/performance.ts with MemoryInfo interface:
       ```typescript
       interface MemoryInfo {
         usedJSHeapSize: number;
         totalJSHeapSize: number;
         jsHeapSizeLimit: number;
       }

       interface PerformanceWithMemory extends Performance {
         memory?: MemoryInfo;
       }
       ```
    3. Update ViewRendererTest.tsx to import MemoryInfo from performance types
    4. Fix the ViewType union type issue - ensure "network" type is properly handled

    Follow the established pattern from Phase 10 for global interface extensions.
  </action>
  <verify>ViewRenderer components compile without import or type errors</verify>
  <done>ViewRenderer components have clean imports and MemoryInfo type is properly defined</done>
</task>

</tasks>

<verification>
- Run `npx tsc --noEmit` on target files and confirm no compilation errors
- Check that Toolbar renders correctly in browser
- Verify ErrorBoundary still catches errors properly
- Test ViewRenderer components load without console errors
</verification>

<success_criteria>
1. Toolbar.tsx compiles successfully with proper 'label' properties
2. ErrorBoundary.tsx handles conditionals without void expression errors
3. ViewRenderer components have clean imports and proper types
4. MemoryInfo interface properly supports performance monitoring
5. Overall TypeScript error count reduced by these quick fixes
</success_criteria>

<output>
After completion, create `.planning/phases/24-typescript-error-resolution/24-01-SUMMARY.md`
</output>