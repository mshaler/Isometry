---
phase: 111-view-continuum-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/views/KanbanView.tsx
  - src/components/views/KanbanColumn.tsx
  - src/components/views/KanbanCard.tsx
  - src/components/views/index.ts
  - src/components/views/__tests__/KanbanView.test.tsx
autonomous: true

must_haves:
  truths:
    - "User can see cards grouped into columns by facet value"
    - "User can drag a card from one column to another"
    - "Card drop persists facet value change to database"
    - "User can click a card to select it"
    - "Column headers show facet value and card count"
  artifacts:
    - path: "src/components/views/KanbanView.tsx"
      provides: "Main Kanban view with SQL integration and column layout"
      exports: ["KanbanView"]
      min_lines: 80
    - path: "src/components/views/KanbanColumn.tsx"
      provides: "Column component with drop zone and header"
      exports: ["KanbanColumn"]
      min_lines: 50
    - path: "src/components/views/KanbanCard.tsx"
      provides: "Draggable card component"
      exports: ["KanbanCard"]
      min_lines: 40
    - path: "src/components/views/__tests__/KanbanView.test.tsx"
      provides: "Unit tests for Kanban view"
      min_lines: 60
  key_links:
    - from: "src/components/views/KanbanView.tsx"
      to: "useSQLiteQuery"
      via: "data fetching hook"
      pattern: "useSQLiteQuery.*SELECT"
    - from: "src/components/views/KanbanColumn.tsx"
      to: "react-dnd useDrop"
      via: "drop zone registration"
      pattern: "useDrop.*KANBAN_CARD"
    - from: "src/components/views/KanbanCard.tsx"
      to: "react-dnd useDrag"
      via: "draggable registration"
      pattern: "useDrag.*KANBAN_CARD"
    - from: "src/components/views/KanbanColumn.tsx"
      to: "useSQLite run"
      via: "SQL UPDATE on drop"
      pattern: "db\\.run.*UPDATE.*nodes"
---

<objective>
Build the Kanban View Renderer for Phase 111 (REQ-A-03).

Purpose: Create a React-based Kanban view that groups cards into columns by facet value (e.g., status), supports drag-and-drop between columns via react-dnd, and persists changes to SQLite.

Output: KanbanView.tsx, KanbanColumn.tsx, KanbanCard.tsx components with unit tests
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-view-continuum-integration/111-RESEARCH.md

# Existing patterns to follow
@src/components/views/GalleryView.tsx
@src/components/views/GalleryCard.tsx
@src/styles/primitives-kanban.css
@src/hooks/database/useSQLiteQuery.ts
@src/state/SelectionContext.tsx
@src/db/SQLiteProvider.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KanbanCard draggable component</name>
  <files>src/components/views/KanbanCard.tsx</files>
  <action>
Create KanbanCard.tsx as a draggable card component using react-dnd's useDrag hook.

Implementation:
1. Define KanbanCardProps interface:
   - card: Card (from @/types/card)
   - columnId: string (current column for drag item)
   - selected: boolean
   - onClick: () => void

2. Use react-dnd useDrag hook:
   - type: 'KANBAN_CARD' (constant)
   - item: function form `() => ({ cardId: card.id, sourceColumnId: columnId })` to avoid stale closure
   - collect: `(monitor) => ({ isDragging: monitor.isDragging() })`

3. Apply selection and drag styling:
   - Use cn() from @/lib/utils for class merging
   - Apply 'opacity-50' when isDragging
   - Apply 'ring-2 ring-blue-500' when selected
   - Use CSS custom properties from primitives-kanban.css:
     - padding: var(--iso-kanban-card-pad)
     - border-radius: var(--iso-kanban-card-radius)

4. Render card content:
   - card.name as title
   - card.folder as subtitle (if present)
   - card.tags as chips (limit to 3, show "+N more" badge)

5. Handle click with stopPropagation to prevent column click interference

Do NOT add DndProvider - it's already at IntegratedLayout root (line 790).
  </action>
  <verify>
TypeScript compiles: `npm run gsd:build`
Component exports KanbanCard with correct props type
  </verify>
  <done>
KanbanCard.tsx exists with useDrag integration, renders card content, applies selection styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Create KanbanColumn drop zone component</name>
  <files>src/components/views/KanbanColumn.tsx</files>
  <action>
Create KanbanColumn.tsx as a column container with drop zone using react-dnd's useDrop hook.

Implementation:
1. Define KanbanColumnProps interface:
   - columnId: string
   - facetValue: string (e.g., "In Progress")
   - facetColumn: string (e.g., "status")
   - cards: Card[]
   - onCardClick: (cardId: string) => void
   - isCardSelected: (cardId: string) => boolean

2. Get db from useSQLite() for SQL UPDATE on drop

3. Use react-dnd useDrop hook:
   - accept: 'KANBAN_CARD'
   - drop callback:
     - Extract cardId, sourceColumnId from item
     - If sourceColumnId === columnId, return (no-op)
     - Execute SQL UPDATE: `UPDATE nodes SET ${facetColumn} = ? WHERE id = ?`
     - Pass [facetValue, cardId] as params
     - Note: No dataVersion increment needed - useSQLiteQuery auto-refetches
   - collect: `(monitor) => ({ isOver: monitor.isOver() })`

4. Apply column styling using primitives-kanban.css variables:
   - min-width: var(--iso-kanban-col-min-w)
   - max-width: var(--iso-kanban-col-max-w)
   - padding: var(--iso-kanban-col-pad)
   - gap between cards: var(--iso-kanban-card-gap)

5. Render column header:
   - facetValue as label
   - cards.length as count badge
   - Header height: var(--iso-kanban-col-hdr-h)

6. Apply drop target visual feedback:
   - 'bg-blue-50 dark:bg-blue-900/20' when isOver

7. Render cards using map with KanbanCard component
  </action>
  <verify>
TypeScript compiles: `npm run gsd:build`
Component exports KanbanColumn with useDrop integration
  </verify>
  <done>
KanbanColumn.tsx exists with useDrop that executes SQL UPDATE, renders header with count, maps cards to KanbanCard
  </done>
</task>

<task type="auto">
  <name>Task 3: Create KanbanView main component with SQL integration</name>
  <files>src/components/views/KanbanView.tsx, src/components/views/index.ts</files>
  <action>
Create KanbanView.tsx as the main Kanban view component with data fetching and column layout.

Implementation:
1. Import dependencies:
   - useFilters from @/state/FilterContext
   - useSelection from @/state/SelectionContext
   - useSQLiteQuery from @/hooks/database/useSQLiteQuery
   - usePAFV from @/hooks (for column facet from Y-axis)
   - compileFilters from @/filters/compiler
   - rowToCard, Card from @/types/card
   - KanbanColumn, KanbanCard
   - CSS: import '@/styles/primitives-kanban.css'

2. Determine column facet:
   - Get pafvState from usePAFV()
   - Find Y-plane mapping: `pafvState.mappings.find(m => m.plane === 'y')`
   - Use facet from mapping, or fall back to 'status' (default Category facet)
   - Store as columnFacet and columnFacetId

3. Fetch cards with useSQLiteQuery:
   - Compile LATCH filters with compileFilters(activeFilters)
   - SQL: `SELECT * FROM nodes WHERE ${compiled.sql} AND deleted_at IS NULL ORDER BY modified_at DESC`
   - Transform: rows.map(rowToCard)

4. Discover unique facet values for columns:
   - Use another useSQLiteQuery: `SELECT DISTINCT ${columnFacet} as value FROM nodes WHERE deleted_at IS NULL AND ${columnFacet} IS NOT NULL ORDER BY ${columnFacet}`
   - This gives the column order

5. Group cards by facet value:
   - Create Map<string, Card[]> for columns
   - Cards with null/undefined facet value go into "(No Status)" or similar

6. Layout with CSS Grid:
   - Container: display: grid, grid-auto-flow: column, gap: var(--iso-kanban-col-gap)
   - Horizontal scroll if columns overflow: overflow-x: auto

7. Selection integration:
   - Get select, isSelected from useSelection()
   - Pass to columns for card selection

8. Register scrollToNode for cross-canvas sync:
   - Use registerScrollToNode, unregisterScrollToNode from useSelection
   - Implementation: find card's column, scroll column into view

9. Update index.ts exports:
   - Add: export { KanbanView } from './KanbanView';
   - Add: export { KanbanColumn } from './KanbanColumn';
   - Add: export { KanbanCard } from './KanbanCard';
  </action>
  <verify>
TypeScript compiles: `npm run gsd:build`
KanbanView renders columns based on Y-axis facet
Cards grouped correctly by facet value
  </verify>
  <done>
KanbanView.tsx exists with useSQLiteQuery, groups cards by facet, renders columns with CSS Grid layout, exports added to index.ts
  </done>
</task>

<task type="auto">
  <name>Task 4: Create unit tests for KanbanView</name>
  <files>src/components/views/__tests__/KanbanView.test.tsx</files>
  <action>
Create unit tests for KanbanView following existing test patterns (see SelectionContext.test.tsx).

Implementation:
1. Create __tests__ directory if not exists

2. Test setup:
   - Mock useSQLiteQuery to return test cards
   - Mock useSQLite to provide db.run for UPDATE verification
   - Mock useFilters, useSelection, usePAFV
   - Wrap with DndProvider + HTML5Backend for react-dnd

3. Test cases:

   a. "renders columns based on facet values"
      - Provide cards with different status values
      - Verify columns render with correct headers
      - Verify card count badges

   b. "groups cards correctly into columns"
      - Provide 3 cards: status='todo', status='done', status='todo'
      - Verify todo column has 2 cards, done column has 1

   c. "handles cards with null facet value"
      - Provide card with status=null
      - Verify it appears in "(No Status)" or uncategorized column

   d. "calls select on card click"
      - Click a card
      - Verify select was called with card.id

   e. "applies selected styling to selected cards"
      - Mock isSelected to return true for specific card
      - Verify card has selection ring class

4. Use Vitest + React Testing Library:
   - import { render, screen, fireEvent } from '@testing-library/react'
   - import { vi, describe, it, expect, beforeEach } from 'vitest'

5. Mock pattern for useSQLiteQuery:
   ```typescript
   vi.mock('@/hooks/database/useSQLiteQuery', () => ({
     useSQLiteQuery: vi.fn().mockReturnValue({
       data: mockCards,
       loading: false,
       error: null,
       refetch: vi.fn()
     })
   }));
   ```
  </action>
  <verify>
Tests pass: `npm run test -- src/components/views/__tests__/KanbanView.test.tsx`
  </verify>
  <done>
KanbanView.test.tsx exists with tests for column rendering, card grouping, null handling, and selection
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `npm run gsd` for full build + test cycle
2. Verify KanbanView renders columns with cards
3. Verify drag-drop between columns triggers SQL UPDATE
4. Verify click selection updates SelectionContext
5. Verify column headers show count badges
</verification>

<success_criteria>
- KanbanView.tsx renders columns based on Y-axis facet (or default 'status')
- KanbanCard.tsx is draggable via react-dnd useDrag
- KanbanColumn.tsx accepts drops and executes SQL UPDATE
- Column headers show facet value + card count
- Card click updates SelectionContext
- Unit tests pass for column rendering and card grouping
- CSS primitives from primitives-kanban.css are applied
- No DndProvider duplication (uses existing at IntegratedLayout root)
</success_criteria>

<output>
After completion, create `.planning/phases/111-view-continuum-integration/111-01-SUMMARY.md`
</output>
