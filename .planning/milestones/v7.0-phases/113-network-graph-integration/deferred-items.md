# Deferred Items for Phase 113

## Pre-existing Test Failures (Not Caused by 113-01)

- RightSidebar.test.tsx: Expected 3 tabs but test expects 2 (UI component mismatch)
- CardDetailModal.test.tsx: Missing SQLiteProvider wrapper in tests

These failures existed before Phase 113-01 implementation.

