---
phase: 42-build-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.json
  - package.json
  - Makefile
  - tests/views/supergrid/SuperGridSizer.test.ts
  - src/**/*.ts
  - tests/**/*.ts
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - STAB-02

must_haves:
  truths:
    - "npx tsc --noEmit exits with zero errors"
    - "npx vitest --run passes with zero failures (2133+ tests)"
    - "npx biome check exits with zero errors on all TypeScript files"
    - "make lint runs biome check successfully"
    - "make fix runs biome check --write successfully"
    - "make ci includes lint step between typecheck and test-web"
  artifacts:
    - path: "biome.json"
      provides: "Biome linter + formatter configuration"
      contains: "recommended"
    - path: "package.json"
      provides: "lint and fix npm scripts"
      contains: "biome"
    - path: "Makefile"
      provides: "lint, fix targets and updated ci target"
      contains: "biome"
    - path: "tests/views/supergrid/SuperGridSizer.test.ts"
      provides: "Fixed SuperGridSizer test expectations"
      contains: "80px"
  key_links:
    - from: "Makefile"
      to: "biome.json"
      via: "npx biome check invocation"
      pattern: "biome check"
    - from: "package.json"
      to: "biome.json"
      via: "npm run lint/fix scripts"
      pattern: "biome"
---

<objective>
Fix the 4 pre-existing SuperGridSizer test failures, install Biome 2.x as the project linter/formatter, configure it with the user's locked style (tabs, 120-char width, recommended rules), bulk reformat the entire codebase, add Makefile targets, and verify that tsc and vitest still pass cleanly.

Purpose: Fix known test failures (STAB-02), establish automated lint/format checks as a prerequisite for CI (Plan 03), and confirm the existing build health (BUILD-01) remains green after reformatting.
Output: Fixed SuperGridSizer tests, biome.json config, reformatted codebase, updated Makefile and package.json with lint/fix targets.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-build-health/42-CONTEXT.md
@.planning/phases/42-build-health/42-RESEARCH.md

@Makefile
@package.json
@tsconfig.json
@tests/views/supergrid/SuperGridSizer.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SuperGridSizer test failures and install Biome</name>
  <files>tests/views/supergrid/SuperGridSizer.test.ts, package.json, biome.json</files>
  <action>
**Step A: Fix 4 pre-existing SuperGridSizer test failures (STAB-02)**

Per user decision: "Update the 4 SuperGridSizer test expected values from 160px to 80px."

In `tests/views/supergrid/SuperGridSizer.test.ts`, find the 4 failing `applyWidths` tests and fix them:

1. Test "applyWidths sets gridTemplateColumns on gridEl": Change expected value from `'160px 200px 150px'` to `'80px 200px 150px'` (row header is 80px at depth=1, not 160px).

2. Test "applyWidths scales by zoomLevel": Change expected value from `'160px 200px'` to `'80px 200px'` (row header is 80px at depth=1, not 160px).

3. Test "applyWidths with empty leafColKeys sets only row header": Change expected value from `'160px'` to `'80px'` (single row header column at depth=1).

4. Test "applyWidths accepts optional rowHeaderWidth parameter" (the 4th test has a different issue per RESEARCH.md Pitfall 2): The parameter is `rowHeaderDepth` (count of 80px columns), not `rowHeaderWidth` (pixel width). Update the test name to reflect `rowHeaderDepth`, pass depth=2 as the 4th argument, and expect `'80px 80px 120px'` (two 80px header columns + 120px data column). See RESEARCH.md Code Examples section for the exact fix.

After fixing, run `npx vitest --run tests/views/supergrid/SuperGridSizer.test.ts` to confirm all 4 tests now pass.

**Step B: Install Biome and create configuration**

1. Install Biome as a dev dependency:
   ```
   npm install --save-dev @biomejs/biome
   ```

2. Create `biome.json` at project root with these LOCKED settings (per user decisions):
   - Formatter: tabs, 120-character line width, LF line endings
   - Linter: recommended rules preset enabled
   - JavaScript formatter: single quotes, semicolons always, arrow parens always, trailing commas all
   - Files include: `src/**/*.ts`, `tests/**/*.ts`
   - Files ignore: `dist/**`, `dist-native/**`
   - JSON formatter: enabled

   Use the exact config from RESEARCH.md Pattern 1 as the starting template.

3. Add npm scripts to package.json:
   - `"lint": "biome check ."` (check only, no modifications)
   - `"fix": "biome check --write ."` (auto-fix)

4. Run the initial bulk reformat:
   ```
   npx biome check --write .
   ```
   This will reformat all 176 TypeScript files from spaces to tabs and apply 120-char width. This is a one-time operation that will produce a large diff -- commit it separately.

5. After bulk reformat, run `npx biome check .` to verify zero remaining issues. If there are lint rule violations that can't be auto-fixed (e.g., complexity, naming), add minimal rule overrides in biome.json under `linter.rules` to suppress only genuinely false positives. Do NOT disable rules broadly -- override per-category only if needed.
  </action>
  <verify>
    <automated>npx vitest --run tests/views/supergrid/SuperGridSizer.test.ts && npx biome check . && echo "Tests fixed and Biome check passed"</automated>
  </verify>
  <done>All 4 SuperGridSizer applyWidths tests pass (STAB-02). biome.json exists with locked settings, @biomejs/biome in devDependencies, npm run lint and npm run fix both work, npx biome check exits 0 on the entire codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Update Makefile targets and verify full build health</name>
  <files>Makefile</files>
  <action>
1. Add two new Makefile targets after the existing `test-web` target:

   ```makefile
   .PHONY: lint
   lint: ## Run Biome lint + format check
   	npm run lint

   .PHONY: fix
   fix: ## Auto-fix Biome lint + format issues
   	npm run fix
   ```

2. Update the existing `ci` target to include `lint` between `typecheck` and `test-web`:
   BEFORE: `ci: typecheck test-web check`
   AFTER: `ci: typecheck lint test-web check`

   This matches the user decision: "Update `make ci` target to include biome check."

3. Verify the full build health pipeline:
   - Run `npx tsc --noEmit` -- must exit 0 (BUILD-01 verification)
   - Run `npx vitest --run` -- must pass all tests with 0 failures (STAB-02 verification)
   - Run `make lint` -- must exit 0 (BUILD-02 verification)

   If `tsc --noEmit` or `vitest --run` fails after the bulk reformat (unlikely but possible if Biome changed import ordering), fix the issues immediately. The reformat should be purely cosmetic.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest --run && make lint</automated>
  </verify>
  <done>Makefile has lint, fix targets. ci target includes lint. tsc --noEmit exits 0 (BUILD-01). vitest passes all tests including SuperGridSizer fixes (STAB-02). make lint exits 0 (BUILD-02).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits with zero errors (BUILD-01)
2. `npx vitest --run` passes with zero failures, including the 4 previously-failing SuperGridSizer tests (STAB-02)
3. `npx biome check .` exits with zero errors (BUILD-02)
4. `make lint` runs biome check successfully
5. `make fix` runs biome check --write successfully
6. `make ci` now includes lint step
</verification>

<success_criteria>
- BUILD-01: tsc --noEmit exits 0
- BUILD-02: Biome installed, configured, all files pass check
- STAB-02: All tests pass (zero pre-existing failures), specifically the 4 SuperGridSizer applyWidths tests now expect 80px instead of 160px
- Makefile has lint, fix, updated ci targets
- Codebase reformatted to tabs + 120-char width
</success_criteria>

<output>
After completion, create `.planning/phases/42-build-health/42-01-SUMMARY.md`
</output>
