---
phase: 38-virtual-scrolling
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/views/supergrid/SuperGridVirtualizer.test.ts
  - src/views/SuperGrid.ts
autonomous: false
requirements:
  - VSCR-05

must_haves:
  truths:
    - "SuperGrid scrolls at 60fps with a 10K+ card dataset (each scroll frame <16ms)"
    - "User can scroll to any position and content renders correctly without gaps or flicker"
    - "Scrollbar thumb size and position accurately reflect total virtual content height"
  artifacts:
    - path: "src/views/supergrid/SuperGridVirtualizer.test.ts"
      provides: "Performance benchmark test for 10K+ scale"
      contains: "benchmark"
  key_links:
    - from: "src/views/supergrid/SuperGridVirtualizer.test.ts"
      to: "SuperGridVirtualizer"
      via: "benchmark measures getVisibleRange computation time"
      pattern: "benchmark.*getVisibleRange"
---

<objective>
Validate that virtual scrolling achieves 60fps performance at 10K+ card scale via benchmark tests and visual human verification.

Purpose: VSCR-05 requires demonstrable 60fps scroll performance. This plan adds benchmark tests measuring per-frame computation time and provides a manual verification checkpoint for visual quality.

Output: Benchmark test confirming <16ms per scroll frame computation. User-verified smooth scrolling with correct visual behavior.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-virtual-scrolling/38-CONTEXT.md
@.planning/phases/38-virtual-scrolling/38-RESEARCH.md
@.planning/phases/38-virtual-scrolling/38-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From src/views/supergrid/SuperGridVirtualizer.ts:
```typescript
export const VIRTUALIZATION_THRESHOLD = 100;
export const OVERSCAN_ROWS = 5;
export class SuperGridVirtualizer {
  constructor(getRowHeight: () => number, getColHeaderHeight: () => number);
  attach(rootEl: HTMLElement): void;
  detach(): void;
  setTotalRows(count: number): void;
  isActive(): boolean;
  getVisibleRange(): { startRow: number; endRow: number };
  getTotalHeight(): number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Performance benchmark for 10K-scale virtualizer computation</name>
  <files>src/views/supergrid/SuperGridVirtualizer.test.ts</files>
  <behavior>
    - Bench: getVisibleRange with 10,000 rows completes in under 1ms (pure math, no DOM)
    - Bench: getVisibleRange with 50,000 rows completes in under 1ms
    - Test: rapid sequential getVisibleRange calls (simulating 60fps scroll at 1000 positions) complete in under 16ms total
    - Test: isActive correctly toggles at threshold boundary (100 vs 101 rows)
    - Test: getTotalHeight at 10K rows with zoom=2.0 returns correct height (10000 * 80)
  </behavior>
  <action>
    Add benchmark and stress tests to the existing `SuperGridVirtualizer.test.ts` file created in Plan 01.

    1. Add a new `describe('performance benchmarks')` block:
       - Create a virtualizer with a mock rootEl (scrollTop, clientHeight via Object.defineProperty)
       - Set totalRows to 10,000
       - Measure getVisibleRange across 1000 sequential scrollTop positions (0 to 400,000 in increments of 400)
       - Assert total time under 16ms (budget for one animation frame)
       - This validates that the scroll handler's hot path is fast enough for 60fps

    2. Add a `describe('scale validation')` block:
       - Test with 50,000 rows -- same getVisibleRange computation must remain O(1)
       - Test getTotalHeight at various zoom levels (1.0, 1.5, 2.0) with 10K rows
       - Test that the windowed row count (endRow - startRow) stays bounded (~20-30 rows) regardless of total rows

    3. Add edge case tests:
       - scrollTop exactly at the last possible position (totalRows * rowHeight - viewportHeight)
       - scrollTop = 0 with very large totalRows
       - scrollTop larger than total content height (clamp behavior)

    Use `performance.now()` for timing in Node/jsdom environment. Do NOT use Vitest `bench` API (it requires `vitest bench` runner). Use standard `it()` tests with timing assertions.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run src/views/supergrid/SuperGridVirtualizer.test.ts</automated>
  </verify>
  <done>Performance benchmarks pass: 1000 sequential getVisibleRange calls complete in under 16ms. Scale tests pass at 10K and 50K rows. Edge cases handled correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of virtual scrolling at scale</name>
  <action>
    Two-layer virtual scrolling for SuperGrid: CSS content-visibility progressive enhancement (VSCR-01) and custom row virtualization with sentinel spacer (VSCR-02/03/04/05). The virtualizer activates at more than 100 leaf rows, renders only visible + 5-row overscan, and maintains accurate scrollbar via sentinel spacer element.
  </action>
  <verify>
    1. Start the dev server: `cd /Users/mshaler/Developer/Projects/Isometry && npx vite`
    2. Open http://localhost:5173 in browser
    3. Import a dataset that produces more than 100 SuperGrid rows (or use a large CSV with many distinct folder/category values)
    4. Open SuperGrid view
    5. Verify scrolling behavior:
       a. Scroll down -- content should appear smoothly without gaps or flicker
       b. Scroll rapidly -- no blank spaces should appear (overscan buffer prevents this)
       c. Check scrollbar: thumb size should reflect total content, not just visible content
       d. Scroll to the very bottom -- last rows should render correctly
       e. Scroll back to top -- first rows should render correctly
    6. Verify headers:
       a. Column headers remain sticky at top during vertical scroll
       b. Row headers remain sticky at left during horizontal scroll
       c. Corner cell stays fixed at top-left
    7. Verify features still work:
       a. Click a cell -- selection should work
       b. Lasso select across visible cells -- should highlight correctly
       c. Sort by a column -- grid should reset scroll and re-render
       d. Filter -- grid should reset scroll and re-render
       e. Zoom (Ctrl+scroll) -- grid should re-render with new row heights
    8. Open DevTools Performance tab, scroll through the grid, verify no long tasks (more than 50ms frames)
    9. For comparison: with a small dataset (under 100 rows), verify the grid works identically to before (no virtualization active)
  </verify>
  <done>User confirms smooth scrolling at 10K+ scale, frozen headers work correctly, scrollbar reflects total content, and all existing features (selection, sort, filter, zoom) remain functional.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/views/supergrid/SuperGridVirtualizer.test.ts` -- all tests including benchmarks pass
2. `npx vitest run` -- full test suite passes
3. Visual confirmation: 60fps scroll at scale with correct headers and scrollbar
</verification>

<success_criteria>
- Performance benchmark proves under 16ms for 1000 sequential getVisibleRange calls (60fps math budget)
- Human verifies smooth scrolling at scale with no visual artifacts
- Human verifies frozen headers, accurate scrollbar, and feature compatibility
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/38-virtual-scrolling/38-02-SUMMARY.md`
</output>
