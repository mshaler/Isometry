---
phase: 48-review-fixes
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - src/mutations/shortcuts.ts
  - tests/mutations/shortcuts.test.ts
  - tests/etl-validation/helpers.ts
  - tests/etl-validation/source-dedup.test.ts
  - tests/etl-validation/source-errors.test.ts
  - tests/etl-validation/source-import.test.ts
  - tests/etl-validation/source-view-matrix.test.ts
  - tests/ui/ErrorBanner.test.ts
  - tests/views/ViewManager.test.ts
  - tests/views/SuperGrid.test.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [BFIX-01, DFIX-01]

must_haves:
  truths:
    - "npx biome check src tests passes with zero errors and zero warnings"
    - "ROADMAP.md, PROJECT.md, and STATE.md consistently reflect v4.2 as shipped and v4.3 as current"
    - "make ci runs end-to-end without failures (typecheck + lint + test + xcode build)"
  artifacts:
    - path: "src/mutations/shortcuts.ts"
      provides: "Cleaned up import ordering per Biome"
    - path: "tests/etl-validation/source-dedup.test.ts"
      provides: "Fixed unused variables"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate milestone/phase completion status"
    - path: ".planning/STATE.md"
      provides: "Accurate current position and focus"
    - path: ".planning/PROJECT.md"
      provides: "Active requirements checklist updated"
  key_links:
    - from: "biome.json"
      to: "all src/ and tests/ files"
      via: "biome check passes"
      pattern: "Checked.*files.*No fixes applied"
    - from: ".planning/ROADMAP.md"
      to: ".planning/STATE.md"
      via: "consistent milestone status"
      pattern: "v4\\.2.*shipped|v4\\.3.*current"
---

<objective>
Clean Biome lint slate (fix all 17 errors + 8 warnings) and reconcile planning docs so ROADMAP, STATE, and PROJECT consistently reflect v4.2 as shipped and v4.3 as current milestone.

Purpose: CI lint gate holds at zero diagnostics (prevents future drift), and planning docs are the single source of truth for project status.
Output: Zero Biome diagnostics, consistent planning docs, `make ci` passes end-to-end.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-review-fixes/48-CONTEXT.md
@.planning/phases/48-review-fixes/48-01-SUMMARY.md

@Makefile
@biome.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all Biome lint diagnostics to zero (BFIX-01)</name>
  <files>src/mutations/shortcuts.ts, tests/mutations/shortcuts.test.ts, tests/etl-validation/helpers.ts, tests/etl-validation/source-dedup.test.ts, tests/etl-validation/source-errors.test.ts, tests/etl-validation/source-import.test.ts, tests/etl-validation/source-view-matrix.test.ts, tests/ui/ErrorBanner.test.ts, tests/views/ViewManager.test.ts, tests/views/SuperGrid.test.ts</files>
  <action>
    Per user decision: fix ALL diagnostics (17 errors + 8 warnings) for a clean slate. Use `biome check --write` for auto-fixable issues, manually review and fix logic-related lint errors.

    The current diagnostics (from `npx biome check src tests --max-diagnostics=50`) break down as:

    **Import ordering (assist/source/organizeImports) -- auto-fixable:**
    - `src/mutations/shortcuts.ts:13` -- import order
    - `tests/etl-validation/helpers.ts:4` -- import order
    - `tests/etl-validation/source-import.test.ts:5` -- import order
    - `tests/etl-validation/source-view-matrix.test.ts:9` -- import order
    - `tests/mutations/shortcuts.test.ts:11` -- import order
    - `tests/ui/ErrorBanner.test.ts:6` -- import order
    - `tests/views/ViewManager.test.ts:8` -- import order

    **Unused imports (lint/correctness/noUnusedImports) -- auto-fixable:**
    - `tests/etl-validation/source-errors.test.ts:17` -- unused import
    - `tests/etl-validation/source-view-matrix.test.ts:26` -- unused import
    - `tests/ui/ErrorBanner.test.ts:8` -- unused import

    **Unused variables (lint/correctness/noUnusedVariables) -- manual fix:**
    - `tests/etl-validation/source-dedup.test.ts:136` -- unused variable (prefix with `_`)
    - `tests/etl-validation/source-dedup.test.ts:172` -- unused variable (prefix with `_`)
    - `tests/etl-validation/source-dedup.test.ts:206` -- unused variable (prefix with `_`)
    - `tests/etl-validation/source-view-matrix.test.ts:448` -- unused variable (prefix with `_`)
    - `tests/mutations/shortcuts.test.ts:388` -- unused variable (prefix with `_`)

    **Formatting (format) -- auto-fixable:**
    - `tests/views/SuperGrid.test.ts` -- formatting issues (constructor argument wrapping)

    Steps:
    1. Run `npx biome check --write src tests` to auto-fix formatting and import ordering issues.
    2. Manually fix remaining unused variable warnings by prefixing with `_` (e.g., `const result` -> `const _result` or remove the variable if the assignment side-effect can be called without capture).
    3. Run `npx biome check src tests` to verify zero diagnostics.
    4. Run `npx vitest --run` to verify no test regressions from the fixes.
    5. Run `npx tsc --noEmit` to verify TypeScript still compiles.

    Note: The 8 Biome rules disabled in biome.json (per decision [42-01]) remain disabled -- this task only fixes the diagnostics that the ENABLED rules flag.
  </action>
  <verify>
    <automated>npx biome check src tests 2>&1 | tail -5</automated>
  </verify>
  <done>npx biome check src tests reports zero errors and zero warnings. All tests pass. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile planning docs to reflect current state (DFIX-01)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <action>
    Per user decision and CONTEXT.md: reconcile ROADMAP.md, PROJECT.md, and STATE.md so they consistently reflect v4.2 as shipped and v4.3 as current milestone.

    Review findings (F-004):
    - ROADMAP marks phases 42-46 complete (correct), but planning docs have stale references
    - STATE.md had stale focus references (already fixed during v4.3 init, but verify)
    - PROJECT.md Active checklist needs to reflect v4.3 scope

    Steps:

    1. **ROADMAP.md**: Update Phase 48 plan list to reflect Plan 01 and Plan 02 with their objectives:
       ```
       Plans:
       - [ ] 48-01-PLAN.md -- Excel ArrayBuffer fix, ? shortcut shift fix, undo/redo toast wiring
       - [ ] 48-02-PLAN.md -- Biome lint cleanup, planning doc reconciliation
       ```
       Update `**Plans:** 0/2 plans complete`.

    2. **STATE.md**: Verify current position is accurate:
       - Phase: 48 of 48 (Review Fixes)
       - Plan: 0 of 2 in Phase 48
       - Status: In progress
       - total_plans: 2
       - Verify no stale references to Phase 45 or earlier

    3. **PROJECT.md**:
       - Verify the Active requirements section lists the v4.3 scope items (F-001 through F-005)
       - Verify Current Milestone section says "v4.3 Review Fixes"
       - Verify Current State section reflects v4.2 as latest shipped milestone
       - Verify all v4.2 items in the Validated section are marked with checkmarks

    Only update fields that are actually stale or incorrect. Do not rewrite content that is already accurate.
  </action>
  <verify>
    <automated>grep -c "v4.3" .planning/STATE.md .planning/PROJECT.md .planning/ROADMAP.md && echo "All docs reference v4.3"</automated>
  </verify>
  <done>ROADMAP.md shows 2 plans for Phase 48, STATE.md reflects Phase 48 in progress with 2 plans, PROJECT.md Active section lists v4.3 scope items -- all three docs are internally consistent.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx biome check src tests` reports zero errors and zero warnings
2. `npx tsc --noEmit` passes
3. `npx vitest --run` passes
4. ROADMAP, STATE, and PROJECT all reference v4.3 as current with consistent status
5. `make ci` runs successfully (typecheck + lint + test + xcode build)
</verification>

<success_criteria>
- Zero Biome diagnostics on `src/` and `tests/` directories
- Planning docs are internally consistent and accurate
- No test regressions
- Full CI pipeline (`make ci`) passes
</success_criteria>

<output>
After completion, create `.planning/phases/48-review-fixes/48-02-SUMMARY.md`
</output>
