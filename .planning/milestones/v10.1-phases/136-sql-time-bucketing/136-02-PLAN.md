---
phase: 136-sql-time-bucketing
plan: 02
type: tdd
wave: 2
depends_on: ["136-01"]
files_modified:
  - src/views/supergrid/SuperGridQuery.ts
  - tests/views/supergrid/SuperGridQuery.test.ts
autonomous: true
requirements: [TIME-05]

must_haves:
  truths:
    - "The '__NO_DATE__' bucket sorts after all dated buckets when sort direction is ASC"
    - "The '__NO_DATE__' bucket sorts after all dated buckets when sort direction is DESC"
    - "Sort-last behavior applies to both buildSuperGridQuery and buildSuperGridCalcQuery ORDER BY clauses"
  artifacts:
    - path: "src/views/supergrid/SuperGridQuery.ts"
      provides: "CASE WHEN sentinel sort-last trick in ORDER BY for time axes"
      contains: "CASE WHEN"
    - path: "tests/views/supergrid/SuperGridQuery.test.ts"
      provides: "TDD tests for __NO_DATE__ sort-last in both sort directions"
      min_lines: 30
  key_links:
    - from: "src/views/supergrid/SuperGridQuery.ts"
      to: "buildSuperGridQuery ORDER BY"
      via: "CASE WHEN sentinel prepended to time axis ORDER BY parts"
      pattern: "CASE WHEN.*__NO_DATE__.*END"
    - from: "src/views/supergrid/SuperGridQuery.ts"
      to: "buildSuperGridCalcQuery ORDER BY"
      via: "Same CASE WHEN sentinel sort-last trick"
      pattern: "CASE WHEN.*__NO_DATE__.*END"
---

<objective>
Add sort-last behavior for the "__NO_DATE__" sentinel bucket in SuperGrid query ORDER BY clauses, ensuring NULL-time cards always appear after all dated buckets regardless of ascending or descending sort direction. Apply identical logic to both buildSuperGridQuery and buildSuperGridCalcQuery.

Purpose: Users expect "No Date" items at the bottom of the grid, not interleaved with dated buckets. The CASE WHEN trick forces the sentinel to sort last without affecting the sort order of real date buckets.
Output: Modified ORDER BY generation with CASE WHEN sentinel detection, comprehensive TDD tests for both sort directions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-sql-time-bucketing/136-CONTEXT.md
@.planning/phases/136-sql-time-bucketing/136-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — executor needs these to build on. -->

From src/views/supergrid/SuperGridQuery.ts (after Plan 01):
```typescript
export const NO_DATE_SENTINEL = '__NO_DATE__';

// compileAxisExpr now returns COALESCE(strftime(...), '__NO_DATE__') for time fields
// and raw field name for non-time fields
function compileAxisExpr(
  field: string,
  granularity: TimeGranularity | null | undefined,
  timeFieldSet?: Set<string>,
): string;
```

From src/providers/types.ts:
```typescript
export type TimeGranularity = 'day' | 'week' | 'month' | 'quarter' | 'year';
export type AxisField = KnownAxisField | (string & {});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — __NO_DATE__ sort-last ORDER BY for both query builders</name>
  <files>src/views/supergrid/SuperGridQuery.ts, tests/views/supergrid/SuperGridQuery.test.ts</files>
  <read_first>
    - src/views/supergrid/SuperGridQuery.ts
    - tests/views/supergrid/SuperGridQuery.test.ts
    - .planning/phases/136-sql-time-bucketing/136-01-SUMMARY.md
    - .planning/phases/136-sql-time-bucketing/136-CONTEXT.md
  </read_first>
  <behavior>
    RED phase — add a new `describe('buildSuperGridQuery — __NO_DATE__ sort-last (TIME-05)')` block:
    - Test: time axis with direction='asc' — ORDER BY contains CASE WHEN expression that sorts '__NO_DATE__' last (value 1) and other values first (value 0)
    - Test: time axis with direction='desc' — ORDER BY STILL sorts '__NO_DATE__' last (the CASE WHEN always puts sentinel last, regardless of ASC/DESC on the time bucket expression)
    - Test: non-time axis — ORDER BY does NOT contain CASE WHEN sentinel logic
    - Test: mixed time + non-time axes — only time axis parts get CASE WHEN, non-time axis parts are plain `field DIR`
    - Test: sort overrides do NOT get CASE WHEN wrapping (they use raw field names, not time-bucketed expressions)

    Add a new `describe('buildSuperGridCalcQuery — __NO_DATE__ sort-last (TIME-05)')` block:
    - Test: calc query with time row axis + direction='asc' — ORDER BY contains CASE WHEN sentinel sort-last
    - Test: calc query with time row axis + direction='desc' — ORDER BY STILL sorts '__NO_DATE__' last
  </behavior>
  <action>
    **RED phase**: Write all tests listed in behavior block. Run `npx vitest run tests/views/supergrid/SuperGridQuery.test.ts` — new tests MUST fail.

    **GREEN phase**: Modify `src/views/supergrid/SuperGridQuery.ts`:

    1. Create a helper function (private to module) that generates the sort-last ORDER BY expression for a time axis:
       ```typescript
       function compileTimeAxisOrderBy(expr: string, direction: string): string {
         return `CASE WHEN ${expr} = '${NO_DATE_SENTINEL}' THEN 1 ELSE 0 END, ${expr} ${direction}`;
       }
       ```
       This produces: `CASE WHEN COALESCE(strftime('%Y-%m', created_at), '__NO_DATE__') = '__NO_DATE__' THEN 1 ELSE 0 END, COALESCE(strftime('%Y-%m', created_at), '__NO_DATE__') ASC`
       The CASE WHEN part always sorts ASC (0 before 1), pushing sentinel last. The actual date sort respects the user's chosen direction.

    2. In `buildSuperGridQuery()`, modify the `axisOrderByParts` construction. For each axis:
       - Determine if the compiled expression contains COALESCE (i.e., it is a time field that was wrapped). The simplest check: compare `expr !== ax.field` AND the expr starts with 'COALESCE(' (or check if the field is in the time field set).
       - If it is a time-axis expression: use `compileTimeAxisOrderBy(expr, ax.direction.toUpperCase())` instead of the plain `${expr} ${direction}`.
       - If it is NOT a time-axis expression: use the existing plain `${expr} ${direction}`.
       - Sort overrides remain unchanged (they already use raw field names per existing logic).

    3. In `buildSuperGridCalcQuery()`, apply the SAME logic to its `orderByParts` construction. The calc query only orders by row axes, but the same CASE WHEN pattern applies when a row axis is a time field.

    4. To detect whether a field is a time field for ORDER BY purposes, reuse the same `effectiveTimeFields` set already constructed in both functions. Check `effectiveTimeFields.has(ax.field)` before calling the helper.

    **REFACTOR phase**: If the ORDER BY logic is duplicated between both functions, extract a shared helper like `compileAxisOrderByPart(ax, granularity, timeFieldSet)` that encapsulates both the expression compilation and the CASE WHEN wrapping. Only do this if it reduces code without adding unnecessary abstraction.

    Run `npx vitest run tests/views/supergrid/SuperGridQuery.test.ts` — ALL tests must pass.
  </action>
  <verify>
    <automated>npx vitest run tests/views/supergrid/SuperGridQuery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/views/supergrid/SuperGridQuery.ts contains `CASE WHEN` in the ORDER BY generation logic
    - src/views/supergrid/SuperGridQuery.ts contains `__NO_DATE__` in the CASE WHEN expression
    - tests/views/supergrid/SuperGridQuery.test.ts contains describe block with string '__NO_DATE__ sort-last'
    - tests/views/supergrid/SuperGridQuery.test.ts contains at least 2 tests checking direction='desc' still sorts sentinel last
    - `npx vitest run tests/views/supergrid/SuperGridQuery.test.ts` exits 0
    - `npx vitest run tests/views/supergrid/supergrid-query-join.test.ts` exits 0 (no regression)
  </acceptance_criteria>
  <done>
    __NO_DATE__ sentinel bucket sorts after all dated buckets regardless of ASC or DESC sort direction. Both buildSuperGridQuery and buildSuperGridCalcQuery produce CASE WHEN sentinel ORDER BY. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/views/supergrid/SuperGridQuery.test.ts` — all tests pass
- `npx vitest run tests/views/supergrid/supergrid-query-join.test.ts` — no regression
- `npx vitest run tests/views/pivot/DataAdapter.test.ts` — no regression
- `grep -c 'CASE WHEN' src/views/supergrid/SuperGridQuery.ts` returns >= 1
- Full test suite: `npx vitest run` passes
</verification>

<success_criteria>
1. Time axis ORDER BY uses CASE WHEN to push __NO_DATE__ sentinel last for ASC direction
2. Time axis ORDER BY uses CASE WHEN to push __NO_DATE__ sentinel last for DESC direction
3. Non-time axis ORDER BY is unaffected (no CASE WHEN)
4. Sort overrides are unaffected (no CASE WHEN)
5. Both buildSuperGridQuery and buildSuperGridCalcQuery implement identical sort-last behavior
6. All existing tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/136-sql-time-bucketing/136-02-SUMMARY.md`
</output>
