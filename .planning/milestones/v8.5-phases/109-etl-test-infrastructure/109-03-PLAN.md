---
phase: 109-etl-test-infrastructure
plan: "03"
type: execute
wave: 1
depends_on: ["109-01", "109-02"]
files_modified:
  - tests/integration/etl-progress.test.ts
  - tests/integration/etl-progress-data.test.ts
  - tests/integration/etl-progress-ui.test.ts
  - tests/seams/ui/view-tab-bar.test.ts
  - tests/seams/ui/calc-explorer.test.ts
  - tests/etl-validation/source-view-matrix.test.ts
  - .github/workflows/ci.yml
autonomous: true
gap_closure: true
requirements: [INFR-02]

must_haves:
  truths:
    - "The CI environment-boundary job passes on the current codebase with zero violations"
    - "No test file combines @vitest-environment jsdom with realDb() or value-imported Database"
    - "All modified test files still pass vitest"
  artifacts:
    - path: "tests/integration/etl-progress-data.test.ts"
      provides: "ETL progress data-layer tests in Node environment"
      contains: "import { Database }"
    - path: "tests/integration/etl-progress-ui.test.ts"
      provides: "ImportToast UI tests in jsdom environment"
      contains: "@vitest-environment jsdom"
    - path: ".github/workflows/ci.yml"
      provides: "Refined grep pattern excluding import type"
      contains: "import type"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/"
      via: "grep-based boundary enforcement"
      pattern: "environment-boundary"
---

<objective>
Fix 3 real WASM/jsdom boundary violations and 1 false-positive grep match so the CI environment-boundary job passes on the current codebase.

Purpose: INFR-02 requires that "any test file mixing realDb() with @vitest-environment jsdom fails CI lint." The CI job exists and is correctly designed, but 3 pre-existing test files violate the rule and 1 triggers a false positive, meaning CI would fail today.

Output: All 4 flagged files resolved. CI environment-boundary job passes cleanly.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-etl-test-infrastructure/109-VERIFICATION.md
@.planning/phases/109-etl-test-infrastructure/109-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 real boundary violations and 1 false positive</name>
  <files>
    tests/integration/etl-progress.test.ts
    tests/integration/etl-progress-data.test.ts
    tests/integration/etl-progress-ui.test.ts
    tests/seams/ui/view-tab-bar.test.ts
    tests/seams/ui/calc-explorer.test.ts
    tests/etl-validation/source-view-matrix.test.ts
    .github/workflows/ci.yml
  </files>
  <read_first>
    tests/integration/etl-progress.test.ts
    tests/seams/ui/view-tab-bar.test.ts
    tests/seams/ui/calc-explorer.test.ts
    tests/etl-validation/source-view-matrix.test.ts
    tests/ENVIRONMENT.md
    .github/workflows/ci.yml
  </read_first>
  <action>
Fix all 4 files flagged by the CI environment-boundary grep check. Three are real violations; one is a false positive from overly broad grep.

**File 1: tests/integration/etl-progress.test.ts — SPLIT (real violation)**

This file has two independent describe blocks with zero shared state:
- Lines 12-91: "ETL Progress Reporting" — uses `new Database()` and `db.initialize()` (WASM). Does NOT need DOM. Needs Node environment.
- Lines 93-208: "onnotification -> ImportToast wiring" — uses `document.createElement('div')` (DOM). Does NOT need Database. Needs jsdom.

Split into two files:

1. Create `tests/integration/etl-progress-data.test.ts`:
   - NO `@vitest-environment jsdom` annotation (runs in Node, default)
   - Copy the imports: `{ afterEach, beforeEach, describe, expect, it, vi } from 'vitest'`, `{ Database } from '../../src/database/Database'`, `{ searchCards } from '../../src/database/queries/search'`, `{ ImportOrchestrator } from '../../src/etl/ImportOrchestrator'`
   - Copy the entire first describe block "ETL Progress Reporting" (lines 12-91) with its beforeEach/afterEach and all 3 test cases
   - Update header comment to: `// Isometry v5 — Phase 10 ETL Progress Reporting Data Tests`
   - Do NOT import ImportToast

2. Create `tests/integration/etl-progress-ui.test.ts`:
   - First line: `// @vitest-environment jsdom`
   - Copy the imports: `{ afterEach, beforeEach, describe, expect, it, vi } from 'vitest'`, `{ ImportToast } from '../../src/ui/ImportToast'`
   - Copy the entire second describe block "onnotification -> ImportToast wiring" (lines 93-208) with its beforeEach/afterEach and all 4 test cases
   - Update header comment to: `// Isometry v5 — Phase 10 ETL Progress ImportToast UI Tests`
   - Do NOT import Database, searchCards, or ImportOrchestrator

3. Delete `tests/integration/etl-progress.test.ts` after the two new files are verified.

**File 2: tests/seams/ui/view-tab-bar.test.ts — REMOVE jsdom annotation (real violation)**

This file uses `@vitest-environment jsdom` AND calls `realDb()`. It needs DOM for `document.createElement('div')` and `document.body.appendChild()`. Fix by removing the jsdom annotation and providing DOM programmatically:

1. Remove the `// @vitest-environment jsdom` line (line 1)
2. Add import at top: `import { JSDOM } from 'jsdom';`
3. In the `beforeEach` block (around line 44), BEFORE the existing `container = document.createElement('div')` line, add:
   ```typescript
   const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>');
   global.document = dom.window.document as unknown as Document;
   ```
4. In the `afterEach` block, after existing cleanup, add:
   ```typescript
   delete (global as any).document;
   ```
5. Keep all `realDb()` and `makeProviders()` calls as-is — they now run in Node environment where WASM works correctly.
6. Keep all `document.createElement` calls as-is — they now reference the programmatic JSDOM document.

**File 3: tests/seams/ui/calc-explorer.test.ts — REMOVE jsdom annotation (real violation)**

Same pattern as view-tab-bar. This file uses `@vitest-environment jsdom` AND calls `realDb()`. Fix identically:

1. Remove the `// @vitest-environment jsdom` line (line 1)
2. Add import at top: `import { JSDOM } from 'jsdom';`
3. In EACH `beforeEach` block (there are multiple describe blocks — check lines ~52 and any other beforeEach), add BEFORE the `container = document.createElement('div')` line:
   ```typescript
   const dom = new JSDOM('<!DOCTYPE html><html><body></body></html>');
   global.document = dom.window.document as unknown as Document;
   ```
4. In EACH `afterEach` block, add:
   ```typescript
   delete (global as any).document;
   ```
5. Keep all `realDb()` and `makeProviders()` calls as-is.

**File 4: tests/etl-validation/source-view-matrix.test.ts — false positive, fix grep pattern**

This file uses `import type { Database }` which is erased at compile time. It does NOT load WASM at runtime. The CI grep pattern `from.*database/Database` matches it because grep cannot distinguish value vs type imports.

Fix the CI grep pattern in `.github/workflows/ci.yml` to filter out `import type` lines:

Replace the current grep script (lines 45-53 in ci.yml):
```bash
VIOLATIONS=$(grep -rl '@vitest-environment jsdom' tests/ | xargs grep -l 'realDb\|from.*harness/realDb\|from.*database/Database' 2>/dev/null || true)
```

With a two-stage check that filters out type-only imports:
```bash
CANDIDATES=$(grep -rl '@vitest-environment jsdom' tests/ || true)
VIOLATIONS=""
for f in $CANDIDATES; do
  # Check for realDb (always a violation in jsdom context)
  if grep -q 'realDb' "$f" 2>/dev/null; then
    VIOLATIONS="$VIOLATIONS $f"
    continue
  fi
  # Check for Database import — but exclude "import type" lines
  if grep -v '^import type' "$f" | grep -q 'from.*database/Database' 2>/dev/null; then
    VIOLATIONS="$VIOLATIONS $f"
  fi
done
VIOLATIONS=$(echo "$VIOLATIONS" | xargs)
```

Keep the rest of the job (the if/then/exit 1/fi block and success message) unchanged.
  </action>
  <verify>
    <automated>
# Verify all tests pass
npx vitest run tests/integration/etl-progress-data.test.ts tests/integration/etl-progress-ui.test.ts tests/seams/ui/view-tab-bar.test.ts tests/seams/ui/calc-explorer.test.ts tests/etl-validation/source-view-matrix.test.ts 2>&1 | tail -5

# Verify old file is deleted
test ! -f tests/integration/etl-progress.test.ts && echo "OLD FILE DELETED OK"

# Simulate the updated CI grep check locally
CANDIDATES=$(grep -rl '@vitest-environment jsdom' tests/ || true)
VIOLATIONS=""
for f in $CANDIDATES; do
  if grep -q 'realDb' "$f" 2>/dev/null; then
    VIOLATIONS="$VIOLATIONS $f"
    continue
  fi
  if grep -v '^import type' "$f" | grep -q 'from.*database/Database' 2>/dev/null; then
    VIOLATIONS="$VIOLATIONS $f"
  fi
done
VIOLATIONS=$(echo "$VIOLATIONS" | xargs)
if [ -n "$VIOLATIONS" ]; then
  echo "FAIL: boundary violations: $VIOLATIONS"
  exit 1
fi
echo "PASS: no boundary violations"
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/etl-progress.test.ts does NOT exist (deleted)
    - tests/integration/etl-progress-data.test.ts exists and does NOT contain "@vitest-environment jsdom"
    - tests/integration/etl-progress-data.test.ts contains "import { Database }" and "ETL Progress Reporting"
    - tests/integration/etl-progress-ui.test.ts exists and contains "@vitest-environment jsdom" on line 1
    - tests/integration/etl-progress-ui.test.ts contains "ImportToast" and does NOT contain "import { Database }"
    - tests/seams/ui/view-tab-bar.test.ts does NOT contain "@vitest-environment jsdom"
    - tests/seams/ui/view-tab-bar.test.ts contains "import { JSDOM } from 'jsdom'"
    - tests/seams/ui/calc-explorer.test.ts does NOT contain "@vitest-environment jsdom"
    - tests/seams/ui/calc-explorer.test.ts contains "import { JSDOM } from 'jsdom'"
    - .github/workflows/ci.yml environment-boundary job contains "import type" in the grep filter
    - All 5 test files (2 new + 3 modified) pass vitest
    - Running the updated CI grep pattern against the full tests/ directory produces zero violations
  </acceptance_criteria>
  <done>
    CI environment-boundary job passes cleanly on the current codebase. No test file combines @vitest-environment jsdom with realDb() or value-imported Database. All existing test assertions still pass.
  </done>
</task>

</tasks>

<verification>
1. Run vitest on all affected files — all tests pass
2. Simulate CI environment-boundary grep check locally — zero violations
3. Confirm old etl-progress.test.ts is deleted
4. Confirm no @vitest-environment jsdom annotation in view-tab-bar.test.ts or calc-explorer.test.ts
5. Confirm source-view-matrix.test.ts is no longer flagged by the refined grep
</verification>

<success_criteria>
- CI environment-boundary job produces zero violations against the full tests/ directory
- All 5 affected test files pass vitest (etl-progress-data, etl-progress-ui, view-tab-bar, calc-explorer, source-view-matrix)
- INFR-02 is fully satisfied: boundary is both documented AND enforced
</success_criteria>

<output>
After completion, create `.planning/phases/109-etl-test-infrastructure/109-03-SUMMARY.md`
</output>
