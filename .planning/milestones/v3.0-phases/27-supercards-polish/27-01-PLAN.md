---
phase: 27-supercards-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/SuperGrid.ts
  - tests/views/SuperGrid.test.ts
autonomous: true
requirements: [CARD-01, CARD-02, CARD-03, CARD-04, CARD-05]

must_haves:
  truths:
    - "Every data cell in matrix mode displays a SuperCard element showing COUNT instead of the old count-badge span"
    - "SuperCard has dashed border and italic text; the parent data-cell has NO heat map background color"
    - "In spreadsheet mode, SuperCard appears as the first child element above card pills"
    - "Clicking a SuperCard opens a tooltip with count header and scrollable card ID list; clicking outside dismisses it"
    - "Clicking a card ID in the tooltip adds that card to SelectionProvider via selectionAdapter"
    - "SuperCard click does NOT trigger data-cell selection (click zone returns 'supergrid-card')"
    - "Lasso selection does NOT start on SuperCard elements"
    - "FTS search does NOT highlight SuperCard cells; SuperCard cells do not inflate match count badge"
  artifacts:
    - path: "src/views/SuperGrid.ts"
      provides: "SuperCard DOM generation in _renderCells(), tooltip lifecycle methods, FTS exclusion logic"
    - path: "tests/views/SuperGrid.test.ts"
      provides: "TDD tests for CARD-01 through CARD-05"
  key_links:
    - from: "src/views/SuperGrid.ts (_renderCells .each())"
      to: "SuperCard DOM element with data-supercard='true'"
      via: "D3 .each() callback in matrix/spreadsheet mode branches"
      pattern: "data-supercard"
    - from: "src/views/SuperGrid.ts (_openSuperCardTooltip)"
      to: "SuperGridSelectionLike.addToSelection()"
      via: "card ID click handler in tooltip list"
      pattern: "addToSelection"
    - from: "src/views/supergrid/SuperGridSelect.ts (classifyClickZone)"
      to: "SuperCard DOM element"
      via: "existing .closest('.supergrid-card') check returns 'supergrid-card' zone"
      pattern: "supergrid-card"
---

<objective>
Implement SuperCard aggregation cards at group intersections in the SuperGrid — replacing the existing count badge in matrix mode and appearing above pills in spreadsheet mode — with a click tooltip showing card details and full exclusion from selection and search paths.

Purpose: SuperCards give users an at-a-glance aggregation summary at every group intersection, with drill-down via tooltip, while maintaining clean separation from data selection and search highlighting.
Output: SuperCard DOM elements rendered in every data cell via D3 .each(), tooltip with card list, selection/search exclusion proven by tests.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-supercards-polish/27-RESEARCH.md

@src/views/SuperGrid.ts
@src/views/types.ts
@src/views/supergrid/SuperGridSelect.ts
@tests/views/SuperGrid.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/views/types.ts:
```typescript
export interface SuperGridSelectionLike {
  select(cardIds: string[]): void;
  addToSelection(cardIds: string[]): void;
  clear(): void;
  isSelectedCell(cellKey: string): boolean;
  isCardSelected(cardId: string): boolean;
  getSelectedCount(): number;
  subscribe(cb: () => void): () => void;
}

export interface SuperGridDensityLike {
  getState(): Readonly<SuperDensityState>;
  setGranularity(granularity: TimeGranularity | null): void;
  setHideEmpty(hide: boolean): void;
  setViewMode(mode: ViewMode): void;
  subscribe(cb: () => void): () => void;
}
```

From src/views/supergrid/SuperGridSelect.ts:
```typescript
export type ClickZone = 'header' | 'data-cell' | 'supergrid-card' | 'grid';
export function classifyClickZone(target: EventTarget | null): ClickZone;
// Already returns 'supergrid-card' for .closest('.supergrid-card') — CARD-04 wiring pre-exists
```

From src/worker/protocol.ts (CellDatum shape):
```typescript
// CellDatum has: { count: number, card_ids: string, [key: string]: unknown }
// card_ids is a comma-separated string from GROUP_CONCAT(id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — SuperCard rendering in matrix and spreadsheet modes (CARD-01, CARD-02)</name>
  <files>src/views/SuperGrid.ts, tests/views/SuperGrid.test.ts</files>
  <behavior>
    - CARD-01 matrix: data cell with count > 0 contains a `.supergrid-card` child element with textContent equal to the count string
    - CARD-01 spreadsheet: data cell in spreadsheet viewMode has `.supergrid-card` as first child, followed by card pill elements
    - CARD-02 style: SuperCard element has `border-style: dashed`, `font-style: italic`
    - CARD-02 heat map exclusion: parent `.data-cell` element has empty backgroundColor when SuperCard is rendered (NO d3.interpolateBlues gradient)
    - CARD-02 attribute: SuperCard element has `data-supercard="true"` attribute
    - Empty cells (count === 0) do NOT render a SuperCard element
  </behavior>
  <action>
    Write failing tests in a new `describe('CARD-01/CARD-02 — SuperCard rendering')` block in SuperGrid.test.ts. Then implement:

    In `_renderCells()` inside the D3 `.each(function(d) { ... })` callback:

    **Matrix mode branch** — Replace the existing `count-badge` span creation with SuperCard:
    1. Clear `el.innerHTML`
    2. Create `div.supergrid-card` element with `data-supercard="true"` attribute
    3. Apply styles: `border: 1px dashed rgba(128,128,128,0.4)`, `border-radius: 4px`, `font-style: italic`, `font-size: calc(12px * var(--sg-zoom, 1))`, `padding: calc(4px * var(--sg-zoom, 1)) calc(8px * var(--sg-zoom, 1))`, `cursor: pointer`, `user-select: none`, subtle gray tint background `rgba(0,0,0,0.03)`
    4. Set `superCard.textContent = String(d.count)` — COUNT ONLY, no label
    5. CRITICAL: Set `el.style.backgroundColor = ''` — cell does NOT get heat map color. SuperCards are always visually distinct from heat map coloring.
    6. Set `el.style.display = 'flex'`, `alignItems: 'center'`, `justifyContent: 'center'`

    **Spreadsheet mode branch** — Prepend SuperCard above card pills:
    1. Create same `div.supergrid-card` element (same styling)
    2. Insert as first child of the cell (before any card pill elements)
    3. Do NOT clear existing pill content — SuperCard is additive in spreadsheet mode

    For cells with `d.count === 0`: skip SuperCard creation entirely (existing empty-cell behavior unchanged).

    Store `self` reference (capture `this` before D3 `.each(function(d)` callback) for tooltip wiring in Task 2.
  </action>
  <verify>
    <automated>npx vitest --run tests/views/SuperGrid.test.ts -t "CARD-01|CARD-02"</automated>
  </verify>
  <done>Every data cell with count > 0 renders a `.supergrid-card` element with dashed border, italic text, count-only content, data-supercard attribute, and no heat map background on the parent cell. Empty cells have no SuperCard. Works in both matrix and spreadsheet view modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — SuperCard tooltip + selection/search exclusion (CARD-03, CARD-04, CARD-05)</name>
  <files>src/views/SuperGrid.ts, tests/views/SuperGrid.test.ts</files>
  <behavior>
    - CARD-03 tooltip open: clicking SuperCard element calls `_openSuperCardTooltip()`; tooltip appears as child of `_rootEl` with count header ("N cards") and list of card IDs
    - CARD-03 tooltip dismiss: clicking outside the tooltip closes it; clicking inside keeps it open
    - CARD-03 tooltip select: clicking a card ID item in the tooltip calls `selectionAdapter.addToSelection([cardId])`; tooltip stays open for multi-select
    - CARD-03 tooltip lifecycle: `_closeSuperCardTooltip()` called at start of `_renderCells()` to clean up stale tooltips before DOM rebuild (Pitfall 3 from research)
    - CARD-04 click zone: SuperCard click returns zone='supergrid-card' from classifyClickZone(); data-cell onclick handler skips when zone !== 'data-cell' (already wired)
    - CARD-04 lasso: SuperGridSelect._handlePointerDown already returns early for zone === 'supergrid-card' (already wired)
    - CARD-05 FTS exclusion: when search is active, SuperCard cells do NOT get search highlight styling; match count badge does not count SuperCard elements
    - CARD-05 opacity: SuperCard cells get normal opacity during search (not dimmed, not highlighted) — they are neutral to search
  </behavior>
  <action>
    Write failing tests in `describe('CARD-03 — SuperCard tooltip')`, `describe('CARD-04 — Selection exclusion')`, and `describe('CARD-05 — FTS search exclusion')` blocks.

    **CARD-03 — Tooltip implementation:**
    1. Add private fields: `_superCardTooltipEl: HTMLDivElement | null = null`, `_boundTooltipOutsideClick: ((e: MouseEvent) => void) | null = null`
    2. Wire SuperCard click handler (from Task 1): `superCard.addEventListener('click', (e) => { e.stopPropagation(); self._openSuperCardTooltip(superCard, d); })`
    3. Implement `_openSuperCardTooltip(anchorEl: HTMLElement, d: CellDatum)`:
       - Call `_closeSuperCardTooltip()` first (close existing)
       - Compute position: `anchorRect.bottom - rootRect.top + _rootEl.scrollTop` and `anchorRect.left - rootRect.left + _rootEl.scrollLeft` (same pattern as filter dropdown)
       - Create tooltip div with class `sg-supercard-tooltip`, z-index 25 (above filter dropdown at 20, above lasso SVG at 5)
       - Add header div: `${d.count} card${d.count !== 1 ? 's' : ''}` with bold styling and bottom border
       - Parse `d.card_ids` (comma-separated string from GROUP_CONCAT) into array: `const ids = d.card_ids ? String(d.card_ids).split(',') : []`
       - For each card ID, create a clickable item div with hover background. Click handler: `e.stopPropagation(); self._selectionAdapter.addToSelection([id.trim()]);` — tooltip stays open for multi-select
       - If no card IDs, show "(empty)" message
       - Append tooltip to `_rootEl`
       - rAF-deferred click-outside listener (same pattern as filter dropdown): `requestAnimationFrame(() => { this._boundTooltipOutsideClick = ...; document.addEventListener('click', ...); })`
    4. Implement `_closeSuperCardTooltip()`: remove tooltip from DOM, remove click-outside listener, null out refs
    5. Call `_closeSuperCardTooltip()` at the START of `_renderCells()` (before DOM clearing) to prevent orphaned tooltips (Pitfall 3)
    6. Call `_closeSuperCardTooltip()` in `destroy()` for cleanup

    **CARD-04 — Selection exclusion (verify existing wiring):**
    - The `data-supercard` attribute from Task 1 enables `classifyClickZone()` to return `'supergrid-card'`
    - Write tests that verify: clicking SuperCard does NOT call `selectionAdapter.select()`, lasso starting on SuperCard does NOT initiate selection
    - No new production code needed — existing wiring in `classifyClickZone()` and `_handlePointerDown()` already excludes `'supergrid-card'` zone

    **CARD-05 — FTS search exclusion:**
    - In the D3 `.each()` search highlight block, when `isSearchActive` is true:
      - Check if the cell contains a `[data-supercard]` child element
      - If so, skip highlight styling (no opacity dimming, no amber border, no mark tags)
      - SuperCard cells remain at normal opacity during search — they are neutral/transparent to search
    - The match count badge (if implemented via `matchedCardIds` count) already counts from `CellDatum` data, not DOM elements, so SuperCards don't inflate it as long as we skip the highlight DOM manipulation
  </action>
  <verify>
    <automated>npx vitest --run tests/views/SuperGrid.test.ts -t "CARD-03|CARD-04|CARD-05"</automated>
  </verify>
  <done>SuperCard click opens a positioned tooltip with card list and dismiss-on-outside-click. Card IDs in tooltip are clickable for multi-select. SuperCards are excluded from all selection paths (click, Cmd+click, Shift+click, lasso). FTS search highlights skip SuperCard cells. Tooltip is cleaned up on re-render and destroy.</done>
</task>

</tasks>

<verification>
1. `npx vitest --run tests/views/SuperGrid.test.ts` — all SuperGrid tests pass including new CARD-01..05 tests
2. `npx vitest --run` — full test suite green (1838+ tests, no regressions)
3. Visual verification in browser: matrix mode shows dashed-border italic count SuperCards at every intersection; spreadsheet mode shows SuperCard above pills; clicking SuperCard shows tooltip; clicking outside dismisses; heat map coloring absent on SuperCard cells
</verification>

<success_criteria>
- Every group intersection cell renders a `.supergrid-card` element with COUNT content, dashed border, italic text, and `data-supercard` attribute
- Matrix mode: SuperCard replaces the old count-badge; cell has no heat map background
- Spreadsheet mode: SuperCard appears as first child above card pills
- SuperCard click opens tooltip; card ID click in tooltip adds to selection; click-outside dismisses
- SuperCard is excluded from single-click, Cmd+click, Shift+click, and lasso selection paths
- FTS search highlights skip SuperCard cells; match count badge unaffected
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/27-supercards-polish/27-01-SUMMARY.md`
</output>
