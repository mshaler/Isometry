---
phase: 17-supergrid-dynamic-axis-reads
plan: 02
type: tdd
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/views/SuperGrid.ts
  - tests/views/SuperGrid.test.ts
autonomous: true
requirements: [FOUN-09, FOUN-11]

must_haves:
  truths:
    - "SuperGrid fetches grouped cell data from Worker via bridge.superGridQuery() and renders cells at correct CSS Grid positions"
    - "D3 data join key function dynamically includes all axis field values (not hardcoded rowKey:colKey)"
    - "Empty intersections (zero count cells) are rendered for dimensional integrity"
    - "Worker error displays inline error message in grid area that auto-clears on next successful query"
    - "Zero results shows empty grid skeleton with headers visible but no data cells"
    - "Multiple provider changes within one 16ms batch produce exactly one superGridQuery() call"
    - "Collapse toggle re-renders from cached CellDatum[] without re-querying Worker"
  artifacts:
    - path: "src/views/SuperGrid.ts"
      provides: "Complete render pipeline with D3 data join, error/empty states, collapse cache"
      contains: "_renderCells"
    - path: "tests/views/SuperGrid.test.ts"
      provides: "FOUN-09 render pipeline tests, FOUN-11 batch dedup test"
      contains: "produces exactly one superGridQuery"
  key_links:
    - from: "src/views/SuperGrid.ts _fetchAndRender()"
      to: "src/worker/WorkerBridge.ts superGridQuery()"
      via: "bridge.superGridQuery(config) with axes from provider and filter from FilterProvider"
      pattern: "bridge\\.superGridQuery\\("
    - from: "src/views/SuperGrid.ts _renderCells()"
      to: "src/views/supergrid/SuperStackHeader.ts"
      via: "buildHeaderCells(colAxisValues, collapsedSet) for nested headers"
      pattern: "buildHeaderCells\\("
    - from: "src/views/SuperGrid.ts _renderCells()"
      to: "D3 data join"
      via: "d3.select(grid).selectAll('.data-cell').data(cellData, makeCellKey) with dynamic key"
      pattern: "makeCellKey"
---

<objective>
Verify and harden the SuperGrid render pipeline: D3 data join with dynamic axis keys, CSS Grid cell positioning from Worker results, error/empty states, collapse re-render from cache, and FOUN-11 batch deduplication proof.

Purpose: Plan 01 delivered the constructor injection + lifecycle + render pipeline implementation. This plan adds thorough TDD coverage for the render pipeline (FOUN-09), verifies the dynamic D3 key function works with multi-axis configurations, proves FOUN-11 batch deduplication (4 rapid changes produce 1 query), and ensures collapse toggle uses cached cells without re-querying.

Output: Comprehensive test coverage for the complete SuperGrid dynamic axis pipeline.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-supergrid-dynamic-axis-reads/17-CONTEXT.md
@.planning/phases/17-supergrid-dynamic-axis-reads/17-RESEARCH.md
@.planning/phases/17-supergrid-dynamic-axis-reads/17-01-SUMMARY.md

@src/views/SuperGrid.ts
@src/views/types.ts
@src/views/supergrid/SuperStackHeader.ts
@src/worker/protocol.ts
@tests/views/SuperGrid.test.ts
@tests/worker/WorkerBridge-supergrid.test.ts

<interfaces>
<!-- Executor needs Plan 01 artifacts. Read 17-01-SUMMARY.md for actual implementation details. -->
<!-- Key interfaces established in Plan 01: -->

From src/views/types.ts (added in Plan 01):
```typescript
export interface SuperGridBridgeLike {
  superGridQuery(config: SuperGridQueryConfig): Promise<CellDatum[]>;
}
export interface SuperGridProviderLike {
  getStackedGroupBySQL(): { colAxes: AxisMapping[]; rowAxes: AxisMapping[] };
}
export interface SuperGridFilterLike {
  compile(): { where: string; params: unknown[] };
}
```

From src/worker/protocol.ts:
```typescript
export interface CellDatum {
  [key: string]: unknown;
  count: number;
  card_ids: string[];
}
```

From src/views/supergrid/SuperStackHeader.ts:
```typescript
export function buildHeaderCells(
  axisValues: string[][],
  collapsedSet: Set<string>
): { headers: HeaderCell[][]; leafCount: number };
```

From tests/worker/WorkerBridge-supergrid.test.ts (rAF mock pattern):
```typescript
// Mock requestAnimationFrame for rAF coalescing tests
let rafCallbacks: (() => void)[] = [];
globalThis.requestAnimationFrame = (cb: FrameRequestCallback) => {
  rafCallbacks.push(() => cb(performance.now()));
  return rafCallbacks.length;
};
function flushRAF() { const cbs = [...rafCallbacks]; rafCallbacks = []; cbs.forEach(cb => cb()); }
```
</interfaces>
</context>

<feature>
  <name>SuperGrid Render Pipeline Verification + FOUN-11 Batch Dedup</name>
  <files>src/views/SuperGrid.ts, tests/views/SuperGrid.test.ts</files>
  <behavior>
    FOUN-09 Tests (render pipeline):
    - Test: mount() triggers bridge.superGridQuery() with config containing colAxes from provider — verify config.colAxes matches provider.getStackedGroupBySQL().colAxes
    - Test: bridge.superGridQuery() returning 3 cells renders 3 .data-cell elements with count badges
    - Test: cell at intersection (card_type='note', folder='Inbox') gets correct gridColumn and gridRow based on header positions
    - Test: D3 key function uses all axis values — cells with key "col:note::row:Inbox" have data-key attribute matching
    - Test: empty intersection (count=0) renders with .empty-cell class and no count badge
    - Test: dimensional integrity — for 2 col values × 3 row values, exactly 6 .data-cell elements exist (including empties)
    - Test: Worker error (rejected promise) shows .supergrid-error element with error message text
    - Test: successful query after error clears the .supergrid-error element
    - Test: zero results (empty cells array) shows headers but no .data-cell elements
    - Test: filter WHERE clause from FilterProvider is passed to bridge.superGridQuery() config

    FOUN-11 Tests (batch deduplication):
    - Test: 4 rapid StateCoordinator callbacks (synchronous) produce exactly 1 bridge.superGridQuery() call after rAF flush — uses rAF mock from WorkerBridge-supergrid.test.ts pattern
    - Test: only the last config (from the 4th callback's provider state) is sent to bridge.superGridQuery()

    Collapse Tests:
    - Test: clicking a column header to collapse does NOT trigger bridge.superGridQuery() — call count stays the same
    - Test: collapse re-renders from cached cells — same .data-cell count as before collapse (minus collapsed cells)

    Multi-axis Tests:
    - Test: with colAxes = [card_type, priority], D3 key includes both fields — key contains "col:note|high"
    - Test: with empty colAxes (falls back to VIEW_DEFAULTS), headers show card_type values
  </behavior>
  <implementation>
    1. Add comprehensive test describe blocks to tests/views/SuperGrid.test.ts:
       - "SuperGrid — render pipeline (FOUN-09)" block with mock bridge returning CellDatum[]
       - "SuperGrid — batch deduplication (FOUN-11)" block with rAF mock
       - "SuperGrid — collapse cache" block verifying no re-query
       - "SuperGrid — error and empty states" block
       - "SuperGrid — multi-axis key function" block

    2. For each test, set up mock dependencies (mockProvider, mockFilter, mockBridge, mockCoordinator) with spies. mockBridge.superGridQuery should return a controlled Promise that resolves with specific CellDatum[] for assertions.

    3. If any test fails (RED), fix the implementation in SuperGrid.ts (GREEN). Plan 01 should have the core implementation, but edge cases may need fixes:
       - Empty intersection generation (cross-product of unique col/row values minus Worker results)
       - Error display/clear logic
       - Collapse cache path (re-render from _lastCells without calling bridge.superGridQuery)
       - VIEW_DEFAULTS fallback when axes are empty

    4. FOUN-11 batch dedup test approach:
       - Mock coordinator.subscribe to capture the callback
       - Call the captured callback 4 times synchronously (simulating 4 provider changes)
       - Since SuperGrid's callback calls _fetchAndRender() which calls bridge.superGridQuery(), and superGridQuery has rAF coalescing, the 4 calls produce 4 superGridQuery() calls on the bridge mock (bridge mock doesn't have real rAF coalescing)
       - HOWEVER: the test should verify the StateCoordinator batching + SuperGrid behavior. Since StateCoordinator fires once per 16ms batch, and SuperGrid calls bridge.superGridQuery once per callback, the test should verify that a single coordinator callback produces a single superGridQuery call. The rAF coalescing is already tested in WorkerBridge-supergrid.test.ts.
       - Alternative: Test at integration level — trigger 4 provider changes, prove only 1 bridge.superGridQuery fires after coordinator's setTimeout(16) fires. Use vi.useFakeTimers().

    5. Verify all Phase 7 existing tests still pass with updated constructor signature.
  </implementation>
</feature>

<verification>
npx vitest run tests/views/SuperGrid.test.ts
npx vitest run
</verification>

<success_criteria>
- FOUN-09: SuperGrid renders correct cells from bridge.superGridQuery() response at correct CSS Grid positions
- FOUN-09: D3 data join uses dynamic key function with all axis field values
- FOUN-09: Empty intersections rendered with .empty-cell class
- FOUN-09: Worker error displays inline, clears on next success
- FOUN-09: Filter WHERE clause passed through to bridge.superGridQuery()
- FOUN-11: Single coordinator callback produces single superGridQuery call
- FOUN-11: 4 rapid provider changes → 1 coordinator callback → 1 superGridQuery
- Collapse toggle re-renders from cache (no bridge.superGridQuery call)
- All tests pass: npx vitest run — zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/17-supergrid-dynamic-axis-reads/17-02-SUMMARY.md`
</output>
