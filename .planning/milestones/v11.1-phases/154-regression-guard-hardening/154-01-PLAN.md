---
phase: 154-regression-guard-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/seams/ui/inline-embedding.test.ts
  - e2e/inline-embedding.spec.ts
autonomous: true
requirements: [REGR-01]

must_haves:
  truths:
    - "Full test suite (unit + seam + E2E) passes green with zero regressions"
    - "Seam test verifies Data dock toggle shows/hides top-slot explorers"
    - "Seam test verifies Projections Explorer auto-visibility for SuperGrid only"
    - "Seam test verifies LATCH Filters persist across view switch"
    - "E2E spec exercises inline embedding flows in a real browser"
  artifacts:
    - path: "tests/seams/ui/inline-embedding.test.ts"
      provides: "jsdom integration tests for all 3 inline embedding flows"
      min_lines: 80
    - path: "e2e/inline-embedding.spec.ts"
      provides: "Playwright smoke spec for inline embedding"
      min_lines: 30
  key_links:
    - from: "tests/seams/ui/inline-embedding.test.ts"
      to: "src/main.ts"
      via: "exercises show/hide/sync functions and onActivateItem handler logic"
      pattern: "syncTopSlotVisibility|syncBottomSlotVisibility"
    - from: "e2e/inline-embedding.spec.ts"
      to: "src/ui/DockNav.ts"
      via: "clicks dock-nav__item buttons and asserts slot visibility"
      pattern: "dock-nav__item.*data-section-key"
---

<objective>
Verify zero regressions from Phases 151-153 inline embedding work, and add integration tests covering all three flows: top-slot Data toggle, Projections auto-visibility, and bottom-slot LATCH Filters persistence.

Purpose: Phase 154 closes the v11.1 milestone with confidence — no silent breakage from the PanelDrawer removal and explorer relocation.
Output: One seam test file, one E2E spec, full green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-regression-guard-hardening/154-CONTEXT.md

@src/main.ts
@src/ui/WorkbenchShell.ts
@src/ui/DockNav.ts
@src/ui/section-defs.ts
@tests/seams/ui/workbench-shell.test.ts
@e2e/fixtures.ts
@e2e/view-switch.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any regressions</name>
  <read_first>
    - src/main.ts (show/hide functions, onActivateItem handler — lines 640-680 for slot setup, 1120-1230 for handler)
    - src/ui/WorkbenchShell.ts (getTopSlotEl, getBottomSlotEl accessors)
    - tests/seams/ui/workbench-shell.test.ts (existing shell seam test — reference for setup pattern)
  </read_first>
  <files>Any files that have failing tests — scope depends on what breaks</files>
  <action>
Run the full Vitest suite (`npx vitest run`) and the Playwright E2E suite (`npx playwright test`). Per D-03, fix anything that is red regardless of whether it was caused by Phases 151-153.

If all tests pass (expected based on current state: 210 files, 4334 tests green), proceed immediately to Task 2.

If any test fails:
1. Read the failing test file and the source file it exercises
2. Fix the production code (anti-patching rule: never weaken assertions)
3. Re-run until green

Do NOT create new test files in this task — that is Task 2.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` output contains "0 failed" or shows only passed/skipped counts
    - `npx vitest run` output shows 210+ test files passed
    - No test file shows a FAIL status in the output
  </acceptance_criteria>
  <done>Full Vitest suite passes green with zero failures</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create seam tests and E2E spec for inline embedding flows</name>
  <read_first>
    - src/main.ts (lines 630-680 for slot child setup, lines 740-1020 for show/hide/sync functions, lines 1120-1230 for onActivateItem handler)
    - src/ui/WorkbenchShell.ts (full file — constructor DOM structure, getTopSlotEl/getBottomSlotEl)
    - src/ui/DockNav.ts (constructor lines 80-160 for DOM structure with data-section-key/data-item-key attributes, setItemPressed method)
    - src/ui/section-defs.ts (DOCK_DEFS array — section keys and item keys)
    - tests/seams/ui/workbench-shell.test.ts (setup pattern: makeConfig, WorkbenchShell constructor with jsdom root)
    - e2e/fixtures.ts (loadBaselineDataset, test fixture pattern)
    - e2e/view-switch.spec.ts (page.evaluate pattern for view switching via __isometry)
  </read_first>
  <files>tests/seams/ui/inline-embedding.test.ts, e2e/inline-embedding.spec.ts</files>
  <behavior>
    Seam tests (jsdom, tests/seams/ui/inline-embedding.test.ts):

    describe "Top-slot: Data Explorer toggle":
    - Test 1: top-slot container (.workbench-slot-top) has display:none initially
    - Test 2: after creating child divs (.slot-top__data-explorer, .slot-top__properties-explorer) and setting their display to '' (visible), calling syncTopSlotVisibility makes .workbench-slot-top display:block
    - Test 3: setting all children back to display:none and calling syncTopSlotVisibility makes .workbench-slot-top display:none

    describe "Top-slot: Projections auto-visibility":
    - Test 4: .slot-top__projection-explorer has display:none by default
    - Test 5: setting .slot-top__projection-explorer display to '' and syncing makes top-slot visible
    - Test 6: setting it back to display:none and syncing hides top-slot (if no other children visible)

    describe "Bottom-slot: LATCH Filters toggle":
    - Test 7: bottom-slot container (.workbench-slot-bottom) has display:none initially
    - Test 8: after creating .slot-bottom__latch-filters child and setting display to '', syncBottomSlotVisibility makes .workbench-slot-bottom visible
    - Test 9: hiding the child and syncing hides bottom-slot

    describe "Bottom-slot: LATCH Filters persistence across view switch":
    - Test 10: after making LATCH filters visible, simulating a view switch (removing and re-appending view content) does NOT change bottom-slot or latch-filters display — they remain visible

    E2E spec (e2e/inline-embedding.spec.ts):
    - Test 1 (smoke): Load baseline dataset, click integrate:catalog dock button, verify .workbench-slot-top is visible with .slot-top__data-explorer child visible
    - Test 2: Click integrate:catalog again, verify .workbench-slot-top is hidden
    - Test 3: Switch to supergrid view via dock, verify .slot-top__projection-explorer is visible
    - Test 4: Switch to timeline view via dock, verify .slot-top__projection-explorer is hidden
    - Test 5: Click analyze:filter dock button, verify .workbench-slot-bottom is visible with .slot-bottom__latch-filters child visible
    - Test 6: Switch to another view while filters on, verify .slot-bottom__latch-filters still visible
  </behavior>
  <action>
Create `tests/seams/ui/inline-embedding.test.ts` (per D-02):

1. Add `// @vitest-environment jsdom` annotation at top
2. Import WorkbenchShell, PanelRegistry, vitest functions
3. Setup helper: create root element, instantiate WorkbenchShell with makeConfig() (same pattern as workbench-shell.test.ts — CommandBarConfig stub + PanelRegistry + bridge stub)
4. After shell construction, manually create the child containers inside the slot elements — mirroring what main.ts does at lines 630-678:
   - Create 3 child divs in topSlotEl: `.slot-top__data-explorer`, `.slot-top__properties-explorer`, `.slot-top__projection-explorer` — all with `style.display = 'none'`
   - Create 2 child divs in bottomSlotEl: `.slot-bottom__latch-filters`, `.slot-bottom__formulas-explorer` — all with `style.display = 'none'`
5. Create local `syncTopSlotVisibility()` and `syncBottomSlotVisibility()` functions that replicate the logic from main.ts (check if any child display !== 'none', set parent display accordingly) — these are module-scoped closures in main.ts so we must replicate the logic to test it
6. Write describe blocks with tests per the behavior spec above
7. TDD: write all tests first (expect them to pass immediately since we're testing real DOM manipulation logic)

Create `e2e/inline-embedding.spec.ts` (per D-02):

1. Import `test, expect` from `./fixtures`
2. Use `test.describe('Inline embedding flows', ...)` wrapper
3. For dock button clicks, use Playwright locator: `page.locator('button.dock-nav__item[data-section-key="integrate"][data-item-key="catalog"]')`
4. For visualize items: `page.locator('button.dock-nav__item[data-section-key="visualize"][data-item-key="supergrid"]')` and similar for timeline
5. For analyze items: `page.locator('button.dock-nav__item[data-section-key="analyze"][data-item-key="filter"]')`
6. Assert visibility via: `page.locator('.workbench-slot-top')` with `toBeVisible()` / `toBeHidden()`
7. Assert child visibility: `page.locator('.slot-top__data-explorer')`, `.slot-top__projection-explorer`, `.slot-bottom__latch-filters`
8. For filter persistence test: click analyze:filter → verify visible → click visualize:timeline (switch view) → verify `.slot-bottom__latch-filters` still visible
  </action>
  <verify>
    <automated>npx vitest run tests/seams/ui/inline-embedding.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/seams/ui/inline-embedding.test.ts` exists and contains `// @vitest-environment jsdom`
    - File contains `describe('Top-slot: Data Explorer toggle'`
    - File contains `describe('Top-slot: Projections auto-visibility'`
    - File contains `describe('Bottom-slot: LATCH Filters toggle'`
    - File contains `describe('Bottom-slot: LATCH Filters persistence'`
    - File contains `syncTopSlotVisibility` and `syncBottomSlotVisibility` function calls
    - File `e2e/inline-embedding.spec.ts` exists and contains `data-section-key="integrate"` and `data-section-key="analyze"`
    - `npx vitest run tests/seams/ui/inline-embedding.test.ts` exits 0 with all tests passing
    - Full suite `npx vitest run` still shows 210+ files passed with 0 failures
  </acceptance_criteria>
  <done>
    - Seam test file has 4 describe blocks covering all 3 inline embedding flows plus persistence
    - E2E spec covers toggle on/off, Projections auto-visibility, and filter persistence across view switch
    - Full test suite remains green
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all test files pass (210+ files, 0 failures)
2. `npx vitest run tests/seams/ui/inline-embedding.test.ts` — new seam tests pass
3. `npx playwright test e2e/inline-embedding.spec.ts` — new E2E spec passes (requires dev server running)
4. `grep -c 'describe' tests/seams/ui/inline-embedding.test.ts` — returns 4 or more (one per flow)
</verification>

<success_criteria>
- Zero test regressions across the full suite (REGR-01)
- Integration test proves Data dock toggle controls top-slot visibility
- Integration test proves Projections Explorer shows only for SuperGrid
- Integration test proves LATCH Filters survive a view switch
- E2E smoke spec confirms flows work in a real browser environment
</success_criteria>

<output>
After completion, create `.planning/phases/154-regression-guard-hardening/154-01-SUMMARY.md`
</output>
