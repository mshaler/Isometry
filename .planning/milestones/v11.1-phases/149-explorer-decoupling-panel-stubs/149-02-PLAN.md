---
phase: 149-explorer-decoupling-panel-stubs
plan: 02
type: execute
wave: 2
depends_on: [149-01]
files_modified:
  - src/ui/section-defs.ts
  - src/ui/panels/MapsPanelStub.ts
  - src/ui/panels/FormulasPanelStub.ts
  - src/ui/panels/StoriesPanelStub.ts
  - src/main.ts
autonomous: false
requirements: [STUB-01, STUB-02, STUB-03]

must_haves:
  truths:
    - "User sees a Maps dock entry that, when activated, shows a 'Coming soon' placeholder panel"
    - "User sees a Formulas dock entry that, when activated, shows a 'Coming soon' placeholder panel"
    - "User sees a Stories dock entry that, when activated, shows a 'Coming soon' placeholder panel"
  artifacts:
    - path: "src/ui/panels/MapsPanelStub.ts"
      provides: "Maps PanelMeta + PanelFactory with Coming soon placeholder"
      exports: ["MAPS_PANEL_META", "mapsPanelFactory"]
    - path: "src/ui/panels/FormulasPanelStub.ts"
      provides: "Formulas PanelMeta + PanelFactory with Coming soon placeholder"
      exports: ["FORMULAS_PANEL_META", "formulasPanelFactory"]
    - path: "src/ui/panels/StoriesPanelStub.ts"
      provides: "Stories PanelMeta + PanelFactory with Coming soon placeholder"
      exports: ["STORIES_PANEL_META", "storiesPanelFactory"]
    - path: "src/ui/section-defs.ts"
      provides: "Maps dock entry added to DOCK_DEFS"
      contains: "maps"
    - path: "src/main.ts"
      provides: "Stub panels registered and routed via dockToPanelMap"
      contains: "mapsPanelFactory"
  key_links:
    - from: "src/main.ts"
      to: "PanelRegistry"
      via: "panelRegistry.register(MAPS_PANEL_META, mapsPanelFactory)"
      pattern: "MAPS_PANEL_META"
    - from: "src/main.ts"
      to: "dockToPanelMap"
      via: "Adding stub composite keys to dockToPanelMap"
      pattern: "synthesize:maps.*maps-stub"
---

<objective>
Register Maps, Formulas, and Stories as PanelMeta stub entries with "Coming soon" placeholder content, add a Maps dock entry to DOCK_DEFS, and wire all three stub dock items through the panel toggle routing from Plan 01.

Purpose: Users see Maps, Formulas, and Stories as real dock entries that open placeholder panels, establishing clean extension points for future implementation (STUB-01, STUB-02, STUB-03).

Output: 3 stub panel factory files, Maps dock entry, all three routed through PanelRegistry toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/149-explorer-decoupling-panel-stubs/149-CONTEXT.md
@.planning/phases/149-explorer-decoupling-panel-stubs/149-UI-SPEC.md
@.planning/phases/149-explorer-decoupling-panel-stubs/149-01-SUMMARY.md

@src/ui/section-defs.ts
@src/ui/panels/PanelTypes.ts
@src/ui/panels/PanelRegistry.ts
@src/main.ts

<interfaces>
<!-- From Plan 01 output — PanelDrawer now has togglePanel/scrollToPanel -->

From src/ui/panels/PanelTypes.ts:
```typescript
export interface PanelMeta {
  id: string;
  name: string;
  icon: string;
  description: string;
  dependencies: string[];
  defaultEnabled: boolean;
}
export interface PanelHook {
  mount(container: HTMLElement): void;
  unmount?(): void;
  resize?(): void;
  update?(): void;
  destroy(): void;
}
export type PanelFactory = () => PanelHook;
```

From src/main.ts (after Plan 01):
```typescript
// dockToPanelMap maps composite keys to panel IDs
const dockToPanelMap: Record<string, string> = {
  'integrate:properties': 'properties',
  'integrate:projection': 'projection',
  'analyze:filter': 'latch',
  'activate:notebook': 'notebook',
};
```

Current DOCK_DEFS sections (from section-defs.ts):
- integrate: catalog, properties, projection
- visualize: supergrid, timeline, network, tree
- analyze: filter, formula
- synthesize: graph
- activate: stories, notebook
- help: settings, help-page

Stubs route through existing dock items:
- analyze:formula -> Formulas stub (already in DOCK_DEFS)
- activate:stories -> Stories stub (already in DOCK_DEFS)
- synthesize:maps -> Maps stub (NEW entry needed in DOCK_DEFS synthesize section)

UI-SPEC placeholder layout per stub:
```
[Lucide icon, 32x32, color: --text-muted]
[Heading: feature name, 14px, 600, --text-primary]
[Body: "Coming soon", 13px, 400, --text-muted]
```
Centered flexbox: align-items: center; justify-content: center; flex-direction: column; gap: 8px; height: 100%; padding: 24px.
"Coming soon" text gets role="status" for VoiceOver.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub panel factories and add Maps dock entry</name>
  <files>src/ui/panels/MapsPanelStub.ts, src/ui/panels/FormulasPanelStub.ts, src/ui/panels/StoriesPanelStub.ts, src/ui/section-defs.ts</files>
  <read_first>
    - src/ui/panels/PanelTypes.ts (PanelMeta, PanelHook, PanelFactory interfaces)
    - src/ui/section-defs.ts (DOCK_DEFS array — to add Maps entry to synthesize section)
    - src/ui/icons.ts (iconSvg function signature for Lucide SVGs)
    - .planning/phases/149-explorer-decoupling-panel-stubs/149-UI-SPEC.md (placeholder layout spec and accessibility)
  </read_first>
  <action>
    **Create src/ui/panels/MapsPanelStub.ts:**

    ```typescript
    // Isometry — Phase 149
    // MapsPanelStub: "Coming soon" placeholder for the Maps feature.

    import type { PanelFactory, PanelHook, PanelMeta } from './PanelTypes';
    import { iconSvg } from '../icons';

    export const MAPS_PANEL_META: PanelMeta = {
      id: 'maps-stub',
      name: 'Maps',
      icon: 'globe',
      description: 'Maps',
      dependencies: [],
      defaultEnabled: false,
    };

    export const mapsPanelFactory: PanelFactory = (): PanelHook => {
      let el: HTMLElement | null = null;

      return {
        mount(container: HTMLElement): void {
          el = document.createElement('div');
          el.style.cssText = 'display:flex;flex-direction:column;align-items:center;justify-content:center;gap:8px;height:100%;padding:24px;';

          const icon = document.createElement('div');
          icon.innerHTML = iconSvg('globe', 32);
          icon.style.color = 'var(--text-muted)';
          el.appendChild(icon);

          const heading = document.createElement('div');
          heading.textContent = 'Maps';
          heading.style.cssText = 'font-size:var(--text-md);font-weight:600;color:var(--text-primary);';
          el.appendChild(heading);

          const body = document.createElement('div');
          body.textContent = 'Coming soon';
          body.setAttribute('role', 'status');
          body.style.cssText = 'font-size:var(--text-base);font-weight:400;color:var(--text-muted);';
          el.appendChild(body);

          container.appendChild(el);
        },
        destroy(): void {
          el?.remove();
          el = null;
        },
      };
    };
    ```

    **Create src/ui/panels/FormulasPanelStub.ts:**
    Same pattern as MapsPanelStub but with:
    - `id: 'formulas-stub'`, `name: 'Formulas'`, `icon: 'code'`, `description: 'Formulas'`
    - Heading text: `'Formulas'`
    - Icon: `iconSvg('code', 32)`
    - Export names: `FORMULAS_PANEL_META`, `formulasPanelFactory`

    **Create src/ui/panels/StoriesPanelStub.ts:**
    Same pattern as MapsPanelStub but with:
    - `id: 'stories-stub'`, `name: 'Stories'`, `icon: 'book-open'`, `description: 'Stories'`
    - Heading text: `'Stories'`
    - Icon: `iconSvg('book-open', 32)`
    - Export names: `STORIES_PANEL_META`, `storiesPanelFactory`

    **section-defs.ts changes:**
    Add a `maps` item to the `synthesize` section in DOCK_DEFS. Insert it BEFORE the existing `graph` item:

    ```typescript
    {
      key: 'synthesize',
      label: 'Synthesize',
      items: [
        { key: 'maps', label: 'Maps', icon: 'globe' },
        { key: 'graph', label: 'Graphs', icon: 'share-2' },
      ],
    },
    ```

    Note: The `visualize:network` item labeled "Maps" in the dock is the existing network graph view — it is a VIEW, not the geospatial Maps STUB. The new `synthesize:maps` is the stub entry for the future geospatial Maps feature. Use `globe` icon to visually distinguish from the `map` icon used by the network view.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/ui/panels/MapsPanelStub.ts exists and contains `id: 'maps-stub'`
    - File src/ui/panels/MapsPanelStub.ts contains `role', 'status'`
    - File src/ui/panels/FormulasPanelStub.ts exists and contains `id: 'formulas-stub'`
    - File src/ui/panels/FormulasPanelStub.ts contains `role', 'status'`
    - File src/ui/panels/StoriesPanelStub.ts exists and contains `id: 'stories-stub'`
    - File src/ui/panels/StoriesPanelStub.ts contains `role', 'status'`
    - section-defs.ts DOCK_DEFS synthesize section contains `{ key: 'maps'`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Three stub panel factory files created with "Coming soon" placeholder and role="status" accessibility; Maps dock entry added to synthesize section in DOCK_DEFS</done>
</task>

<task type="auto">
  <name>Task 2: Register stubs in PanelRegistry and add to dock routing</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (panel registration block ~lines 1330-1540, and dockToPanelMap from Plan 01)
    - src/ui/panels/MapsPanelStub.ts (MAPS_PANEL_META, mapsPanelFactory exports)
    - src/ui/panels/FormulasPanelStub.ts (FORMULAS_PANEL_META, formulasPanelFactory exports)
    - src/ui/panels/StoriesPanelStub.ts (STORIES_PANEL_META, storiesPanelFactory exports)
  </read_first>
  <action>
    **Add imports at top of main.ts:**
    ```typescript
    import { MAPS_PANEL_META, mapsPanelFactory } from './ui/panels/MapsPanelStub';
    import { FORMULAS_PANEL_META, formulasPanelFactory } from './ui/panels/FormulasPanelStub';
    import { STORIES_PANEL_META, storiesPanelFactory } from './ui/panels/StoriesPanelStub';
    ```

    **Register stubs in the panel registration block** (after the existing `panelRegistry.register(...)` calls for algorithm, around line 1530):
    ```typescript
    panelRegistry.register(MAPS_PANEL_META, mapsPanelFactory);
    panelRegistry.register(FORMULAS_PANEL_META, formulasPanelFactory);
    panelRegistry.register(STORIES_PANEL_META, storiesPanelFactory);
    ```

    **Add stub composite keys to dockToPanelMap** (the object created in Plan 01):
    ```typescript
    const dockToPanelMap: Record<string, string> = {
      'integrate:properties': 'properties',
      'integrate:projection': 'projection',
      'analyze:filter': 'latch',
      'activate:notebook': 'notebook',
      // Stub panels (Phase 149)
      'synthesize:maps': 'maps-stub',
      'analyze:formula': 'formulas-stub',
      'activate:stories': 'stories-stub',
    };
    ```

    No other changes needed — the generic toggle routing from Plan 01 handles the rest.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - main.ts contains `import { MAPS_PANEL_META, mapsPanelFactory }` from MapsPanelStub
    - main.ts contains `import { FORMULAS_PANEL_META, formulasPanelFactory }` from FormulasPanelStub
    - main.ts contains `import { STORIES_PANEL_META, storiesPanelFactory }` from StoriesPanelStub
    - main.ts contains `panelRegistry.register(MAPS_PANEL_META, mapsPanelFactory)`
    - main.ts contains `panelRegistry.register(FORMULAS_PANEL_META, formulasPanelFactory)`
    - main.ts contains `panelRegistry.register(STORIES_PANEL_META, storiesPanelFactory)`
    - main.ts dockToPanelMap contains `'synthesize:maps': 'maps-stub'`
    - main.ts dockToPanelMap contains `'analyze:formula': 'formulas-stub'`
    - main.ts dockToPanelMap contains `'activate:stories': 'stories-stub'`
    - TypeScript compiles with no errors
    - All existing tests pass
  </acceptance_criteria>
  <done>All three stub panels registered in PanelRegistry and routed through dock item clicks; clicking Maps/Formulas/Stories dock items opens "Coming soon" placeholder in the side drawer</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify explorer panels and stubs in side drawer</name>
  <what-built>
    PanelDrawer relocated from hidden tray to visible side drawer. Dock clicks toggle explorer panels. Maps, Formulas, and Stories show "Coming soon" placeholders.
  </what-built>
  <how-to-verify>
    1. Open the app (`npx vite dev` or existing dev server)
    2. Click "Properties" in the dock -> side drawer opens showing Properties explorer panel
    3. Click "Properties" again -> panel closes, drawer closes
    4. Click "LATCH Filters" -> drawer opens with LATCH filter controls
    5. Click "Notebooks" -> Notebook explorer opens in drawer (LATCH stays open too)
    6. Click "Maps" in the Synthesize section -> drawer shows "Coming soon" placeholder with globe icon
    7. Click "Formulas" in Analyze section -> drawer shows "Coming soon" with code icon
    8. Click "Stories" in Activate section -> drawer shows "Coming soon" with book-open icon
    9. Click "SuperGrids" -> view switches as before (no panel toggle)
    10. Click "Data" -> DataExplorer toggles as before (separate from panel drawer)
    11. Verify dock navbar contains NO filter controls or explorer widgets — only icons and labels
    12. Test across at least 2 themes (dark + light) — drawer and placeholders render correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `npx vitest run` — all existing tests pass
3. Manual: Maps dock item opens "Coming soon" placeholder in side drawer
4. Manual: Formulas dock item opens "Coming soon" placeholder
5. Manual: Stories dock item opens "Coming soon" placeholder
6. Manual: Dock navbar contains only navigation affordances
7. Manual: All 8 existing explorers work in main panel
</verification>

<success_criteria>
- Maps, Formulas, Stories each show "Coming soon" when their dock item is clicked
- Each placeholder has centered layout: icon (32px), heading (feature name), body ("Coming soon")
- "Coming soon" text has role="status" for VoiceOver
- Maps has a dock entry in the Synthesize section with globe icon
- Formulas and Stories use their existing dock entries (analyze:formula, activate:stories)
- All stub panels toggle open/closed via the same PanelRegistry mechanism as real explorers
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/149-explorer-decoupling-panel-stubs/149-02-SUMMARY.md`
</output>
