---
phase: 144-harness-production-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/pivot/ProductionSuperGrid.ts
  - src/views/pivot/plugins/FeatureCatalog.ts
  - docs/HARNESS-PRODUCTION-DIFF.md
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "ProductionSuperGrid comment accurately reflects 28 plugins (not 27)"
    - "FeatureCatalog comment accurately reflects 28 plugins (not 27)"
    - "A diff document exists listing all intentional differences between HarnessShell and ProductionSuperGrid"
    - "Both HarnessShell and ProductionSuperGrid call registerCatalog() — verified by grep"
  artifacts:
    - path: "src/views/pivot/ProductionSuperGrid.ts"
      provides: "Corrected plugin count comment (28)"
    - path: "src/views/pivot/plugins/FeatureCatalog.ts"
      provides: "Corrected plugin count comment (28)"
    - path: "docs/HARNESS-PRODUCTION-DIFF.md"
      provides: "Intentional differences document"
  key_links:
    - from: "src/views/pivot/ProductionSuperGrid.ts"
      to: "src/views/pivot/plugins/FeatureCatalog.ts"
      via: "registerCatalog() call + enable-all loop"
      pattern: "registerCatalog\\(this._registry\\)"
    - from: "src/views/pivot/harness/HarnessShell.ts"
      to: "src/views/pivot/plugins/FeatureCatalog.ts"
      via: "registerCatalog() call"
      pattern: "registerCatalog\\(this._registry\\)"
---

<objective>
Correct stale plugin count comments (27 → 28) in ProductionSuperGrid.ts and FeatureCatalog.ts after Phase 143-02 added SuperSizeRowHeaderResize. Document all intentional differences between HarnessShell and ProductionSuperGrid.

Purpose: SYNC-01 — both paths use registerCatalog() with all 28 plugins enabled; code comments and catalog doc-comment must reflect this. The diff document satisfies the "document intentional differences" success criterion.
Output: Updated source comments + docs/HARNESS-PRODUCTION-DIFF.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/143-visual-polish/143-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale "27 plugins" comments in ProductionSuperGrid and FeatureCatalog</name>
  <files>src/views/pivot/ProductionSuperGrid.ts, src/views/pivot/plugins/FeatureCatalog.ts</files>
  <read_first>
    - src/views/pivot/ProductionSuperGrid.ts (full file — 123 lines)
    - src/views/pivot/plugins/FeatureCatalog.ts (lines 1-65, catalog header + FEATURE_CATALOG array)
  </read_first>
  <action>
    Two surgical comment fixes only — no logic changes:

    1. In `src/views/pivot/ProductionSuperGrid.ts`:
       - Line 7: Change "Creates PluginRegistry with all 27 plugins via registerCatalog()" to "Creates PluginRegistry with all 28 plugins via registerCatalog()"
       - Lines 45-55 block comment: Change "Enable all 27 plugins in production." to "Enable all 28 plugins in production." and update the inline comment that says "the remaining 24" to "the remaining 25" (since 28 total minus 3 base = 25 non-base plugins)

    2. In `src/views/pivot/plugins/FeatureCatalog.ts`:
       - Line 60: Change "Full feature taxonomy — 10 categories, 27 sub-features." to "Full feature taxonomy — 10 categories, 28 sub-features."

    Do NOT change any logic, imports, or exports. Only JSDoc/comment strings change.

    Verify grep confirms 28 appears in both files after the change:
    `grep -n "28 plugin\|28 sub-feature" src/views/pivot/ProductionSuperGrid.ts src/views/pivot/plugins/FeatureCatalog.ts`
  </action>
  <verify>
    <automated>grep -n "27" src/views/pivot/ProductionSuperGrid.ts src/views/pivot/plugins/FeatureCatalog.ts | grep -v "// Phase 122\|// Phase 98\|28\|dependencies\|string\|number\|pixel\|width\|height\|timeout\|line\|HAR"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "27 plugin" src/views/pivot/ProductionSuperGrid.ts` returns no matches
    - `grep "27 sub-feature" src/views/pivot/plugins/FeatureCatalog.ts` returns no matches
    - `grep "28" src/views/pivot/ProductionSuperGrid.ts` matches the updated comment lines
    - `grep "28 sub-feature" src/views/pivot/plugins/FeatureCatalog.ts` matches
    - TypeScript compiles: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both files have accurate "28 plugins" / "28 sub-features" comments. No logic changed. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/HARNESS-PRODUCTION-DIFF.md documenting intentional differences</name>
  <files>docs/HARNESS-PRODUCTION-DIFF.md</files>
  <read_first>
    - src/views/pivot/harness/HarnessShell.ts (full file — 197 lines)
    - src/views/pivot/ProductionSuperGrid.ts (full file — 123 lines)
    - src/views/pivot/plugins/FeatureCatalog.ts (lines 50-65, FEATURE_CATALOG comment + first entries to confirm defaultEnabled values)
  </read_first>
  <action>
    Create `docs/HARNESS-PRODUCTION-DIFF.md` with this exact content (fill in plugin IDs from FeatureCatalog.ts as needed):

    ```markdown
    # Harness vs Production SuperGrid: Intentional Differences

    Both `HarnessShell` and `ProductionSuperGrid` call `registerCatalog()` to populate a
    `PluginRegistry` with all 28 plugins. Their configurations then diverge intentionally.

    ## Shared Foundation

    | Aspect | HarnessShell | ProductionSuperGrid |
    |--------|-------------|---------------------|
    | Plugin registration | `registerCatalog(this._registry)` | `registerCatalog(this._registry)` |
    | Plugin count | 28 | 28 |
    | Base plugins enabled | yes (defaultEnabled: true) | yes (defaultEnabled: true) |

    ## Intentional Differences

    | Aspect | HarnessShell | ProductionSuperGrid |
    |--------|-------------|---------------------|
    | Non-base plugin enablement | Preserves `defaultEnabled: false` — user toggles via FeaturePanel | Forces all 25 non-base plugins enabled at construction |
    | Toggle state persistence | Saves/restores from `localStorage` (`isometry:harness:toggles`) | No toggle UI — all plugins always on |
    | Toggle UI | `FeaturePanel` sidebar with per-plugin checkboxes | None |
    | Data source | `MockDataAdapter` (no providers, no bridge) | `BridgeDataAdapter` wrapping PAFVProvider, FilterProvider, SuperDensityProvider, bridge |
    | Data flow | Static mock cells rebuilt on `rerender()` | Bridge-driven: coordinator subscription triggers re-render |
    | Shell container | `HarnessShell` (self-contained, mounts its own sidebar + main layout) | `ProductionSuperGrid` implements `IView` — mounted by `ViewManager` inside `.workbench-view-content` |
    | Explorer panels | None (harness has its own FeaturePanel toggle tree) | WorkbenchShell panels (CalcExplorer, PropertiesExplorer, etc.) wired via `setCalcExplorer()`, `setSchemaProvider()`, `setDepthGetter()` |
    | Entry point | `harness.html` → `harness-entry.ts` | `index.html` → `main.ts` |
    | `window.__harness` | Exposed — programmatic enable/disable for E2E tests | Not exposed |
    | `window.__harnessReady` | Set to `true` on mount | Not set |

    ## Why Keep Both

    - **HarnessShell** is a development/testing tool: individual plugins are toggled on/off to verify
      their isolated behavior. Mock data keeps the harness dependency-free (no sql.js, no providers).
    - **ProductionSuperGrid** is the shipping implementation: all plugins always on, real data from
      the bridge, lifecycle managed by ViewManager.

    The two paths verify complementary properties:
    - Harness: per-plugin isolation and feature toggle correctness
    - Production smoke E2E: full integration with real data and all 28 plugins active simultaneously
    ```

    Run `ls docs/HARNESS-PRODUCTION-DIFF.md` to confirm the file was created.
  </action>
  <verify>
    <automated>ls /Users/mshaler/Developer/Projects/Isometry/docs/HARNESS-PRODUCTION-DIFF.md && grep -c "28" /Users/mshaler/Developer/Projects/Isometry/docs/HARNESS-PRODUCTION-DIFF.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/HARNESS-PRODUCTION-DIFF.md` exists
    - File contains "28 plugins" references
    - File contains sections for "Shared Foundation" and "Intentional Differences"
    - File contains entries for: MockDataAdapter vs BridgeDataAdapter, FeaturePanel vs none, localStorage persistence vs none, IView vs self-contained shell
  </acceptance_criteria>
  <done>docs/HARNESS-PRODUCTION-DIFF.md created with accurate diff table. All intentional differences documented.</done>
</task>

</tasks>

<verification>
- `grep "27 plugin" src/views/pivot/ProductionSuperGrid.ts` → no matches
- `grep "27 sub-feature" src/views/pivot/plugins/FeatureCatalog.ts` → no matches
- `grep "registerCatalog" src/views/pivot/ProductionSuperGrid.ts src/views/pivot/harness/HarnessShell.ts` → both files contain the call
- `ls docs/HARNESS-PRODUCTION-DIFF.md` → file exists
- `npx tsc --noEmit` → exits 0 (no TypeScript errors from comment-only changes)
</verification>

<success_criteria>
- Plugin count comments are accurate (28) in both source files
- docs/HARNESS-PRODUCTION-DIFF.md documents all intentional differences between the two paths
- No logic changes — surgical comment updates only
- SYNC-01 satisfied: both paths verified to use registerCatalog() with 28 plugins
</success_criteria>

<output>
After completion, create `.planning/phases/144-harness-production-sync/144-01-SUMMARY.md`
</output>
