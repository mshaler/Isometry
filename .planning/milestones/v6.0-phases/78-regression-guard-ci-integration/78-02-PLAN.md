# Plan 78-02: Performance Contracts Documentation

---
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
requirements:
  - RGRD-02
  - RGRD-04
autonomous: true
---

## Goal

Add a `## Performance Contracts` section to PROJECT.md documenting all 4 performance budget categories, the CI enforcement mechanism, and the promotion procedure from soft gate to blocking gate.

## Context

- All budget constants live in `src/profiling/PerfBudget.ts` with inline comments documenting Phase 74-77 measurements
- Budget tests (`budget.test.ts` + `budget-render.test.ts`) assert against these constants
- CI bench job (78-01) starts as `continue-on-error: true` — promotion to blocking requires 3 consecutive green runs on main
- The contracts table format is locked by CONTEXT.md: Path | Measured Baseline | CI Budget | Chrome Est. | Source Constant
- Section goes after `## Key Decisions` in PROJECT.md

## Tasks

<task id="78-02-01">
Add a `## Performance Contracts` section to `.planning/PROJECT.md` after the `## Key Decisions` section. The section must include:

1. **Header paragraph** explaining:
   - All budgets enforced in CI via `npm run bench:budgets`
   - Source of truth is `src/profiling/PerfBudget.ts`
   - Thresholds are CI-relative (jsdom overhead baked in); Chrome estimates = CI budget / jsdom factor
   - Cross-reference the bench job in `.github/workflows/ci.yml`

2. **Promotion procedure** paragraph:
   - Bench job starts as soft gate (`continue-on-error: true`)
   - After 3 consecutive green runs on main, promote to blocking: flip `continue-on-error: true` to `false` in `.github/workflows/ci.yml`
   - No PR label or override mechanism — if benchmarks fail, fix the regression or update the budget with justification

3. **Render Budgets table** (Path | Measured Baseline | CI Budget | Chrome Est. | Source Constant):
   - Single axis, 20K cards: 37.8ms p99 jsdom | 128ms | ~16ms | BUDGET_RENDER_JSDOM_MS
   - Dual axis, 2500 cells (50x50): 183ms mean jsdom | 240ms | ~18ms | BUDGET_RENDER_DUAL_JSDOM_MS

4. **SQL Query Budgets table** (20K cards, p99):
   - GROUP BY folder, card_type: 24.93ms baseline | 12ms | ~1.5ms | BUDGET_QUERY_GROUP_BY_20K_MS
   - GROUP BY strftime month: 20.64ms baseline | 10ms | ~1ms | BUDGET_QUERY_STRFTIME_20K_MS
   - GROUP BY status: 1.87ms baseline | 5ms | ~0.2ms | BUDGET_QUERY_STATUS_20K_MS
   - FTS 3-word search: 1.70ms baseline | 5ms | ~0.2ms | BUDGET_QUERY_FTS_20K_MS

5. **ETL Import Throughput table** (20K cards):
   - Shared budget across all sources | 1000ms | varies | BUDGET_ETL_20K_MS

6. **Memory / Launch Baselines table** (constants defined but NOT enforced in CI):
   - Cold start: ~26ms vitest | 3000ms device target | BUDGET_LAUNCH_COLD_MS
   - Heap steady-state: ~363MB RSS vitest | 150MB device target | BUDGET_HEAP_STEADY_MB
   - Note: these are device-only metrics; vitest values are reference, not CI-enforced

All values must match PerfBudget.ts constants exactly. Read PerfBudget.ts to confirm values before writing.
</task>

## Verification

- [ ] `## Performance Contracts` section exists in PROJECT.md after `## Key Decisions`
- [ ] All 4 categories documented: render, SQL query, ETL import, memory/launch
- [ ] Table columns match locked format: Path | Measured Baseline | CI Budget | Chrome Est. | Source Constant
- [ ] All constant names match `src/profiling/PerfBudget.ts` exports exactly
- [ ] Promotion procedure is documented (3 green runs on main → flip continue-on-error)
- [ ] Memory/launch baselines noted as device-only, not CI-enforced

## must_haves

- All 4 performance budget categories documented with measured baselines and CI budgets
- Promotion procedure from soft gate to blocking gate is explicitly documented
- PerfBudget.ts is identified as the canonical source of truth
