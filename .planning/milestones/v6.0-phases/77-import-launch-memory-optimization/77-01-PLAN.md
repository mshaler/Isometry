---
phase: 77-import-launch-memory-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/etl/SQLiteWriter.ts
  - tests/profiling/etl-batch-size.test.ts
  - tests/profiling/etl-fts-timing.test.ts
autonomous: true
requirements: [IMPT-01, IMPT-02, IMPT-03]

must_haves:
  truths:
    - "ETL batch size comparison (100/500/1000) runs at 20K cards with throughput numbers logged"
    - "FTS stages (disable/rebuild/restore) are individually timed with PerfTrace spans"
    - "All 4 parser throughputs (json, markdown, csv, apple_notes) are re-measured at 20K"
  artifacts:
    - path: "src/etl/SQLiteWriter.ts"
      provides: "Constructor-injectable batchSize parameter"
      contains: "constructor(private db: Database, private batchSize"
    - path: "tests/profiling/etl-batch-size.test.ts"
      provides: "Batch size comparison benchmark"
      contains: "batchSize"
    - path: "tests/profiling/etl-fts-timing.test.ts"
      provides: "FTS stage isolation timing"
      contains: "etl:fts:"
  key_links:
    - from: "tests/profiling/etl-batch-size.test.ts"
      to: "src/etl/SQLiteWriter.ts"
      via: "new SQLiteWriter(db, batchSize)"
      pattern: "new SQLiteWriter\\(db,\\s*batchSize\\)"
    - from: "src/etl/SQLiteWriter.ts"
      to: "src/profiling/PerfTrace.ts"
      via: "startTrace/endTrace for FTS stages"
      pattern: "startTrace\\('etl:fts:"
---

<objective>
Validate ETL batch size at 20K cards and isolate FTS rebuild timing.

Purpose: Determine whether the current 100-card batch size is optimal or if 500/1000 is faster. Isolate FTS disable/rebuild/restore timing to identify whether FTS dominates import cost. Re-measure all parser throughputs against Phase 74 baselines.

Output: SQLiteWriter with injectable batch size, batch comparison test, FTS timing test, parser throughput confirmation.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-import-launch-memory-optimization/77-CONTEXT.md
@.planning/phases/77-import-launch-memory-optimization/77-RESEARCH.md

@src/etl/SQLiteWriter.ts
@src/profiling/PerfTrace.ts
@src/profiling/PerfBudget.ts
@tests/profiling/budget.test.ts
@tests/profiling/etl-smoke.bench.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/etl/SQLiteWriter.ts:
```typescript
export class SQLiteWriter {
  constructor(private db: Database) {}
  async writeCards(cards: CanonicalCard[], isBulkImport = false, onProgress?: ProgressCallback): Promise<void>
}
// Module-level constants:
const BATCH_SIZE = 100;
const BULK_THRESHOLD = 500;
```

From src/profiling/PerfTrace.ts:
```typescript
export function startTrace(name: string): void;
export function endTrace(name: string): void;
export function getTraces(name: string): PerformanceEntry[];
export function clearTraces(): void;
```

From src/database/Database.ts:
```typescript
export class Database {
  async initialize(wasmBinary?: ArrayBuffer, dbData?: ArrayBuffer): Promise<void>
  run(sql: string, params?: BindParams): void
  exec(sql: string): SqlJsStatic.QueryExecResult[]
  close(): void
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize SQLiteWriter batch size + add FTS PerfTrace spans</name>
  <files>src/etl/SQLiteWriter.ts</files>
  <behavior>
    - SQLiteWriter constructor accepts optional batchSize parameter (default 100, preserving existing behavior)
    - writeCards uses this.batchSize instead of module BATCH_SIZE constant
    - writeConnections uses this.batchSize instead of module BATCH_SIZE constant
    - FTS disable/rebuild/restore each wrapped in PerfTrace spans (etl:fts:disable, etl:fts:rebuild, etl:fts:restore)
    - Existing tests pass unchanged (default batchSize=100 preserves behavior)
  </behavior>
  <action>
    1. Modify SQLiteWriter constructor to accept optional second parameter: `constructor(private db: Database, private batchSize = 100)`. The module-level `BATCH_SIZE` constant stays as documentation but is no longer read in writeCards/writeConnections/updateCards — all three use `this.batchSize`.
    2. In writeCards() FTS optimization block, wrap `this.disableFTSTriggers()` with `startTrace('etl:fts:disable')`/`endTrace('etl:fts:disable')`, wrap `this.rebuildFTS()` with `startTrace('etl:fts:rebuild')`/`endTrace('etl:fts:rebuild')`, and wrap `this.restoreFTSTriggers()` with `startTrace('etl:fts:restore')`/`endTrace('etl:fts:restore')`.
    3. Replace all references to `BATCH_SIZE` in writeCards, writeConnections, and the updateCards loop with `this.batchSize`.
    4. Keep `BULK_THRESHOLD` as module constant (unchanged) — it controls whether FTS optimization kicks in, not batch size.
    5. Run existing tests to confirm no regressions from the constructor change.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/etl/ tests/profiling/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>SQLiteWriter accepts injectable batchSize, FTS stages have PerfTrace spans, all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create ETL batch size comparison + FTS timing + parser throughput tests</name>
  <files>tests/profiling/etl-batch-size.test.ts, tests/profiling/etl-fts-timing.test.ts</files>
  <action>
    1. Create `tests/profiling/etl-batch-size.test.ts` (IMPT-01):
       - Single shared Database instance (beforeAll init, afterAll close) per the Phase 74 convention.
       - For each batchSize in [100, 500, 1000]: instantiate `new SQLiteWriter(db, batchSize)`, generate 20K JSON cards (reuse genJSON pattern from budget.test.ts), run `DELETE FROM cards WHERE 1=1` before each run, call `writeCards(cards, true)`, measure with `performance.now()`, log throughput as `batchSize=N: Xms (Y cards/s)`.
       - Use 120_000 timeout on each test.
       - After all 3 runs, log a comparison summary table.
       - The card generator must produce CanonicalCard objects with all required fields (id, title, body, card_type, folder, status, source, source_id, created_at, modified_at). Reuse the generator pattern from budget.test.ts or etl-smoke.bench.ts.

    2. Create `tests/profiling/etl-fts-timing.test.ts` (IMPT-02):
       - Single shared Database instance.
       - Import 20K cards with `writeCards(cards, true)` (bulk import triggers FTS optimization).
       - After import, read PerfTrace entries for `etl:fts:disable`, `etl:fts:rebuild`, `etl:fts:restore` using `getTraces()`.
       - Log each FTS stage duration and the total FTS overhead as percentage of total import time.
       - Use `clearTraces()` in beforeEach to ensure clean measurements.

    3. For IMPT-03 (parser throughput re-measurement): The existing `tests/profiling/etl-smoke.bench.ts` already measures all 4 parser throughputs. No new file needed — verify existing test runs and captures 20K throughput numbers. Add a note in the batch-size test file referencing etl-smoke.bench.ts for parser baselines.

    Key constraints:
    - Use `it()` + `performance.now()` pattern (NOT vitest bench() — empty samples bug in forks pool).
    - Single shared DB per describe block (Phase 74 OOM prevention).
    - `DELETE FROM cards WHERE 1=1` between runs, never `db.close()` + `new Database()`.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/profiling/etl-batch-size.test.ts tests/profiling/etl-fts-timing.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Batch comparison runs at 100/500/1000 with throughput logged. FTS stages individually timed. Console output shows numeric comparison data for batch size selection.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/profiling/etl-batch-size.test.ts` passes and logs 3 batch-size throughput lines
- `npx vitest run tests/profiling/etl-fts-timing.test.ts` passes and logs FTS stage durations
- `npx vitest run tests/profiling/etl-smoke.bench.ts` still passes (IMPT-03 parser throughput baseline)
- `npx vitest run tests/etl/` passes (no regression from SQLiteWriter constructor change)
</verification>

<success_criteria>
- SQLiteWriter constructor accepts optional batchSize parameter
- Batch comparison test produces throughput numbers at 100/500/1000 for 20K cards
- FTS disable/rebuild/restore individually timed and logged
- All existing ETL tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/77-import-launch-memory-optimization/77-01-SUMMARY.md`
</output>
