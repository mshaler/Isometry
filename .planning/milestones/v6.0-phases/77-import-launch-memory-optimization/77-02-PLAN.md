---
phase: 77-import-launch-memory-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/Database.ts
  - src/profiling/PerfBudget.ts
  - tests/profiling/cold-start.test.ts
  - tests/profiling/heap-cycle.test.ts
  - tests/profiling/checkpoint-save-cost.test.ts
autonomous: true
requirements: [LNCH-01, MMRY-01, MMRY-02, MMRY-03]

must_haves:
  truths:
    - "Cold start is decomposed into WASM init, DB creation, and schema apply with per-stage timing"
    - "WASM heap + JS heap measured at 20K cards with peak and steady-state identified"
    - "3 import-delete-reimport cycles show no unbounded growth (< 20% RSS growth)"
    - "Checkpoint save cost (exportDatabase + base64 encode) is measured at 20K cards and debounced if >50ms"
  artifacts:
    - path: "src/database/Database.ts"
      provides: "PerfTrace markers for cold-start decomposition"
      contains: "db:wasm:init"
    - path: "tests/profiling/cold-start.test.ts"
      provides: "Cold-start decomposition timing test"
      contains: "db:wasm:init"
    - path: "tests/profiling/heap-cycle.test.ts"
      provides: "Heap measurement and import-delete-reimport cycle test"
      contains: "process.memoryUsage"
    - path: "tests/profiling/checkpoint-save-cost.test.ts"
      provides: "Checkpoint save cost measurement at 20K cards"
      contains: "exportDatabase"
    - path: "src/profiling/PerfBudget.ts"
      provides: "Updated launch and heap budget constants with measured values"
      contains: "BUDGET_LAUNCH_COLD_MS"
  key_links:
    - from: "tests/profiling/cold-start.test.ts"
      to: "src/database/Database.ts"
      via: "PerfTrace markers inside initialize()"
      pattern: "getTraces\\('db:wasm:init'\\)"
    - from: "tests/profiling/heap-cycle.test.ts"
      to: "src/database/Database.ts"
      via: "process.memoryUsage() before/after 20K import"
      pattern: "process\\.memoryUsage\\(\\)"
    - from: "tests/profiling/checkpoint-save-cost.test.ts"
      to: "src/worker/WorkerBridge.ts"
      via: "exportDatabase() timing measurement"
      pattern: "exportDatabase"
---

<objective>
Decompose cold-start timing, characterize WASM heap behavior at 20K cards, and measure checkpoint save cost.

Purpose: Identify which cold-start stage (WASM init, DB creation, schema apply) dominates startup time. Verify that import-delete-reimport cycles do not cause unbounded heap growth. Measure checkpoint save cost at 20K cards to determine if debouncing is needed. Document baseline numbers for Phase 78 CI guards.

Output: PerfTrace markers in Database.initialize(), cold-start timing test, heap cycle test, checkpoint save cost test, updated PerfBudget constants.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-import-launch-memory-optimization/77-CONTEXT.md
@.planning/phases/77-import-launch-memory-optimization/77-RESEARCH.md

@src/database/Database.ts
@src/profiling/PerfTrace.ts
@src/profiling/PerfBudget.ts
@src/native/NativeBridge.ts
@src/worker/WorkerBridge.ts
@tests/profiling/budget.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/database/Database.ts:
```typescript
export class Database {
  async initialize(wasmBinary?: ArrayBuffer, dbData?: ArrayBuffer): Promise<void>
  // Inside initialize():
  //   const SQL = await initSqlJs(sqlOptions);  // WASM init
  //   this.db = new SQL.Database(...)            // DB creation
  //   await this.applySchema()                   // Schema apply
  run(sql: string, params?: BindParams): void
  exec(sql: string): SqlJsStatic.QueryExecResult[]
  export(): Uint8Array
  close(): void
}
```

From src/profiling/PerfTrace.ts:
```typescript
export function startTrace(name: string): void;
export function endTrace(name: string): void;
export function getTraces(name: string): PerformanceEntry[];
export function clearTraces(): void;
```

From src/profiling/PerfBudget.ts:
```typescript
export const BUDGET_LAUNCH_COLD_MS = 3000; // TODO Phase 77
export const BUDGET_HEAP_STEADY_MB = 150;  // TODO Phase 77
```

From src/etl/ImportOrchestrator.ts (for heap cycle test):
```typescript
export class ImportOrchestrator {
  constructor(db: Database)
  async import(sourceType: string, data: unknown): Promise<ImportResult>
}
```

From src/native/NativeBridge.ts (for checkpoint save cost):
```typescript
export async function sendCheckpoint(bridge: WorkerBridge): Promise<void> {
  const dbBytes = await bridge.exportDatabase();   // sql.js db.export()
  const base64 = uint8ArrayToBase64(dbBytes);      // Uint8Array -> base64 string
  // Then posts to Swift via messageHandlers
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PerfTrace markers to Database.initialize() + create cold-start test</name>
  <files>src/database/Database.ts, tests/profiling/cold-start.test.ts</files>
  <action>
    1. In `src/database/Database.ts` initialize() method, add PerfTrace markers around the three distinct stages:
       - `startTrace('db:wasm:init')` before `initSqlJs()`, `endTrace('db:wasm:init')` after it returns.
       - `startTrace('db:instance:create')` before `new SQL.Database(...)`, `endTrace('db:instance:create')` after.
       - `startTrace('db:schema:apply')` before the PRAGMA + `this.applySchema()` block, `endTrace('db:schema:apply')` after.
       - Import startTrace/endTrace from `../profiling/PerfTrace` (PerfTrace is already imported in SQLiteWriter — may need adding to Database.ts if not already there).
    2. Create `tests/profiling/cold-start.test.ts` (LNCH-01):
       - Call `clearTraces()` before test.
       - Create new `Database()` and call `initialize()`.
       - Read traces via `getTraces('db:wasm:init')`, `getTraces('db:instance:create')`, `getTraces('db:schema:apply')`.
       - Log each stage duration and total. Format: `WASM init: Xms | DB create: Yms | Schema apply: Zms | Total: Tms`.
       - Close the database after measurement.
       - Use 30_000 timeout (WASM init can be slow in CI).
    3. Do NOT add vitest assertions for cold-start timing (per CONTEXT.md: "Document cold-start baseline only — no hard time budget").
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/profiling/cold-start.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Database.initialize() has 3 PerfTrace spans. Cold-start test logs per-stage timing breakdown. No timing assertions — documentation only.</done>
</task>

<task type="auto">
  <name>Task 2: Create heap cycle test + update PerfBudget constants</name>
  <files>tests/profiling/heap-cycle.test.ts, src/profiling/PerfBudget.ts</files>
  <action>
    1. Create `tests/profiling/heap-cycle.test.ts` (MMRY-01, MMRY-02):
       - Single shared Database instance (beforeAll init, afterAll close).
       - Helper function `snapshot(label: string)` that calls `process.memoryUsage()` and logs `[label] rss=XMB heapUsed=YMB heapTotal=ZMB`. Returns rss value (primary metric — captures WASM heap via RSS, not just V8 heapUsed).
       - Baseline snapshot before any imports.
       - First test (MMRY-01): Import 20K JSON cards via ImportOrchestrator, snapshot after import (peak), then delete all cards (`db.run('DELETE FROM cards WHERE 1=1')`), snapshot after delete (steady-state). Log peak and steady-state numbers. NO assertion — this is baseline documentation.
       - Second test (MMRY-02): Run 3 import-delete-reimport cycles. For each cycle: import 20K cards, wait 200ms for GC settle (`await new Promise(r => setTimeout(r, 200))`), snapshot, delete all cards, wait 200ms, snapshot. Call `clearTraces()` between cycles (prevents performance.mark accumulation — see Research pitfall 3). After 3 cycles, compute RSS growth = (cycle3_import_rss - cycle1_import_rss) / cycle1_import_rss. Assert growth < 0.20 (20%). Log growth percentage.
       - Key constraints: NEVER call `db.close()` + `new Database()` within the cycle (D-011 locked decision — WASM heap fragmentation). Use `DELETE FROM cards WHERE 1=1` not DB recreation. Use 300_000ms timeout (3 cycles of 20K imports).
       - Card generator: reuse the genJSON pattern from budget.test.ts (generate CanonicalCard array with required fields).

    2. Update `src/profiling/PerfBudget.ts`:
       - Replace the TODO comments on `BUDGET_LAUNCH_COLD_MS` and `BUDGET_HEAP_STEADY_MB` with "Phase 77: measured baseline" comments.
       - Keep the numeric values as-is for now (3000ms / 150MB) — the cold-start and heap tests will produce actual numbers. If the measured values differ significantly, update the constants to match measured data. The key constraint: these are documentation constants, not vitest assertion targets (per CONTEXT.md: "no hard time budget" and "no hard memory ceiling").
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/profiling/heap-cycle.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>Heap measurement test logs RSS at baseline, peak (20K import), and steady-state. Cycle test runs 3 rounds with < 20% growth assertion. PerfBudget constants updated with measured baseline comments.</done>
</task>

<task type="auto">
  <name>Task 3: Measure checkpoint save cost at 20K cards and debounce if needed</name>
  <files>tests/profiling/checkpoint-save-cost.test.ts</files>
  <action>
    Per CONTEXT.md locked decision: "Measure checkpoint save cost at 20K cards — if >50ms, consider debouncing."

    1. Create `tests/profiling/checkpoint-save-cost.test.ts`:
       - Single shared Database instance (beforeAll init, afterAll close).
       - Import 20K JSON cards via ImportOrchestrator (same genJSON pattern as heap-cycle.test.ts).
       - Measure the checkpoint save path cost by timing the two expensive operations separately:
         a. `db.export()` — the sql.js database export to Uint8Array. Time with `performance.now()`.
         b. Base64 encoding of the exported Uint8Array. Implement `uint8ArrayToBase64()` inline (or import from NativeBridge if exported) — it converts Uint8Array to a base64 string. Time with `performance.now()`.
         c. Log total checkpoint save cost: `Checkpoint save at 20K cards: export=Xms, base64=Yms, total=Zms`.
       - Run the measurement 3 times and log each run (captures variance).
       - Assert: log whether total exceeds 50ms threshold. Format: `Checkpoint save cost: Xms (WITHIN budget)` or `Checkpoint save cost: Xms (EXCEEDS 50ms budget — debouncing recommended)`.
       - Use 120_000ms timeout.

    2. If the measured cost exceeds 50ms (per Claude's discretion from CONTEXT.md):
       - Add debouncing to the checkpoint save call in `src/native/NativeBridge.ts`. Wrap the mutation-triggered autosave with a debounce (e.g., 100ms trailing debounce on `sendCheckpoint` calls) so rapid mutations don't trigger redundant expensive exports.
       - If the cost is within budget (<= 50ms), skip debouncing and document the measurement result in a console.log note in the test.

    Key constraints:
    - This test exercises the SAME database instance already loaded with 20K cards — it measures the real-world checkpoint cost.
    - The `db.export()` call is the sql.js method that serializes the entire database to a Uint8Array — this is the primary cost center.
    - Do NOT mock the export — measure the actual sql.js serialization cost.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/profiling/checkpoint-save-cost.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Checkpoint save cost at 20K cards is measured and logged. If cost exceeds 50ms, debouncing is added to sendCheckpoint in NativeBridge.ts. If within budget, measurement is documented with no code change needed.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/profiling/cold-start.test.ts` passes and logs WASM init / DB create / schema apply breakdown
- `npx vitest run tests/profiling/heap-cycle.test.ts` passes with < 20% RSS growth across 3 cycles
- `npx vitest run tests/profiling/checkpoint-save-cost.test.ts` passes and logs checkpoint save cost at 20K cards
- `npx vitest run tests/profiling/` passes (no regression)
- PerfBudget.ts has updated comments reflecting measured values
</verification>

<success_criteria>
- Database.initialize() has PerfTrace spans for all 3 cold-start stages
- Cold-start test produces per-stage timing breakdown in console
- Heap cycle test measures RSS at 20K cards with peak/steady-state documented
- 3 import-delete-reimport cycles pass < 20% growth threshold
- PerfBudget constants updated with measured baseline data
- Checkpoint save cost measured at 20K cards with result logged
- If checkpoint save exceeds 50ms, debouncing is applied to sendCheckpoint
</success_criteria>

<output>
After completion, create `.planning/phases/77-import-launch-memory-optimization/77-02-SUMMARY.md`
</output>
