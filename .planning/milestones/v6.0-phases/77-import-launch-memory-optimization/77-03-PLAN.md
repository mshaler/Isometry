---
phase: 77-import-launch-memory-optimization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - native/Isometry/Isometry/IsometryApp.swift
  - native/Isometry/Isometry/Views/ContentView.swift
  - native/Isometry/Isometry/Bridge/BridgeManager.swift
  - native/Isometry/IsometryTests/BridgeManagerTests.swift
autonomous: true
requirements: [LNCH-02, MMRY-03]

must_haves:
  truths:
    - "WKWebView setup begins during app launch (before ContentView.onAppear) to hide WASM compile behind splash"
    - "WKWebView content process termination resets isJSReady and shows recovery overlay"
    - "Silent crash detection (checkForSilentCrash) guard on isJSReady + nil webView is covered by test"
  artifacts:
    - path: "native/Isometry/Isometry/IsometryApp.swift"
      provides: "Earlier WKWebView warm-up trigger"
      contains: "setupWebView"
    - path: "native/Isometry/IsometryTests/BridgeManagerTests.swift"
      provides: "WKWebView termination recovery integration test"
      contains: "webViewWebContentProcessDidTerminate"
  key_links:
    - from: "native/Isometry/Isometry/IsometryApp.swift"
      to: "native/Isometry/Isometry/Bridge/BridgeManager.swift"
      via: "setupWebView() called earlier in lifecycle"
      pattern: "setupWebView"
    - from: "native/Isometry/IsometryTests/BridgeManagerTests.swift"
      to: "native/Isometry/Isometry/Bridge/BridgeManager.swift"
      via: "webViewWebContentProcessDidTerminate test"
      pattern: "webViewWebContentProcessDidTerminate"
---

<objective>
Implement WKWebView warm-up during app launch and verify crash recovery with integration tests.

Purpose: Hide WASM compile latency behind the native launch animation by starting WKWebView setup earlier. Verify that WKWebView content process termination (memory pressure) produces correct recovery behavior (isJSReady reset, overlay shown, reload triggered).

Output: WKWebView warm-up wiring in IsometryApp, crash recovery integration test in BridgeManagerTests.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-import-launch-memory-optimization/77-CONTEXT.md
@.planning/phases/77-import-launch-memory-optimization/77-RESEARCH.md

@native/Isometry/Isometry/IsometryApp.swift
@native/Isometry/Isometry/Views/ContentView.swift
@native/Isometry/Isometry/Bridge/BridgeManager.swift
@native/Isometry/IsometryTests/BridgeManagerTests.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move WKWebView setup earlier in app lifecycle for warm-up</name>
  <files>native/Isometry/Isometry/IsometryApp.swift, native/Isometry/Isometry/Views/ContentView.swift</files>
  <action>
    Per CONTEXT.md locked decision: "Preload WKWebView + WASM compile in background during app launch animation to hide init behind native splash."

    Per RESEARCH.md recommended approach (lower-risk alternative): Keep WKWebView creation in `setupWebView()` but call it from `IsometryApp.body` via `.task {}` modifier before `ContentView` appears, rather than from ContentView's `.onAppear`. This preserves the existing setup logic while starting WASM compilation earlier.

    1. Read `IsometryApp.swift` and `ContentView.swift` to understand the current flow:
       - Currently: `ContentView` calls `setupWebView()` in `.onAppear` — this creates the WKWebView, configures it, and loads the URL.
       - Target: Trigger `setupWebView()` from `IsometryApp` body using `.task {}` modifier on the `WindowGroup`, so it fires during the launch animation before ContentView's `.onAppear`.

    2. In `IsometryApp.swift`:
       - Add a `.task {}` modifier on the `WindowGroup` (or the root view inside it) that calls `bridgeManager.setupWebView()` if not already set up.
       - This ensures WASM streaming compile begins during the native splash screen.
       - Guard against double-setup: only call `setupWebView()` if the webView has not already been created (check if `bridgeManager.webView == nil` or add an `isSetUp` flag).

    3. In `ContentView.swift`:
       - Remove or guard the `.onAppear { setupWebView() }` call so it does not double-initialize.
       - If `setupWebView()` was already called from IsometryApp, ContentView's `.onAppear` should skip it.

    4. Verify the setup order is preserved: user scripts and bridge handler are added BEFORE `loadURL` is called. The existing `setupWebView()` method already handles this correctly — moving the call site earlier does not change the internal order.

    Key constraint from Research: `userContentController.addUserScript()` CAN be called after WKWebView creation. The existing setup order (add scripts, configure, load URL) is preserved because `setupWebView()` itself is moved, not decomposed.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry/native/Isometry && xcodebuild build -scheme Isometry -destination 'platform=macOS' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>WKWebView setup triggers from IsometryApp.task{} before ContentView.onAppear. Build succeeds with no warnings from the warm-up change.</done>
</task>

<task type="auto">
  <name>Task 2: Add WKWebView termination recovery integration test</name>
  <files>native/Isometry/IsometryTests/BridgeManagerTests.swift</files>
  <action>
    Per CONTEXT.md locked decisions:
    - "Write integration test simulating WKWebView termination -> verify checkpoint data survives recovery cycle"
    - "Silent crash detection (checkForSilentCrash) already handled with isJSReady + nil webView guard — include in integration test coverage"

    1. Read `BridgeManager.swift` to understand the termination handler:
       - `webViewWebContentProcessDidTerminate(_:)` is a nonisolated WKNavigationDelegate method.
       - It dispatches to `@MainActor` via `Task { @MainActor in ... }`.
       - Expected behavior: sets `isJSReady = false`, calls `showRecoveryOverlay()` which sets `showingRecoveryOverlay = true`, calls `webView.reload()`.

    2. Read existing `BridgeManagerTests.swift` to understand test patterns and what's already covered.

    3. Add test `webContentTermination_resetsReadyStateAndShowsOverlay`:
       - Create a `BridgeManager` instance.
       - Create a minimal `WKWebView` with default configuration.
       - Call `bridgeManager.configure(webView: webView)` (or whatever the setup method is) to wire the WKNavigationDelegate.
       - Simulate termination: call `bridgeManager.webViewWebContentProcessDidTerminate(webView)`.
       - Since the termination handler dispatches to MainActor via Task, wait briefly: `try? await Task.sleep(nanoseconds: 100_000_000)` (0.1s).
       - Assert: `bridgeManager.isJSReady == false`.
       - Assert: `bridgeManager.showingRecoveryOverlay == true`.

    4. Add test `silentCrashDetection_guardsNilWebView` (if not already covered):
       - Verify that `checkForSilentCrash()` handles the case where `isJSReady` is false and `webView` weak reference is nil.
       - This tests the guard condition from the 2026-03-07 fix.

    Key constraint: Tests must use `@MainActor` context or await Task settling since the termination handler uses `Task { @MainActor in }`. Use Swift Testing `@Test` macro (not XCTest) per the existing test file pattern.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry/native/Isometry && xcodebuild test -scheme Isometry -destination 'platform=macOS' -only-testing:IsometryTests/BridgeManagerTests -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>Termination recovery test passes: isJSReady resets to false, recovery overlay shows. Silent crash detection guard covered by test.</done>
</task>

</tasks>

<verification>
- `xcodebuild build -scheme Isometry -destination 'platform=macOS'` succeeds with no new warnings
- `xcodebuild test -scheme Isometry -destination 'platform=macOS' -only-testing:IsometryTests/BridgeManagerTests` passes
- WKWebView warm-up fires before ContentView.onAppear (verified by log timing or build success)
- Termination test verifies isJSReady=false and showingRecoveryOverlay=true
</verification>

<success_criteria>
- WKWebView setup begins during app launch animation (before ContentView.onAppear)
- No double-initialization of WKWebView
- Termination recovery test proves checkpoint-safe state after content process death
- Silent crash detection guard covered
- Xcode build + test both pass
</success_criteria>

<output>
After completion, create `.planning/phases/77-import-launch-memory-optimization/77-03-SUMMARY.md`
</output>
