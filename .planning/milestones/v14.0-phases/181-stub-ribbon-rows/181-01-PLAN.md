---
phase: 181-stub-ribbon-rows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/superwidget.css
  - src/styles/stub-ribbon.css
  - src/ui/icons.ts
  - src/ui/section-defs.ts
  - src/superwidget/SuperWidget.ts
autonomous: true
requirements: [STOR-01, STOR-02, STOR-03, DSET-01, DSET-02, DSET-03]

must_haves:
  truths:
    - "A Stories ribbon row is visible below the navigation ribbon with 3 labeled placeholder items"
    - "A Datasets ribbon row is visible below the Stories ribbon with 3 labeled placeholder items"
    - "All Stories and Datasets items appear greyed out with not-allowed cursor"
    - "Clicking any stub item produces no action or navigation change"
  artifacts:
    - path: "src/styles/superwidget.css"
      provides: "CSS Grid with stories and datasets rows between ribbon and canvas"
      contains: "stories  sidecar"
    - path: "src/styles/stub-ribbon.css"
      provides: "Disabled stub ribbon styling"
      contains: "stub-ribbon"
    - path: "src/ui/section-defs.ts"
      provides: "STORIES_STUB_DEFS and DATASETS_STUB_DEFS item arrays"
      exports: ["STORIES_STUB_DEFS", "DATASETS_STUB_DEFS"]
    - path: "src/ui/icons.ts"
      provides: "Lucide SVG strings for file-plus, presentation, send, upload, download, hard-drive"
      contains: "file-plus"
    - path: "src/superwidget/SuperWidget.ts"
      provides: "Stories and Datasets slot elements created and appended to grid"
      contains: "stories-ribbon"
  key_links:
    - from: "src/superwidget/SuperWidget.ts"
      to: "src/ui/section-defs.ts"
      via: "import STORIES_STUB_DEFS, DATASETS_STUB_DEFS"
      pattern: "STORIES_STUB_DEFS|DATASETS_STUB_DEFS"
    - from: "src/superwidget/SuperWidget.ts"
      to: "src/ui/icons.ts"
      via: "iconSvg() for stub item icons"
      pattern: "iconSvg"
    - from: "src/styles/superwidget.css"
      to: "src/styles/stub-ribbon.css"
      via: "grid areas stories/datasets consumed by stub-ribbon styling"
      pattern: "stories|datasets"
---

<objective>
Add two disabled stub ribbon rows (Stories and Datasets) below the navigation ribbon in the SuperWidget CSS grid, with placeholder Lucide icons and labels rendered from data definitions.

Purpose: Communicate future Stories and Datasets capabilities to users without enabling interaction.
Output: Two visible-but-disabled ribbon rows in the layout, a new stub-ribbon.css file, icon and definition additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/181-stub-ribbon-rows/181-CONTEXT.md
@.planning/phases/181-stub-ribbon-rows/181-UI-SPEC.md

@src/styles/superwidget.css
@src/styles/dock-nav.css
@src/ui/section-defs.ts
@src/ui/icons.ts
@src/superwidget/SuperWidget.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stub definitions, icons, CSS grid rows, and stub-ribbon.css</name>
  <files>src/ui/section-defs.ts, src/ui/icons.ts, src/styles/superwidget.css, src/styles/stub-ribbon.css</files>
  <read_first>
    src/ui/section-defs.ts
    src/ui/icons.ts
    src/styles/superwidget.css
    src/styles/dock-nav.css
    .planning/phases/181-stub-ribbon-rows/181-UI-SPEC.md
  </read_first>
  <action>
**1. section-defs.ts — Add stub ribbon definitions (per D-05, UI-SPEC Placeholder Item Definitions)**

Export two new arrays after DOCK_DEFS, using the existing `SidebarItemDef` interface:

```typescript
export const STORIES_STUB_DEFS: readonly SidebarItemDef[] = [
  { key: 'story-new', label: 'New Story', icon: 'file-plus' },
  { key: 'story-play', label: 'Present', icon: 'presentation' },
  { key: 'story-share', label: 'Publish', icon: 'send' },
];

export const DATASETS_STUB_DEFS: readonly SidebarItemDef[] = [
  { key: 'dataset-import', label: 'Import', icon: 'upload' },
  { key: 'dataset-export', label: 'Export', icon: 'download' },
  { key: 'dataset-browse', label: 'Browse', icon: 'hard-drive' },
];
```

**2. icons.ts — Add 6 missing Lucide icon SVG strings**

Add these entries to the ICONS record (standard Lucide viewBox="0 0 24 24", stroke="currentColor", stroke-width="1.5", fill="none", aria-hidden="true" focusable="false"):

- `'file-plus'`: Lucide file-plus icon (file shape with + symbol)
- `'presentation'`: Lucide presentation icon (screen with pointer)
- `'send'`: Lucide send icon (paper plane shape)
- `'upload'`: Lucide upload icon (upward arrow with line)
- `'download'`: Lucide download icon (downward arrow with line)
- `'hard-drive'`: Lucide hard-drive icon (rectangular drive with indicator)

Follow exact pattern of existing entries — no width/height attributes, same stroke attributes.

**3. superwidget.css — Expand CSS grid to include stories and datasets rows (per D-01, D-02)**

Change the grid-template-rows from:
```css
grid-template-rows: auto auto auto 1fr auto;
```
to:
```css
grid-template-rows: auto auto auto auto auto 1fr auto;
```

Change grid-template-areas from current 5-row layout to:
```css
grid-template-areas:
  "header   sidecar"
  "tabs     sidecar"
  "ribbon   sidecar"
  "stories  sidecar"
  "datasets sidecar"
  "canvas   sidecar"
  "status   sidecar";
```

Add slot rules for stories-ribbon and datasets-ribbon immediately after the existing `[data-slot="ribbon"]` rule block (copy the same pattern):

```css
[data-component="superwidget"] [data-slot="stories-ribbon"] {
  grid-area: stories;
  height: var(--sw-ribbon-height);
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-subtle);
  overflow: hidden;
  position: relative;
  z-index: 2;
  cursor: not-allowed;
  opacity: 0.5;
}

[data-component="superwidget"] [data-slot="datasets-ribbon"] {
  grid-area: datasets;
  height: var(--sw-ribbon-height);
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border-subtle);
  overflow: hidden;
  position: relative;
  z-index: 2;
  cursor: not-allowed;
  opacity: 0.5;
}
```

**4. stub-ribbon.css — Create new stylesheet for stub ribbon item styling**

Create `src/styles/stub-ribbon.css`. The classes mirror dock-nav.css naming but use `.stub-ribbon__*` prefix. All items are permanently disabled. Use the project design tokens directly (not duplicated values).

```css
/* Stub ribbon rows — Phase 181
 * Disabled placeholder ribbons for Stories and Datasets.
 * Follows dock-nav.css class naming pattern with .stub-ribbon__ prefix.
 */

.stub-ribbon {
  display: flex;
  align-items: center;
  height: 100%;
  padding: 0 var(--space-xl);
  gap: var(--space-md);
}

.stub-ribbon__list {
  display: flex;
  align-items: center;
  list-style: none;
  margin: 0;
  padding: 0;
  gap: var(--space-md);
}

.stub-ribbon__section {
  display: flex;
  flex-direction: column;
  gap: 0;
}

.stub-ribbon__section-header {
  font-size: var(--text-xs);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  height: 16px;
  line-height: 1.0;
  padding-top: var(--space-md);
  user-select: none;
}

.stub-ribbon__section-items {
  display: flex;
  align-items: center;
  list-style: none;
  margin: 0;
  padding: 0;
  gap: var(--space-xs);
}

.stub-ribbon__item {
  display: flex;
  align-items: center;
  gap: var(--space-sm);
  height: 32px;
  padding: 0 var(--space-md);
  border: none;
  background: transparent;
  color: var(--text-muted);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  font-weight: 400;
  line-height: 1.2;
  cursor: not-allowed;
  pointer-events: none;
  user-select: none;
}

.stub-ribbon__item-icon {
  display: flex;
  align-items: center;
  justify-content: center;
}

.stub-ribbon__item-icon svg {
  width: 18px;
  height: 18px;
}

.stub-ribbon__item-label {
  white-space: nowrap;
}
```

Ensure this CSS file is imported in the application entry point (check how dock-nav.css is imported and follow the same pattern).
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/section-defs.ts contains `export const STORIES_STUB_DEFS`
    - src/ui/section-defs.ts contains `export const DATASETS_STUB_DEFS`
    - src/ui/section-defs.ts STORIES_STUB_DEFS has keys: story-new, story-play, story-share
    - src/ui/section-defs.ts DATASETS_STUB_DEFS has keys: dataset-import, dataset-export, dataset-browse
    - src/ui/icons.ts contains entries for 'file-plus', 'presentation', 'send', 'upload', 'download', 'hard-drive'
    - src/styles/superwidget.css grid-template-areas contains "stories  sidecar" and "datasets sidecar"
    - src/styles/superwidget.css grid-template-rows has 7 values: auto auto auto auto auto 1fr auto
    - src/styles/superwidget.css contains [data-slot="stories-ribbon"] with opacity: 0.5 and cursor: not-allowed
    - src/styles/superwidget.css contains [data-slot="datasets-ribbon"] with opacity: 0.5 and cursor: not-allowed
    - src/styles/stub-ribbon.css exists with .stub-ribbon__item containing pointer-events: none
    - TypeScript compilation passes with zero errors
  </acceptance_criteria>
  <done>All definitions, icons, grid layout, and stub CSS exist. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create and mount stub ribbon slot elements in SuperWidget.ts</name>
  <files>src/superwidget/SuperWidget.ts</files>
  <read_first>
    src/superwidget/SuperWidget.ts
    src/ui/section-defs.ts
    src/ui/icons.ts
    .planning/phases/181-stub-ribbon-rows/181-UI-SPEC.md
  </read_first>
  <action>
**1. Add imports at top of SuperWidget.ts:**

```typescript
import { STORIES_STUB_DEFS, DATASETS_STUB_DEFS } from '../ui/section-defs';
import { iconSvg } from '../ui/icons';
```

(If `iconSvg` is already imported, just add STORIES_STUB_DEFS and DATASETS_STUB_DEFS to existing section-defs import.)

**2. Add private fields for the two new slot elements:**

Near the existing `private _ribbonEl: HTMLElement;` declaration, add:
```typescript
private _storiesRibbonEl: HTMLElement;
private _datasetsRibbonEl: HTMLElement;
```

**3. In the constructor, after the ribbon slot creation (after `this._ribbonEl.dataset['slot'] = 'ribbon';`), create both stub ribbon slots:**

```typescript
// Stories stub ribbon slot (Phase 181 — STOR-01, STOR-02, STOR-03)
this._storiesRibbonEl = document.createElement('div');
this._storiesRibbonEl.dataset['slot'] = 'stories-ribbon';
this._renderStubRibbon(this._storiesRibbonEl, 'STORIES', STORIES_STUB_DEFS);

// Datasets stub ribbon slot (Phase 181 — DSET-01, DSET-02, DSET-03)
this._datasetsRibbonEl = document.createElement('div');
this._datasetsRibbonEl.dataset['slot'] = 'datasets-ribbon';
this._renderStubRibbon(this._datasetsRibbonEl, 'DATASETS', DATASETS_STUB_DEFS);
```

**4. Insert stub elements into the DOM append sequence.**

Currently the append order is: ribbonEl, headerEl, tabsEl, canvasEl, statusEl, sidecarEl. Insert the two new elements after ribbonEl:

```typescript
this._root.appendChild(this._ribbonEl);
this._root.appendChild(this._storiesRibbonEl);   // NEW
this._root.appendChild(this._datasetsRibbonEl);   // NEW
this._root.appendChild(this._headerEl);
this._root.appendChild(this._tabsEl);
this._root.appendChild(this._canvasEl);
this._root.appendChild(this._statusEl);
this._root.appendChild(this._sidecarEl);
```

Note: DOM order does not matter for CSS Grid layout — grid-area assignments control visual placement. But placing them near ribbonEl keeps source order logical.

**5. Add the `_renderStubRibbon` private method** (place it near other private render methods):

```typescript
/**
 * Render a disabled stub ribbon row from item definitions.
 * Purely presentational — no event handlers. (Phase 181)
 */
private _renderStubRibbon(
  slotEl: HTMLElement,
  sectionLabel: string,
  items: readonly { key: string; label: string; icon: string }[],
): void {
  const container = document.createElement('div');
  container.className = 'stub-ribbon';

  const list = document.createElement('ul');
  list.className = 'stub-ribbon__list';

  const section = document.createElement('li');
  section.className = 'stub-ribbon__section';

  const header = document.createElement('span');
  header.className = 'stub-ribbon__section-header';
  header.textContent = sectionLabel;
  section.appendChild(header);

  const itemsList = document.createElement('ul');
  itemsList.className = 'stub-ribbon__section-items';

  for (const def of items) {
    const li = document.createElement('li');
    const btn = document.createElement('button');
    btn.className = 'stub-ribbon__item';
    btn.disabled = true;

    const iconSpan = document.createElement('span');
    iconSpan.className = 'stub-ribbon__item-icon';
    iconSpan.innerHTML = iconSvg(def.icon, 18);

    const labelSpan = document.createElement('span');
    labelSpan.className = 'stub-ribbon__item-label';
    labelSpan.textContent = def.label;

    btn.appendChild(iconSpan);
    btn.appendChild(labelSpan);
    li.appendChild(btn);
    itemsList.appendChild(li);
  }

  section.appendChild(itemsList);
  list.appendChild(section);
  container.appendChild(list);
  slotEl.appendChild(container);
}
```

**6. Add public accessors** (near existing `get ribbonEl()` accessor):

```typescript
get storiesRibbonEl(): HTMLElement { return this._storiesRibbonEl; }
get datasetsRibbonEl(): HTMLElement { return this._datasetsRibbonEl; }
```

Do NOT add any click handlers, event delegation, or state management to the stub rows.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/superwidget/SuperWidget.ts contains `private _storiesRibbonEl: HTMLElement`
    - src/superwidget/SuperWidget.ts contains `private _datasetsRibbonEl: HTMLElement`
    - src/superwidget/SuperWidget.ts contains `dataset['slot'] = 'stories-ribbon'`
    - src/superwidget/SuperWidget.ts contains `dataset['slot'] = 'datasets-ribbon'`
    - src/superwidget/SuperWidget.ts contains `_renderStubRibbon` method
    - src/superwidget/SuperWidget.ts contains `btn.disabled = true` inside _renderStubRibbon
    - src/superwidget/SuperWidget.ts imports STORIES_STUB_DEFS and DATASETS_STUB_DEFS from section-defs
    - src/superwidget/SuperWidget.ts contains `get storiesRibbonEl()` and `get datasetsRibbonEl()` accessors
    - No click handlers or addEventListener calls exist on stub ribbon elements
    - TypeScript compiles with zero errors
    - All existing tests pass
  </acceptance_criteria>
  <done>Stories and Datasets stub ribbon slots are created in SuperWidget, rendered from data definitions with disabled buttons, appended to grid. No event wiring. TypeScript compiles and all tests pass.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npx vitest run` — all existing tests pass (no regressions)
3. Visual: open the app in browser — two greyed-out ribbon rows appear below the navigation ribbon with section headers "STORIES" and "DATASETS", each showing 3 icon+label items
4. Click test: clicking any stub item does nothing (pointer-events: none + disabled buttons)
5. Keyboard test: Tab key does not focus into stub ribbon items (native disabled removes from tab order)
</verification>

<success_criteria>
- Stories ribbon row visible below nav ribbon with "New Story", "Present", "Publish" items (STOR-01, STOR-02)
- Datasets ribbon row visible below Stories with "Import", "Export", "Browse" items (DSET-01, DSET-02)
- All stub items greyed out at 0.5 opacity with not-allowed cursor (STOR-03, DSET-03)
- Zero click handlers on stub rows — pointer-events: none on items
- CSS grid has 7 rows: header, tabs, ribbon, stories, datasets, canvas, status
- All existing tests pass, TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/181-stub-ribbon-rows/181-01-SUMMARY.md`
</output>
