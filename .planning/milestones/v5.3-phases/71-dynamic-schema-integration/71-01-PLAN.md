---
phase: 71-dynamic-schema-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/types.ts
  - src/providers/latch.ts
  - src/providers/allowlist.ts
  - tests/providers/latch.test.ts
  - tests/providers/allowlist.test.ts
autonomous: true
requirements: [DYNM-01, DYNM-02, DYNM-03, DYNM-04]

must_haves:
  truths:
    - "AxisField and FilterField types accept any string while preserving autocomplete for known fields"
    - "LATCH_FAMILIES constant has a dynamic alternative getLatchFamily() that reads from SchemaProvider when wired"
    - "toLetter() and toFullName() map between protocol LatchFamily and UI LatchFamily"
    - "allowlist frozen sets remain as fallback but SchemaProvider delegation already works from Phase 70"
  artifacts:
    - path: "src/providers/types.ts"
      provides: "Widened AxisField and FilterField types with (string & {}) pattern"
      contains: "KnownAxisField"
    - path: "src/providers/latch.ts"
      provides: "LatchFamily mapping bridge + dynamic getLatchFamily() + setLatchSchemaProvider()"
      exports: ["toLetter", "toFullName", "getLatchFamily", "setLatchSchemaProvider", "LATCH_FAMILIES_FALLBACK"]
    - path: "tests/providers/latch.test.ts"
      provides: "Tests for toLetter/toFullName/getLatchFamily with and without SchemaProvider"
  key_links:
    - from: "src/providers/latch.ts"
      to: "src/providers/SchemaProvider.ts"
      via: "setLatchSchemaProvider() module-level injection"
      pattern: "setLatchSchemaProvider"
    - from: "src/providers/latch.ts"
      to: "src/worker/protocol.ts"
      via: "import LatchFamily as SchemaLatchFamily"
      pattern: "toLetter|toFullName"
---

<objective>
Widen core TypeScript types and build the LatchFamily mapping bridge so all downstream plans can consume SchemaProvider data through UI-compatible types.

Purpose: AxisField/FilterField must accept dynamic column names for custom-schema databases. The dual LatchFamily type system (protocol full names vs UI single letters) needs an explicit bridge. These are foundational changes that every other plan in this phase depends on.

Output: Widened types in types.ts, mapping functions in latch.ts, dynamic getLatchFamily() with SchemaProvider delegation.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-dynamic-schema-integration/71-RESEARCH.md

@src/providers/types.ts
@src/providers/latch.ts
@src/providers/allowlist.ts
@src/providers/SchemaProvider.ts
@src/worker/protocol.ts

<interfaces>
<!-- SchemaProvider API (from Phase 70) -->
From src/providers/SchemaProvider.ts:
```typescript
class SchemaProvider {
  get initialized(): boolean;
  getColumns(table: 'cards' | 'connections'): readonly ColumnInfo[];
  isValidColumn(name: string, table?: 'cards' | 'connections'): boolean;
  getFilterableColumns(): readonly ColumnInfo[];
  getAxisColumns(): readonly ColumnInfo[];
  getNumericColumns(): readonly ColumnInfo[];
  getFieldsByFamily(family: LatchFamily): readonly ColumnInfo[];
  getLatchFamilies(): Map<LatchFamily, string[]>;
  subscribe(callback: () => void): () => void;
}
```

From src/worker/protocol.ts:
```typescript
interface ColumnInfo {
  name: string;
  type: string;
  notnull: boolean;
  dflt_value: string | null;
  pk: boolean;
  isNumeric: boolean;
  latchFamily: LatchFamily;  // 'Location' | 'Alphabet' | 'Time' | 'Category' | 'Hierarchy'
}
type LatchFamily = 'Location' | 'Alphabet' | 'Time' | 'Category' | 'Hierarchy';
```

From src/providers/latch.ts (current):
```typescript
type LatchFamily = 'L' | 'A' | 'T' | 'C' | 'H';
const LATCH_FAMILIES: Readonly<Record<AxisField, LatchFamily>>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen AxisField/FilterField types + LatchFamily mapping bridge</name>
  <files>src/providers/types.ts, src/providers/latch.ts, tests/providers/latch.test.ts</files>
  <behavior>
    - Test: toLetter('Time') returns 'T', toLetter('Category') returns 'C', etc. for all 5 families
    - Test: toFullName('T') returns 'Time', toFullName('C') returns 'Category', etc. for all 5 families
    - Test: getLatchFamily('created_at') returns 'T' when SchemaProvider is NOT wired (fallback)
    - Test: getLatchFamily('custom_field') returns 'A' when SchemaProvider is NOT wired (default fallback)
    - Test: getLatchFamily('custom_field') returns correct letter when SchemaProvider IS wired with mock data
    - Test: setLatchSchemaProvider(null) resets to fallback behavior
  </behavior>
  <action>
In src/providers/types.ts:
1. Rename existing AxisField union to KnownAxisField (keep all 9 literal values).
2. Export `type AxisField = KnownAxisField | (string & {})` -- preserves autocomplete via the branded string trick.
3. Rename existing FilterField union to KnownFilterField (keep all 15 literal values).
4. Export `type FilterField = KnownFilterField | (string & {})`.
5. Keep FilterOperator unchanged (operators are fixed, not schema-derived).
6. In AxisMapping interface, field type is already AxisField -- no change needed.
7. In SuperDensityState, displayField is already `AxisField | undefined` -- no change needed.
8. In Filter interface, field is already FilterField -- no change needed.

In src/providers/latch.ts:
1. Import `type { LatchFamily as SchemaLatchFamily }` from `../worker/protocol`.
2. Add FAMILY_TO_LETTER mapping: `Record<SchemaLatchFamily, LatchFamily>` with Location->L, Alphabet->A, Time->T, Category->C, Hierarchy->H.
3. Add LETTER_TO_FAMILY mapping: `Record<LatchFamily, SchemaLatchFamily>` (reverse).
4. Export `toLetter(family: SchemaLatchFamily): LatchFamily` function.
5. Export `toFullName(letter: LatchFamily): SchemaLatchFamily` function.
6. Rename LATCH_FAMILIES to LATCH_FAMILIES_FALLBACK with type `Readonly<Record<string, LatchFamily>>` (widen from AxisField to string since AxisField is now open).
7. Keep the old LATCH_FAMILIES export as a re-export of LATCH_FAMILIES_FALLBACK for backward compat during this phase (will be removed in DYNM-13 audit).
8. Add module-level `_schemaProvider: SchemaProvider | null = null` with `setLatchSchemaProvider()` setter.
9. Export `getLatchFamily(field: string): LatchFamily` function that:
   - If _schemaProvider?.initialized, looks up field in _schemaProvider.getColumns('cards'), returns toLetter(col.latchFamily) if found.
   - Falls back to LATCH_FAMILIES_FALLBACK[field] ?? 'A' (Alphabet as default for unknown fields).

In tests/providers/latch.test.ts:
- Write tests per behavior spec above. Use a minimal mock SchemaProvider (only needs initialized, getColumns methods).

IMPORTANT: Do NOT change the type of LATCH_FAMILIES_FALLBACK values -- they stay as single-letter LatchFamily. The mapping bridge converts between protocol's full-name LatchFamily and UI's single-letter LatchFamily.
  </action>
  <verify>
    <automated>npx vitest run tests/providers/latch.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>toLetter/toFullName map all 5 families bidirectionally. getLatchFamily returns correct family with and without SchemaProvider. KnownAxisField/KnownFilterField preserve literal unions while AxisField/FilterField accept any string. All latch tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire setLatchSchemaProvider in main.ts + verify TypeScript compiles</name>
  <files>src/main.ts, tests/providers/allowlist.test.ts</files>
  <action>
In src/main.ts:
1. Import `setLatchSchemaProvider` from `./providers/latch` (or from `./providers` barrel if re-exported).
2. After the existing `setSchemaProvider(schemaProvider)` call (line ~118), add `setLatchSchemaProvider(schemaProvider)` to wire latch.ts delegation.
3. Verify no other changes needed -- allowlist.ts delegation was already wired in Phase 70.

In tests/providers/allowlist.test.ts:
1. Verify existing tests still pass with the widened types. The `(string & {})` pattern should not break any existing test assertions since known literals are still valid AxisField/FilterField values.
2. If any tests have explicit type annotations that conflict with the widened type, update them.

Run full TypeScript compilation to catch any cascading type errors from the widening:
- `npx tsc --noEmit` must pass with zero errors.
- If there are type errors from switch statements that exhaustively match AxisField, add default cases.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20 && npx vitest run tests/providers/allowlist.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>main.ts wires both setSchemaProvider and setLatchSchemaProvider. TypeScript compiles with zero errors. All allowlist tests pass. DYNM-01 through DYNM-04 requirements satisfied.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors (type widening does not break existing code).
2. `npx vitest run tests/providers/latch.test.ts` -- all new mapping/delegation tests pass.
3. `npx vitest run tests/providers/allowlist.test.ts` -- existing allowlist tests unchanged.
4. `npx vitest run` -- full test suite passes (no regressions from type widening).
</verification>

<success_criteria>
- AxisField = KnownAxisField | (string & {}) -- accepts dynamic fields, preserves autocomplete
- FilterField = KnownFilterField | (string & {}) -- same pattern
- toLetter/toFullName bridge between protocol and UI LatchFamily types
- getLatchFamily() delegates to SchemaProvider when wired, falls back to hardcoded map
- setLatchSchemaProvider() called in main.ts after bridge ready
- Zero TypeScript compilation errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/71-dynamic-schema-integration/71-01-SUMMARY.md`
</output>
