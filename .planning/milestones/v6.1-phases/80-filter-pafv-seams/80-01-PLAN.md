---
phase: 80-filter-pafv-seams
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/seams/filter/filter-sql.test.ts
autonomous: true
requirements: [FSQL-01, FSQL-02, FSQL-03, FSQL-04, FSQL-05]

must_haves:
  truths:
    - "eq/neq/in filters return only matching rows from real sql.js"
    - "FTS search returns only cards matching the search term"
    - "FTS+field compound filter narrows results to intersection"
    - "Range filters (>=, <=) and axis filters (IN) return correct subsets"
    - "Allowlist validation throws 'SQL safety violation' for invalid field names before SQL executes"
    - "Soft-deleted rows are absent from ALL filter query results"
  artifacts:
    - path: "tests/seams/filter/filter-sql.test.ts"
      provides: "Filter-to-SQL seam tests covering all filter types against real sql.js"
  key_links:
    - from: "tests/seams/filter/filter-sql.test.ts"
      to: "src/providers/FilterProvider.ts"
      via: "compile() returns { where, params } executed against real db"
      pattern: "filter\\.compile\\(\\)"
    - from: "tests/seams/filter/filter-sql.test.ts"
      to: "src/providers/allowlist.ts"
      via: "validateFilterField throws on invalid field"
      pattern: "SQL safety violation"
    - from: "tests/seams/filter/filter-sql.test.ts"
      to: "tests/harness/realDb.ts"
      via: "realDb() factory for in-memory sql.js"
      pattern: "import.*realDb"
    - from: "tests/seams/filter/filter-sql.test.ts"
      to: "tests/harness/seedCards.ts"
      via: "seedCards() for test data"
      pattern: "import.*seedCards"
---

<objective>
Write seam tests that verify every filter type (eq, neq, in, range, axis, FTS, compound) executes correctly against a real sql.js database, that allowlist validation blocks invalid fields, and that soft-deleted rows never appear in results.

Purpose: These tests prove that FilterProvider.compile() produces SQL WHERE fragments that actually select the correct rows when run against a real database -- not a mock. This is the critical seam between provider state and SQL execution.

Output: 1 test file in `tests/seams/filter/` with ~15-20 test cases covering all 5 FSQL requirements.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-test-infrastructure/79-CONTEXT.md

Source files to read BEFORE writing tests:
@src/providers/FilterProvider.ts (compile(), addFilter(), setSearchQuery(), setAxisFilter(), setRangeFilter())
@src/providers/allowlist.ts (validateFilterField, setSchemaProvider)
@src/providers/types.ts (Filter, FilterField, FilterOperator, CompiledFilter)
@tests/harness/realDb.ts (realDb factory)
@tests/harness/seedCards.ts (seedCards factory, SeedCard interface)
@tests/harness/makeProviders.ts (makeProviders factory — needed for SchemaProvider wiring)
@tests/harness/smoke.test.ts (flushCoordinatorCycle pattern, test lifecycle)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write filter-to-SQL seam tests for all filter types, allowlist, soft-delete, and FTS</name>
  <files>tests/seams/filter/filter-sql.test.ts</files>
  <action>
Delete the `.gitkeep` in `tests/seams/filter/` and create `tests/seams/filter/filter-sql.test.ts`.

Read `src/providers/FilterProvider.ts` to confirm the compile() signature and all filter APIs (addFilter, setSearchQuery, setAxisFilter, setRangeFilter). Read `src/providers/allowlist.ts` to confirm the error message format ("SQL safety violation:..."). Read `src/providers/types.ts` to confirm Filter, FilterField, FilterOperator types.

Test lifecycle (same pattern as smoke.test.ts):
- `beforeEach`: `db = await realDb()`, `providers = makeProviders(db)` — makeProviders wires SchemaProvider into allowlist
- `afterEach`: `providers.coordinator.destroy()`, `db.close()`
- Extract `filter` from `providers` for convenience

Seed data strategy: Create a standard seed set in `beforeEach` (after db/providers init) with ~6 cards that have distinct values for name, folder, status, priority, card_type, and created_at. Include 1 soft-deleted card (deleted_at set). This seed set supports all filter assertions without per-test re-seeding.

Example seed data:
```
seedCards(db, [
  { name: 'Alpha',   folder: 'Work',     status: 'active',   priority: 1, card_type: 'note' },
  { name: 'Beta',    folder: 'Work',     status: 'done',     priority: 2, card_type: 'task' },
  { name: 'Gamma',   folder: 'Personal', status: 'active',   priority: 3, card_type: 'note' },
  { name: 'Delta',   folder: 'Personal', status: 'archived', priority: 4, card_type: 'event' },
  { name: 'Epsilon', folder: 'Work',     status: 'active',   priority: 5, card_type: 'task' },
  { name: 'Zeta',    folder: 'Archive',  status: 'done',     priority: 0, card_type: 'note', deleted_at: '2026-01-01T00:00:00.000Z' },
]);
```

Helper function to run compiled filter against db:
```typescript
function queryWithFilter(db: Database, filter: FilterProvider): string[] {
  const { where, params } = filter.compile();
  const rows = db.exec(`SELECT name FROM cards WHERE ${where} ORDER BY name ASC`, params as BindParams);
  return (rows[0]?.values ?? []).map(r => r[0] as string);
}
```

**describe('FSQL-01: eq/neq/in filters'):**
1. `eq` — addFilter({ field: 'folder', operator: 'eq', value: 'Work' }) → returns Alpha, Beta, Epsilon (3 cards, Zeta excluded by deleted_at)
2. `neq` — addFilter({ field: 'status', operator: 'neq', value: 'active' }) → returns Beta, Delta
3. `in` — addFilter({ field: 'card_type', operator: 'in', value: ['note', 'event'] }) → returns Alpha, Delta, Gamma
4. `contains` — addFilter({ field: 'name', operator: 'contains', value: 'a' }) → returns Alpha, Beta, Delta, Epsilon, Gamma (case-insensitive LIKE in SQLite)
5. `isNull` — addFilter({ field: 'folder', operator: 'isNull', value: null }) → returns [] (all live cards have folders)

**describe('FSQL-02: FTS search and FTS+field compound'):**
6. FTS search only — setSearchQuery('Alpha') → returns ['Alpha']
7. FTS + field compound — setSearchQuery('a') AND addFilter({ field: 'folder', operator: 'eq', value: 'Work' }) → returns only cards matching BOTH conditions
8. FTS no results — setSearchQuery('nonexistent_xyz') → returns []

**describe('FSQL-03: range and axis filters'):**
9. Range filter — setRangeFilter('priority', 2, 4) → returns Beta, Gamma, Delta (priority 2, 3, 4)
10. Range filter min only — setRangeFilter('priority', 3, null) → returns Gamma, Delta, Epsilon
11. Axis filter — setAxisFilter('folder', ['Work', 'Archive']) → returns Alpha, Beta, Epsilon (Zeta excluded by deleted_at)
12. Axis + range compound — setAxisFilter('folder', ['Work']) AND setRangeFilter('priority', 1, 2) → returns Alpha, Beta

**describe('FSQL-04: allowlist validation'):**
13. addFilter with invalid field throws — `filter.addFilter({ field: 'DROP TABLE cards' as any, operator: 'eq', value: 'x' })` → throws /SQL safety violation/
14. setAxisFilter with invalid field throws — `filter.setAxisFilter('bobby_tables', ['x'])` → throws /SQL safety violation/
15. setRangeFilter with invalid field throws — `filter.setRangeFilter('1=1; --', 0, 10)` → throws /SQL safety violation/

**describe('FSQL-05: soft-deleted rows excluded'):**
16. Base compile (no filters) excludes soft-deleted — queryWithFilter returns 5 names, none is 'Zeta'
17. eq filter matching deleted card's folder returns live cards only — addFilter({ field: 'folder', operator: 'eq', value: 'Archive' }) → returns [] (only Zeta is in Archive, and it's deleted)
18. FTS search for deleted card name returns nothing — setSearchQuery('Zeta') → returns [] (Zeta is soft-deleted; compile() adds `deleted_at IS NULL` which excludes it). NOTE: The FTS index DOES contain Zeta's data (FTS trigger fires on INSERT regardless of deleted_at). The exclusion happens because compile() wraps the FTS subquery inside `WHERE deleted_at IS NULL AND rowid IN (...)`. Verify this by checking `compile().where` contains both `deleted_at IS NULL` and `rowid IN`.

Import sql.js BindParams type for the helper: `import type { BindParams } from 'sql.js'`.

IMPORTANT: The FTS search test for deleted cards may need careful attention. The FTS5 trigger `cards_fts_ai` fires on INSERT regardless of `deleted_at`. So `cards_fts` DOES contain Zeta. But `compile().where` starts with `deleted_at IS NULL`, so the full query `SELECT ... FROM cards WHERE deleted_at IS NULL AND rowid IN (SELECT rowid FROM cards_fts WHERE ...)` correctly excludes Zeta. If a test fails because FTS returns Zeta, the fix is to ensure the query uses the full `where` clause, not just the FTS subquery.
  </action>
  <verify>
    <automated>npx vitest --run tests/seams/filter/filter-sql.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>All filter seam tests pass green. eq/neq/in/contains/isNull filters return correct subsets. FTS search and FTS+field compounds return correct results. Range and axis filters return correct subsets. Invalid fields throw SQL safety violation before SQL execution. Soft-deleted rows never appear in any filter result.</done>
</task>

</tasks>

<verification>
1. `npx vitest --run tests/seams/filter/` — all filter seam tests pass green
2. `npm run test:seams` — includes filter tests, exits clean
3. `npx tsc --noEmit` — zero type errors in new test file
</verification>

<success_criteria>
- eq/neq/in/contains/isNull filters each return only matching live rows from real sql.js
- FTS search returns correct results; FTS+field compound narrows to intersection
- Range filters (min/max) and axis filters (IN) return correct subsets
- Invalid field names throw "SQL safety violation" before any SQL executes
- Soft-deleted rows (deleted_at set) never appear in any query result
- All tests run against real sql.js (no mocks for database layer)
</success_criteria>

<output>
After completion, create `.planning/phases/80-filter-pafv-seams/80-01-SUMMARY.md`
</output>
