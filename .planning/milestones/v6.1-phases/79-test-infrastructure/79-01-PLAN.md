---
phase: 79-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/harness/realDb.ts
  - tests/harness/seedCards.ts
  - tests/harness/seedConnections.ts
  - tests/harness/makeProviders.ts
  - tests/harness/smoke.test.ts
  - package.json
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03, SCRP-01]

must_haves:
  truths:
    - "realDb() returns a working in-memory sql.js database with production schema"
    - "makeProviders(db) returns a fully-wired provider stack where filter changes fire coordinator notifications"
    - "seedCards() inserts cards with FTS5 auto-populated via trigger"
    - "npm run test:harness and npm run test:seams scripts exist and execute"
  artifacts:
    - path: "tests/harness/realDb.ts"
      provides: "In-memory sql.js database factory with production schema"
      exports: ["realDb"]
    - path: "tests/harness/makeProviders.ts"
      provides: "Wired provider stack factory with SchemaProvider injection"
      exports: ["makeProviders", "ProviderStack"]
    - path: "tests/harness/seedCards.ts"
      provides: "Minimal card seeding helper with auto-FTS via trigger"
      exports: ["seedCards", "SeedCard"]
    - path: "tests/harness/seedConnections.ts"
      provides: "Connection seeding helper"
      exports: ["seedConnections", "SeedConnection"]
    - path: "tests/harness/smoke.test.ts"
      provides: "Smoke tests proving both factories work"
    - path: "package.json"
      provides: "test:harness and test:seams npm scripts"
  key_links:
    - from: "tests/harness/makeProviders.ts"
      to: "src/providers/allowlist.ts"
      via: "setSchemaProvider() module-level singleton"
      pattern: "setSchemaProvider\\(schema\\)"
    - from: "tests/harness/makeProviders.ts"
      to: "src/providers/SchemaProvider.ts"
      via: "initialize() with PRAGMA-derived ColumnInfo"
      pattern: "schema\\.initialize"
    - from: "tests/harness/realDb.ts"
      to: "src/database/Database.ts"
      via: "new Database() + await db.initialize()"
      pattern: "new Database.*initialize"
    - from: "tests/harness/smoke.test.ts"
      to: "tests/harness/realDb.ts"
      via: "import realDb"
      pattern: "import.*realDb"
---

<objective>
Create the test harness infrastructure for v6.1: database factory, provider stack factory, seed helpers, smoke tests, and npm scripts.

Purpose: Every subsequent seam test phase (80-83) can create a real sql.js database and a fully-wired provider stack in one line each. Without this infrastructure, each phase would redundantly solve WASM init, schema creation, provider wiring, and SchemaProvider injection.

Output: 5 new files in `tests/harness/`, 2 new npm scripts in `package.json`, empty `tests/seams/` directory tree.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-test-infrastructure/79-CONTEXT.md
@.planning/phases/79-test-infrastructure/79-RESEARCH.md

Source files to read BEFORE writing any code (Session 0 reading list from CONTEXT.md):
@src/providers/FilterProvider.ts
@src/providers/PAFVProvider.ts
@src/providers/SuperDensityProvider.ts
@src/providers/StateCoordinator.ts
@src/providers/SchemaProvider.ts
@src/providers/SelectionProvider.ts
@src/providers/allowlist.ts
@src/worker/protocol.ts (for ColumnInfo type definition)
@src/database/Database.ts
@src/database/schema.sql (for table names, FTS trigger names, v6.0 indexes)
@tests/integration/seam-coordinator-batch.test.ts (for flushCoordinatorCycle pattern)
@tests/setup/wasm-init.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create realDb, seedCards, and seedConnections factories</name>
  <files>tests/harness/realDb.ts, tests/harness/seedCards.ts, tests/harness/seedConnections.ts</files>
  <action>
Read Session 0 files first: `src/database/Database.ts` (constructor, initialize, close), `src/database/schema.sql` (table columns for cards/connections, FTS trigger names, v6.0 indexes), `src/worker/protocol.ts` (ColumnInfo type).

Create `tests/harness/realDb.ts`:
- Export async function `realDb()` that returns `Promise<Database>`
- Implementation: `new Database()` then `await db.initialize()` — Database.ts handles WASM path via `SQL_WASM_PATH` env set by globalSetup
- No seed data, no parameters — bare schema per INFR-01

Create `tests/harness/seedCards.ts`:
- Export `SeedCard` interface with all optional fields except none required: `id?`, `card_type?`, `name?`, `folder?`, `status?`, `priority?`, `content?`, `tags?`, `deleted_at?`, `created_at?`, `modified_at?`
- Export `seedCards(db: Database, cards: SeedCard[]): string[]` — returns inserted IDs
- Auto-generate: `id` via `crypto.randomUUID()`, `created_at`/`modified_at` via `new Date().toISOString()`, `card_type` defaults to `'note'`, `name` defaults to `'Unnamed'`, `priority` defaults to `0`, everything else defaults to `null`
- Use `db.run('BEGIN')` / `db.run('COMMIT')` batch pattern
- Do NOT manually insert into `cards_fts` — the `cards_fts_ai` trigger handles it automatically
- Match column list exactly from schema.sql INSERT — verify all columns before writing

Create `tests/harness/seedConnections.ts`:
- Export `SeedConnection` interface: `id?`, `from_card_id` (required), `to_card_id` (required), `via_card_id?`, `relation_type?`, `created_at?`
- Export `seedConnections(db: Database, connections: SeedConnection[]): string[]` — returns inserted IDs
- Same batch pattern as seedCards, same auto-generation for id/created_at

Also create empty directory structure for seam tests:
- `tests/seams/filter/` (for Phase 80)
- `tests/seams/coordinator/` (for Phase 81)
- `tests/seams/ui/` (for Phase 82)
- `tests/seams/etl/` (for Phase 83)
- Place a `.gitkeep` in each empty directory
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/harness/realDb.ts tests/harness/seedCards.ts tests/harness/seedConnections.ts 2>&1 | head -20</automated>
  </verify>
  <done>Three factory files exist, typecheck clean, empty seams directory tree created with .gitkeep files</done>
</task>

<task type="auto">
  <name>Task 2: Create makeProviders factory with SchemaProvider wiring</name>
  <files>tests/harness/makeProviders.ts</files>
  <action>
Read Session 0 files first (if not already read in Task 1): `src/providers/SchemaProvider.ts` (constructor, initialize signature), `src/providers/FilterProvider.ts` (confirm NO setSchemaProvider instance method), `src/providers/PAFVProvider.ts` (confirm setSchemaProvider instance method), `src/providers/SuperDensityProvider.ts` (confirm setSchemaProvider instance method), `src/providers/SelectionProvider.ts` (constructor), `src/providers/StateCoordinator.ts` (registerProvider signature), `src/providers/allowlist.ts` (setSchemaProvider module-level function), `src/worker/protocol.ts` (ColumnInfo type).

Create `tests/harness/makeProviders.ts`:

1. Export `ProviderStack` interface: `{ filter: FilterProvider, pafv: PAFVProvider, density: SuperDensityProvider, selection: SelectionProvider, coordinator: StateCoordinator, schema: SchemaProvider }`

2. Export `makeProviders(db: Database): ProviderStack` (synchronous — all constructors are zero-arg, no async needed)

3. Implementation order (CRITICAL — init order matters):
   a. Create SchemaProvider via `new SchemaProvider()`
   b. Build ColumnInfo arrays from PRAGMA: `db.exec('PRAGMA table_info(cards)')` and `db.exec('PRAGMA table_info(connections)')` — map rows to ColumnInfo objects with `name`, `type`, `isNumeric`, `latchFamily` fields
   c. Call `schema.initialize({ cards: cardCols, connections: connCols })`
   d. Call `setSchemaProvider(schema)` from `src/providers/allowlist` (module-level singleton — this is the ONLY way to wire schema into FilterProvider validation)
   e. Create providers: `new FilterProvider()`, `new PAFVProvider()`, `new SuperDensityProvider()`, `new SelectionProvider()`
   f. Call `pafv.setSchemaProvider(schema)` and `density.setSchemaProvider(schema)` (instance method setters — v5.3 pattern)
   g. Do NOT call `filter.setSchemaProvider(schema)` — FilterProvider has no such method
   h. Create coordinator: `new StateCoordinator()`
   i. Register providers: `coordinator.registerProvider('filter', filter)`, same for pafv, density, selection

4. Internal helper `buildColumnInfo(db, tableName)`: reads PRAGMA table_info, maps to ColumnInfo[]. Read the actual ColumnInfo type from `src/worker/protocol.ts` to get the exact shape. The latchFamily classification should replicate SchemaProvider's heuristic — read `SchemaProvider.ts` classify method to get the exact field-to-family mapping.

CRITICAL GUARD: Verify against actual source that:
- FilterProvider does NOT have setSchemaProvider (research says it doesn't — confirm)
- PAFVProvider and SuperDensityProvider DO have setSchemaProvider (research says they do — confirm)
- SchemaProvider.initialize() takes `{ cards: ColumnInfo[], connections: ColumnInfo[] }` shape (confirm from source)
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/harness/makeProviders.ts 2>&1 | head -20</automated>
  </verify>
  <done>makeProviders.ts typechecks clean, exports ProviderStack and makeProviders, correctly wires SchemaProvider via both module singleton and instance setters</done>
</task>

<task type="auto">
  <name>Task 3: Write smoke tests and add npm scripts</name>
  <files>tests/harness/smoke.test.ts, package.json</files>
  <action>
Read `tests/integration/seam-coordinator-batch.test.ts` for the exact `flushCoordinatorCycle()` pattern.

Create `tests/harness/smoke.test.ts` with two describe blocks:

**describe('realDb() smoke'):**
1. INSERT/SELECT round-trip — insert a card via `seedCards(db, [{ name: 'Test Card' }])`, SELECT it back, verify name matches
2. FTS5 round-trip — insert a card with name 'Quantum Physics', search via `SELECT * FROM cards_fts WHERE cards_fts MATCH 'Quantum'`, verify result found
3. All tables exist — `SELECT name FROM sqlite_master WHERE type='table' ORDER BY name`, verify cards, connections, cards_fts, ui_state all present
4. Performance indexes exist — `SELECT name FROM sqlite_master WHERE type='index' AND name LIKE 'idx_%'`, verify the 6 covering/expression indexes from v6.0 exist (read schema.sql to get exact index names)

**describe('makeProviders() smoke'):**
5. All providers present — destructure returned object, verify all 6 keys (filter, pafv, density, selection, coordinator, schema) are defined and instances of correct classes
6. SchemaProvider initialized — verify `schema.initialized === true`
7. Filter -> coordinator notify — use `vi.useFakeTimers()`, subscribe to coordinator, call `filter.setFilter(...)` (use a valid filter), flush with `flushCoordinatorCycle()`, assert callback called once
8. Destroy cleanup — call `coordinator.destroy()`, mutate a provider, flush, assert no callback fires

Test lifecycle:
- `beforeEach`: `vi.useFakeTimers()`, `db = await realDb()`, `providers = makeProviders(db)`
- `afterEach`: `providers.coordinator.destroy()`, `db.close()`, `vi.useRealTimers()`

Update `package.json` scripts section — add two new entries:
- `"test:harness": "vitest --run tests/harness"`
- `"test:seams": "vitest --run tests/seams"`
Keep all existing scripts unchanged. Place new scripts after `"test:coverage"` for logical grouping.
  </action>
  <verify>
    <automated>npm run test:harness 2>&1 | tail -20</automated>
  </verify>
  <done>All smoke tests pass green via `npm run test:harness`. package.json has test:harness and test:seams scripts. INSERT/SELECT round-trip, FTS5 round-trip, all-tables-exist, indexes-exist, all-providers-present, schema-initialized, filter-coordinator-notify, and destroy-cleanup all pass.</done>
</task>

</tasks>

<verification>
1. `npm run test:harness` — all smoke tests pass green
2. `npm run test:seams` — exits cleanly with 0 test files (no seam tests yet)
3. `npm run test` — all existing tests plus new harness tests pass
4. `npx tsc --noEmit` — zero type errors across all new files
</verification>

<success_criteria>
- realDb() creates a database where INSERT/SELECT round-trips work and FTS5 MATCH queries return results
- makeProviders(db) returns all 6 providers, SchemaProvider is initialized, filter changes fire coordinator notifications
- seedCards() inserts cards with FTS5 auto-populated (no manual FTS insert)
- `npm run test:harness` runs smoke tests; `npm run test:seams` targets the seams directory
- All 8 smoke tests pass green
- Zero TypeScript errors in new files
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-infrastructure/79-01-SUMMARY.md`
</output>
