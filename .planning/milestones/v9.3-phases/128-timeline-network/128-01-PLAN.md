---
phase: 128-timeline-network
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/TimelineView.ts
  - tests/views/TimelineView.test.ts
autonomous: true
requirements: [TMLN-01, TMLN-02, TMLN-03]

must_haves:
  truths:
    - "TimelineView renders cards with due_at dates as positioned g.card elements on the SVG timeline"
    - "When all cards lack due_at, TimelineView shows a contextual empty panel with clock icon, 'No scheduled cards' heading, and descriptive text"
    - "Swimlane grouping by status/field produces distinct horizontal rows with sub-row stacking for overlapping cards"
    - "A dashed today-line marker appears at the current date position on the time axis"
    - "Swimlane rows have background rects and label text styled per design tokens"
  artifacts:
    - path: "src/views/TimelineView.ts"
      provides: "TimelineView with contextual empty state, today-line, swimlane-bg"
      contains: "No scheduled cards"
    - path: "tests/views/TimelineView.test.ts"
      provides: "Test coverage for empty state, today-line, swimlane rendering"
      contains: "No scheduled cards"
  key_links:
    - from: "src/views/TimelineView.ts"
      to: "src/styles/views.css"
      via: "view-empty CSS classes"
      pattern: "view-empty-panel"
    - from: "src/views/ViewManager.ts"
      to: "src/views/TimelineView.ts"
      via: "currentView.render(cards)"
      pattern: "render\\(cards\\)"
---

<objective>
Fix TimelineView rendering: add contextual empty state when all cards lack due_at dates (TMLN-02), add today-line marker, add swimlane background rects, and verify temporal positioning and swimlane grouping work correctly (TMLN-01, TMLN-03).

Purpose: TimelineView currently silently clears its SVG when no cards have due_at — users see a blank area with no explanation. The view also lacks a today-line reference marker and swimlane background rects that the UI-SPEC requires.
Output: TimelineView with contextual empty state, today-line, styled swimlanes, and updated tests.
</objective>

<execution_context>
@/Users/mshaler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mshaler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-timeline-network/128-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/views/types.ts:
```typescript
export interface CardDatum {
  id: string;
  name: string;
  folder: string | null;
  status: string;
  card_type: string;
  created_at: string;
  modified_at: string;
  priority: number;
  sort_order: number;
  due_at: string | null;
  body_text: string | null;
  source: string | null;
}

export interface IView {
  mount(container: HTMLElement): void;
  render(cards: CardDatum[]): void | Promise<void>;
  destroy(): void;
}
```

From src/views/TimelineView.ts existing constants:
```typescript
const LABEL_COL_WIDTH = 120;
const AXIS_HEIGHT = 40;
const SWIMLANE_HEIGHT = 60;
const CARD_WIDTH = 80;
const CARD_HEIGHT = CARD_DIMENSIONS.height; // 48px
const SWIMLANE_PADDING = 8;
```

Existing CSS empty state classes from src/styles/views.css:
- `.view-empty` — flex centering wrapper
- `.view-empty-panel` — centered content card with gap: --space-sm
- `.view-empty-icon` — 32px font-size icon
- `.view-empty-heading` — --text-xl, weight 600, --text-primary
- `.view-empty-description` — --text-md, --text-secondary
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TimelineView contextual empty state, today-line, and swimlane-bg</name>
  <files>src/views/TimelineView.ts, tests/views/TimelineView.test.ts</files>
  <read_first>
    - src/views/TimelineView.ts (full file — 433 lines, current render logic)
    - tests/views/TimelineView.test.ts (full file — 363 lines, existing test patterns)
    - src/styles/views.css (lines 110-170 — .view-empty* CSS classes)
    - .planning/phases/128-timeline-network/128-UI-SPEC.md (states/interactions section)
  </read_first>
  <behavior>
    - Test: when render() called with cards that ALL have due_at=null, container shows .view-empty with .view-empty-panel containing icon, heading "No scheduled cards", and description "Add a due date to any card to see it on the timeline."
    - Test: when render() called with mix of due_at and null cards, SVG renders only due_at cards (no empty state shown)
    - Test: when render() called with due_at cards, SVG contains a line.timeline-today element
    - Test: each swimlane group has a rect.swimlane-bg element
    - Test: swimlane labels use text class .swimlane-label with fill var(--text-secondary)
    - Test: empty state is removed when subsequent render() provides cards with due_at values
  </behavior>
  <action>
Modify `src/views/TimelineView.ts`:

1. **Contextual empty state (TMLN-02):** In the `render()` method, when `timeCards.length === 0` (all cards lack due_at), instead of just clearing swimlanes and returning, create and show a contextual empty panel:
   - BEFORE clearing swimlanes, remove any previous `.view-empty` element from `this.container`
   - Hide the SVG element (`this.svg.style('display', 'none')`)
   - Create DOM structure: `div.view-empty > div.view-empty-panel > [span.view-empty-icon "🕐", h3.view-empty-heading "No scheduled cards", p.view-empty-description "Add a due date to any card to see it on the timeline."]`
   - Append to `this.container`
   - Return early

2. **Clear empty state on valid render:** At the top of `render()`, before any logic, remove any existing `.view-empty` element from `this.container` and restore SVG display (`this.svg.style('display', null)`).

3. **Today-line (UI-SPEC SVG structure):** After rendering swimlanes (end of render method), add a `line.timeline-today` element:
   - Only render if today falls within or near the xScale domain (within padded extent)
   - Position: `x1 = x2 = xScale(new Date())`, `y1 = 0`, `y2 = totalHeight - AXIS_HEIGHT`
   - Stroke: `var(--accent)`, stroke-width: 1, stroke-dasharray: "4 2"
   - Add SVG `<title>Today</title>` child for tooltip
   - Use D3 data join with single-element array `[new Date()]` keyed by constant so it updates on re-render

4. **Swimlane background rects:** In the swimlane rendering section, add a `rect.swimlane-bg` to each swimlane group (before the label text):
   - Width: full chart width (use the computed `chartWidth` variable)
   - Height: `SWIMLANE_HEIGHT * d.numSubRows`
   - Fill: `var(--bg-secondary)`
   - x: 0 (relative to swimlane group which is already translated past label column)

5. **Swimlane label typography fix:** Change the swimlane label font-size from hardcoded `'12px'` to `'var(--text-sm)'` (11px per design tokens) and keep fill as `var(--text-secondary)`.

6. **Update tests** in `tests/views/TimelineView.test.ts`:
   - Add test: "shows contextual empty state when all cards have null due_at" — render cards with `due_at: null`, assert container has `.view-empty-heading` with textContent "No scheduled cards"
   - Add test: "removes empty state when valid cards rendered" — render null cards, then render cards with due_at, assert no `.view-empty` element exists
   - Add test: "renders today-line when cards have dates" — render dated cards, assert `line.timeline-today` exists in SVG
   - Add test: "renders swimlane-bg rect for each group" — render cards with different statuses, assert `rect.swimlane-bg` count matches group count
   - Add test: "swimlane label uses design token font size" — assert swimlane-label text has font-size "var(--text-sm)"
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx vitest run tests/views/TimelineView.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/views/TimelineView.ts contains the string "No scheduled cards"
    - src/views/TimelineView.ts contains the string "Add a due date to any card to see it on the timeline."
    - src/views/TimelineView.ts contains "view-empty-panel"
    - src/views/TimelineView.ts contains "timeline-today"
    - src/views/TimelineView.ts contains "swimlane-bg"
    - src/views/TimelineView.ts contains "var(--text-sm)" (swimlane label font size)
    - src/views/TimelineView.ts contains "var(--accent)" (today-line stroke)
    - src/views/TimelineView.ts contains "stroke-dasharray" with value "4 2"
    - tests/views/TimelineView.test.ts contains "No scheduled cards"
    - tests/views/TimelineView.test.ts contains "timeline-today"
    - tests/views/TimelineView.test.ts contains "swimlane-bg"
    - All TimelineView tests pass (exit code 0)
  </acceptance_criteria>
  <done>TimelineView renders contextual empty state for no-due_at cards, today-line marker on time axis, swimlane background rects, and design-token typography. All new and existing tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/views/TimelineView.test.ts` — all tests pass
2. `npx tsc --noEmit` — no TypeScript errors
3. `grep -n "No scheduled cards" src/views/TimelineView.ts` — empty state copy present
4. `grep -n "timeline-today" src/views/TimelineView.ts` — today-line rendered
5. `grep -n "swimlane-bg" src/views/TimelineView.ts` — swimlane backgrounds rendered
</verification>

<success_criteria>
- TimelineView renders cards with due_at dates on SVG timeline (TMLN-01)
- TimelineView shows contextual empty panel when all cards lack due_at (TMLN-02)
- Swimlane grouping works with sub-row overlap handling (TMLN-03)
- Today-line dashed marker appears at current date
- All tests pass, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/128-timeline-network/128-01-SUMMARY.md`
</output>
