---
phase: 135-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presets/LayoutPresetManager.ts
  - src/presets/builtInPresets.ts
  - src/presets/presetCommands.ts
autonomous: false
requirements: [UATX-02]

must_haves:
  truths:
    - "Applying each of the 4 built-in presets sets panel visibility to match the preset definition"
    - "Cycling through all 4 presets and back to the original state leaves the app in the starting configuration"
    - "Preset apply works from both a fresh default state and a manually customized state"
    - "Undo after preset apply restores the previous panel layout"
  artifacts:
    - path: "src/presets/builtInPresets.ts"
      provides: "4 built-in preset definitions with panel visibility"
    - path: "src/presets/LayoutPresetManager.ts"
      provides: "Preset apply/save/delete lifecycle"
  key_links:
    - from: "LayoutPresetManager.apply"
      to: "WorkbenchShell"
      via: "restoreSectionStates callback"
      pattern: "restoreSectionStates"
    - from: "presetCommands"
      to: "CommandPalette"
      via: "command registration"
      pattern: "registerCommand"
    - from: "LayoutPresetManager.apply"
      to: "MutationManager"
      via: "CallbackMutation undo registration"
      pattern: "CallbackMutation"
---

<objective>
Verify all 4 built-in layout presets apply correctly, round-trip cleanly, and undo works. Test from both fresh default state and manually customized state. Fix any defects found.

Purpose: UATX-02 requires cycling through all 4 presets and back to the original configuration with no state leakage.
Output: Preset UAT verified, any fixes committed, UAT log documenting results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-uat/135-CONTEXT.md

Source files under test:
@src/presets/builtInPresets.ts
@src/presets/LayoutPresetManager.ts
@src/presets/presetCommands.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preset switching UAT — cycle all 4 presets, verify round-trip fidelity</name>
  <files>
    src/presets/LayoutPresetManager.ts,
    src/presets/builtInPresets.ts,
    src/presets/presetCommands.ts
  </files>
  <read_first>
    src/presets/builtInPresets.ts,
    src/presets/LayoutPresetManager.ts,
    src/presets/presetCommands.ts,
    .planning/phases/135-uat/135-CONTEXT.md
  </read_first>
  <action>
Execute preset switching UAT across all 4 built-in presets per D-08, D-09.

**The 4 built-in presets and their expected panel states** (true = collapsed/hidden, false = expanded/visible):

Panel storageKeys: notebook, properties, projection, latch, calc, algorithm.

1. **Data Integration**: all 6 panels collapsed (notebook:true, properties:true, projection:true, latch:true, calc:true, algorithm:true) — clean data-focused view
2. **Writing**: notebook + properties visible, rest collapsed (notebook:false, properties:false, projection:true, latch:true, calc:true, algorithm:true)
3. **LATCH Analytics**: properties + projection + latch visible, rest collapsed (notebook:true, properties:false, projection:false, latch:false, calc:true, algorithm:true)
4. **GRAPH Synthetics**: projection + algorithm visible, rest collapsed (notebook:true, properties:true, projection:false, latch:true, calc:true, algorithm:false)

**Test A — Fresh default state (per D-09a):**
1. Start with app default panel layout (no customization)
2. Record initial panel state (which panels are expanded/collapsed) using ui_state key snapshot or visual inspection (per D-08)
3. Apply "Data Integration" preset via command palette
4. Verify: all 6 panels collapsed per definition above
5. Apply "Writing" preset
6. Verify: notebook + properties expanded, others collapsed
7. Apply "LATCH Analytics" preset
8. Verify: properties + projection + latch expanded, others collapsed
9. Apply "GRAPH Synthetics" preset
10. Verify: projection + algorithm expanded, others collapsed
11. Return to initial state (undo 4 times or re-apply original layout)
12. Verify: panel state matches step 2 snapshot

**Test B — Customized starting state (per D-09b):**
1. Manually customize: expand notebook + calc + algorithm, collapse others
2. Record this state
3. Cycle through all 4 presets (same sequence as Test A)
4. Return to customized state via undo
5. Verify: panel state matches step 2 snapshot

**Undo verification:**
- After each preset apply, verify undo (Cmd+Z) restores previous layout
- This tests the CallbackMutation integration with MutationManager

**Fix workflow:** Same as Plan 01 — inline fix mode, switch to batch at 10+ findings.

Write results to `.planning/phases/135-uat/135-02-UAT-LOG.md` with entries for:
- Each preset apply: preset_name, expected_panels, actual_panels, match (yes/no)
- Round-trip: initial_state_restored (yes/no)
- Undo: undo_restores_previous (yes/no)
- Test A pass/fail, Test B pass/fail
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/135-uat/135-02-UAT-LOG.md exists
    - Log contains entries for all 4 presets in both Test A (fresh) and Test B (customized)
    - Every preset_name entry has expected_panels and actual_panels columns that match
    - Round-trip entries show initial_state_restored = yes for both Test A and Test B
    - Undo entries show undo_restores_previous = yes
    - Zero functional defects remain (per D-10)
    - If any source files were modified, `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0
  </acceptance_criteria>
  <done>All 4 built-in presets apply correctly from both fresh and customized states. Round-trip fidelity confirmed. Undo works for every apply. Zero functional defects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies preset switching UAT results</name>
  <files>.planning/phases/135-uat/135-02-UAT-LOG.md</files>
  <action>
Present UAT log to user for review. User performs a quick manual verification:
1. Open command palette, apply "LATCH Analytics" preset
   - Verify: Properties, Projection, and LATCH explorer panels are visible; Notebook, Calc, Algorithm are collapsed
2. Apply "Data Integration" preset
   - Verify: all explorer panels are collapsed (clean data view)
3. Press Cmd+Z to undo — should restore LATCH Analytics layout
4. If any fixes were made during the UAT, user confirms they look correct
  </action>
  <verify>
    <automated>test -f .planning/phases/135-uat/135-02-UAT-LOG.md && echo "UAT log exists"</automated>
  </verify>
  <done>User has reviewed the preset UAT log and manually verified at least one preset cycle with undo, confirming all 4 presets pass.</done>
</task>

</tasks>

<verification>
- All 4 built-in presets apply with correct panel visibility
- Cycling all 4 and returning leaves app in original state
- Undo restores previous layout after each apply
- Both fresh-default and customized starting states tested
- Zero functional defects (D-10)
- TypeScript compiles cleanly, all tests pass
</verification>

<success_criteria>
Preset UAT log exists with all 4 presets passing from both starting states. Round-trip and undo verified. User spot-checks and approves.
</success_criteria>

<output>
After completion, create `.planning/phases/135-uat/135-02-SUMMARY.md`
</output>
