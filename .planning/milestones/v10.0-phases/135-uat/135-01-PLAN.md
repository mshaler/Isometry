---
phase: 135-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/ViewDefaultsRegistry.ts
  - src/providers/PAFVProvider.ts
  - src/views/ViewManager.ts
  - src/ui/SidebarNav.ts
autonomous: false
requirements: [UATX-01]

must_haves:
  truths:
    - "Importing each of the 20 dataset types renders rows/columns/cells with real data — no empty grids"
    - "Auto-switched view matches ViewDefaultsRegistry recommendation for dataset types that have one"
    - "Recommendation badges appear in SidebarNav for dataset types with VIEW_RECOMMENDATIONS entries"
    - "Schema-mismatch errors do not appear in the console during any import"
    - "All functional defects found during the pass are fixed before sign-off"
  artifacts:
    - path: "src/providers/ViewDefaultsRegistry.ts"
      provides: "Correct source-type-to-view mappings"
    - path: "src/providers/PAFVProvider.ts"
      provides: "applySourceDefaults axis validation"
  key_links:
    - from: "ViewDefaultsRegistry"
      to: "PAFVProvider.applySourceDefaults"
      via: "resolveDefaults(sourceType, schema)"
      pattern: "resolveDefaults"
    - from: "ViewDefaultsRegistry"
      to: "SidebarNav"
      via: "resolveRecommendation(sourceType)"
      pattern: "resolveRecommendation"
    - from: "ViewManager.switchTo"
      to: "PAFVProvider"
      via: "auto-switch after import"
      pattern: "switchTo"
---

<objective>
Manual UAT pass through all 20 dataset types verifying default view configuration, auto-switch behavior, recommendation badges, and axis defaults render correctly with real data. Inline-fix any functional defects found.

Purpose: UATX-01 requires every dataset type to render without empty grids, schema errors, or incorrect auto-switches before the v10.0 milestone ships.
Output: All 20 dataset types verified, any fixes committed, UAT log documenting results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/135-uat/135-CONTEXT.md

Source files under test:
@src/providers/ViewDefaultsRegistry.ts
@src/providers/PAFVProvider.ts
@src/views/ViewManager.ts
@src/ui/SidebarNav.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: UAT pass — import all 20 dataset types, verify defaults, inline-fix defects</name>
  <files>
    src/providers/ViewDefaultsRegistry.ts,
    src/providers/PAFVProvider.ts,
    src/views/ViewManager.ts,
    src/ui/SidebarNav.ts
  </files>
  <read_first>
    src/providers/ViewDefaultsRegistry.ts,
    src/providers/PAFVProvider.ts,
    src/views/ViewManager.ts,
    src/ui/SidebarNav.ts,
    .planning/phases/135-uat/135-CONTEXT.md
  </read_first>
  <action>
Execute a systematic UAT pass through all 20 dataset types. Per D-03, start with alto-index as warm-up.

**Test matrix (per D-01, D-02):**

The 20 source types in ViewDefaultsRegistry are:
1. alto_index (also covers alto_index_* prefix variants)
2. apple_notes
3. native_notes
4. native_reminders
5. native_calendar
6. csv
7. excel
8. json
9. html
10. markdown
(Plus any alto_index_contacts, alto_index_notes, etc. variants that resolve via the alto_index catch-all)

For EACH dataset type, verify (per D-04, D-05):
1. **Import succeeds** — no errors in console, data appears in grid
2. **Auto-switch fires** — if VIEW_RECOMMENDATIONS has an entry for this source type, the view auto-switches to the recommended view (native_calendar -> timeline, native_reminders -> timeline, apple_notes -> tree, native_notes -> tree, alto_index -> network). If no recommendation exists, SuperGrid should remain active.
3. **Axis defaults apply** — colAxes and rowAxes from VIEW_DEFAULTS_REGISTRY appear in the grid headers. Verify through PAFVProvider state or visual inspection.
4. **Recommendation badges** — SidebarNav shows a star badge next to the recommended view for source types with VIEW_RECOMMENDATIONS entries
5. **No empty grid** — at least one row of real data renders with content in cells
6. **No schema-mismatch** — no console errors about invalid columns or missing fields

**Fix workflow (per D-06, D-07):**
- Inline fix mode: find issue, diagnose, fix the source code, verify fix, move to next dataset
- If findings accumulate to 10+, switch to batch mode: log all remaining findings, then fix as a batch
- If patterns emerge (same root cause across types), consider switching to batch earlier

**Log format:**
For each dataset type, record: source_type, view_auto_switched_to (or "none"), axes_applied, badges_visible, pass/fail, notes.

After completing the pass, write the UAT results log to `.planning/phases/135-uat/135-01-UAT-LOG.md`.
  </action>
  <verify>
    <automated>cd /Users/mshaler/Developer/Projects/Isometry && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/135-uat/135-01-UAT-LOG.md exists and contains entries for all 20 dataset types
    - Each entry in the UAT log has: source_type, view_auto_switched_to, axes_applied, badges_visible, pass/fail
    - Zero entries have pass/fail = "fail" (per D-10 — zero functional defects)
    - If any source files were modified to fix defects, `npx tsc --noEmit` exits 0 and `npx vitest run` exits 0
    - Console shows no schema-mismatch errors during any import
  </acceptance_criteria>
  <done>All 20 dataset types import and render with correct auto-switch, axis defaults, and recommendation badges. Zero functional defects remain. UAT log documents every result.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies default view UAT results</name>
  <files>.planning/phases/135-uat/135-01-UAT-LOG.md</files>
  <action>
Present UAT log to user for review. User spot-checks 3-4 dataset types by importing them in the running app:
- Import alto-index: should auto-switch to Network view, star badge on Network in sidebar
- Import a calendar file: should auto-switch to Timeline view
- Import a CSV: should stay on SuperGrid with card_type/folder as columns
- Import Apple Notes: should auto-switch to Tree view
User confirms no empty grids, no errors, correct view auto-switches.
  </action>
  <verify>
    <automated>test -f .planning/phases/135-uat/135-01-UAT-LOG.md && echo "UAT log exists"</automated>
  </verify>
  <done>User has reviewed the UAT log and spot-checked dataset imports, confirming all 20 types pass the functional bar.</done>
</task>

</tasks>

<verification>
- All 20 dataset types tested against functional bar (D-04)
- Auto-switch matches VIEW_RECOMMENDATIONS for 5 types that have entries
- Recommendation badges visible for types with VIEW_RECOMMENDATIONS
- Zero functional defects (D-10)
- Cosmetic nits logged but non-blocking (D-11)
- TypeScript compiles cleanly, all tests pass
</verification>

<success_criteria>
UAT log exists with 20 entries, all passing. User spot-checks and approves.
</success_criteria>

<output>
After completion, create `.planning/phases/135-uat/135-01-SUMMARY.md`
</output>
